# Attachment-layer charter — DRAFT (awaiting [USER] blessing)

Date: 2026-08-30. Provenance: [AGENT: orchestrator], synthesizing
the reviewed evidence chain: port map (ratified) → relational
candidates memo + shape study + eunseq census → two hostile reviews
(`2026-08-30_relational-spine-hostile-review.md`,
`2026-08-30_statement-view-hostile-review.md`, both
UPHOLD-WITH-AMENDMENTS). Governing constraints: the NORTH STAR and
TRUST ARCHITECTURE blocks (CLAUDE.md) and the DECISIONS register.
Nothing below is ratified until blessed; amendments from both
reviews are folded, none silently.

## 1. What this arc builds

The attachment layer: the derived relational interface over the
cerberus-lean engine (the ONLY trusted semantics), the WP over it,
and the statement-shaped view that lets RefinedC's typing layer
port shape-literally. Deliverable shape per the trust architecture:
every artifact's downward theorem into the engine exists at the
moment the artifact lands — the adequacy spine is built first and
continuously, never retrofitted.

## 2. The relational trunk (amended candidate E)

- Rehabilitate the per-step spine ON THE TRIMMED MAINLINE
  (`relsemcore`: Call/Cerberus/ExecModel/Machine/RunND at the
  semantics pin) — not the fat park branch. Park material remains
  design-donor + proof quarry; everything re-proved lands against
  the pinned engine.
- **Declared temporal boundaries (MAJOR-1 fold — the honesty
  amendments), each with a named mover, registered in the ledger at
  birth:**
  (a) consistency filtration with an instrument-checked fixture —
  mover: kernel-proved consistency or hypothesis-explicit statement
  faces; (b) the seed-explicit threaded initial state vs the
  production entry — mover: an initial-state correspondence lemma;
  (c) the extern-digest hypothesis — mover: digest kernel-check;
  (d) `runEffectful` on the production bridge — mover: inherited
  from the semantics repo's own boundary register (theirs, not
  ours, to move). NO adequacy claim is recorded as "discharged"
  while any of (a)-(d) qualifies it.
- **D-promotion is parity-required, not optional** (Atomic is
  uninstantiable on the trunk's language by type inspection): the
  hybrid is a bridge with a named destination; the promotion row
  carries the atomics temporal row's mover.
- Disposition-at-introduction (north-star instrument 2): the trunk's
  interpreter-shaped vocabulary is scaffolding — dies from final
  statements at D-promotion; the adequacy faces are product.

## 3. The re-registered falsification probe (slice α-0, FIRST)

Kill axes kept: F1 statability at ∀-context; F2 the measured
16-48G/10-min unfolding tripwire (approach = stop-and-report).
Exemplars per the spine review's re-registration:
- P1 as originally registered (Eif-redex round characterization);
- P2 restated on a T1 operand-unseq context with a
  leftmost-advanceable premise (standardization lineage) against
  `get_ctx` traversal order;
- P3: one ∀-state per-template lemma at a symbolic truthiness/case
  payload — aimed at the class the park measured as the wall;
- the vertical consumption exemplar: syntactic premise → a wpk rule
  → the honestly-restated adequacy face (replaces the bind
  secondary).
Probe fails ⇒ stop; the charter returns to the operator with the
evidence. Probe passes ⇒ slices α-1+ open.

## 4. The statement view (amended candidate A)

- Fail-closed partial classifier over the Esseq spine as UNTRUSTED
  dispatch metadata (verified property: misclassification can only
  produce stuck goals, never wrong theorems — this property is a
  standing review checkpoint on every classifier change).
- **Species set revised per the hostile probes**: + the
  statement-position `Ecase`-with-spine-arms species (switch,
  do-while, incl. the `DUMMY(unspecified AilSdo)` undef arm) and
  the expression-position conditional-effect shapes (`&&`/`||`/`?:`).
- **Calling-convention fix (MAJOR-1 fold)**: row A-3 keeps the
  donor's `f_args` points-to clause in the call post-condition
  (caller kills arg slots after ccall — T4 is the caller's), and
  drops only the `f_local_vars` clause; ledger row re-priced.
- Ledger rows: the counted set + the tenth row (the
  configuration-flavored WP carrier, conditional on §2's trunk) —
  all bins re-audited at charter close, none recorded as (a)/(b)
  without the forcing-fact text.
- **Closure sweep before the spike** (converts assumptions A1/A2
  into measurements): lint-only classifier run over the frozen
  15-program corpus + the census files + the review's ten probes;
  the `SUnclassified` census is published in the slice record. KA1
  is measured against THIS sweep, not the derivation corpus.

## 5. The widened spike (slice α-1)

Four files, not two: `wrapping_add` (straight-line), `p02_if`, one
loop with a body-local forcing a real `typed_block`/Löb discharge,
one call file forcing the caller T3/T4 + callee `typed_function`
round-trip. Kill criteria: the design's five per candidate + KA6
(arg-slot ownership returns without changing `fn_ret_prop` beyond
the amended A-3). Pre-registered; a kill ⇒ park-and-report, the
flip conditions in the design doc govern.

## 6. Gauges and reporting (standing instruments applied)

- Build ledger: probe/spike/wall metrics. Product ledger: the arc
  closes with a NATIVE VERIFICATION EXHIBIT — a never-seen C
  program through classify → typed rules → WP → the (qualified,
  boundaries-declared) adequacy face. Both ledgers in the arc
  report, never conflated.
- All work on arc branches in worktrees; one substantive commit per
  slice; gates green before any claim; the in-build audit's
  boundary list grows ONLY via §2's declared entries.

## 7. What blessing this charter ratifies

(i) the amended trunk recommendation (§2) incl. the four temporal
boundaries and D-promotion-at-parity; (ii) the re-registered probe
as slice α-0; (iii) the amended statement view (§4) and widened
spike (§5) as slice α-1, gated on α-0 and the closure sweep;
(iv) the ledger rows opened by §§2-4. Anything outside this scope
returns to conversation before dispatch.
