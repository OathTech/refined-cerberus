proc main (): eff loaded integer :=
  let strong i: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong s: pointer = create(Ivalignof('signed int'), 'signed int') in
  store('signed int', i, conv_loaded_int('signed int', Specified(0))) ;
  store('signed int', s, conv_loaded_int('signed int', Specified(0))) ;
  save while_515: unit (i: pointer:= i, s: pointer:= s) in
    let strong a_517: loaded integer =
      bound(
        let weak a_519: loaded integer =
          let strong a_540: loaded integer =
            let weak a_524: loaded integer =
              let weak a_529: loaded integer =
                let weak a_535: loaded integer = load('signed int', i) in
                let weak a_536: loaded integer = pure(Specified(5)) in
                case (a_535, a_536) of
                  | (Specified(a_537: integer), Specified(a_538: integer)) =>
                      pure(
                        if conv_int('signed int', a_537) <
                        conv_int('signed int', a_538) then
                          Specified(1)
                        else
                          Specified(0)
                      )
                  | _: (loaded integer,loaded integer) =>
                      pure(Unspecified('signed int'))
                end in
              let weak a_530: loaded integer = pure(Specified(0)) in
              pure(
                case (a_529, a_530) of
                  | (Specified(a_531: integer), Specified(a_532: integer)) =>
                      if conv_int('signed int', a_531) =
                      conv_int('signed int', a_532) then
                        Specified(1)
                      else
                        Specified(0)
                  | _: (loaded integer,loaded integer) =>
                      Unspecified('signed int')
                end
              ) in
            let weak a_525: loaded integer = pure(Specified(0)) in
            pure(
              case (a_524, a_525) of
                | (Specified(a_526: integer), Specified(a_527: integer)) =>
                    if conv_int('signed int', a_526) =
                    conv_int('signed int', a_527) then
                      Specified(1)
                    else
                      Specified(0)
                | _: (loaded integer,loaded integer) =>
                    Unspecified('signed int')
              end
            ) in
          case a_540 of
            | Specified(a_541: integer) =>
                if a_541 = 0 then
                  pure(conv_loaded_int('signed int', Specified(0)))
                else
                  let strong a_554: loaded integer =
                    let weak a_543: loaded integer =
                      let weak a_549: loaded integer = load('signed int', s) in
                      let weak a_550: loaded integer = pure(Specified(7)) in
                      case (a_549, a_550) of
                        | (Specified(a_551: integer), Specified(a_552: integer)) =>
                            pure(
                              if conv_int('signed int', a_551) <
                              conv_int('signed int', a_552) then
                                Specified(1)
                              else
                                Specified(0)
                            )
                        | _: (loaded integer,loaded integer) =>
                            pure(Unspecified('signed int'))
                      end in
                    let weak a_544: loaded integer = pure(Specified(0)) in
                    pure(
                      case (a_543, a_544) of
                        | (Specified(a_545: integer), Specified(a_546: integer)) =>
                            if not(conv_int('signed int', a_545) =
                            conv_int('signed int', a_546)) then
                              Specified(1)
                            else
                              Specified(0)
                        | _: (loaded integer,loaded integer) =>
                            Unspecified('signed int')
                      end
                    ) in
                  pure(conv_loaded_int('signed int', a_554))
            | Unspecified(_: ctype) =>
                pure(undef(<<UB_CERB004_unspecified__conditional>>))
          end in
        let weak a_520: loaded integer = pure(Specified(0)) in
        pure(
          case (a_519, a_520) of
            | (Specified(a_521: integer), Specified(a_522: integer)) =>
                if conv_int('signed int', a_521) = conv_int('signed int', a_522) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_516: boolean =
      case a_517 of
        | Specified(a_518: integer) =>
            pure(if not(a_518 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_516 then
      let strong _: loaded integer =
        bound(
          let weak a_555: pointer = pure(s) in
          let weak a_563: loaded integer =
            let weak a_556: loaded integer = load('signed int', s) in
            let weak a_557: loaded integer = load('signed int', i) in
            pure(
              case (a_556, a_557) of
                | (Specified(a_558: integer), Specified(a_559: integer)) =>
                    Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_558), __conv_int__('signed int', a_559)))
                | _: (loaded integer,loaded integer) =>
                    undef(<<UB036_exceptional_condition>>)
              end
            ) in
          let weak _: unit =
            neg(store('signed int', a_555, conv_loaded_int('signed int', a_563))) in
          pure(conv_loaded_int('signed int', a_563))
        ) in
      let strong _: loaded integer =
        bound(
          let weak a_564: pointer = pure(i) in
          let weak a_571: loaded integer =
            let weak a_565: loaded integer = load('signed int', i) in
            let weak a_566: loaded integer = pure(Specified(1)) in
            pure(
              case (a_565, a_566) of
                | (Specified(a_567: integer), Specified(a_568: integer)) =>
                    Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_567), __conv_int__('signed int', a_568)))
                | _: (loaded integer,loaded integer) =>
                    undef(<<UB036_exceptional_condition>>)
              end
            ) in
          let weak _: unit =
            neg(store('signed int', a_564, conv_loaded_int('signed int', a_571))) in
          pure(conv_loaded_int('signed int', a_571))
        ) in
      save continue_511: unit (i: pointer:= i, s: pointer:= s) in  pure(Unit) ;
      run while_515(i, s)
    else
      pure(Unit) ;
  save break_512: unit (i: pointer:= i, s: pointer:= s) in  pure(Unit) ;
  let strong a_573: loaded integer = bound(load('signed int', s)) in
  kill('signed int', i) ;
  kill('signed int', s) ;
  run ret_510(conv_loaded_int('signed int', a_573)) ;
  kill('signed int', i) ;
  kill('signed int', s) ;
  save ret_510: loaded integer (a_574: loaded integer:= Specified(0)) in
    pure(a_574)

