proc add (a: pointer, b: pointer): eff loaded integer :=
  let strong a_569: loaded integer =
    bound(
      let weak a_562: loaded integer = load('signed int', a) in
      let weak a_563: loaded integer = load('signed int', b) in
      pure(
        case (a_562, a_563) of
          | (Specified(a_564: integer), Specified(a_565: integer)) =>
              Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_564), __conv_int__('signed int', a_565)))
          | _: (loaded integer,loaded integer) =>
              undef(<<UB036_exceptional_condition>>)
        end
      )
    ) in
  run ret_561(conv_loaded_int('signed int', a_569)) ;
  save ret_561: loaded integer (a_570: loaded integer:= undef(<<UB088_reached_end_of_function>>)) in
    pure(a_570)

proc main (): eff loaded integer :=
  let strong s: pointer = create(Ivalignof('signed int'), 'signed int') in
  store('signed int', s, conv_loaded_int('signed int', Specified(0))) ;
  let strong i: pointer = create(Ivalignof('signed int'), 'signed int') in
  store('signed int', i, conv_loaded_int('signed int', Specified(0))) ;
  save while_519: unit (i: pointer:= i, s: pointer:= s) in
    let strong a_521: loaded integer =
      bound(
        let weak a_523: loaded integer =
          let weak a_529: loaded integer = load('signed int', i) in
          let weak a_530: loaded integer = pure(Specified(3)) in
          case (a_529, a_530) of
            | (Specified(a_531: integer), Specified(a_532: integer)) =>
                pure(
                  if conv_int('signed int', a_531) <
                  conv_int('signed int', a_532) then
                    Specified(1)
                  else
                    Specified(0)
                )
            | _: (loaded integer,loaded integer) =>
                pure(Unspecified('signed int'))
          end in
        let weak a_524: loaded integer = pure(Specified(0)) in
        pure(
          case (a_523, a_524) of
            | (Specified(a_525: integer), Specified(a_526: integer)) =>
                if conv_int('signed int', a_525) = conv_int('signed int', a_526) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_520: boolean =
      case a_521 of
        | Specified(a_522: integer) =>
            pure(if not(a_522 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_520 then
      let strong _: loaded integer =
        bound(
          let weak a_534: pointer = pure(s) in
          let weak a_552: loaded integer =
            let strong (a_536: loaded pointer,
            (a_537: ctype, a_538: [ctype], a_539: boolean, a_540: boolean)) =
              pure(
                (Specified(Cfunction(add)), cfunction(Specified(Cfunction(add))))
              ) in
            let strong a_543: loaded integer = load('signed int', s) in
            let strong a_545: loaded integer = load('signed int', i) in
            if not(params_length(a_538) = 2) then
              pure(undef(<<UB038_number_of_args>>))
            else
              if a_539 \/ not(are_compatible ('signed int', a_537)) then
                pure(undef(<<UB041_function_not_compatible>>))
              else
                let strong a_541: pointer =
                  let a_547: ctype = params_nth(a_538, 0) in
                  if not(are_compatible ('signed int', a_547)) then
                    pure(undef(<<UB041_function_not_compatible>>))
                  else
                    let weak a_548: pointer =
                      create(Ivalignof('signed int'), 'signed int') in
                    let weak _: unit =
                      store('signed int', a_548,
                      conv_loaded_int('signed int', a_543)) in
                    pure(a_548) in
                let strong a_542: pointer =
                  let a_549: ctype = params_nth(a_538, 1) in
                  if not(are_compatible ('signed int', a_549)) then
                    pure(undef(<<UB041_function_not_compatible>>))
                  else
                    let weak a_550: pointer =
                      create(Ivalignof('signed int'), 'signed int') in
                    let weak _: unit =
                      store('signed int', a_550,
                      conv_loaded_int('signed int', a_545)) in
                    pure(a_550) in
                let strong a_551: loaded integer =
                  ccall('signed int (*) (signed int, signed int)', a_536, a_541,
                  a_542) in
                kill('signed int', a_541) ;
                kill('signed int', a_542) ;
                pure(a_551) in
          let weak _: unit =
            neg(store('signed int', a_534, conv_loaded_int('signed int', a_552))) in
          pure(conv_loaded_int('signed int', a_552))
        ) in
      save __cerb_continue0: unit (i: pointer:= i, s: pointer:= s) in
        let strong _: loaded integer =
          bound(
            seq_rmw('signed int', i, a_554 => case a_554 of
              | Specified(a_555: integer) =>
                  Specified(conv_int('signed int',
                  catch_exceptional_condition_add('signed int', conv_int('signed int',
                  a_555), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('signed int')
            end)
          ) in
        pure(Unit) ;
      save continue_515: unit (i: pointer:= i, s: pointer:= s) in  pure(Unit) ;
      run while_519(i, s)
    else
      pure(Unit) ;
  save break_516: unit (i: pointer:= i, s: pointer:= s) in  pure(Unit) ;
  kill('signed int', i) ;
  let strong a_557: loaded integer = bound(load('signed int', s)) in
  kill('signed int', s) ;
  run ret_514(conv_loaded_int('signed int', a_557)) ;
  kill('signed int', s) ;
  save ret_514: loaded integer (a_558: loaded integer:= Specified(0)) in
    pure(a_558)

