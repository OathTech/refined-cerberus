proc main (): eff loaded integer :=
  let strong x: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong r: pointer = create(Ivalignof('signed int'), 'signed int') in
  store('signed int', x, conv_loaded_int('signed int', Specified(2))) ;
  store('signed int', r, conv_loaded_int('signed int', Specified(0))) ;
  let strong a_517: loaded integer = bound(load('signed int', x)) in
  case a_517 of
    | Specified(a_518: integer) =>
        let strong a_519: integer = pure(conv_int('signed int', a_518)) in
        if a_519 = 1 then run case_521(x, r) else pure(Unit) ;
        if a_519 = 2 then run case_520(x, r) else pure(Unit) ;
        run default_522(x, r) ;
        run break_513(x, r) ;
        save case_521: unit (x: pointer:= x, r: pointer:= r) in
          let strong _: loaded integer =
            bound(
              let weak _: unit =
                neg(store('signed int', r,
                conv_loaded_int('signed int', Specified(10)))) in
              pure(conv_loaded_int('signed int', Specified(10)))
            ) in
          pure(Unit) ;
        run break_513(x, r) ;
        save case_520: unit (x: pointer:= x, r: pointer:= r) in
          let strong _: loaded integer =
            bound(
              let weak _: unit =
                neg(store('signed int', r,
                conv_loaded_int('signed int', Specified(20)))) in
              pure(conv_loaded_int('signed int', Specified(20)))
            ) in
          pure(Unit) ;
        run break_513(x, r) ;
        save default_522: unit (x: pointer:= x, r: pointer:= r) in
          let strong _: loaded integer =
            bound(
              let weak _: unit =
                neg(store('signed int', r,
                conv_loaded_int('signed int', Specified(30)))) in
              pure(conv_loaded_int('signed int', Specified(30)))
            ) in
          pure(Unit) ;
        run break_513(x, r) ;
        pure(Unit)
    | Unspecified(_: ctype) =>
        pure(undef(<<UB036_exceptional_condition>>))
  end ;
  save break_513: unit (x: pointer:= x, r: pointer:= r) in  pure(Unit) ;
  let strong a_530: loaded integer = bound(load('signed int', r)) in
  kill('signed int', x) ;
  kill('signed int', r) ;
  run ret_511(conv_loaded_int('signed int', a_530)) ;
  kill('signed int', x) ;
  kill('signed int', r) ;
  save ret_511: loaded integer (a_531: loaded integer:= Specified(0)) in
    pure(a_531)

