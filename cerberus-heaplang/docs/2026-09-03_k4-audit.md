# K4 range audit — the exhibits and the kill/free arc's documentation closure (`100eb83..3dd7843`)

Independent auditor, 2026-09-03. Audited on the fixed detached copy
`worktrees/audit-k4` (HEAD `3dd7843`), read-only: no build, no commit; the
environment queries below ran through `../scripts/capped ~/.elan/bin/lake env
lean --stdin` from `cerberus-heaplang/` against the primed `.lake`. Brief:
`docs/AUDIT-BRIEF.md` (substantive logical/coverage gaps within scope; the
exhibits are the demo's CLAIMS, read as a skeptical PL reader would).
Provenance: every judgment here is [AUDITOR]; quoted text is verbatim from
the tree at `3dd7843`; tallies I computed are labelled DERIVED.

## Verdict

**PASS — no High findings.** The two exhibits are what the record says they
are: `dl_wps`/`dl_wpt` is the classical `{list p} dispose(p) {emp}` (with the
strengthened `deadNodes` post and the textbook `emp` face derived), stated
through the public `wps_kill`/`wpt_kill` at the node cell, with a
remaining-list loop invariant and honestly derived budgets; `rl_wps`/`rl_wpt`
carries the allocation budget as a loop invariant split per iteration by
`allocBudget_split`, spent by `wps_alloc`/`wpt_alloc` and returned by
`wps_free_emp`/`wpt_free_emp`. Both production statements are over the
shipped `CerbND.runND (drive …) (initial_driver_state …).1` with no
package-defined driver in their text (measured by a constant trace of the
statement types, §Q1). Every constant of the two modules (201) has an axiom
set within the classical trio (measured). The snapshot is ADDED 136 /
REMOVED 0 / CHANGED 0 (re-derived by entry). The 21 new pins are present and
trio-exact. The region-access finding is correct and on every shop-window
surface. Two Medium findings, both documentation claims the tree does not
literally support (a hypothesis vocabulary and a readout description); five
Notes.

Merge recommendation: **merge after M-1 and M-2** (each a sentence-level
correction, or — for M-1 — a one-line restatement of `hB`; the operator's
call).

## Findings

### M-1 (Medium — a shop-window claim not supported by one root-of-trust export): `region_loop_certified_production`'s `hB` puts three package definitions into the statement, and README/WALKTHROUGH say none but the program, `prodFile` and the readouts appear

- Location. `CerberusHeapLang/RegionLoopExhibit.lean:692-696`, verbatim:
  ```
  theorem region_loop_certified_production (sup : Nat) (hcost : 0 < regionCost al sz)
      (n : Int) (hn : 0 ≤ n)
      (hB : n.toNat * regionCost al sz ≤ headroom prodMem₀.lastAddress)
      (hfuel : 7 * n.toNat + 5 ≤ lemDefaultFuel)
  ```
- Tree's truth (DERIVED: `getUsedConstants` of the statement's type,
  filtered to the package's namespace): `CerberusHeapLang.headroom [Heap]`,
  `CerberusHeapLang.regionCost [Heap]`, `CerberusHeapLang.prodMem₀
  [ProdEntry]`, plus `loc0 [Exhibit]`, `prodFile [ProdEntry]`, `rlProg
  [RegionLoopExhibit]`. The dispose production's type has only `dlProdProg`
  and `prodFile` from the package; the four earlier productions' `hfuel`
  bounds are over `lemDefaultFuel` (LemLib), not a package definition.
- The claims. README.md:187-190: "no package-defined driver appears in
  their statements, and they carry no termination hypothesis (only the
  explicit in-budget bounds `hfuel` where the step count depends on an
  input)". WALKTHROUGH.md:291-293 ("The two lanes, labelled"): "the genuine
  Cerberus driver, and nothing package-defined in the statement but the
  authored program, its `prodFile` wrapper and the pure readout
  predicates." WALKTHROUGH names `region_loop_certified_production` (§1.3
  list, §4) and never mentions `hB` or `hfuel` (grep: no hit for `hB`,
  `7 * n.toNat + 5`, `headroom prodMem₀`). ARCHITECTURE §6 (lines 198-203)
  DOES state both premises exactly. The K4 record §7 says "the region loop
  carries `hB` and `hfuel` as the fib/counter productions carry theirs" —
  an inexact analogy: `hfuel` is over an engine/LemLib constant, `hB` over
  the package's cost function, headroom function and cold-start literal.
- Consequence. None for soundness: `hB` is a hypothesis; the conclusion's
  execution function is the genuine pipeline; `prodMem₀` is tied to the
  shipped `initial_driver_state` inside `prod_run_eqJ`'s proof
  (`drive_after_setup`), and `prodMem₀_deadAllocations`/`_lastAddress` are
  `rfl`. But the "nothing package-defined but …" sentence is now false for
  one of the six root-of-trust exports, and a reader of README/WALKTHROUGH
  is not told the region loop's premises at all. The referent rule
  (CLAUDE.md "no hand-written definition (driver loop, discharge,
  scheduler) may appear in the statement of an exported theorem") is not
  breached in its letter — `prodMem₀` is none of the three — but this is
  the first root-of-trust export with a package definition anywhere in its
  statement, and the operator should see it.
- Fix (one statement). Either (a) restate `hB` in engine vocabulary:
  `n.toNat * (sz.toNat + max al.toNat 1 - 1) ≤ 281474976710647` (this is
  `headroom errnoAddr`; the bridge to `prodMem₀_launchCoh` is `rfl`/`omega`
  inside the proof), leaving only the program and `prodFile` package-defined
  in the statement; or (b) amend README:189-190 and WALKTHROUGH:291-293 to
  "…the pure readout predicates, and — for `region_loop_certified_production`
  — the budget side condition `hB : n.toNat * regionCost al sz ≤ headroom
  prodMem₀.lastAddress` (the package's cost function at the package's
  cold-start cursor literal) with its `hfuel`", and add both premises where
  WALKTHROUGH §4 names the theorem.

### M-2 (Medium — a readout described more strongly than stated): "the two engine-picked node ids" is not what `dispose_list_certified_production` says

- Location. `DisposeExhibit.lean:602-606`, verbatim:
  ```
  (∃ i₁ i₂ : Int, i₁ ≠ i₂ ∧
    (dst'.layout_state.deadAllocations.contains i₁ = true ∧
      dst'.layout_state.allocations.get? i₁ = none) ∧
    (dst'.layout_state.deadAllocations.contains i₂ = true ∧
      dst'.layout_state.allocations.get? i₂ = none)) ∧
  ```
- The claims. Docstring (:585-587): "the final production memory has the
  two engine-picked node ids — DISTINCT — in `deadAllocations` with their
  records erased"; README table (:540): "whose final memory has the two
  engine-picked node ids, DISTINCT, in `deadAllocations` with their records
  erased"; WALKTHROUGH:838-839: "the two engine-picked ids distinct, dead,
  erased"; the arc record and K4 record likewise.
- Tree's truth. The statement says: two DISTINCT ids exist that are dead
  and erased. Nothing in its text ties `i₁ i₂` to the program's two
  `create`s. The identification is by the proof (`ψD` is discharged from
  the nodes' `deadObj` through `dlProd_readout`) and by an argument outside
  the statement (`prodMem₀_deadAllocations : prodMem₀.deadAllocations = []`
  is `rfl`, ProdEntry.lean:211, and `killM` is the only writer of that
  list, so any dead id in the final state was killed by the program, which
  kills only the two nodes). The K4 record §1 is honest that the readout is
  per id ("nothing speaks for the WHOLE dead list or the WHOLE allocation
  table"), but every surface then adds "node" to the ids.
- Consequence. A skeptical reader who reads the theorem finds a weaker
  claim than the prose: "two distinct dead-and-erased ids", not "the two
  nodes are dead". True of the run; not in the theorem.
- Fix (one sentence, on the four surfaces). "…the final memory has two
  DISTINCT allocation ids dead with their records erased (the proof
  witnesses them as the two created nodes; the statement itself names no
  node)". Or strengthen the statement to name the ids through the cells the
  prefix built — not available without a `deadObj`-shaped `Sat` readout,
  which is the recorded API item.

### N-1 (Note): the local `deadObj` readout states `CohG`/`metaInterp`/`byteInterp`/`AllocCursor`/`MetaCell` in three exhibit-side signatures; it opens none of them

- `DisposeExhibit.lean:919-923` (`deadObj_dead_keep`), `:928-933`
  (`deadNodes_dead`), and the inner function of `dlPost_readout` (:983-988)
  carry `(hG : CohG σ mm mb mk)` and `metaInterp mm` in their statements.
  API.lean's documented exception admits these as "opaque tokens a client
  never opens"; the exhibit indeed never unfolds them — the direct-reference
  trace of the two modules (DERIVED, 268 package constants referenced) has
  no `stateInterp_iff`, no `metaHeap_*`, no `metaOwn`/`bytesOwn`/`cursorOwn`,
  no `Step.*`, no judgment unfolding; the only coupling names are `CohG`,
  `metaInterp`, `byteInterp`, `MetaCell`, `AllocCursor` (the last two as
  binder types forced by `stateInterp_readout`'s own signature) and the
  readout `deadObj_dead`. API.lean lists `deadObj_dead` in the INTERNAL
  table's name column while the same row's "why" column says "Its
  client-facing consequences are exported: the readouts, the
  pure-consequence lemmas" — the classification is ambiguous on the page.
  The K4 record §7 already names the remedy (public
  `deadObj_readout`/`deadRegion_readout`); this Note only adds: the
  remedy also fixes API.lean's ambiguous row, and `rlPost_readout`
  (RegionLoopExhibit.lean:448-455) discards the state interpretation with
  the fupd plumbing directly (`iintro %σ' %ns %κs %nt Hσ; iapply
  fupd_mask_intro_discard …`) — the same pattern as ProdLoopExhibit.lean:466,
  so a precedent, but a one-line public `readoutPost_pure` would remove the
  plumbing from every client, which is what `stateInterp_readout`'s
  docstring promises ("Exhibit modules no longer touch … the fupd plumbing
  directly").

### N-2 (Note): the region loop's production statement says nothing about the regions; the docs do not claim otherwise, but say so nowhere either

- `region_loop_certified_production`'s conclusion is the `runND` equation
  plus `dres.dres_core_value = Vunit`, `dres_blocked = false`, empty
  stdout/stderr — no fact about the final allocation table. README's row
  ("`n` `alloc`/`free` pairs from one budget — EXACTLY ONE Active execution
  delivering `Vunit`") describes the program, then the conclusion; the
  docstring describes the proof ("every `alloc` through the PUBLIC
  `wpt_alloc` …"). Honest, and the `Vunit` delivery does exclude the
  out-of-memory kill. One clause on the README row and in the record —
  "(no readout of the final table: the regions are freed through the `_emp`
  faces, which drop the dead knowledge)" — would pre-empt the question.

### N-3 (Note): "first loop client" and "one delivery item" — both true, with a precision

- `allocBudget` appears in ProdLoopExhibit.lean (:499, :1041) only in
  straight-line create prefixes, never in a `LabelSpec`; the K4 `rlLs` is
  the first label-spec (loop-invariant) owner of a budget — "first loop
  client" holds. The professor's review-2 (docs/2026-09-02_professor-review-2.md:387)
  lists "Fix required items 1–4 and deliver the additive capacity face";
  README:593-596 calls the capacity face "its one delivery item" — the one
  item phrased as a delivery, the four others being fixes; acceptable, but
  "its delivery item" would be exact.

### N-4 (Note): the arc record's correction 7 — I could not locate its premise in the design note

- `docs/2026-09-03_kill-free-arc-record.md` item 7: "§1's alloc sketch `∃ p,
  ⌜w = ptr p⌝ ∗ …`". `docs/2026-09-02_kill-free-design-spike.md` §1 is the
  KILL rule sketch; grep for `∃ p`, `ptr p`, `wps_alloc`, `alloc.*⌜w` finds
  no alloc sketch in the note. The delivered shape (K3 record §4(a)/(b),
  `cellPtr id a` with `0 < a ∧ a + size ≤ 2^64`) is real; the "corrected"
  premise may live in a brief rather than the note. Items 1, 4, 5 and 9
  verified against the note's text (below).

### N-5 (Note): WALKTHROUGH labels the two `driveU` exports PROVISIONAL by class, not by name

- WALKTHROUGH:835 and :920 name `dispose_list_certified_total` and
  `region_loop_certified_total` as "the `driveU` equation" without the word
  PROVISIONAL at the line; §1.3 (:286-293) labels "every exhibit the
  README's table lists at `driveU`". The docstrings (DisposeExhibit:33-34,
  RegionLoopExhibit:607-608), module headers, README table rows (:317-318)
  and ARCHITECTURE §6 (:216-221, `*_total`) all carry the label. Adequate;
  a parenthetical "(PROVISIONAL)" at the two WALKTHROUGH sites costs
  nothing.

## The questions, answered

### Q1. The production statements

Both are over the shipped composite. Verbatim conclusion heads:
`DisposeExhibit.lean:593-600`
```
CerbND.runND
    (_root_.drive fmapEmpty false
      (prodFile (dlProdProg ra mo bty sbty cbty bbty nbty ubty))
      args)
    ((initial_driver_state sup
      (prodFile (dlProdProg ra mo bty sbty cbty bbty nbty ubty))
      fs).1) =
  [(nd_status.Active dres, ([] : List String), dst')] ∧
```
and `RegionLoopExhibit.lean:698-705` with `prodFile (rlProg loc0
empty_annotation ra al sz pref sbty ibty pbty ubty n)`.

Package definitions in each statement's TEXT (DERIVED by
`getUsedConstants` on the theorem type, package namespace only):

| theorem | package constants in the statement | class |
|---|---|---|
| `dispose_list_certified_production` | `dlProdProg` (DisposeExhibit), `prodFile` (ProdEntry) | authored program; the synthetic one-procedure wrapper — the same two classes as `list_reverse_certified_production` |
| `region_loop_certified_production` | `rlProg`, `loc0` (the authored program and its `.unknown` location), `prodFile`; in `hB`: `regionCost` (Heap), `headroom` (Heap), `prodMem₀` (ProdEntry) | program / wrapper; budget side condition — see M-1 |

No package-defined driver, discharge, scheduler or loop appears in either
statement (every other constant is Lean core, LemLib, or the generated
engine: `drive`, `initial_driver_state`, `CerbND.runND`, `driver_result`,
`driver_state`, `nd_status.Active`, `CerbMem.MemState.*`, `lemDefaultFuel`,
`empty_annotation`). The readouts are pure facts on `dres`/`dst'`.

Conclusions vs the docs. Dispose: `dres_core_value = Vunit`, and `∃ i₁ i₂,
i₁ ≠ i₂ ∧ DeadAt i₁ ∧ DeadAt i₂` where `DeadAt` unfolds in the statement to
`deadAllocations.contains i = true ∧ allocations.get? i = none` — BOTH dead
AND erased, DISTINCT (the `i₁ ≠ i₂` is in the text). The ids are
existential; "node" is the prose's addition (M-2). Region loop: `Vunit`,
`dres_blocked = false`, empty stdout/stderr — nothing about the final
allocation table; the docs describe the program and the proof, not a table
readout (N-2).

Premises in README's table (:317-318 for the Iris/`driveU` rows, :540 for
the production row) — checked against the signatures: exhaustive and
in order (dispose production `sup ra mo bty sbty cbty bbty nbty ubty fs
args`; region production `sup ra al sz pref sbty ibty pbty ubty`, `hcost`,
`n`, `hn`, `hB`, `hfuel`, `fs args`; `_total` rows likewise). `hfuel`/`hB`
are stated in the docstring, the README table and ARCHITECTURE §6;
WALKTHROUGH states neither (M-1). `hfuel : 7 * n.toNat + 5 ≤ lemDefaultFuel`
is exactly `prod_run_eqJ`'s `k + 2 ≤ lemDefaultFuel` at `k = rlCost n.toNat
+ 1 = 7 * n.toNat + 3` (DERIVED). The dispose production's bound is fixed
(`51 + 2`, discharged by `omega` at :649-652), so no `hfuel` — correct.

### Q2. The logic-level theorems

Statements (verbatim, DisposeExhibit.lean:598-601 / 621-624;
RegionLoopExhibit.lean:425-428 / 564-567):
```
isList (GF := GF) head ns ⊢
  wps (procCtx p rs) (dlLs ns) (dlPost ns)
    (dlProg loc ann ra mo sbty cbty bbty nbty ubty head) [fmapEmpty]
```
with `dlPost ns w _ := ⌜w = .pure Vunit⌝ ∗ deadNodes ns` and the `_emp` face
at `fun w _ => ⌜w = .pure Vunit⌝`;
```
allocBudget (GF := GF) (n.toNat * regionCost al sz) ⊢
  wps (procCtx p rs) (rlLs al sz) rlPost
    (rlProg loc ann ra al sz pref sbty ibty pbty ubty n) [fmapEmpty]
```
with `rlPost w _ := ⌜w = .pure Vunit⌝`.

Invariants. `dlLs` (:378-383): `∃ done rest' pCur f renv, ⌜args = [ptrVal
pCur] ∧ ns = done ++ rest' ∧ ρ = f :: renv ∧ SymFrame f⌝ ∗ deadNodes done ∗
isList pCur rest'` — the classical remaining-list invariant (`isList cur
rest`) strengthened by the persistent dead prefix; `dlLsT` adds the variant
pin `m = dlCost rest'.length`. `rlLs` (:335-338): `⌜args = [ivVal i] ∧ 0 ≤
i ∧ …⌝ ∗ allocBudget (i.toNat * regionCost al sz)` — the budget IS the
invariant; the taken branch rewrites `i.toNat * c = c + (i-1).toNat * c`
(`omega`, `Nat.add_mul`) and splits by `allocBudget_split` (:365-369).

Rules consumed (DERIVED: the direct-reference trace of the two modules,
268 package constants): the rule modules contribute exactly `wps_kill`,
`wps_kill_eval`, `wps_alloc`, `wps_free_emp`, `wps_load_eval`,
`wps_memop_eval`, `wps_memop_ptreq`, `wps_if_true/false`, `wps_ofVal`,
`wps_run`, `wps_save`, `wps_seq/_spec/_sym`, `wps_wand`,
`wps_frame_labels`, `blockSpecs_intro/_frame` and the `wpt` twins
(`wpt_kill`, `wpt_kill_eval`, `wpt_alloc`, `wpt_free_emp`, `wpt_create`,
`wpt_store_eval`, `wpt_store_cell_at`, `wpt_load_eval`, `wpt_memop_*`,
`wpt_if_*`, `wpt_ofVal`, `wpt_run`, `wpt_save`, `wpt_save_vals`, `wpt_seq*`,
`wpt_mono`, `wpt_mono_Ls`, `wpt_frame_labels`, `blockSpecsT_intro/_frame/
_mono`), the assertion laws `pointsToCell_cellOwn_iff`, `cellPtr_inj`,
`allocBudget_split`, the sibling client rules `wps_load_node_field`/
`wpt_load_node_field`/`wpt_store_node_field` (ListRevExhibit), and the
launches `wpt_engine_boundU`, `wpt_engine_boundU_alloc`,
`wpt_driver_done_alloc`, `prod_run_eqJ`. NOTHING internal: no `Step.*`, no
`CohG.*` lemma, no `metaHeap_*`, no ghost carrier, no judgment unfolding
(N-1 for the opaque coupling tokens in the readout signatures).

Budgets (DERIVED against the rule bounds printed from Wpt.lean). Dispose,
per node: `wpt_memop_eval` +1, `wpt_memop_ptreq` at `2 ≤ k` (used at 2)
→ 3; `wpt_if_false` +1; `wpt_load_eval` +1, `wpt_load_node_field` (=
`wpt_load_cell_at`, `3 ≤ k`) at 3 → 4; `wpt_kill_eval` +1, `wpt_kill` at
`2 ≤ k` used at 2 → 3; `wpt_run` at `1 + m ≤ k` with `m = dlCost
vs.length` → 1. Total 12. Exit: 3 + `wpt_if_true` 1 + `wpt_ofVal` at
`deliveryCost (.pure Vunit) ≤ 1` (`Nat.le_refl 1` compiles, so the cost is
exactly 1) = 5. `dlCost r = 12r + 5` (`dlCost_zero : dlCost 0 = 3 + (1 + 1)
:= rfl`, `dlCost_succ` by `omega`), program `+1` by `wpt_save_vals`:
`12·|ns| + 6`. Region loop, per iteration: `wpt_if_true` 1 + `wpt_alloc` at
2 + (`wpt_kill_eval` 1 + `wpt_free_emp` at 2) + `wpt_run` 1 = 7; exit 1 + 1
= 2; `rlCost k = 7k + 2`, program `7·n.toNat + 3`. Every bound is used at
its rule's minimum (`Nat.le_refl`), so the budgets are tight to the rules
and honest.

### Q3. The `driveU` lane

PROVISIONAL on: docstrings (DisposeExhibit:32-34 "PROVISIONAL: stated over
`driveU`"; RegionLoopExhibit:607-608 same), module headers, README rows
(:317-318 "`driveU (procCtx …)` — PROVISIONAL"), ARCHITECTURE §6 (`*_total`
in the class list), WALKTHROUGH by class (N-5). Readouts: `dlPost_readout`
goes through `stateInterp_readout` (public, API.lean "Base stratum" row)
with `deadObj_dead` (Heap) and `cellsOwn_consequence` (Adequacy, the
header's named consequence lemma); `rlPost_readout` is pure (no state
opened). No internal is opened (N-1 for the tokens).

### Q4. The region-access finding

Confirmed by grep: in Rules.lean/Wps.lean/Wpt.lean the only occurrences of
`regionOwn`/`regionView` are `alloc_atomic`/`free_atomic` and their faces
(Rules.lean:1068-1286, Wps.lean:1941-1994, Wpt.lean:2216-2256); every
load/store rule (`store_atomic` :261, `load_atomic` :362,
`load_atomic_readonly` :457, `loadAt_atomic` :554, `storeAt_atomic` :645;
`wps_load*`/`wps_store*` at Wps.lean:1292-2215; `wpt_load*`/`wpt_store*` at
Wpt.lean:823-2300) mentions neither bundle. The coupling argument is
correct: `LiveCoh.alloc` (Heap.lean:974-978) requires `al.ty = mc.ty` and
`(al.isReadonly = .IsWritable ↔ mc.readonly = false)`; `MetaCoh.dynamic`
requires `mc.dynamic = true → mc.addr ∈ σ.dynamicAddrs`; `regionCell a n
alive := ⟨a, none, n, alive, false, true⟩`, `objCell tds a ty alive
readonly := ⟨a, some ty, sizeofCtype tds ty, alive, readonly, false⟩`. A
ghost update `regionCell → objCell` at a live id would assert `al.ty = some
ty` against an engine record with `ty := none` (`allocateRegion`), so
`CohG` would be false after the update — no frame-preserving update exists;
"not a law, a falsehood" is exact. Could a coercion exist soundly? Not as a
cell-type change; what CAN exist is a typed view over a region: the seams
`loadM_live` (Heap.lean:1142) and `storeM_live` (:1207) take `MetaCoh σ id
mc`, `mc.alive = true`, `off + sizeofCtype tds vty ≤ mc.size`, `mc.readonly
= false` (store) and use `mc.ty` only through `hmeta.nonAtomic`
(`atomicTyOpt none = false`) — a `regionView`-shaped `loadAt`/`storeAt`
atomic step is provable from them without touching the coupling, as the
record says. Surfaces: README "Scope, exactly" (iv) (:484-499),
ARCHITECTURE §7 "THE OTHER OPEN ITEMS" first bullet, WALKTHROUGH §4
(:906-911) and the registered-limitations bullet (:1406-1413), Heap.lean
`loadM_live` docstring, both module headers. The region loop is presented
as the `alloc`/`free` exhibit and explicitly NOT as the chartered malloc'd
list (RegionLoopExhibit.lean:16-35; K4 record §3 "It is NOT the chartered
exhibit and is not presented as one").

### Q5. Exhibit imports

DisposeExhibit imports `ProdLoopExhibit` (which brings ListRevExhibit,
ProdEntry, API); RegionLoopExhibit imports `API`, `Exhibit`, `ProdEntry`.
Names taken from siblings (DERIVED trace): ListRevExhibit's node vocabulary
and client rules (`isList*`, `SeedChain`, `seedChain_isList`, `lrCellFrame`,
`nodeTy*`, `nullNode`, `nullVal`, `nodePtrTy`, `lrShiftPe`,
`wps/wpt_load_node_field`, `wpt_store_node_field`, `node_ptr_*`, `ptrImg*`,
`get?_union'`, `evalArrayShift_long_one`, `valueFromMemValue_ptr`, `longTy`,
`ptrVal`), ProdLoopExhibit's prefix vocabulary (`lrProdPrefix`, `lrPFrame*`,
`lrBuilt*`, `lrN1Sym`, `lrN2Sym`, `longVal`, `longMval`, `nodeUndefBytes`,
`col_aux_sseq`, `col_aux_action`, `lr_two_node_budget_fits`,
`lrProdPrefix_frag/_pot`), `loc0` (Exhibit), `ivVal` (LoopExhibit). All are
client-level definitions/theorems over the public API, none is an internal
of the logic. The core-module references are the API table's public rows
plus the documented-exception tokens (N-1) and the mirror evaluator's
equations (`evalPexpr_op`, `evalBinop` unfolded in `rl_guard_eval`, the
LoopExhibit.lean:166-167 precedent). P5 (API-only clients): kept, with the
one recorded bend (N-1) that the record itself flags as an API item.

### Q6. Snapshot and pins

DERIVED, parsing both snapshot files by entry (`kind name :` header + the
printed type): pre 2152 entries, post 2288; **ADDED 136 (46 `def`, 90
`theorem`) / REMOVED 0 / CHANGED 0**; pre is byte-identical to
`2026-09-03_k3-signatures-post.txt` (`cmp`). Split: 88 dispose-side (incl.
`bindSaveParams_dlProd`), 48 region-side — as the record. Pins: `Audit.lean
trioExports` has 269 names, 269 unique, 21 of them new and exactly the
record's list. Axioms: `collectAxioms` over all 201 constants of the two
modules — 0 outside `[propext, Classical.choice, Quot.sound]`; the four
exports' axiom sets are exactly the trio. Manifest: the consumer column is
computed by `scripts/capability_manifest.lean` as the proof-term dependency
cone of every constant of an exhibit module through the package's
constants (memoised `getUsedConstants`, transitive, pruned only by module
import-reachability) — a real dependency trace, not name-level. The new
rows are real: `wps_kill` is applied in `dl_body_wps` (:481), `wps_alloc`
in `rl_body_wps` (:371), `wps_kill_eval` in both, and `wps_free_emp`'s proof
is `iapply wps_free …` (Wps.lean), so RegionLoopExhibit's `wps_free` row is
a genuine cone membership.

### Q7. Docs

ARCHITECTURE §7 as the ledger: goal 1 "OPEN, pending the upstream
restatement" with the sequence (re-pin → calls → restatement) and the
PROVISIONAL rule — matches DECISIONS; goal 2 "CLOSED fail-closed on the
declared fragment (2026-09-02), with two characterized residuals"
(`eval_uncovered`, `run_surplus`) plus "A third registered premise stays
carried, not proved: `hbsz`" (:288) — the three named residuals; goal 3
"CLOSED (K0–K3)" naming `MemWF.loadM/storeM/allocateObject/killM/
allocateRegion`, `prodMem₀_memWF`, `create_fresh_global`, `la_pos` — the
theorems exist and are pinned (K3 audit). Professor's residual: CLOSED at
K2.5 with K4's first loop client — true (N-3). Arc record's ten
corrections, spot-checked: (1) design note §3 `size_pos : … 0 < al.size`
vs Heap `size_nonneg` — verified; (4) §5 `ψ v σ' := σ'.deadAllocations =
[1]` vs the per-id `contains`/erased readout — verified; (5) §5 "drive
length 4" vs README "drive length exactly 5" — verified; (9) §5's `(fun w _
=> ⌜w = .pure Vunit⌝)` at `c · |ns| + d` vs `dlPost` with `deadNodes` at
`12·|ns| + 6` — verified; (7) not located (N-4). K3-audit items: M-1 Heap
header (:121-123 now "every stated obligation of `MemWF` is a theorem");
N-1 the three comments (Heap `loadM_live` :1135-1138 "no load or store RULE
exists yet (the K4 finding …)", the K1-bundle header :3291-3292, Wps
`wps_kill` :1904-1905); N-2 `alloc_atomic` docstring (Rules.lean:1082-1086)
and README (ii); N-4 Heap ×3 (:390-393, :398-400, :409-410), ARCHITECTURE
goal-3 paragraph (the value wording removed), WALKTHROUGH :1020-1022, the
K3 record's dated erratum (:64-68) — all applied.

## What I could not verify

- The FULL-gate transcript in the K4 record §10 (no build permitted here);
  I re-derived its measurable claims (pins 269, axiom trio, snapshot
  counts) independently and they agree.
- Correction 7's premise in the design note (N-4).
- That `deadAllocations` has no writer other than `killM` in the pinned
  `CerbMem.lean` (asserted by the K0 record; not re-read here) — M-2's
  informal argument relies on it, the theorem does not.
- The manifest's exact output (regeneration is a `lake env lean` of the
  script; I read the script and the committed table instead).

## Merge recommendation

Merge, after M-1 (either the `hB` restatement or the two-sentence doc
amendment, operator's call) and M-2 (one phrase on four surfaces). The
Notes are optional; N-1's remedy is already in the arc record's "what
remains". No new check recommended: the gap class M-1 exposes (a package
definition entering a root-of-trust statement's hypotheses) was caught by
reading one theorem, and the existing constant trace I ran is a
one-off measurement, not gate material.
