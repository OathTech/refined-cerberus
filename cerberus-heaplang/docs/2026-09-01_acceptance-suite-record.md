# Foundations arc — the acceptance-suite record (arc exit gate)

Worker slice record, the ACCEPTANCE-SUITE slice (the final work
slice of the foundations arc, before the fresh re-audit). Charter:
the 2026-08-31 foundational audit's "Recommended acceptance suite"
(`docs/2026-08-31_cerberus-heaplang-foundational-audit.md` §
Recommended acceptance suite — the 8 tests, adopted verbatim as the
ARC EXIT GATE by `docs/2026-08-31_foundations-arc-plan.md`).

Every piece of evidence below was RE-RUN OR RE-VERIFIED ON THIS
TREE during this slice (2026-09-01), not quoted from phase notes;
where a phase note first established a fact, it is cited as
history, and the current-tree check is stated alongside. Quoted
transcripts are verbatim; derived tallies are labeled as derived.

Slice work products (beyond the record itself): the census
freeze-gate (test_unit.sh gate 5 — the formerly registered future
gate), five new curated pins completing test 7's "exact pins for
all public exports", the test-8 plant transcripts, and the
perturbation run for test 3. Gate baseline at slice start and
gate close at slice end: `./scripts/test_unit.sh` ALL GREEN.

Scorecard (each test's evidence in its section):

| # | Test | Verdict |
|---|------|---------|
| 1 | Rule-consumption | PASS |
| 2 | Fresh-client | PASS |
| 3 | Perturbation | PASS (demonstrated this slice, strongest form) |
| 4 | Totality | PASS |
| 5 | State-read census | PASS |
| 6 | Production | PASS |
| 7 | Axiom | PASS (fallback form: boundary origin-checked; retirement still upstream) |
| 8 | Mutation gates | PASS (a: in-build gates 3+4; b, c, d: structurally-forced via gate 3, documented plants with transcripts) |

## Test 1 — rule-consumption

**Criterion**: every advertised syntax rule reaches an
engine-facing adequacy theorem.

**Evidence (this tree)**: the capability manifest regenerated by
gate 4 (green in both this slice's gate runs). The row set is
DERIVED from `Frag`'s constructor list in the built environment
(18 cone rows + the supplementary evaluator row); every `Step`
mirror constructor must be claimed by exactly one row; every OK
cell is name-and-kind checked. The machine-readable tail of the
committed `docs/CAPABILITY_MANIFEST.md`, verbatim:

```
ADEQUACY-EXPORTABLE: value store load create sseq-wild annot save if run sseq-spec pure-sym load-op sseq-sym memop-ptreq memop-op store-op case-value wseq-wild pure-operands
FULL-ROW: value store load create sseq-wild annot save if run sseq-spec pure-sym load-op sseq-sym memop-ptreq memop-op store-op case-value wseq-wild pure-operands
LOCAL-RULE-ONLY:
```

Every row is ADEQUACY-EXPORTABLE and FULL-ROW; the LOCAL-RULE-ONLY
set is EMPTY — no advertised rule lacks an adequacy lane. Each
row's partial-adequacy cell names its engine-facing adequacy
theorem (`engine_complete`/`engine_adequacyJ`/`engine_adequacyU`)
and each consumer cell names a consuming exhibit theorem; all
checked in the environment by the generator. The audit's original
counterexample (F-01: `wps_case_value` with no consumer) now has
the full row: cone `Frag.case_value`, two-sided engine pair,
consumer `case_certified` (CaseExhibit). The README scope-token tie
(gate 4b) confirms no claims surface outruns this set.

## Test 2 — fresh-client

**Criterion**: verify a new struct-manipulating program without
adding core WP rules.

**Evidence**: two clients, each landed in one commit whose diff is
the proof of zero core edits (re-checked with `git show --stat` on
this branch this slice):

- `StructExhibit.lean` (Phase 2, commit `2f15f98`) — two-field
  struct update `{int x @ 0; int y @ 8}` in one 16-byte allocation,
  end-to-end (`struct_update_certified`), plus the allocation
  consumer (`struct_create_store_wps`). Commit touches:
  `StructExhibit.lean` (new), lib-root + Audit import/pins,
  README, the manifest generator's create row + regenerated
  manifest. NO core module.
- `TreeRotExhibit.lean` (Phase 4, commit `4e86c08`) — binary-tree
  right rotation, a third layout (three 8-byte fields), branching
  representation predicates, partial + unconditional total exports.
  Commit touches exactly 3 files: `TreeRotExhibit.lean` (new),
  lib-root import, Audit pins. NO core module, no manifest change
  (no new construct — the point).

Every memory rule in both modules is a one-line client instance of
the generic `wps/wpt_{load,store}_cell_at` subrange rules.

## Test 3 — perturbation

**Criterion**: alter field offsets/layout through an interface
instance; examples should require only instance proofs.

**Status honesty**: until this slice the layout-swap claim was
ARGUED (Phase 2's evidence was three coexisting layouts through the
same generic rules), not demonstrated by an actual perturbation.
Demonstrated NOW, and the result is stronger than the criterion:

Run (this tree, this slice): `StructExhibit.lean` layout constant
changed `def fieldY : Nat := 8` → `:= 12` (the y field moved to the
allocation's fourth int slot, across the padding gap); NOTHING ELSE
edited (`git diff` at the run showed the single-constant change).
Full package rebuild:

```
info: CerberusHeapLang/Audit.lean:624:0: CerberusHeapLang axiom sweep: 1123 theorems BOUNDED by the declared upper bounds (…)
Build completed successfully (443 jobs).
```

GREEN with ZERO edits beyond the constant — not even the instance
proofs needed touching: the field rules' layout premises
(`by rw [structTy_size]; decide` bounds, `rfl` decode/serialization
facts) recompute at the new offset, and the end-to-end
`struct_update_certified` (whose statement is phrased over
`fieldY`) re-elaborates at the new layout. Reverted after the run;
post-revert build green. The layout constants are referenced ONLY
inside the exhibit module (grep: no `fieldX`/`fieldY` outside
`StructExhibit.lean`), so a layout change CANNOT require a core
edit.

## Test 4 — totality

**Criterion**: Fibonacci and list reversal derive from total WP; no
operational replay in exhibit proofs (zero `Step` constructors /
`driveJ_step` chains in total client proofs).

**Evidence (grep re-run on this tree, verbatim)**:

```
$ grep -nE "Step\.[a-z_]+|driveJ_step|drive_step" CerberusHeapLang/{Fib,ListRev,TreeRot,Loop,Array}Exhibit.lean
CerberusHeapLang/FibExhibit.lean:19:  rule (`wps_pure` / `Step.pure_eval` — one big-step engine
CerberusHeapLang/FibExhibit.lean:38:  `Step` constructors / `driveJ_step` chains remain (the former
CerberusHeapLang/FibExhibit.lean:628:    example-level `Step` constructors, no `driveJ_step` chains
CerberusHeapLang/ListRevExhibit.lean:38:own `PtrEq` memop (Step.lean `Step.memop_ptreq`; the eqPtrval null
CerberusHeapLang/ListRevExhibit.lean:1626:driveJ_step chains, per the audit's acceptance criterion. -/
CerberusHeapLang/ListRevExhibit.lean:1961:    the generic simulation: zero Step constructors, zero driveJ_step
```

All six hits are documentation lines (module headers/docstrings —
each verified by reading the line; five of the six are the
retirement notes themselves). ZERO code occurrences across the five
loop-exhibit modules. The total theorems are corollaries of the
total statement judgment: `fib_certified_total` via `fib_wpt` →
`wpt_engine_boundJ`; `list_reverse_certified_total` at the derived
bound `13·|ns|+7`; `tree_rotate_certified_total` at constant budget
19; the Phase-5 counter total lane (`loop_wpt`, LoopExhibit) rides
the same judgment. Iris `TotalWeakestPre` is consumed by
`wpt_sound`, `TotalAdequacy`'s `twp_total` by
`wpt_strongly_normalizing` (`fib_terminates`,
`list_reverse_terminates`). The negative test
`diverge_total_unprovable` (DivergeExhibit) shows a diverging
loop's total derivation is semantically FALSE.

## Test 5 — state-read census

**Criterion**: a read-set census for every supported Cerberus step
is covered by relational state or explicit immutable context.

**Evidence (re-verified against the current cone this slice)**: the
Phase-1 design record's per-field table
(`docs/2026-08-31_phase1-design-record.md` §1) enumerates every
engine slot of a sequential step (`step_ctx tds σ file ext tid
(parent, th)` + discharge `rs`) with the fragment's read of it.
Re-checked NOW against the tree:

- `MachineCtx` (Step.lean:326-338) carries EXACTLY the table's
  eleven immutable fields: tagDefs, file, extern, tid, parent,
  stack, errno, proc, execLoc, currentLoc, runState — field-for-
  field the design-record enumeration, none dropped or added since.
- Live state is the relational triple `(CoreExpr × EnvStack × Mem)`
  (`Step (M : MachineCtx) : CoreExpr × EnvStack × Mem → … → Prop`,
  Step.lean:812); the label map is DERIVED
  (`MachineCtx.labels`, Step.lean:383 — computed from
  proc/extern/runState.labeled exactly as step_ctx's Erun arm reads
  it), not tuple-carried, so no tie hypotheses remain.
- The reads spot-checked at source: tagDefs (the store rule's
  encoding premise at `M.tagDefs`), extern (`resolveExtern` in the
  evaluator bridge and `labels`), proc/runState (`labels`),
  parent/stack (the `SeqWF` value-protocol constraint), tid
  (passed to requests, discarded by the fragment's memops — the
  `_arg_irrel` bridges).
- The characterization is proved AT ANY `MachineCtx`
  (`engine_step_matchU`, Soundness.lean:4396) — no frozen profile
  remains in any judgment; `spikeCtx`/`procCtx` are instances used
  only where exported statements pin a launch profile.

## Test 6 — production

**Criterion**: at least one loop theorem concludes directly about
the shipped `runND (Driver.drive ...)` computation.

**Evidence**: three, re-read at source this slice
(ProdLoopExhibit.lean:97/:222/:684). Each statement's execution
function is the shipped composite

```
CerbND.runND (_root_.drive tagDefs false file args) (initial_driver_state file fs)
```

(`_root_.drive` IS the shipped driver entry — generated
Driver.lean:500; `initial_driver_state` Driver.lean:435) — with no
package `drive`/`driveJ` in any statement (grep of the three
statement blocks: none; program artifacts like `prodFile` and the
authored program terms are data, not execution functions):

- `fib_certified_production` — back-edge loop, delivers
  `ivVal (fibSpec n.toNat)`; hypotheses `0 ≤ n`, the engine's own
  budget `2·n + 6 ≤ lemDefaultFuel`, fs/argv.
- `counter_loop_certified_production` — HEAP-EFFECTING
  self-contained program (cell engine-created cold), value `Vunit`,
  final bytes pinned data-dependently via `CellCoh` of the final
  `layout_state`.
- `list_reverse_certified_production` — the flagship's
  self-contained two-node demo instance: engine-built chain,
  authored reversal loop, delivered pointer heads
  `SeedChain Q p' lrProdNs.reverse` satisfied by the final
  production memory; hypotheses fs/argv only.

Label maps are computed by the shipped registration
(`collect_labeled_continuations_NEW` — the `*_labeledAt` ties).

## Test 7 — axiom

**Criterion**: exhaustive upper-bound sweep, exact pins for all
public exports, zero `runEffectful` after retirement.

**The retirement is NOT LANDED upstream** (orchestrator-checked at
Phase-5 dispatch, 2026-09-01: lem-lean mainline LemLib.lean:54
still carries the axiom), so this test takes the arc plan's
documented FALLBACK form, strengthened by the Phase-5 origin
discipline. Evidence on this tree:

- **Sweep + origin discipline (in-build, quoted from this slice's
  green gate run)**:

```
info: CerberusHeapLang/Audit.lean:624:0: CerberusHeapLang axiom sweep: 1123 theorems BOUNDED by the declared upper bounds (71 in the production-entry boundary modules, of which 13 carry the boundary axiom — each STATEMENT-BORNE, origin-checked, so every boundary cone is exact-by-construction: trio + runEffectful iff the statement carries it; all other theorems bounded by the trio; headline cones additionally pinned above)
info: CerberusHeapLang/Audit.lean:624:0: CerberusHeapLang banned-axiom sweep: 2075 constants of every kind checked; sorryAx/ofReduceBool/ofReduceNat absent from all cones
```

  The boundary is the minimal statement-carrying module set
  (ProdEntry / ProdExhibit / ProdLoopExhibit); the entire collapse
  machinery (DriverCollapse, ProdLoop) is trio-only; a proof-borne
  boundary axiom fails the build (plant-tested in the Phase-5
  slice).
- **Exact pins for all public exports — COMPLETED THIS SLICE**: the
  curated pin list (Audit.lean `#guard_msgs in #print axioms`,
  62 pins after this slice) now covers every EXPORTED theorem in
  the README's exhibit table plus the manifest's consumer
  regressions; the table's one explicitly-interior row (`wp_store`/
  `wp_load`) carries no curated pins and is sweep-BOUNDED only
  (wording trued at arc close — re-audit finding L3).
  Added this slice (each pinned exactly the trio, verified by the
  green build): `exhibitA_semantic`, `exhibitB_semantic`,
  `engine_adequacyU`, `case_certified`, `wseq_certified`. Every
  boundary export pin is exactly trio + `runEffectful`
  (statement-borne, per the origin discipline).
- **Census re-run NOW, output committed**: the statement-surface
  census was re-run on this tree and its output committed as
  `docs/STATEMENT_CENSUS.txt`, now enforced by gate 5 (below). It
  confirms the Phase-5 statement surfaces: the ghost-functor binder
  survives only on `semantic_triple_sound` (load-bearing — its
  hypothesis is an Iris judgment); the only IRIS-bin constants on
  any other pinned export are iris-lean's finite-map library
  vocabulary (`Iris.Std.PartialMap.singleton`,
  `Std.ExtTreeMap.instPartialMapCompare`).
- **The mover**: upstream retirement remains registered
  (cerberus-lean/lem-lean register; Audit.lean header carries the
  status line). When it lands, the boundary shrinks to the trio at
  a pin bump with no restatement here.

## Test 8 — mutation gates (the slice's build work)

**Criterion**: deleting a fragment constructor, engine match case,
readout field, or measure-decrease premise must make a NAMED gate
fail.

The named gates are: **gate 3** (the cerberus-heaplang build —
kernel + elaborator + the in-build Audit sweeps), **gate 4** (the
capability-manifest coverage gate), **gate 5** (the census
freeze-gate, NEW this slice). House gate doctrine applies
(gates minimal; discipline points get structurally-forcing
consumers, not gate proliferation): where a mutation is already
structurally forced to fail gate 3 by a load-bearing consumer, that
is the named gate, and the plant transcript below is the evidence
it actually fires. All plants were run on this tree this slice:
mutate → red transcript (verbatim) → revert → green.

### (a) Deleting a fragment/cone constructor — gates 3 AND 4

Plant: the `Frag.wseq` constructor deleted (Soundness.lean:3649).
Gate 3 (the build), verbatim (first errors; exit 1):

```
error: CerberusHeapLang/Soundness.lean:3716:2: Invalid alternative name `wseq`: There are no unhandled alternatives
error: CerberusHeapLang/Soundness.lean:3737:6: Invalid alternative name `wseq`: There are no unhandled alternatives
error: CerberusHeapLang/Soundness.lean:3759:8: Invalid alternative name `wseq`: There are no unhandled alternatives
```

(the cone-consuming proofs — decomposition, closure, step-match —
each fail; reverted, green). Independently, gate 4's derived
coverage catches the counterfactual where the build were repaired
around the deletion: with the row gone from the enumeration, the
`Step.wseq_*` mirror constructors become unclaimed. Re-planted this
slice through the identical code path (the S1c plant method:
`Step.wseq_ctx` removed from the wseq row's mirror cell); generator
run, verbatim (exit 1):

```
scripts/capability_manifest.lean:427:0: error: manifest FAIL: mirror constructor CerberusHeapLang.Step.wseq_ctx has NO manifest row — the Step relation was extended without the manifest (fail-closed by design; add it to a row's mirror cell, or give the new construct its own row).
```

Reverted; committed-manifest DRIFT (the other gate-4 channel a
deletion opens) was plant-tested at S1c (Plant C,
`docs/2026-08-31_foundations-notes.md`) and the drift code path is
re-exercised green on every gate run.

### (b) Deleting an engine match case — gate 3 (structurally forced); documented plant

Plant: the `load` arm of `engine_step_matchU`'s redex case analysis
deleted (Soundness.lean:4424-4436). Gate 3, verbatim (exit 1):

```
error: CerberusHeapLang/Soundness.lean:4411:4: Alternative `load` has not been provided
```

Reverted, green. **Why no additional in-build gate-form**: the
match-completeness property IS enforced in-build — the elaborator's
case-exhaustiveness check inside gate 3 rejects any
`engine_step_matchU` (or `Frag.decomp`/`Frag.step`) proof that
drops a cone case, because the theorem quantifies over `Frag`
membership and a missing alternative is an elaboration error, never
a weaker theorem. A separate gate re-checking what the kernel
already forces would be vacuous duplication (gates-minimal ruling);
the documented plant above is the periodic evidence the forcing is
real.

### (c) Deleting a readout field — gate 3 (structurally forced) + gate 5 for the statement surface; documented plant

Plant: the `bytes` field of `CellCoh` — THE readout field: it is
what makes "final bytes pinned" a claim in every readout-bearing
export — deleted (Heap.lean:257). Gate 3, verbatim (first errors;
exit 1):

```
error: CerberusHeapLang/Heap.lean:342:13: Invalid field `bytes`: The environment does not contain `CerberusHeapLang.CellCoh.bytes`, so it is not possible to project the field `bytes` from an expression
error: CerberusHeapLang/Heap.lean:608:36: Invalid field `bytes`: The environment does not contain `CerberusHeapLang.CellCoh.bytes`, so it is not possible to project the field `bytes` from an expression
```

Reverted, green. **Why this is structurally forced, not luck**: the
readout fields are LOAD-BEARING PREMISES of the certification chain
— `loadM_success`/`loadM_at` consume `hcoh.bytes` to compute the
engine's load result (Heap.lean:342), and the coupling readout
consumes it again at :608 — so a deleted readout field cannot ride
a green build. The complementary channel (weakening the readout
VOCABULARY in an export's statement — e.g. replacing `CellCoh` by a
weaker predicate) is caught by gate 5: any statement-surface change
to a pinned export drifts the committed census (plant below).

### (d) Deleting a measure-decrease premise — gate 3 (structurally forced); documented plant

Plant (the Phase-3 design's predicted tripwire, executed): the
mandatory decrease `⌜1 + m ≤ k⌝ ∗` removed from `wpt.pre`'s jump
clause (Wpt.lean:126) together with its mirror in the `wpt_jump_eq`
equation statement (:167) — the consistent mutation, so the failure
lands on the CONSUMERS of the decrease, not on a local equation
mismatch. Gate 3, verbatim (exit 1):

```
error: CerberusHeapLang/Wpt.lean:209:8: No goals to be solved
error: CerberusHeapLang/Wpt.lean:239:8: No goals to be solved
error: CerberusHeapLang/Wpt.lean:354:2: isplit: Ls l m vs (ev0 :: evs) is not a conjunction
error: CerberusHeapLang/Wpt.lean:766:8: No goals to be solved
error: CerberusHeapLang/Wpt.lean:838:2: No goals to be solved
error: CerberusHeapLang/Wpt.lean:1863:18: icases: Ls l m vs ρ is not an existential quantifier
```

Line 1863 is inside `wpt_sound` (Wpt.lean:1833) — the collapse into
Iris TotalWeakestPre destructures the decrease in its jump case and
BREAKS without it, exactly as the Phase-3 record predicted ("the
collapse's and the simulation's inductions are ON the budget");
line 354 is `wpt_run`, the jump rule. Reverted, green. The SEMANTIC
half of this tripwire is permanent in-tree:
`diverge_total_unprovable` proves any total derivation for the
self-jump loop FALSE — a decrease-free total judgment that somehow
re-proved the collapse would make that exhibit's premise satisfiable
and its conclusion False.

### Gate 5 — the census freeze-gate (implemented this slice) + its plants

The registered future gate (statement_census expected partitions
frozen in-build) is IMPLEMENTED as `scripts/test_unit.sh` gate 5:
the census re-runs and any drift against the committed
`docs/STATEMENT_CENSUS.txt` fails, fail-closed (missing file, red
generator, drift — each a failure). Plants, this tree:

Direction RED 1 (drift — end-to-end through the full gate script):
a line planted in the committed census; full `./scripts/test_unit.sh`
run, gates 1-4 green then, verbatim:

```
== gate 5: statement-surface census freeze (pinned-export statement surfaces) ==
--- cerberus-heaplang/docs/STATEMENT_CENSUS.txt	2026-09-01 12:00:33.298234765 +0000
+++ /home/dev/projects/cerberus-lean-proj/.tmp/statement_census.SnX0ve	2026-09-01 12:05:02.073304979 +0000
-    PLANT-DRIFT-LINE
FAIL: statement census drift (diff above) — a pinned export's statement surface changed; re-baseline docs/STATEMENT_CENSUS.txt deliberately, same commit
GATE FAILURE
```

(exit 1). Reverted.

Direction RED 2 (fail-closed on a missing pinned theorem): a bogus
name added to the script's pinned list; generator run, verbatim
(exit 1; the gate's red-generator branch fails on this exit):

```
scripts/statement_census.lean:76:0: error: census: pinned theorem CerberusHeapLang.plant_no_such_theorem not found (FAIL)
```

Reverted. Direction GREEN: the full gate run at slice close (below).

## Gate close (this tree, slice end — verbatim tail)

```
== gate 1: kernel-method grep ban ==
ok: no banned proof-method references
== gate 2: capped build, root package (elaborates its in-build axiom audit) ==
ok: root build green (axiom sweep + pins passed in-build)
== gate 3: capped build, cerberus-heaplang demo (its own audit rides) ==
ok: cerberus-heaplang build green (axiom sweep + pins passed in-build)
== gate 4: capability manifest (drift + README certified-scope tie) ==
ok: capability manifest regenerated, no drift
ok: README certified-scope tokens all within the manifest's adequacy-exportable set
== gate 5: statement-surface census freeze (pinned-export statement surfaces) ==
ok: statement census regenerated, no drift
ALL GATES GREEN
```

## Findings of this slice (re-verification failures — reported, then fixed here)

1. **Stale expected-tail blocks** (claims surfaces): README "How to
   build" and walkthrough §6 still quoted the pre-Phase-5 sweep
   tail (1067 theorems / 40 boundary / 1995 constants / old
   message text); the tree's actual tail is 1123 / 71 (13
   statement-borne) / 2075 with the origin-discipline message.
   Phase 5 changed the sweep output and did not refresh the quoted
   blocks. FIXED in this slice's records commit (blocks re-pasted
   from this tree's green build).
2. **Stale GF-binder claim** (walkthrough §5.4 item 1): it still
   said `counter_loop_certified`/`fib_certified`/
   `array_sum_certified` carry the ghost-functor binder; Phase 5
   removed those binders (its sanctioned statement-change list §4).
   The re-run census confirms the binder survives only on
   `semantic_triple_sound`. FIXED in the same commit.
3. **Pin-coverage gap vs test 7's wording** (minor):
   `exhibitA_semantic`, `exhibitB_semantic`, `engine_adequacyU`,
   `case_certified`, `wseq_certified` were sweep-BOUNDED but not
   exactly pinned. CLOSED this slice (five pins added, all exactly
   the trio).

No test FAILED re-verification; test 3's demonstration debt (argued
vs demonstrated) was called out by the charter and is paid above.
