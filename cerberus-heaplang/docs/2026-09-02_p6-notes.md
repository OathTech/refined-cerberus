# 2026-09-02 — ALLOC ARC P6: the authoritative documentation rewrite (R-10)

Worker record. Provenance: the arc charter's P6 (the 2026-09-01
skeptical re-audit, `../../docs/2026-09-01_cerberus-heaplang-skeptical-re-audit.md`
§P6 and finding R-10), read under the [USER 2026-09-02] rulings in
`../../docs/DECISIONS.md` (speedbumps not gates; P3.5; one change at a
time; parametric interfaces DEFERRED; feature freeze + roadmap; TWO
TRUST CLAIMS; the projection theorem; the demo is classical SL over
Core / derisking on a copy / the demo seeds the next logic) and the
shop-window doctrine ([USER]: front documents describe the system AS IT
IS; history migrates to dated records). Slice classification: DOCS
ONLY — no `.lean` statement or proof changed (the header/docstring
edits are listed in §3 with their justifying theorems); the FULL gate
was run once before the closing commit (§5). Quoted outputs are
verbatim; tallies marked DERIVED are computed as stated.

The charter's "generated scope ledger with five statuses per construct"
is CUT by the [USER 2026-09-02] gates ruling (no enumerative tables):
the generated capability manifest (one row per `Frag` constructor) is
the per-construct authority, and the trust diagram replaces the ledger.

## 1. What was rewritten

**`README.md`** — rewritten (not patched) around the spine: (a) what
this is, in the operator's framing (classical SL over the real Cerberus
Core, on iris-lean, exports about cerberus-lean's execution and memory
states) and the exact scope with what is deliberately NOT here, pointing
at the roadmap ruling; (b) THE CLAIM — the [USER] target shape mapped
symbol by symbol onto `MemTripleU`/`project_triple`/`Sat`/`driveU`, the
production lane, and the exhibits table; (c) THE TRUST STORY structured
by the two trust claims, then what is taken on faith (kernel, the
generated semantics + its differential validation, the pure readout
predicates), then the registered seams (fuel; synthetic Core entry;
frozen well-formedness; the recorded donor divergences — fractional
metadata as the exclusivity anchor, the discarded-fraction persistent
stratum instead of a killable liveness token; no `wp_bind` because
`Erun` discards its context; memory orders), then "two presentations,
one engine"; (d) ONE theorem-labelled trust diagram (every arrow a
theorem name with direction, fragment and axiom cone); (e) the rule
families, one line each; (f) the public API and the readiness smoke
test as the worked client; (g) build/verify with the current gate tail
(107 pins) and the speedbumps described as speedbumps; (h) the
divergences/seams table pruned of closed rows (one line points at the
closure table); (i) the module table, current; a single Records section
at the end holding every dated reference.

**`docs/WALKTHROUGH.md`** — restructured as "this is the theorem, this
is how to read it": §1 the claim (`MemTripleU`, `project_triple`
verbatim) and one exhibit end to end (the reversal program → the
production statement `list_reverse_certified_production` verbatim → the
drive-lane statement described → the Iris triple `lr_wps_frame` +
`isList` verbatim → the rules used); §2 the trust tiers (the two claims,
the diagram's arrows in order, what is taken on faith, why a wrong
mirror cannot make a false statement provable, the readout predicates
printed: `DriveResult`, `stepOutcomes`, `driveU`, `SpikeCell`, `cellPtr`,
`Coh`, `Sat`, `SeedChain`); §3 the logic (verbatim: `wp_store`,
`triple_frame`, `frameLs` + `wps_frame_labels`, `wps_create`,
`blockSpecs_intro`, `wpt.pre` + `wpt`, `wpt_sound`); §4 the memory-model
view (the three ghost heaps, `cellOwn`/`pointsToCell`/`allocMeta`/
`locInBounds`/`AllocReq`/`allocCap` verbatim, the tag-environment
parameter, the allocation-failure policy — Cerberus's refusal/kill vs
RefinedC's `AllocFailed`); §5 the engine attachment (the `Language`
instance, `CerberusRound`, `engine_step_matchU` verbatim, the round
classification, `engine_adequacyU`/`wpt_engine_boundU` described,
`prod_run_eqJ` verbatim, the supply quantifier, `exhibitA_prod`); §6
reading the audit; §7 what is deliberately out and why. The retired
census pastes and every "Observed output (2026-08-31)" block are gone;
the manifest is referenced, not pasted.

Verbatim discipline, checked mechanically (DERIVED: a script extracted
every `lean` fenced block from both documents and looked each non-empty
line up, `rstrip`-exact, among the lines of `CerberusHeapLang/**/*.lean`
+ `scripts/*.lean`):

```
== verbatim check cerberus-heaplang/docs/WALKTHROUGH.md: 22 lean blocks, 0 lines not found verbatim in the tree
== verbatim check cerberus-heaplang/README.md: 1 lean blocks, 0 lines not found verbatim in the tree
```

Identifier discipline (DERIVED, same script: every backticked name in
both documents looked up as a token of the package or its scripts): the
only unresolved names are non-Lean terms — Core surface syntax
(`Eunseq`, `Unspecified`, `goto`), RefinedC's `AllocFailed`, the
semantics workspace's `md5Hex`/`digestIO` and `relsemcore`, the root
package `RefinedCerberus`, the banned methods `native_decide`/
`bv_decide`, the `UNCAPPED` warning text, and law-name suffix fragments
(`_join`, `_agree`, …) written as `pointsToView_split`/`_join`.

## 2. Line counts (DERIVED, `wc -l`)

| File | Before (b84c86f) | After |
|---|---:|---:|
| `README.md` | 592 | 497 |
| `docs/WALKTHROUGH.md` | 1152 | 980 |
| `docs/2026-09-01_alloc-arc-plan.md` | 54 | 54 (the R-10 row restated) |

Targets were ≲ 450 and ≲ 800. Both overshoot: the README by the
exhibits table (one line per exhibit family, 15 rows) and the module
table (20 rows); the WALKTHROUGH by the verbatim rule statements the
brief asked for (22 fenced blocks, 6 of them 15-25 lines each —
`project_triple`, `list_reverse_certified_production`, `wp_store`,
`wps_create`, `wpt.pre`, `prod_run_eqJ`). Judgement call [AGENT]: the
verbatim statements are the professor-standard content and were kept;
a further cut would have to drop one of them.

`docs/2026-*` references in the README body: 21 before → 0 outside the
Records section after (DERIVED: `grep -n "docs/2026-" README.md` hits
only lines 484-491, the Records section).

## 3. Stale-claim fixes (each with the theorem that justifies the new text)

Claims-surface edits in `.lean` files — comment/docstring text ONLY (no
statement, proof or import changed):

| Surface | Stale text | New text | Justification in the tree |
|---|---|---|---|
| `CerberusHeapLang/Wps.lean` header ("THE ALLOCATION RULES") | "HONESTY LINE (R-02, owner alloc arc P2): the allocating production exhibits do NOT yet consume these rules — their cold-start creates still cross the driver as handwritten certified operational rounds …" | the consumer list: `struct_create_store_wps`/`struct_create_store_adequacy`, `alloc_two_creates_wps`/`alloc_create_wpt`/`alloc_create_launch_smoke`, `progAProd_wpt`/`ctrProd_wpt`/`lrProd_wpt` through `wpt_create` and `wpt_driver_done_alloc` → `prod_run_eqJ`; no positive exhibit contains a handwritten operational create round | `progAProd_wpt` (ProdExhibit.lean:133), `ctrProd_wpt` (ProdLoopExhibit.lean:522), `lrProd_wpt` (ProdLoopExhibit.lean:1092) each apply `wpt_create` (10 textual uses in ProdLoopExhibit); closure-table row R-02 (plant B transcript: renaming `wpt_create` breaks all three) |
| `CerberusHeapLang/Wps.lean` `wps_create` docstring | "pure form pending the P4 metadata split" | "kept in pure form — the persistent-metadata form of bounds knowledge is `pointsToView_locInBounds`, Heap.lean" | `pointsToView_locInBounds` (Heap.lean:2025), `locInBounds` (Heap.lean:1990) exist; `wps_create`'s statement still carries `⌜0 < addrOf p ∧ addrOf p < 2 ^ 64⌝` (Wps.lean:2503-2514) |
| `CerberusHeapLang/ProdEntry.lean` `sem_triple_prod` docstring | "is a RETIREMENT CANDIDATE for the P5 scaffolding pass (re-audit R-07)" | "The P5 scaffolding pass (R-07) did NOT retire it … that decision remains OPEN and operator-visible" | `sem_triple_prod` (ProdEntry.lean:393) still present and pinned in `Audit.lean`'s `trioExports`; the P5 record (`2026-09-02_p5-notes.md`) does not mention it — FINDING, see §6 |
| `CerberusHeapLang/Audit.lean` header | "the export list below (62 names, same exact assertion each)" | "(62 names at P3.5 …; the list grows with every spec-addition slice — the build prints the current count)" | the build tail: `export pins: 107 trio-exact` (§5) |
| `CerberusHeapLang/Audit.lean` `trioExports` docstring | "the README table and WALKTHROUGH §7 name these" | "the README's exhibits table and trust diagram, and WALKTHROUGH §6, name these" | the rewritten documents' section structure |
| `CerberusHeapLang/Adequacy.lean` header | "Three layers:" (followed by four numbered items) | "Four layers:" | items 1-4 of the same header (`drive`, `spike_step_adequacy`, `spike_engine_adequacy`, `project_triple`) |

Claims-surface fixes in the documents (the old text is gone with the
rewrite; listed because the brief named them):

| Old surface | Stale text | Truth, with justification |
|---|---|---|
| README:366 | "The two allocating LOOP exports do NOT yet consume the path — P2 steps 4-5." | They do: `ctrProd_wpt`/`lrProd_wpt` (ProdLoopExhibit.lean) go through `wpt_create`; closure row R-02 CLOSED [AGENT 2026-09-01, P2] |
| README:383-388 | "`sem_triple_prod` … is a retirement candidate for the P5 scaffolding pass" | not retired at P5; the divergences table now carries it as an OPEN operator decision (§6) |
| README:426-432 | the ~~struck~~ rows for `runEffectful`, R-02, R-04 | removed; one line points at the closure table (`2026-09-01_alloc-arc-plan.md`) |
| README:479-482, WALKTHROUGH:1046-1049 | gate tail "62 trio-exact / 1115 theorems / 1948 constants / 441 jobs" | current tail: 107 / 1184 / 2030 / 444 (§5) |
| README:516-531, WALKTHROUGH:1082-1088 | "Observed output (2026-09-01 …)" `#print axioms` pastes | removed; the recipe stays with the expected line |
| WALKTHROUGH §3.1 | `wp_store` quoted at `fmapEmpty`/`Q` (pre-re-pin spelling) | current statement at `M.tagDefs`/`M` (Rules.lean:177), verbatim-checked |
| WALKTHROUGH §3.2 | `SemTriple` quoted at "Adequacy.lean:371" without `tds`; `Coh` without `tds` | `MemTripleU` (Adequacy.lean:1201) and `Coh tds σ m` (Heap.lean:331) verbatim; `SemTriple` described as the fixed-profile instance (`SemTriple_iff_U`) |
| WALKTHROUGH §5.1-5.3 | `driveJ` quoted as the old 10-line loop; `exhibitA_prod` quoted without `sup`; census pastes "(2026-08-31, this checkout)" | `driveJ rs aids = driveU (rsCtx rs) aids` (Adequacy.lean:1633); `exhibitA_prod (sup : Nat) …` (ProdExhibit.lean:245); the census instrument was deleted at P3.5 — no paste |
| WALKTHROUGH §1.2 (new) first draft | "`wps_load_at` … `wps_store_at` for the next-field load/store" | corrected before commit to `wps_load_cell_at`/`wps_store_cell_at` (wrapped as `wps_load_node_field`/`wps_store_node_field`): `grep -o` over ListRevExhibit.lean shows 0 uses of `wps_load_at`/`wps_store_at` and 3+3 of the `_cell_at` forms |

Cross-references preserved: the section names other files point at —
"Registered divergences and seams" (Step.lean:39,136; ProdEntry.lean:44;
DriverCollapse.lean:73), "What you are asked to take on faith"
(WALKTHROUGH), the "two presentations, one engine" paragraph
(Round.lean:25) — exist in the new README under those names.

## 4. The search transcript (the P6 closure test)

The charter's P6 test for R-10: "no stale-name/status search hits; a
new reader gets the accurate end-to-end trust story from the headline
documents alone". The second half is the orchestrator's fresh-eyes
dispatch. The first half, run on the closing tree from
`cerberus-heaplang/`:

```
$ grep -n -E "DecompJ\.step_factor|engine_step_matchJ|blockSpecs_intro_variant|IsXFrame|StmtProbe|NO wps_create|local rule only|Ecase stays OUT|per-byte split|production loop.*pending|do NOT yet|not yet consume|HONESTY LINE|RETIREMENT CANDIDATE|P2 steps 4-5|MIXED" README.md docs/WALKTHROUGH.md docs/CAPABILITY_MANIFEST.md CerberusHeapLang/*.lean CerberusHeapLang/Examples/*.lean scripts/*.lean
CerberusHeapLang/Audit.lean:41:StmtProbe pins went with the deleted probe.
CerberusHeapLang/LoopExhibit.lean:36:former exact-shape pin `IsXFrame` (a one-node `Fmap` tree at `xSym`)
CerberusHeapLang/Wps.lean:2603:/-! `blockSpecs_intro_variant` (the invariant+variant-shaped lemma
CerberusHeapLang/ProdLoopExhibit.lean:29:PROOF CLASSIFICATION (alloc arc P2 steps 4-5 — the R-02 conversion):
CerberusHeapLang/Soundness.lean:3561:old S3 note that "Ecase stays OUT" is superseded): the straight-line
CerberusHeapLang/Soundness.lean:3573:retired J-lane `engine_step_matchJ`): wherever the MIRROR steps, the
CerberusHeapLang/Soundness.lean:4387:/-- THE UNIFIED STEP-MATCH (S1b — the old `engine_step_matchJ` over
CerberusHeapLang/Wpt.lean:56:`blockSpecs_intro_variant` (the audit's F-02 zero-consumer variant
CerberusHeapLang/Wpt.lean:2204:    `blockSpecs_intro_variant`: because the body is verified at
rc=0
```

(Run on the closing tree after every edit of this slice; `rc=0` is
grep's "hits found".)

Zero hits in README.md, WALKTHROUGH.md and the manifest. Every module
hit is a RETIREMENT RECORD naming the retired thing as retired
("deleted probe", "former … pin", "is RETIRED" — Wpt.lean:57 and
Wps.lean:2605 are the continuation lines of the two `blockSpecs_intro_variant`
hits, Wpt.lean:2204 is inside the docstring "replaces the retired
`blockSpecs_intro_variant`", "superseded", "retired J-lane", "the old
`engine_step_matchJ`") or a completed-step provenance label ("alloc arc
P2 steps 4-5 — the R-02 conversion"), none presents a retired name or a
pending status as live. Re-run with the retirement-record lines
excluded:

```
$ grep -n -E "<the same pattern>" <the same files> | grep -v -i -E "retired|former|superseded|deleted|is gone|the old |R-02 conversion"
CerberusHeapLang/Wps.lean:2603:/-! `blockSpecs_intro_variant` (the invariant+variant-shaped lemma
CerberusHeapLang/Wpt.lean:56:`blockSpecs_intro_variant` (the audit's F-02 zero-consumer variant
CerberusHeapLang/Wpt.lean:2204:    `blockSpecs_intro_variant`: because the body is verified at
```

— the three multi-line retirement notes whose "RETIRED"/"retired" word
falls on the following line (Wpt.lean:57 "rule) is RETIRED in this
slice"; Wps.lean:2605 "meta-level hypotheses) is RETIRED (foundations
Phase 3"; Wpt.lean:2203 "the real total rule that replaces the retired").
No live stale claim remains. The broader status-word sweep over the two
documents (`pending|not yet|TODO|planned|interim|will be|~~|retirement
candidate|census output|Observed output|62 trio-exact|1115 theorems|1948
constants|441 jobs|runEffectful|cursorOwn|…`) hits only the `~~>` of the
[USER] target shape and the two by-design mentions of `cursorOwn` as the
INTERNAL resource `allocCap` hides (README diagram; WALKTHROUGH §4).

## 5. Gates

Docs-only slice; the comment-only edits in `Wps.lean`, `Adequacy.lean`,
`Audit.lean` and `ProdEntry.lean` still rebuild the package (Lean
rebuilds on any source change), so the FULL run is the real build. FULL gate (`scripts/test_unit.sh`, the claim point) on the
closing tree, verdict lines verbatim (linter warnings from the
dependency's `generated/*` elided):

```
== gate 1: banned proof-method grep (native_decide / bv_decide / ofReduce*) ==
ok: no banned proof-method references
== gate 2: capped build, root package (elaborates its axiom audit) ==
Build completed successfully (370 jobs).
ok: root build green
== gate 3: capped build, cerberus-heaplang (elaborates its axiom audit) ==
info: CerberusHeapLang/Audit.lean:164:0: CerberusHeapLang export pins: 107 trio-exact
info: CerberusHeapLang/Audit.lean:164:0: CerberusHeapLang axiom sweep: 1184 theorems bounded by the trio
info: CerberusHeapLang/Audit.lean:164:0: CerberusHeapLang banned-axiom sweep: 2030 constants of every kind checked; sorryAx/ofReduceBool/ofReduceNat absent from all cones
✔ [443/444] Built CerberusHeapLang (815ms)
Build completed successfully (444 jobs).
ok: cerberus-heaplang build green
== speedbump: capability manifest (regenerate; red on a red row or drift) ==
cerberus-lean-proj env: switch=/home/dev/projects/cerberus-lean-proj/cerberus-lean/_opam, git redirects active
ok: capability manifest regenerated, no drift
== speedbump: import direction (semantics → heap → rules → adequacy → clients) ==
ok: import direction — no core module imports an exhibit/example/production module
ALL GATES GREEN
./scripts/test_unit.sh  396.08s user 19.24s system 129% cpu 5:21.01 total
rc=0
```

The Audit line moved from `162:0` to `164:0` (two header lines added in
`Audit.lean` §3); the expected tails quoted in README and WALKTHROUGH
carry the new position. The capability manifest did not drift (no
consumer change).

## 6. Borderline items and findings (reported, not softened)

1. **`sem_triple_prod`/`prod_run_eq` — an exported face with no
   consumer, retirement decision open.** The ProdEntry.lean docstring
   called it a "RETIREMENT CANDIDATE for the P5 scaffolding pass"; P5
   closed (R-07) without touching it and without recording a decision.
   Trued to "not retired at P5; decision OPEN, operator-visible" and
   carried in the README's divergences table. Not a logic gap (the
   theorem is proved, trio-exact, and its operational premises are on
   its face); a surface-hygiene decision for the operator.
2. **Value-scrutinee `Ecase` and wildcard `Ewseq` are partial-stratum
   only** (`wps_case_value`, `wps_wseq`; no `wpt_` counterpart exists —
   DERIVED: `grep "^theorem wpt_case\|^theorem wpt_wseq" Wpt.lean` is
   empty). The old README's scope prose did not distinguish strata; the
   new README's scope section states it. Consistent with the
   no-consumer rule (no total consumer of either construct exists).
3. **Line-count targets overshot** (§2), by the verbatim statements the
   brief asked for. Reported as a judgement call, not hidden.
4. **`MachineCtx.SeqWF` as "a premise of every adequacy theorem"** was
   softened on measurement: it is explicit on the theorems stated at a
   general context (`engine_adequacyU`, `project_triple`,
   `wpt_engine_boundU`, `cerberusRound_classify`) and discharged
   internally at the demo profiles (`spikeCtx_wf`, `procCtx_wf` — the
   `engine_adequacyJ` and `spike_engine_adequacy` faces carry none).
5. **The differential-validation lane numbers** in "What you are asked
   to take on faith" were re-checked against the pinned workspace's
   `VALIDATION.md` (106/106, 213/213, 16/16, 1,669 csmith, 2,186-file
   sweep with 1,316 comparable): present at the pin; the authority
   pointer stays on that file.
6. **The remaining module-header mentions of retired names** (§4) are
   deliberate retirement records. If a later pass wants a zero-hit
   search without an exclusion list, they would have to be deleted,
   which erases the record of what was retired and why; not done here.
7. **DECISIONS.md not touched** (the orchestrator's register). The
   closure-table row R-10 in `2026-09-01_alloc-arc-plan.md` is flipped
   to CLOSED [AGENT 2026-09-02, P6] with the test as run.
