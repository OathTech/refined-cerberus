# 2026-09-02 — ALLOC ARC P5: module layering + the readiness smoke test (R-07)

Worker record. Provenance: the charter is the adopted skeptical re-audit
(`docs/2026-09-01_cerberus-heaplang-skeptical-re-audit.md` §P5, finding
R-07), RE-SCOPED under [USER 2026-09-02] "speedbumps, not adversarial
gates" (no dependency-graph tool, no enumerative table; the import
direction is a grep) and [USER 2026-09-02] "parametric interfaces
DEFERRED" (not used). Slice classification ([USER 2026-09-02] one-change-
at-a-time): an INTERNALS/LAYERING slice plus ONE real consumer — public
rule/adequacy statements FROZEN, verified by `scripts/signature_snapshot.lean`
pre/post (§5). Quoted outputs are verbatim; tallies marked DERIVED are
computed by the scripts quoted.

Commits (both on `heaplang-alloc-arc`): P5.1-2 `1b24f03` (fast-gate: the
moves + API + import rewiring) and the closing commit (this file; the
smoke test, the grep speedbump, the audit pins, the records; FULL gate —
§8).

## 1. R-07: the example constants leave the rule modules

New module `CerberusHeapLang/Examples/Layout.lean` (334 lines: a 40-line
header + the moved text), imported by the exhibits. MOVED, statement-
unchanged (the pre/post snapshot has no line for any of them — §5):

| From | Constants (20) | Lines removed |
|---|---|---|
| `Rules.lean` (tail, from `/-! ## The exhibit -/` to `end`) | `intTy`, `sevenVal`, `sevenMval`, `sevenBytes`, `seven_encodes`, `seven_storable`, `exhibit`, `fiveVal`, `fiveMval`, `fiveBytes`, `five_encodes`, `five_storable`, `sixVal`, `sixMval`, `sixBytes`, `six_encodes`, `six_storable`, `exhibitC_triple` (18) + the anti-frame negative transcript (comment) | 208 (1245 → 1038 lines; the header gained 4) |
| `Wps.lean` (tail, `/-! ## Coverage preservation …`) | `wps_exhibit_store_frame`, `wps_exhibit_seq_stores` (2) | 72 (2817 → 2746) |
| `Wpt.lean` | — (its one hit was the prose word "exhibit" in a comment) | 0 |

DERIVED (grep over the three rule modules for the 20 names + the word
`exhibit`, `\b…\b`): BEFORE (0751751) Rules 75 / Wps 31 / Wpt 1 matching
lines; AFTER Rules 2 / Wps 3 / Wpt 1 — every remaining hit is the prose
word "exhibit"/"exhibits" or the new header pointer naming
`exhibitC_triple`; zero identifier references. (The brief's "70 reference
lines" was measured with a different pattern; this is the one used here.)

FINDING CHECK ("if a rule genuinely depends on an example constant, that
is a finding"): NONE. The cut compiled with no edit to any rule; the
rules' own type-level side conditions are stated over `M.tagDefs` and an
arbitrary `ty`, never `intTy`.

Header edits: `Rules.lean` header no longer advertises "the first derived
exhibit"/the anti-frame check (a pointer to `Examples/Layout.lean`
replaces the two bullets); `Exhibit.lean` header cites
`exhibit`/`exhibitC_triple` at their new home.

## 2. `API.lean` and the import rewiring

`CerberusHeapLang/API.lean` (72 lines, no declarations): imports `Heap`,
`EnvLaws`, `Rules`, `Wps`, `Wpt`, `Adequacy`, `TotalAdequacy`; every
public name already lives in `namespace CerberusHeapLang`, so no `export`
re-declarations are needed. HONESTY LINE (in the header): Lean 4 imports
are transitive with no export control, so the public/internal line is
DOCUMENTED, not enforced; it is MEASURED by the client-module section of
`scripts/parametric_inventory.lean` (§4). The header table, verbatim:

## The surface (PUBLIC — import and use freely)

| Family | Names (module) |
|---|---|
| Program vocabulary | `CoreExpr`, `EnvStack`, `SpikeVal` (`.pure`/`.annot`, `mergeInto`), `ofVal`, `MachineCtx` (`.tagDefs`, `.extern`, `.labels`, `.SeqWF`), `spikeCtx`, `spikeEnv`, `symPat`, `storeOpRedex` (Step); `storeExpr`, `loadExpr`, `sseqExpr`, `addrOf`, `loadedVal` (Rules); `createExpr` (Wps); `Frag` — fragment membership, the adequacy premise (Soundness) |
| Pointer/location assertions | `cellPtr`, `SpikeCell`, `pointsToCell` (`pv ↦c[tds]{dq} ty ; bs`), `pointsToView` (typed sub-range view), `cellOwn` (whole-allocation bundle at a ghost id), `allocMeta` (persistent allocation knowledge), `locInBounds`, `allocCap` (abstract allocation capacity), `AllocReq` (Heap) |
| Assertion laws | `pointsToCell_cellOwn_iff`, `cellOwn_view`, `pointsToView_split`, `pointsToView_join`, `pointsToView_fractional`, `pointsToView_agree`, `pointsToView_persist`, `pointsToView_locInBounds`, `cellOwn_fractional`, `pointsToCell_fractional`, `pointsToCell_agree`, `pointsToCell_combine`, `allocMeta_persistent`, `allocMeta_dup`, `allocMeta_agree`, `locInBounds_persistent`, `cellPtr_inj`, `cellPtr_arrayShift` (provenance-preserving shift), `allocCap_intro`, `allocCap_weaken` (Heap) |
| Side-condition vocabulary (appears in rule statements) | `StorableAt`, `decIndep`, `atomicTy`, `loadTrapV`, `cellLoadTrap`, `undefByte`, `spliceBytes` with `spliceBytes_length`/`_getElem?`/`_slice_below`/`_slice_self`/`_slice_above` (Heap) |
| Environment seam | `SymFrame`, `symFrame_empty`, `SymFrame.add`, `envAdd`, `envAdd_lookup`, `symCmpK`, `update_env_sym`, `update_env_spec`, `lookup_env_head` (EnvLaws) |
| Base logic | `triple`, `triple_frame`, `triple_conseq`, `triple_seq`, `wp_store`, `wp_load`, `wp_ofVal`, `wp_sseq`, `wp_annot`, `wp_annot_reindex` (Rules) |
| Statement judgment (partial) | `wps`, `LabelSpec`, `blockSpecs`, `frameLs`; structural: `wps_ofVal`, `wps_wand`, `wps_fupd`, `wps_frame`, `wps_frame_labels`, `wps_annot`, `wps_annot_reindex`; control: `wps_seq`, `wps_seq_spec`, `wps_seq_sym`, `wps_if_true`, `wps_if_false`, `wps_save`, `wps_run`, `wps_pure`, `wps_case_value`, `wps_wseq`, `wps_load_eval`, `wps_store_eval`, `wps_memop_eval`, `wps_memop_ptreq`; memory: `wps_store`, `wps_load`, `wps_load_at`, `wps_store_at`, `wps_load_cell_at`, `wps_store_cell_at`, `wps_create`; loops: `blockSpecs_intro`, `blockSpecs_frame`; collapse: `wps_sound`, `wps_sound_frame` (Wps) |
| Statement judgment (total) | `wpt`, `LabelSpecT`, `blockSpecsT`, `frameLsT`; `wpt_ofVal`, `wpt_mono`, `wpt_mono_k`, `wpt_mono_Ls`, `wpt_frame`, `wpt_frame_labels`, `wpt_annot`, `wpt_annot_reindex`, `wpt_seq`, `wpt_seq_spec`, `wpt_seq_sym`, `wpt_if_true`, `wpt_if_false`, `wpt_save`, `wpt_run`, `wpt_pure`, `wpt_load_eval`, `wpt_store_eval`, `wpt_memop_eval`, `wpt_memop_ptreq`, `wpt_load_at`, `wpt_store_at`, `wpt_load_cell_at`, `wpt_store_cell_at`, `wpt_store_cell`, `wpt_create`, `blockSpecsT_frame`, `wpt_sound` (Wpt) |
| Adequacy (partial) | `SemTripleU`, `SemTriple`, `ProvenTripleU`, `ProvenTriple`, `Sat`, `CellMap`, `semantic_triple_soundU`, `semantic_frameU`, `SemTriple_iff_U`, `semantic_triple_sound`, `semantic_frame`; the drive lanes `driveU`, `drive`, `driveJ`, `DriveResult`, `driveJ_step`, `driveJ_done`; the launches `spike_step_adequacy`, `spike_step_adequacy_alloc`, `engine_adequacyU`, `engine_adequacyU_alloc`, `engine_adequacyJ`, `spike_engine_adequacy`, `spike_engine_adequacy_alloc`; launch vocabulary `LaunchCoh`, `launchResources`, `PlanFits` (the plan-fits-the-cold-start-memory obligation, stated over the engine's `lastAddress`/`nextAllocId`); the public readouts `cellOwn_readout`, `pointsToCell_readout` (Adequacy) |
| Adequacy (total) | `wpt_engine_boundU`, `wpt_engine_boundJ`, `wpt_engine_boundU_alloc`, `wpt_engine_boundJ_alloc`, `wpt_strongly_normalizing`, `wpt_strongly_normalizing_alloc`, `readoutPost`, `DriveDoneAt`, `pot`, `Frag.pot_step_bound` (TotalAdequacy) |

## Below the line (INTERNAL — visible, not part of the surface)

| Family | Names | Why internal |
|---|---|---|
| The coupling invariant and the ghost carrier | `CohG`, `SpikeGS`'s fields, `byteOwn`, `metaOwn`, `cursorOwn`, `bytesOwn`, `byteInterp`/`metaInterp`/`cursorInterp`, `SpikeState`, `stateInterp_eq`, `byteHeap_*`, `metaHeap_*`, `cursorHeap_*`, `bytesOwn_get`/`_read`/`_update`, `metaOwn_ne`, `bigSepM_own_disjoint`, `cellsOwn_*`, `cells_readout`, `genHeap_valid_big`, `MetaCell`, `metaOf`, `MetaCoh`, `CellCoh`, `Coh`, `cellOwn_cellCoh` (Heap, Adequacy) | The state interpretation is the logic's implementation. Its two client-facing consequences are exported: the readouts above |
| The allocator cursor | `AllocCursor` (as a resource), `advanceCursor`, `freshBase`, `cursorOwn`, `wps_create_cursor_internal`, `wpt_create_cursor_internal`, `CohG.create`, `allocateObject_success` (Heap, Wps, Wpt) | `allocCap` is the abstract capacity face (alloc arc P1); the cursor is how the heap implements it |
| The engine transition and its certification | `Step` and every `Step.*` lemma, `Decomp`, `jumpRedex?`, `toVal`, `primStep`, the `Language` instance's facts (Lang); all of Soundness except `Frag` (`engine_complete`, `stepDischarge_*`, `engine_step_matchU`, …); all of Round (`CerberusRound`, `cerberusRound_classify`, `step_iff_cerberusRound`) | They certify the rules against the engine. A client reasoning through them bypasses the logic (the R-02 failure mode) |
| Judgment unfoldings | `wps.pre`, `wps_unfold`, `wpt.pre`, `wpt_val_eq`, `wpt_jump_eq`, `wpt_step_eq`, `wpt_zero_step_eq`, `wpt_det_step`, `wpt_drive_aux` | The judgments are used through their rules |
| memM seams and byte-map algebra | `storeM_success`, `loadM_success`, `loadM_at`, `storeM_at`, `writeBytesTo_*`, `readBytesFrom_*`, `byteAt_*`, `MetaByteOf`, `spikeCells_alloc`, `intToBytes_*`, `bytesToInt_*` (Heap, Adequacy) | Engine-memory facts consumed by the rule proofs and the launch |

## Not imported here at all

`DriverCollapse`, `ProdLoop`, `ProdEntry` (the production-export
layer: itself a client of adequacy, imported directly by the
production exhibits), every `*Exhibit`, `Examples.*`, and `Audit`.

Import rewiring (pure import-line changes; every module compiles
unchanged otherwise):

| Module | Before | After |
|---|---|---|
| `Exhibit` | Adequacy, Wpt, TotalAdequacy | API, Examples.Layout |
| `LoopExhibit` | Adequacy, Wps, EnvLaws, Wpt, TotalAdequacy | API, Examples.Layout |
| `CaseExhibit`, `WseqExhibit` | Adequacy, Wps | API |
| `StructExhibit` | Adequacy, Wps, EnvLaws, ProdEntry | API, Examples.Layout, ProdEntry |
| `FibExhibit` | LoopExhibit, TotalAdequacy | API, LoopExhibit |
| `AllocExhibit` | TotalAdequacy, ProdEntry, Exhibit | API, Examples.Layout, ProdEntry, Exhibit |
| `ArrayExhibit` | FibExhibit | API, Examples.Layout, FibExhibit |
| `ProdEntry`, `ProdExhibit`, `ProdLoopExhibit` | (unchanged) | + Examples.Layout (explicit: they consume `intTy`/`sevenBytes`) |
| `DivergeExhibit`, `ListRevExhibit`, `TreeRotExhibit` | FibExhibit / ArrayExhibit / ListRevExhibit | unchanged (exhibit-to-exhibit) |
| lib root `CerberusHeapLang.lean` | — | + API, Examples.Layout, Examples.ReadinessSmoke |
| `Audit.lean` | — | + Examples.ReadinessSmoke |

RESIDUALS (not restructured, per the brief): (a) exhibit-to-exhibit
imports (Fib→Loop, Array→Fib, ListRev→Array, TreeRot→ListRev,
Diverge→Fib, Alloc→Exhibit, Prod*→…) — clients sharing client
definitions, not internal names; (b) `StructExhibit` and `AllocExhibit`
import `ProdEntry` for the production cold-start memory (`prodMem₀`) their
allocation-aware launches are stated at — a production-export module, not
an attachment internal; the API deliberately does not import the
production layer. No exhibit needed an INTERNAL name to keep compiling
(no `open`/name edits were required anywhere).

## 3. The import-direction SPEEDBUMP and its one-time plant

`scripts/test_unit.sh`, after the manifest speedbump (10 lines incl. the
blank; one header line reworded):

```
  echo "== speedbump: import direction (semantics → heap → rules → adequacy → clients) =="
  # No core module may import an exhibit, example-support or production module (a missing core file is red too).
  core=(Step Lang Heap EnvLaws Rules Wps Wpt Soundness Round Adequacy TotalAdequacy DriverCollapse API)
  files=(); for m in "${core[@]}"; do files+=("cerberus-heaplang/CerberusHeapLang/$m.lean"); done
  if ! ls "${files[@]}" > /dev/null || grep -nE '^import CerberusHeapLang\.([A-Za-z]*Exhibit|Examples\.|Prod)' "${files[@]}"; then
    echo "FAIL (speedbump): a core module is missing or imports an exhibit/example/production module (above)" >&2; fail=1
  else
    echo "ok: import direction — no core module imports an exhibit/example/production module"
  fi
```

`EnvLaws` and `API` are in the list beyond the brief's eleven (both sit
below the clients). A missing core file is red (fail-closed) rather than
a grep warning. Plant-tested ONCE by hand under bash (the script's
interpreter), not retained — transcript verbatim (the snippet was
extracted from the script and sourced; `plant.sh` is the extract):

```
$ sed -n "/== speedbump: import direction/,/^  fi\$/p" scripts/test_unit.sh > plant.sh   # the speedbump lines, verbatim
$ bash -c 'fail=0; source plant.sh; echo "fail=$fail"'    # clean tree
== speedbump: import direction (semantics → heap → rules → adequacy → clients) ==
ok: import direction — no core module imports an exhibit/example/production module
fail=0
$ sed -i "s/^import CerberusHeapLang.Step$/import CerberusHeapLang.Step
import CerberusHeapLang.StructExhibit/" cerberus-heaplang/CerberusHeapLang/Heap.lean   # PLANT: Heap imports an exhibit
$ grep -n "^import" cerberus-heaplang/CerberusHeapLang/Heap.lean
105:import Iris
106:import CerberusHeapLang.Step
107:import CerberusHeapLang.StructExhibit
$ bash -c 'fail=0; source plant.sh; echo "fail=$fail"'    # planted tree
== speedbump: import direction (semantics → heap → rules → adequacy → clients) ==
cerberus-heaplang/CerberusHeapLang/Heap.lean:107:import CerberusHeapLang.StructExhibit
FAIL (speedbump): a core module is missing or imports an exhibit/example/production module (above)
fail=1
$ git checkout -- cerberus-heaplang/CerberusHeapLang/Heap.lean   # REVERT the plant
$ git status --short cerberus-heaplang/CerberusHeapLang/Heap.lean; bash -c 'fail=0; source plant.sh; echo "fail=$fail"'    # reverted
== speedbump: import direction (semantics → heap → rules → adequacy → clients) ==
ok: import direction — no core module imports an exhibit/example/production module
fail=0
```

Borderline, recorded: the FIRST plant attempt ran the extract under the
interactive zsh, where the unquoted `$core` of the first draft did not
word-split — the grep saw file names without `.lean`, printed twelve
"No such file" warnings and STILL reported ok (grep's missing-file
warning is not a failure). That is the plant doing its job on the
harness, and it exposed a fail-open in the draft: the snippet was
rewritten to the array form above with the `ls` existence check, and the
plant re-run under bash is the transcript quoted.

## 4. The readiness smoke test — the one real consumer

`CerberusHeapLang/Examples/ReadinessSmoke.lean` (295 lines), imports ONLY
`CerberusHeapLang.API`; namespace `CerberusHeapLang.ReadinessSmoke`. The
layout is ONE `long[2]` allocation (`fieldTy = signed long`, 8 bytes;
`objTy = long[2]`, 16 bytes; both sizes `rfl` at any tag environment);
the second field is addressed as a Core program addresses it, by the
engine's own arithmetic:

```
def fieldTy : ctype := Ctype [] (.Basic (.Integer (.Signed .Long)))
def objTy : ctype := Ctype [] (.Array0 fieldTy (some 2))
abbrev undefField : List CerbMem.AbsByte := List.replicate 8 undefByte
abbrev fieldYPtr (tds : CerbTags.TagDefsMap) (p : CerbMem.PointerValue) : CerbMem.PointerValue :=
  CerbMem.arrayShiftPtrval tds p fieldTy (CerbMem.integerIval 1)

def twoField (tds : CerbTags.TagDefsMap) (p : CerbMem.PointerValue)
    (xb yb : List CerbMem.AbsByte) : IProp GF :=
  iprop(∃ (id a : Int), ⌜p = cellPtr id a⌝ ∗
    pointsToView tds id a objTy 0 (.own (Qp.half 1)) (.own 1) fieldTy xb ∗
    pointsToView tds id a objTy 8 (.own (Qp.half 1)) (.own 1) fieldTy yb)
```

THE DERIVED RULES, statements verbatim (context: `{hlc} {GF} [SpikeGS hlc
GF] {M : MachineCtx} {Ls : LabelSpec GF}`; `twoField_of_cell` is the
allocate rule's bundle→object conversion):

```
theorem twoField_of_cell (tds : CerbTags.TagDefsMap) (p : CerbMem.PointerValue) :
    pointsToCell tds (GF := GF) p (.own 1) objTy
        (List.replicate (CerbMem.sizeofCtype tds objTy) undefByte) ⊢
      twoField tds p undefField undefField :=

theorem twoField_load_x {Ψ : SpikeVal → EnvStack → IProp GF}
    (loc : CerbLocation.Loc) (ann : core_run_annotation)
    (p : CerbMem.PointerValue) (mo : memory_order)
    (xb yb : List CerbMem.AbsByte) (ρ : EnvStack) {mv : CerbMem.MemValue}
    (hdec : ∀ lum fpm ad, CerbMem.reconstructValue M.tagDefs lum fpm ad fieldTy xb = mv)
    (htrap : loadTrapV fieldTy mv = false) :
    iprop(twoField M.tagDefs (GF := GF) p xb yb ∗
      (∀ fp, twoField M.tagDefs p xb yb -∗
        Ψ (SpikeVal.annot [DA_pos [] fp] ((valueFromMemValue mv).2)) ρ)) ⊢
      wps M Ls Ψ (loadExpr loc ann fieldTy p mo) ρ :=

theorem twoField_load_y {Ψ : SpikeVal → EnvStack → IProp GF}
    (loc : CerbLocation.Loc) (ann : core_run_annotation)
    (p : CerbMem.PointerValue) (mo : memory_order)
    (xb yb : List CerbMem.AbsByte) (ρ : EnvStack) {mv : CerbMem.MemValue}
    (hdec : ∀ lum fpm ad, CerbMem.reconstructValue M.tagDefs lum fpm ad fieldTy yb = mv)
    (htrap : loadTrapV fieldTy mv = false) :
    iprop(twoField M.tagDefs (GF := GF) p xb yb ∗
      (∀ fp, twoField M.tagDefs p xb yb -∗
        Ψ (SpikeVal.annot [DA_pos [] fp] ((valueFromMemValue mv).2)) ρ)) ⊢
      wps M Ls Ψ (loadExpr loc ann fieldTy (fieldYPtr M.tagDefs p) mo) ρ :=

theorem twoField_store_x {Ψ : SpikeVal → EnvStack → IProp GF}
    (loc : CerbLocation.Loc) (ann : core_run_annotation)
    (p : CerbMem.PointerValue) (cv : value) (mo : memory_order)
    (xb yb : List CerbMem.AbsByte) (ρ : EnvStack) {mv : CerbMem.MemValue}
    (hmv : memValueFromValue M.tagDefs (Ctype [] (unatomic_ fieldTy)) cv = some mv)
    (hst : StorableAt M.tagDefs fieldTy mv) :
    iprop(twoField M.tagDefs (GF := GF) p xb yb ∗
      (∀ fp, twoField M.tagDefs p (CerbMem.memValueToBytes M.tagDefs [] mv).2 yb -∗
        Ψ (SpikeVal.annot [DA_pos [] fp] Vunit) ρ)) ⊢
      wps M Ls Ψ (storeExpr loc ann fieldTy p cv mo) ρ :=

theorem twoField_store_y {Ψ : SpikeVal → EnvStack → IProp GF}
    (loc : CerbLocation.Loc) (ann : core_run_annotation)
    (p : CerbMem.PointerValue) (cv : value) (mo : memory_order)
    (xb yb : List CerbMem.AbsByte) (ρ : EnvStack) {mv : CerbMem.MemValue}
    (hmv : memValueFromValue M.tagDefs (Ctype [] (unatomic_ fieldTy)) cv = some mv)
    (hst : StorableAt M.tagDefs fieldTy mv) :
    iprop(twoField M.tagDefs (GF := GF) p xb yb ∗
      (∀ fp, twoField M.tagDefs p xb (CerbMem.memValueToBytes M.tagDefs [] mv).2 -∗
        Ψ (SpikeVal.annot [DA_pos [] fp] Vunit) ρ)) ⊢
      wps M Ls Ψ (storeExpr loc ann fieldTy (fieldYPtr M.tagDefs p) cv mo) ρ :=

theorem twoField_create {Ψ : SpikeVal → EnvStack → IProp GF}
    (loc : CerbLocation.Loc) (ann : core_run_annotation)
    (aprov : CerbMem.Provenance) (alignN : Int) (rest : List AllocReq)
    (pref : prefix0) (ρ : EnvStack) :
    iprop(allocCap M.tagDefs (GF := GF) (⟨alignN, objTy⟩ :: rest) ∗
      (∀ p : CerbMem.PointerValue,
        (twoField M.tagDefs p undefField undefField ∗ allocCap M.tagDefs rest ∗
          ⌜0 < addrOf p ∧ addrOf p < 2 ^ 64⌝) -∗
        Ψ (SpikeVal.pure (Vobject (OVpointer p))) ρ)) ⊢
      wps M Ls Ψ (createExpr loc ann (.IV aprov alignN) objTy pref) ρ :=

```

Proof shape and the API lemmas CONSUMED (all PUBLIC, table §2):
- `twoField_load_x` / `twoField_store_x`: `twoField_iff` (local) →
  `wps_load_at` / `wps_store_at` at `id a objTy 0 fieldTy`, metadata
  fraction ½, bytes fraction 1; the store's five side conditions are the
  fields of the `StorableAt` premise (`.compat`, `.fpm`, `.bytes_fpm`,
  `.len []`) plus `hmv`; the pointer `cellPtr id a` is rewritten to the
  rule's `cellPtr id (a + ((0 : Nat) : Int))` by `simp`.
- `twoField_load_y` / `twoField_store_y`: the same rules at offset 8 after
  `fieldYPtr_cellPtr` — `cellPtr_arrayShift tds id a fieldTy 1 (void
  exclusion) ; fieldTy_size ; Int.one_mul`.
- `twoField_create`: `wps_create` at `⟨alignN, objTy⟩ :: rest` with
  `objTy_nonatomic` (`rfl`) and `objTy_decIndep` (`rfl` — the long-array
  decode consults no side table), then `twoField_of_cell`:
  `pointsToCell_cellOwn_iff` → `cellOwn_view` → ONE `pointsToView_split`
  at `Qp.half 1 + Qp.half 1` (`Qp.half_add_half`, iris), the fresh image
  `replicate 16 undefByte = undefField ++ undefField` by `rfl`.
- Vocabulary used: `pointsToCell`, `pointsToView`, `cellPtr`, `allocCap`,
  `AllocReq`, `undefByte`, `decIndep`, `atomicTy`, `loadTrapV`,
  `StorableAt`, `addrOf`, `loadExpr`/`storeExpr`/`createExpr`,
  `SpikeVal`, `wps`, `LabelSpec`, `MachineCtx`.

NO internal name was needed — so no API change was forced by the smoke
test (the finding channel the brief named stayed empty). The predicate is
indexed by the fields' BYTE IMAGES and the load rules take the decode
premise (`∀ lum fpm ad, reconstructValue … fieldTy xb = mv`) exactly as
the raw rules do: the package has no value↔bytes round-trip lemma for an
ARBITRARY integer (only `rfl` decodes at the concrete 5/6/7 images and the
pointer-image round trips of the list/tree clients), and a value-indexed
object predicate would need one. Recorded as a future type layer's first
need, not this slice's (the brief: "a plain predicate and three derived
lemmas").

MEASURED (`scripts/parametric_inventory.lean`, client-module section, the
smoke module added to `clientModules`; run after the final build,
verbatim):

```
- ArrayExhibit: 30 theorems; Ghost-direct 0; StepDef-direct 0; StepLem-direct 0; Judg-direct 0; Plan-direct 0
- StructExhibit: 31 theorems; Ghost-direct 0; StepDef-direct 0; StepLem-direct 0; Judg-direct 0; Plan-direct 1: `struct_plan_fits`
- ListRevExhibit: 91 theorems; Ghost-direct 0; StepDef-direct 0; StepLem-direct 0; Judg-direct 0; Plan-direct 0
- TreeRotExhibit: 77 theorems; Ghost-direct 0; StepDef-direct 0; StepLem-direct 0; Judg-direct 0; Plan-direct 0
- LoopExhibit: 22 theorems; Ghost-direct 0; StepDef-direct 0; StepLem-direct 0; Judg-direct 0; Plan-direct 0
- Examples.ReadinessSmoke: 13 theorems; Ghost-direct 0; StepDef-direct 0; StepLem-direct 0; Judg-direct 0; Plan-direct 0
```

(13 theorems = the 6 rules + `twoField_iff` + the 6 layout facts.)

## 5. THE SNAPSHOT (public statements frozen)

`scripts/signature_snapshot.lean` at 0751751
(`docs/2026-09-02_p4-signatures-post.txt (byte-identical to the former 2026-09-02_p5-signatures-pre.txt, deduplicated 2026-09-02)`, 19218 lines — byte-identical to
the committed P4 post snapshot) vs after the closing commit's tree
(`docs/2026-09-02_p5-signatures-post.txt`, 19376 lines). DERIVED by
`diff pre post`: 0 lines removed or changed (`<`), 158 lines added (`>`),
18 constants added — ALL in `CerberusHeapLang.ReadinessSmoke.*`
(`fieldTy`, `fieldTy_ne_void`, `fieldTy_size`, `fieldYPtr`,
`fieldYPtr_cellPtr`, `objTy`, `objTy_decIndep`, `objTy_nonatomic`,
`objTy_size`, `twoField`, `twoField_create`, `twoField_iff`,
`twoField_load_x`, `twoField_load_y`, `twoField_of_cell`,
`twoField_store_x`, `twoField_store_y`, `undefField`).

Classification per the brief: (i) MOVES — invisible: the snapshot records
name + kind + type, not module, and every moved constant's line is
unchanged (an intermediate snapshot taken after the moves alone, before
the smoke module existed, was identical to `pre`); (ii) the smoke-test
module's 18 declarations — the whole diff; (iii) the API module — adds
no constant. Nothing else differs: no strike.

## 6. Records touched

- `Audit.lean` `trioExports`: 91 → 97 pins (the six smoke-test theorems,
  under a P5 comment). Gate-3 tail: 97 trio-exact / 1173 theorems / 2018
  constants (§8).
- `docs/2026-09-01_alloc-arc-plan.md` closure table, row R-07: CLOSED AT
  SPEEDBUMP LEVEL (the gates ruling supersedes the charter's
  "mechanically acyclic (gate)"); `StmtProbe` was deleted at P3.5.
- `README.md` "The modules": rows for `API.lean`, `Examples/Layout.lean`,
  `Examples/ReadinessSmoke.lean` (no other section rewritten — P6).
- `scripts/parametric_inventory.lean`: the smoke module in `clientModules`.
- `docs/CAPABILITY_MANIFEST.md`: regenerated, NO drift (the smoke module is
  not a `*Exhibit` module, so it is not a manifest consumer; the rows'
  consumer lists are unchanged) — `MANIFEST: 18 rows, 0 red, 13 exhibit
  modules`.

## 7. Borderline observations

1. Lean 4 cannot enforce "imports only the API": the smoke test's
   discipline is the measured inventory line (§4) plus the header's
   documented line, not a check. Consistent with the gates ruling; a
   cheap report line already exists (the instrument) if a claim point
   ever wants it.
2. The stale surfaces left for P6 (not this slice's): README "How to
   build" still quotes the 62-pin expected tail; the `Wps.lean` header's
   "HONESTY LINE (R-02, owner alloc arc P2): the allocating production
   exhibits do NOT yet consume these rules" predates P2's closure.
3. Commit `1b24f03` is the items 1-2 SUBSET of a working tree that also
   held the smoke module and its pins; the fast gate ran on that
   superset (its tail reads 97 pins). The subset differs only by an
   import line and six pins (the 91-pin state) — stated in the message.
4. `EnvLaws`/`API` added to the grep's core list beyond the brief's list;
   the first plant attempt exposed a fail-open in the draft snippet (§3).
5. No DECISIONS.md entry was added by the worker (P4's was added at its
   close by the same worker; left to the orchestrator's call here).
6. Build times: every package build completed in under a minute; the
   fast gate ~5 minutes (root package dominated); nothing near the grind
   tripwire.

## 8. Gates

Fast-gate (`scripts/test_unit.sh --fast`) after items 1-4 were in the
tree, verdict lines verbatim (per-module progress and the dependency's
generated-code linter warnings omitted):

```
== gate 1: banned proof-method grep (native_decide / bv_decide / ofReduce*) ==
ok: no banned proof-method references
== gate 2: capped build, root package (elaborates its axiom audit) ==
info: RefinedCerberus/Audit.lean:75:0: RefinedCerberus axiom sweep: 2 theorems, all cones within the classical trio
info: RefinedCerberus/Audit.lean:75:0: RefinedCerberus banned-axiom sweep: 3 constants of every kind checked; sorryAx/ofReduceBool/ofReduceNat absent from all cones
Build completed successfully (370 jobs).
ok: root build green
== gate 3: capped build, cerberus-heaplang (elaborates its axiom audit) ==
info: CerberusHeapLang/Audit.lean:154:0: CerberusHeapLang export pins: 97 trio-exact
info: CerberusHeapLang/Audit.lean:154:0: CerberusHeapLang axiom sweep: 1173 theorems bounded by the trio
info: CerberusHeapLang/Audit.lean:154:0: CerberusHeapLang banned-axiom sweep: 2018 constants of every kind checked; sorryAx/ofReduceBool/ofReduceNat absent from all cones
Build completed successfully (444 jobs).
ok: cerberus-heaplang build green
FAST-GATE GREEN (gates 1-3 only — not a claim-point result; say fast-gate in the commit)
rc=0
```

FULL gate (`scripts/test_unit.sh`, the claim point) on the closing tree,
verdict lines verbatim:

```
== gate 1: banned proof-method grep (native_decide / bv_decide / ofReduce*) ==
ok: no banned proof-method references
== gate 2: capped build, root package (elaborates its axiom audit) ==
info: RefinedCerberus/Audit.lean:75:0: RefinedCerberus axiom sweep: 2 theorems, all cones within the classical trio
info: RefinedCerberus/Audit.lean:75:0: RefinedCerberus banned-axiom sweep: 3 constants of every kind checked; sorryAx/ofReduceBool/ofReduceNat absent from all cones
Build completed successfully (370 jobs).
ok: root build green
== gate 3: capped build, cerberus-heaplang (elaborates its axiom audit) ==
info: CerberusHeapLang/Audit.lean:154:0: CerberusHeapLang export pins: 97 trio-exact
info: CerberusHeapLang/Audit.lean:154:0: CerberusHeapLang axiom sweep: 1173 theorems bounded by the trio
info: CerberusHeapLang/Audit.lean:154:0: CerberusHeapLang banned-axiom sweep: 2018 constants of every kind checked; sorryAx/ofReduceBool/ofReduceNat absent from all cones
Build completed successfully (444 jobs).
ok: cerberus-heaplang build green
== speedbump: capability manifest (regenerate; red on a red row or drift) ==
ok: capability manifest regenerated, no drift
== speedbump: import direction (semantics → heap → rules → adequacy → clients) ==
ok: import direction — no core module imports an exhibit/example/production module
ALL GATES GREEN
rc=0
```
