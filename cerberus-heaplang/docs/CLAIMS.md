# The claim matrix

HAND-WRITTEN PROSE, stated as such (ar5-manifest, 2026-09-04 — the external
audit's remediation 6, `../../docs/2026-09-04_reynolds-ohearn-separation-logic-audit.md`;
record `2026-09-04_ar5-manifest-notes.md`). For every headline claim of this
package: the exported theorem(s), the kind of statement, the exhibits that
demonstrate it, the operation variants it supports (pointing at the rows of
[the rule-use and classification manifest](CAPABILITY_MANIFEST.md)), the
known exclusions (pointing at `../../docs/KNOWN-OPEN-ITEMS.md`, "KOI" below)
and the check that keeps the row fresh.

WHAT IS MACHINE-CHECKED HERE, AND NOTHING ELSE: `scripts/capability_manifest.lean`
(run by the full gate) parses every table row whose first cell begins `C<n>` and checks
that each backticked name in the SECOND cell ("Exported theorem(s)") is a
theorem, definition or inductive of the built environment — a renamed or deleted export
turns the manifest red until this file is updated — and (since the E3 range
audit's D-2, applied at E4: a prose cell had named two DELETED theorems as
the current state) that every declaration-shaped backticked span of EVERY
cell of such a row is a constant of the environment or a word of the
script's `claimVocabulary` (Core/std.core keywords and other prose tokens);
the check is planted. The prose itself is reviewed, not checked. Axiom sets:
every name below that Audit.lean pins is trio-exact by the build (gate 2);
this file does not repeat the pin list.

Kinds: **partial** — a statement about the shipped driver's per-thread loop
at every fuel (delivery or `CerbND.fuelExhaustedKill`, never a kill
otherwise); **total** — a closed equation about the shipped pipeline's
result; **projected** — an Iris-free memory/execution statement obtained from
a proved Iris triple; **semantic** — a fact about the engine or the mirror
with no logic in its statement; **rule** — a proof rule of a judgment.

| Claim | Exported theorem(s) | Kind | Demonstrating exhibits | Supported variants (manifest rows) | Known exclusions | Freshness check |
|---|---|---|---|---|---|---|
| C1 — A proved partial triple is a fact about the SHIPPED per-thread loop at every fuel: from any driver state holding the configuration, the loop exhausts (`CerbND.fuelExhaustedKill`) or delivers a value satisfying the post; it never kills otherwise. | `engine_adequacy`, `engine_adequacy_alloc`, `DriverSafeCtl`, `spike_step_adequacy`, `spike_step_adequacy_alloc` | partial | every `*_engine`/`*_certified` partial statement: `list_reverse_certified` (ListRevExhibit), `call_smoke_engine` (Examples.CallSmoke), the Exhibit A/B/C engine forms | all RULE rows (the adequacy premise is `Frag` + `MachineCtx.FragProcs`; the derivation uses the rules) | KOI A1/A2 (the fuel constants; the outer-fuel quantifier does no work), B2 (straight-line exhibits at the no-procedure profile), B4 (`htd`/`hex` empty tag definitions and extern), B7 (mirror residuals are outside `Frag`'s reach, not inside the claim) | the build + Audit.lean pins; this row's names checked by the manifest generator |
| C2 — The boring memory triple: memory splits as P ⊎ R and the frame R is returned unchanged; the final memory satisfies a pure post. Conclusion Iris-free. | `MemTriple`, `project_triple_pure`, `project_triple`, `MemTriple_alloc`, `project_triple_pure_alloc`, `project_triple_alloc`, `MemTriple_alloc_of_MemTriple`, `SemTriple`, `SemTriple_iff_Mem`, `semantic_triple_sound`, `semantic_frame` | projected | `struct_create_store_adequacy` (StructExhibit), `list_reverse_certified` (ListRevExhibit), the `*_wp_readout`/`*_adequacy` forms of every exhibit | as C1 | KOI B4, B5 (the singleton-execution equation is stronger than the outcome-list reading); the `hpost` premise names `CohG`/`metaInterp` (the documented exception, API.lean) — discharged through the `*_consequence` lemmas | as C1 |
| C3 — TOTAL correctness on the shipped pipeline (the root-of-trust lane): `runND (drive …) (initial_driver_state …).1` IS the delivered result with the stated readout, no termination hypothesis (an in-budget bound where the step count depends on an input). | `exhibitA_prod`, `fib_certified_production`, `counter_loop_certified_production`, `list_reverse_certified_production`, `dispose_list_certified_production`, `region_loop_certified_production`, `malloc_list_certified_production`, `fib_rec_certified_production`, `even_odd_certified_production`, via `prod_run_eqJ`, `prod_run_eqJ_procs`, `wpt_driver_done`, `wpt_driver_done_alloc`, `wpt_driver_done_procs` | total | ProdExhibit, ProdLoopExhibit, DisposeExhibit, RegionLoopExhibit, MallocListExhibit, FibRecExhibit, EvenOddExhibit (the nine closed statements) | the total rules of every RULE row (every RULE row's total rule has a consumer since 2026-09-04, but three of them — `wpt_load` via `progA_wpt`, `wpt_case_value`/`wpt_wseq` via `caseProg_wpt`/`wseqProg_wpt` — are consumed by total derivations that NO closed statement runs on: `exhibitA_prod` runs on `progAProd_wpt`) | KOI A1 (`hfuel` bounds are shipped-constant instances), B1 (six any-memory total equations deleted at F1; tree rotation has no pipeline statement), B6 (one unit of slack in `fib_rec_certified_production`'s bound) | as C1 |
| C4 — The closed PARTIAL form over the pipeline at every `CerbND.drive_lemFuel` fuel: exactly one execution, exhaustion or the post. | `prod_run_safe_procs`, `fib_rec_certified`, `even_odd_certified` | partial | FibRecExhibit (`fib_rec_certified`: every `n ≥ 0`, no budget bound), EvenOddExhibit (`even_odd_certified`, mutual recursion) | as C1 | KOI A2 (the quantified fuel bounds the OUTER `driver2` rounds only — measured; the run-length content is `DriverSafeCtl`), B3 (seeded exhibits have no closed form), B5 | as C1 |
| C5 — The NEGATIVE total result: the self-jump loop has NO total derivation at any budget, footprint, label context or post — false, not merely unprovable — because the shipped loop exhausts at every fuel on it. | `diverge_total_unprovable`, `dg_loop_exhausts` | semantic (negative) | DivergeExhibit | `Frag.run` RULE row (the self-jump is a `run`) | none beyond the fragment boundary; the module is classified `negative-test` (it names `Step.run` and the driver round by design) | as C1 |
| C6 — Mirror certification and completeness: wherever the mirror steps, the shipped round is the singleton discharged match; every fragment round is a mirror step, a classified kill/panic, or one of two characterized residuals. | `engine_step_matchU`, `frag_round_complete`, `cerberusRound_classify`, `step_iff_cerberusRound` | semantic | Examples.MirrorCoverage (`Step`-level witnesses and `engine_step_matchU` instances) | every RULE and NO-RULE row is mirrored (`Step`); the OUT-OF-SCOPE rows are the mirror's stated absences | KOI B7 (`OpenRound.eval_uncovered`, `run_surplus` characterized, not closed; `hbsz` in `Frag.case_value` carried), B8 (mutual recursion, function pointers, concurrency, external calls outside the fragment) | as C1 |
| C7 — The global memory well-formedness invariant holds at the cold start and is preserved by every memory operation of the fragment (both kill arms, either locking mode, any initializer). | `MemWF`, `prodMem₀_memWF`, `MemWF.loadM`, `MemWF.storeM`, `MemWF.allocateObject`, `MemWF.killM`, `MemWF.allocateRegion`, `create_fresh_global` | semantic | AllocExhibit, RegionLoopExhibit, MallocListExhibit (the launches under `LaunchCoh`) | the `store`/`load`/`create`/`alloc`/`kill` rows — RULE and NO-RULE alike (the invariant is about the engine, not the rules: it holds across the locking store and the static kill of a region too) | KOI A5 (`killM`'s dead-static-kill arm becomes `panic!` at the next re-pin — re-check `MemWF.killM` there), A6 (re-pin drift) | as C1 |
| C8 — Frame reasoning covers heap resources AND label/procedure specifications, in both judgments and at the projected level. | `wps_frame`, `wps_frame_labels`, `blockSpecs_frame`, `wpt_frame`, `wpt_frame_labels`, `blockSpecsT_frame`, `semantic_frame` | rule / projected | every loop exhibit (the invariant framed across the back edge — TwoLabelExhibit across TWO labels' back edges under a label-dependent specification); `semantic_frame` consumed by Exhibit (exhibit C, disjoint sequential stores) | all RULE rows (framing is judgment-generic) | none recorded | as C1 |
| C9 — Recursive procedures through the specification table (Hoare's rule for recursive procedures; no Löb in the client), collapsed by the CPS collapses, run on the shipped pipeline in both lanes. | `procSpecs_intro`, `procSpecsT_intro`, `wps_call`, `wps_call_root`, `wpt_call`, `wpt_call_root`, `wps_sound_cps`, `wpt_sound_cps`, `fib_rec_certified`, `fib_rec_certified_production`, `even_odd_certified`, `even_odd_certified_production` | rule / partial / total | FibRecExhibit, EvenOddExhibit (MUTUAL recursion, the symbol-dependent table `eoSpec`/`eoSpecT`; H1b 2026-09-04), Examples.CallSmoke | the two `Frag.call` RULE rows | KOI B8 (function pointers `Eccall` outside the fragment; mutual recursion is exhibited since H1b), the `Impl` call OUT-OF-SCOPE row | as C1 |
| C10 — The local memory rules are the classical ones: `{p ↦ -} store {p ↦ v}`, load at any fraction, `{emp} cons {p ↦ -}` in budgeted form (`create` for objects, `alloc` for regions), `{p ↦ -} dispose {emp}` (`kill` for objects, `free` for regions), typed sub-range access into objects and regions. | `wps_store`, `wps_load`, `wps_store_at`, `wps_load_at`, `wps_store_region_at`, `wps_load_region_at`, `wps_create`, `wps_alloc`, `wps_kill`, `wps_kill_emp`, `wps_free`, `wps_free_emp`, `wpt_store`, `wpt_load`, `wpt_store_at`, `wpt_load_at`, `wpt_store_region_at`, `wpt_load_region_at`, `wpt_create`, `wpt_alloc`, `wpt_kill`, `wpt_free` | rule | Exhibit, StructExhibit, AllocExhibit, DisposeExhibit, RegionLoopExhibit, MallocListExhibit, ListRevExhibit, TreeRotExhibit, ArrayExhibit | exactly the `store`/`load`/`create`/`alloc`/`kill` RULE rows; the NO-RULE rows of the same constructors are what these rules do NOT cover (the locking store, union-member pointers, the read-only-cell load, zero-size/atomic/non-inert `create`, the static kill of a region, `free(NULL)`, the colliding `free`, the zero-cost `alloc`) | KOI A3 (the colliding-base `free` is the upstream `dynamic_addrs` defect; outside the logic) | the manifest's RULE/NO-RULE classification is regenerated and diffed by the full gate; this row's names checked by the generator |
| C11 — Trust base: every export's axiom set is exactly `propext`, `Classical.choice`, `Quot.sound`; no `native_decide`/`bv_decide`/`ofReduce*`; the pinned semantics tree declares no axiom. | `Audit.trioExports`, `Audit.allowedAxioms` | semantic (about the artifacts) | — (the build itself) | — | KOI E (the FULL gate's expected tail) | gate 1 (the banned-methods grep) and gate 2 (the build elaborates Audit.lean's exact pins and the exhaustive sweep) |
| C12 — The emitted-Core dialect (E1–E2): the elaborator's annotations with the LIVE source location, `bound`, `Ivalignof`, the loaded-value currency (`Specified`/`Unspecified`), the `Unspecified` store, pure `case` at a tuple of loaded values with the `undef(UB)` wildcard arm, the flat-tuple and weak plain-symbol binders — admitted, mirrored, ruled and certified on the shipped pipeline through the generic route; a hand transcription of the oracle's emitted Core is tied to its text by the corpus skeleton speedbump, pure expressions included. | `exhibitA_prod_e1`, `exhibitB_prod_e2`, `wps_bound`, `wpt_bound`, `wps_create_eval`, `wpt_create_eval`, `wps_seq_tuple`, `wpt_seq_tuple`, `wps_wseq_tuple`, `wpt_wseq_tuple`, `wps_wseq_sym`, `wpt_wseq_sym`, `wps_pure`, `wpt_pure` | total / rule | EmittedAExhibit, EmittedBExhibit (the two closed statements, verbatim exhibit A's shape); Examples.CorpusE0 (t1's transcription, its E2 sub-terms' `Frag` witnesses `CorpusE0.t1LoadX_frag`/`CorpusE0.t1Spec3_frag`/…; at E2 the two E3 operands — `conv_loaded_int`, the `catch_exceptional_condition` `case` — were kernel-decided OUT of `PePure` and `main` was outside `Frag`; those exclusions flipped at E3 (C13) and the `unseq` at E4 (C14), and the E2 witnesses were retired with them) | the `Frag.bound`, `Frag.create_op`, `Frag.pure_op`, `Frag.sseq_tuple`, `Frag.wseq_tuple`, `Frag.wseq_sym` RULE rows; the four annotated-head binder rows were NO-RULE at E2; weak tuple binding is ruled since E4, strong symbol binding since E5 (C17), and the other two remain NO-RULE | KOI B8 (what the fragment still lacks): negative assignment now covered by C15; `Eccall` remains E6 — the E1–E2 exclusions `conv_loaded_int`/`catch_exceptional_condition` (E3) and `unseq` (E4) are admitted since C13/C14; the residual `eval_uncovered` at operands the mirror evaluator does not compute (KOI B7) | as C1; the corpus skeleton speedbump (FULL gate) with its four plants |
| C13 — The emitted-Core dialect, E3: the elaborator's C `int` arithmetic — `__conv_int__` and `catch_exceptional_condition_add` through the engine's own `mk_conv_int`/`mk_call_catch_exceptional_condition` — and the standard-library calls (`conv_loaded_int → conv_int → is_representable_integer`) unfolded through the FILE OBJECT's `stdlib` (the mirror evaluator reads the same file the engine does; the transcribed fragment `stdlibE3` is tied to the pinned std.core source by the corpus speedbump): the first C integer rules at both strata, `x + 1` certified on the shipped pipeline over the library-carrying file — a SYNTHETIC file: `prodFileLib stdlibE3` carries a THREE-function fragment of std.core (`is_representable_integer`, `conv_int`, `conv_loaded_int`, checked constructor for constructor against the pinned std.core source) with `impl0 = ∅`, so it is behaviour-equal to the pipeline's `--nolibc` file only on programs that reach those three functions and no `Impl` constant (`conv_loaded_int('signed int', Specified(INT_MAX+1))` classifies `.kill` on it where the pipeline's file would wrap; StdCore.lean's header, the manifest's OUT-OF-SCOPE `Impl` row) — and the overflowing value's UB036 kill exhibited over the genuine driver's round; at E3 t1's `main` was in the cone modulo its one `unseq` (admitted at E4, C14). | `wps_c_add`, `wpt_c_add`, `wps_conv_loaded_int`, `wpt_conv_loaded_int`, `exhibitC_prod_e3`, `prod_run_eqJ_lib`, `prod_run_eqJ_lib1`, `prod_run_safe_lib`, `evalClass_cAdd_overflow`, `overflow_driver2_killed`, `overflow_driver2_killed_frame`, `loop_step_withrs_eval_killed`, `CorpusE0.t1MainWith_frag` | rule / total / semantic (negative) | EmittedCExhibit — a SYNTHETIC program, t1 with its `unseq` replaced by `let weak p = pure(x) in load(…)`, with no `.annot.core` twin and outside the corpus speedbump (`progCE3 3`: the closed statement over `prodFileLib stdlibE3`), OverflowExhibit (`progCE3 2147483647` at the `+` round), Examples.CorpusE0 (t1 modulo `unseq` at E3 — E3's witnesses `t1_unseq_not_frag` (retired at E4) and `t1_uncovered_exactly_unseq` (retired at E4) are both false since `Frag.unseq`; C14), Examples.MirrorCoverage (`cAdd_pure_round`, `store_conv_loaded_int_round`) | the E3 `Frag.pure_op` RULE rows (the emitted `+`, `pure(conv_loaded_int …)`); the E3 NO-RULE rows are what E3 mirrors but does not rule (`-`/`*`, `/`/`%`/shifts, `wrapI`, standalone `__conv_int__`/`conv_int`/`is_representable_integer`, the std.core leaves); the `Impl`-call row is OUT-OF-SCOPE | the positive exhibit is at the LITERAL 3 (no storability lemma for a symbolic `int` image, e3-notes §7); the negative is the driver's KILL ROUND, not the whole run from the initial state (the prefix's concrete memory states and the kernel's non-reduction of the driver, e3-notes §6 — the whole run was MEASURED by compiled evaluation, recorded verbatim there); `unseq` (E4), negative assignment now covered by C15; `Eccall` remains E6; `PElet` withdrawn from E3's grammar (the pull/strip non-commutation, e3-notes §5) | as C1; the corpus skeleton speedbump's std.core section (three rows, three plants) |
| C14 — The emitted-Core dialect, E4 — THE MILESTONE: `unseq` mirrored exactly as the sequential driver runs it (the LAST reducible component steps under the `Cunseq` frame, `get_ctx_unseq_aux`'s last-first step list read at its head; the components' values complete into the annotated tuple `{A_1 ++ … ++ A_n}(v_1, …, v_n)`, a race is the UB035 kill), the certification restated in HEAD form (`CerberusRound`: the engine's step list is `s :: post`, the shipped loop takes its head), the rule faces `wps_unseq_focus`/`wps_unseq_vals` and the annotated tuple binder `wps_wseq_tuple_annot` (and the `wpt` twins), and `docs/corpus-e0/t1.c`'s `main` — transcribed VERBATIM from the oracle's emitted Core, tied to its text by the corpus speedbump with NO opaque position (save initialisers read since E4; the comparison is of constructor skeletons, leaves excluded — freshness cell) — in `Frag` by kernel decision and certified end to end on the shipped pipeline: exactly one Active execution delivering `Specified(4)`, the oracle's `--exec` result. | `CorpusE0.t1Main_frag`, `CorpusE0.t1_unseq_frag`, `t1_wps`, `t1_wpt`, `t1_certified_production`, `wps_unseq_focus`, `wpt_unseq_focus`, `wps_unseq_vals`, `wpt_unseq_vals`, `wps_wseq_tuple_annot`, `wpt_wseq_tuple_annot`, `complete_unseq_vals`, `step_ctx_unseq_vals`, `step_ctx_unseq_race`, `step_ctx_singleton_of_root`, `Step.unseq_inv` | rule / total / semantic | CorpusT1Exhibit (`t1_certified_production`: the TENTH closed shipped-driver statement, over `prodFileLib stdlibE3 [] t1Main`), Examples.CorpusE0 (`CorpusE0.t1Main_frag`; the kernel-decided walk `CorpusE0.t1_uncovered_none`), Examples.MirrorCoverage (`unseq_focus_round`, `unseq_vals_round`) | the `Frag.unseq` RULE rows (focus, completion) and the annotated-tuple `Frag.wseq_tuple` RULE row; the race kill and the jump/call-under-`unseq` rows are NO-RULE (mirrored, classified) | the components must be `ccallFree` (no `Eccall`, `Ecase`, `Elet`, `End` component — `is_unseq_with_ccall` false, so every request under the frame is advanceable); no `run`/call under an `unseq` is ruled; the file is the E3 synthetic library-carrying file (C13); the driver round's step list is characterised at its HEAD only (the tail is the earlier reducible components' contexts, which the sequential loop never reads) | as C1; the corpus skeleton speedbump (FULL gate; four plants, the E4 one at the save initialiser) — it compares constructor SKELETONS with opaque LEAVES (literals, ctypes, memory orders, locations), so a dead literal in a save initialiser is not checked (by design: the E4 range audit's plant `Specified(0)` → `Specified(7)` in the a_518 initialiser passed the skeleton, and neither the theorem nor the oracle readout would see it — the initialiser is overwritten by the `run` argument; N-2); the value path's literals are pinned by the readout = the oracle; the E4 record's oracle cross-check (`docs/2026-09-05_e4-notes.md`) |
| C15 — The emitted conditional t5_ifelse: complete fragment membership, a public-rule total derivation with budget 88, and exactly one shipped-driver Active result Specified(1), unblocked with empty output streams. | `CorpusE0.t5Main_frag`, `t5_wpt`, `t5_certified_production`, `wpt_t5Cond`, `wpt_t5AssignBlock`, `wpt_neg_bound`, `wpt_bound_wseq_tuple`, `wpt_case_eval`, `wpt_unseq_pure_right`, `neg_bound_round`, `excluded_store_round`, `excluded_store_eval_round`, `case_eval_round` | rule / total / semantic | CorpusT5Exhibit; Examples.CorpusE5 covers every branch, including the unspecified-value choice; Examples.MirrorCoverage ties the four E5 rounds to the engine | negative assignment under its enclosing bound, excluded-store evaluation and discharge, expression-case evaluation, and ccall-free conditional operands inside unseq | The production theorem requires initial symbol supply at least 600. Same three-function library and empty implementation-map boundary as C13. Partial rule faces are proved but not consumed by a corpus derivation; reported as RULE-PARTIAL-UNDEMONSTRATED. The unspecified choice is in the fragment but the certified execution never takes it; scheduler forks remain outside the logic. t6 is certified in C16; t4's condition and addition RHSs are proved in C17; its assignments, loop and production result remain pending. | Build and axiom pins; skeleton checker includes expression-case/if scrutinees and case patterns with tuple-wrapping plants; the checker compares constructor structure and leaves symbol/literal identity to transcription review. |
| C16 — The emitted switch t6_switch: whole-term membership, a public-rule total proof at budget 78, and one shipped-driver Active result Specified(20), unblocked with empty output streams. | `CorpusE0.t6Main_frag`, `t6_wpt`, `t6_blockSpecsT`, `t6_certified_production`, `wpt_t6AssignStmt`, `wpt_t6Case2`, `wpt_t6Break`, `wpt_t6Return`, `t6Q_frag`, `t6Q_pot`, `labelAdd_lookup` | rule / total / semantic | CorpusT6Exhibit; Examples.CorpusE5 | Five label continuations checked against the engine collector; the public proof follows case 2 through negative assignment, break and return. All other branches and registered continuations retain fragment witnesses. | Initial symbol supply at least 600; the same three-function standard-library fragment and empty implementation map as C13. This certifies the transcribed emitted body, not the complete frontend file. Partial E5 faces remain undemonstrated; t4 and the full E5 range review remain open. | Build and exact axiom pins; all-outcome equality for the actual shipped driver; 299-token structural transcription comparison and all applicable mutation plants. |
| C17 — The emitted t4_while expression components: whole-term membership, all four registered continuations checked, and public total proofs of the raw short-circuit condition at budget 72, its Boolean decoding at budget 4, and the RHSs s+i at budget 18 and i+1 at budget 14. | `CorpusE0.t4Main_frag`, `t4Q_frag`, `t4Q_pot`, `wpt_t4And`, `wpt_t4Cond`, `wpt_t4Bool`, `wps_seq_sym_annot`, `wpt_seq_sym_annot`, `wpt_t4Add`, `wpt_t4AddSI`, `wpt_t4AddI1`, `wps_load_footprint`, `wpt_load_footprint`, `do_race_loadFootprint` | rule / total | CorpusT4Exhibit; Examples.CorpusE5 | In-range integer operands in two owned cells and in-range sums; nested truth conversions, both short-circuit branches, annotation-preserving strong binding, exact read footprints and the two-read race check. The condition removes footprints at its emitted bound; the addition RHSs retain them for the assignment. | Assignments, the complete loop, its decreasing total budget and production result remain open. File/library connection remains C13's boundary. Partial annotated strong-binding and exact-footprint load faces are proved and undemonstrated; their total faces have this client. | Build and exact axiom pins; 591-token skeleton with all applicable plants. No t4 shipped-driver execution claim yet. |

## Not claimed (so no row)

- Concurrency, function pointers (`Eccall`), external C calls, scheduler forks, and a `run` or call inside an `unseq`: outside `Frag` or covered by no demonstrated rule. Ccall-free expression-case components are supported at E5. The corpus transcriptions with whole-term fragment witnesses are t1, t4, t5 and t6 (C14–C17) (ARCHITECTURE §6, KOI B8; Soundness.lean `Frag` header).
- Mask-polymorphic judgments (the audit's Finding 3): `wps`/`wpt` are top-mask; not a claim of this package.
- Exhaustiveness of the manifest's variant table over the engine's success shapes: the table is a reviewed reading of the engine, not a theorem (CAPABILITY_MANIFEST.md header).
