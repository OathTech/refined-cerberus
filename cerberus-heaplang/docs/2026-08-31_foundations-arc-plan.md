# The foundations arc: getting cerberus-heaplang right

[USER 2026-08-31]: "we're going to get cerberus-heaplang right, we're
not kicking off refinedC until this is perfect." Input: the
foundational audit (`docs/2026-08-31_cerberus-heaplang-foundational-audit.md`,
verdict retain-and-refactor, F-01..F-10) — its findings verified by
the orchestrator (F-01 FragJ/README contradiction, F-02
zero-consumer variant rule + operational fib proof, F-07
containment-only sweep all confirmed at source). This arc adopts the
audit's phase structure and its 8-test acceptance suite as the ARC
EXIT GATE, executed under house practice: worktree slices,
frozen-corpus regression discipline, gates green per commit, hostile
close audits per phase, pause-before-merge per phase.

## Phase 0 — claims corrected, coverage gated (STOP THE BLEEDING)

Fixes F-01(claims), F-02(claims), F-05, F-07(wording), F-09.
1. README/walkthrough corrections: Ecase OUT of the certified-scope
   list (marked "local rule only; not adequacy-exportable — red
   until Phase 1 exports it"); `fib_certified_total` reclassified
   everywhere as an OPERATIONAL ENGINE THEOREM (the variant-rule
   narrative removed until Phase 3 earns it);
   `counter_loop_certified_production` renamed
   `counter_loop_certified_registration` (production reserved for
   shipped-runner statements); the stale Lang.lean row (no
   `Language.Context` exists — say what IS there); sweep language
   "bounded, headline pins exact" (never "trio-exact" for the
   sweep); the mirror slogan qualified with the idiom-faithfulness
   caveat in every place it appears.
2. THE CAPABILITY MANIFEST (the audit's core instrument): a
   generated table — one row per Core construct: mirror rule /
   logic rule / fragment-cone membership / engine-match theorem /
   partial adequacy lane / total lane / production lane / example
   consumer. Generated by a script (statement-census pattern),
   committed output, and a GATE: a construct may appear in any
   claims surface only at its manifest level; "certified" requires
   the full row. Plant-tested (delete a cone case → gate red).
3. `Ecase` shows RED in the manifest until Phase 1.
Exit: no overview claims more than the manifest; gate plant-tested
both directions; naive-reader spot re-check of the changed sections.

## Phase 1 — one authoritative relation, one coverage cone (F-03, F-01 root cause)

The architecture-closure phase; riskiest first, probe-gated.
1. DESIGN DECISION (recorded with reasoning, operator-visible
   before the big build): the authoritative relation remains the
   hand-written syntax-facing mirror (compositionality-guard
   compliant) but is REBUILT as one relation over an explicit
   configuration: (MachineCtx — every immutable field the fragment
   reads: file/tags/externs/tid/proc, with preservation theorems |
   live state: expr tuple × env × label map × Mem). No frozen
   example profiles; no parallel cones.
2. UNIFY FragP/FragJ/Redex/Decomp/RedexJ/DecompJ into ONE
   capability predicate + one decomposition, with closure-under-
   steps/substitution theorems. The manifest generates FROM it.
3. The engine characterization re-proved against the unified
   relation: two-sided on the deterministic supported fragment
   where obtainable; where one-sided, the exact needed direction
   documented per-construct in the manifest (audit-sanctioned).
4. Iris `Language` re-instantiated over the unified relation;
   `primStep` IS the authoritative relation.
5. `Ecase`(value) joins the cone properly: closure constructors,
   substitution lemmas, decompJ+match extension, an adequacy-level
   regression theorem whose program executes the rule.
6. The RelSem question (audit's two-presentations point): the trust
   diagram documents the relationship honestly — RelSem is the
   semantics repo's own instrument, not part of this package's
   chain; a bridging theorem is OUT of scope here (recorded).
7. ORACLE: the frozen-corpus regression gate — every current
   exported theorem re-proves, statements unchanged/strengthened;
   plus one NEW non-example construct passed through the generic
   route as the drift test.
S1a is a PROBE slice (kill-criteria: the unified configuration
forces statement changes beyond the sanctioned env/ctx plumbing
class; the characterization resists two-sidedness everywhere; perf
wall) before the migration is paid.
Exit: audit's Phase-1 criteria verbatim (one theorem ties primStep
to the engine relation on the fragment; cones cannot diverge
without a failed check; Ecase + the new construct through the
generic route).

## Phase 2 — the generic memory layer; examples become clients (F-04, F-10)

1. Ownership split: allocation-metadata/provenance authority vs
   typed byte-range views; subrange split/join + agreement laws.
2. Generic in-bounds typed load + full-ownership store for
   subranges (serialization/decode/alignment/compat/side-table
   premises), replacing every interior rule.
3. Allocator-cursor resource; sound `wp_create`/`wps_create`
   (retires the registered D26 dodge).
4. Array element and node field rules re-derived as CLIENT lemmas
   in exhibit modules; all program-independent discharge facts move
   out of exhibit files.
Exit: audit criteria verbatim — deleting array+list modules leaves
the complete rule library; a layout swap needs only instance
proofs; a FRESH struct example (built here as the test) verifies
with zero core edits.

## Phase 3 — total correctness in the logic (F-02 structural)

1. Iris `TotalWeakestPre`/`TotalAdequacy` (pinned, present, unused
   — use it) or a justified least-fixpoint total judgment.
2. Jump-aware TOTAL statement judgment: back-edge rule REQUIRES the
   strictly-decreasing well-founded measure (not optional).
3. Collapse to total WP + adequacy over the Phase-1 relation; the
   generic measure→driveJ-step-bound theorem (the cost half),
   separated from the logical half per the audit.
4. fib totality re-derived as a CLIENT of the generic theorems;
   `fib_certified_total` becomes a corollary; total list-reverse.
Exit: audit criteria verbatim — removing a decrease proof makes the
loop unprovable; total proofs contain zero Step constructors /
driveJ_step chains.

## Phase 4 — flagship specifications at full strength (F-06)

isList/ChainAt/SeedChain indexed by node allocation IDs; the
exported list theorem states: same node set (permutation of IDs),
in-place relinking, arbitrary disjoint frame preserved, plus the
Phase-3 total version; concrete-`SpikeGF` wrappers so engine-facing
flagships carry no ghost-functor binder; a SECOND structurally
different client (tree rotation) as the accident-detector.
Exit: the public theorem literally says in-place + same-footprint +
frame + termination.

## Phase 5 — production attachment completed; boundary retired (F-05, F-08)

1. The loop scheduler/driver collapse for proc-carrying,
   populated-label threads; loop exhibits exported as
   `CerbND.runND (Driver.drive …)` equations (the name "production"
   restored only here).
2. `runEffectful`: consume the upstream retirement when its pin
   lands (external-gated; if not landed by Phase-5 close, the
   boundary narrows to the minimal statement-carrying module and
   every public boundary theorem gets an exact pin — F-07's
   strengthening — with the retirement remaining the registered
   mover).
Exit: public production theorems mention no package drive/driveJ;
axiom count per the boundary's then-current honest state.

## The arc exit gate — the audit's acceptance suite, verbatim

1 rule-consumption; 2 fresh-client; 3 perturbation; 4 totality;
5 state-read census; 6 production; 7 axiom (exact pins for all
public exports); 8 MUTATION TESTS as plant-tested gates (deleting a
fragment constructor / match case / readout field /
measure-decrease premise each fails a named gate). Close: a fresh
FOUNDATIONAL RE-AUDIT (new auditor, this audit's brief + suite as
its terms) must return no High findings before the arc is declared
done and the port unblocks.

## Process

Each phase: own branch off this one (or sequential slices on this
branch where phases are small), gates per commit, frozen-corpus
regression at every restructuring slice, hostile close per phase,
PAUSE for [USER] merge word per phase. Estimated shape: Phase 0 one
slice; Phase 1 probe + 2-3 slices (the big one); Phase 2 two
slices; Phase 3 two slices; Phase 4 one-two; Phase 5 one +
external-gated. The [USER] checkpoints: phase merges, the Phase-1
design decision record, and the final re-audit verdict.
