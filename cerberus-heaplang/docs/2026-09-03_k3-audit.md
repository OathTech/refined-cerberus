# K3 range audit (`f7bf520..b77d9a7`): dynamic allocation and free — `alloc`, the dynamic kill, `alloc_atomic`/`free_atomic`, goal 3

Independent auditor, 2026-09-03. Audited in the fixed detached copy
`worktrees/audit-k3` at `b77d9a7` (`.lake` primed from that tree — the
environment I queried answers `#check @Frag.kill` with the post-K3
statement, so the oleans are HEAD's). Pinned semantics read at
`.cerberus-ws/lean_frontend/generated/` (cerberus-lean
`ddcfc919972a31bc43a0454e6b2e76a19e6c4594`; the `.cerberus-ws` symlink
resolves to the primary copy, read-only). Graded against
`docs/AUDIT-BRIEF.md`. Provenance: every judgement here is [AUDITOR];
quoted text is verbatim (modulo whitespace where a generated line was
reflowed, stated where so); tallies I computed are labelled DERIVED.
Nothing in the tree was modified except this file. No `lake build` was
run; environment queries went through
`../scripts/capped ~/.elan/bin/lake env lean --stdin`.

## Verdict

**PASS.** No High findings. The logic is sound and the coverage claims
are supported by the tree: `Step.alloc`/`alloc_eval` are the engine's
Alloc0 dispatch at its generality; `MemWF.allocateRegion` is proved
(trio-exact) for every active outcome including size 0; the new
`la_pos` component is a genuine engine invariant (both cursor writers
are guarded, no other writer exists, the cold start satisfies it);
`alloc_atomic`/`free_atomic` are proved against the real
`allocateRegion`/`killM` with the byte image and the dynamic flag
exactly as the engine writes them; completeness is exhaustive over the
engine's arms (OOM is `allocateRegion`'s only refusal; the dynamic
kill's three reasons are exact; `free(NULL)` is a mirrored no-op step);
the `Frag.kill` lift is a strict generalization on which no proof
relied; the snapshot tallies re-derive exactly; 248 pins.

One Medium (a stale in-code header sentence that says goal 3 is still
open — the tree proves it closed) and five Notes. Merge recommendation
at the end.

## Findings

### M-1 (Medium — a core-file header contradicts the tree): Heap.lean's MODULE header still says `allocateRegion` preservation is "K3's one remaining stated obligation"

- Location. `CerberusHeapLang/Heap.lean:121-123` (the module-level
  header, "FRESHNESS IS GLOBAL"), verbatim:
  "`loadM`/`storeM`/`allocateObject` preserve it
  (`MemWF.loadM`/`MemWF.storeM`/`MemWF.allocateObject`) and `killM`
  preserves it (`MemWF.killM`, K2, both arms); `allocateRegion` is K3's
  one remaining stated obligation."
- Tree's truth. `MemWF.allocateRegion` is proved (Heap.lean, the
  `MemWF` section; `#print axioms`: `[propext, Classical.choice,
  Quot.sound]`) and pinned; the SECTION header ~1400 lines below says
  "goal 3 is CLOSED"; ARCHITECTURE/WALKTHROUGH/README say CLOSED. The
  file contradicts itself; a reader of the module header is told the
  obligation is open.
- Consequence. None for soundness; a shop-window/record-integrity drift
  in the file that carries the invariant. Same class as the K2.5 audit's
  M-1 (a sentence the tree does not support).
- Fix (one sentence). Replace the clause by: "and `killM`/
  `allocateRegion` preserve it (`MemWF.killM`, K2, both arms;
  `MemWF.allocateRegion`, K3 — goal 3 closed)."

### N-1 (Note): three forward-tense "K3 will" comments survive, one misattributing region loads to K3

- `Heap.lean:3284`: "K3's `free` will deliver `deadRegion`" — it does
  (`free_atomic`'s post). `Wps.lean:1904` (`wps_kill` docstring):
  "`hstatic`: the kill is static — the dynamic `free` is K3." — reads
  as pending; K3 has landed (`wps_free` is 60 lines below).
  `Heap.lean:1137` (`loadM_live` docstring): "K3's region loads will
  [consume it]" — region loads are NOT in K3 (K3 is alloc + dynamic
  kill; the record's §6/§7 never claims region loads); the attribution
  is wrong, the forward pointer belongs to K4.
- Fix: present tense at the first two; "K4's region loads" (or "the
  region-load rule, when it lands") at the third.

### N-2 (Note): the zero-COST shape is `sizeN ≤ 0 ∧ alignN ≤ 1`, not only "size 0" — negative sizes collapse to 0 in the engine and in the rule

- Engine: `allocateRegion` (CerbMem.lean:1538) `let size := sizeN.toNat`
  — a negative `sizeN` is a SUCCESSFUL size-0 region (no UB, no check).
  Mirror: `Step.alloc` is stated at any `size : IntegerValue` with the
  engine run as premise, so `alloc(al, -7)` is a mirror STEP.
  Rule: `regionCost alignN sizeN = sizeN.toNat + alignN.toNat.max 1 - 1`;
  decided in the environment: `regionCost 4 (-7) = 3`, so `alloc(4, -7)`
  IS covered by `alloc_atomic` and delivers `regionOwn id a 0 (.own 1)
  []` — honest to the engine. Also decided: `regionCost 1 0 = 0`,
  `regionCost 0 0 = 0`, `regionCost 2 0 = 1`.
- README "Scope, exactly" (ii) and Rules.lean's `alloc_atomic` docstring
  name the excluded shape as "`alloc(al, 0)` at alignment `al ≤ 1`". The
  exact shape outside the rule is `sizeN ≤ 0` (the `toNat` collapse) at
  `alignN ≤ 1`. Not a gap (the `hcost` premise is what it is; the round
  is classified by `complete_alloc` either way); a precision point on
  two surfaces.
- Fix: "`alloc(al, n)` at `n ≤ 0` and `al ≤ 1` (a negative `n` is a
  size-0 region in the engine, `sizeN.toNat`)".

### N-3 (Note): `wps_alloc_lit_sym`/`wpt_alloc_lit_sym` satisfy the manifest's consumed-by check as INSTANCE theorems, not as a program proof

- The manifest row `Frag.alloc_op → wps_alloc_eval` is green because
  AllocExhibit.lean contains `wps_alloc_lit_sym … := wps_alloc_eval loc
  ann _ _ pref ρ rfl rfl hn` (one line; likewise the `wpt` twin). No
  program exhibit uses the alloc operand form (`allocFreeProg` is at
  literal operands). This exactly follows the precedent
  `ProdExhibit.wps_store_sym_lit` (also a one-line instance of
  `wps_store_eval`, consumed by no program proof — I grepped: only its
  own definition and a header mention), so it is not new padding; but
  the check accepts it, which is the check's known name-level
  weakness. Verified: the lemmas landed in `af309f4` (2/3), as the record
  says (`git show af309f4:…/AllocExhibit.lean | grep -c lit_sym` = 4;
  `752eb18` = 0); the "fired once" episode is an uncommitted intermediate
  I cannot verify — plausible, since `--fast` skips the manifest and the
  2/3 commit's manifest is already 0 red.
- Not a gap: `wps_alloc_eval` is proved (trio-exact) and sound
  regardless. If a real consumer is wanted cheaply, K4's malloc'd-list
  exhibit can allocate at a symbol size. No new check recommended.

### N-4 (Note): the record's "cold start is `0xFFFFFFFFFFFF`" conflates the engine default with `prodMem₀`'s cursor

- Record §0 / Heap.lean `la_pos` docstring: "the cold start is
  `0xFFFFFFFFFFFF` (`prodMem₀_memWF` re-proved: `rw
  [prodMem₀_lastAddress]; decide`)". CerbMem.lean:122 is
  `lastAddress : Address := 0xFFFFFFFFFFFF` (the engine's initial
  state); `prodMem₀` is that state after the errno allocation, and
  `prodMem₀_lastAddress : prodMem₀.lastAddress = errnoAddr` with
  `errnoAddr = 281474976710648` (`= 0xFFFFFFFFFFF8`; I decided
  `errnoAddr ≠ 0xFFFFFFFFFFFF − 4` by accident, then read the def).
  Both are positive; `la_pos` holds at both. Wording only.

### N-5 (Note): `killM_success_dynamic` spends the dynamic premise through `MetaCoh.dynamic` directly, not through `regionOwn_facts`

- The brief asks whether "`regionOwn_facts`' `base ∈ dynamicAddrs`"
  is bridged at the exact instance `killM` uses. The proof of
  `free_atomic` does not call `regionOwn_facts`; it obtains `MetaCoh σ₁
  id (regionCell a n true)` from `HG.metas id _ Hgetm` and
  `killM_success_dynamic` applies `mem_contains_int _ _ (hmeta.dynamic
  hdyn) : σ.dynamicAddrs.contains mc.addr = true`, with `hbase : al.base
  = mc.addr` from `hmeta.live`, so the simp set rewrites exactly `!
  st.dynamicAddrs.contains alloc.base` (CerbMem.lean:1573) to `!true`.
  Same clause, same instance (`List Int`, LemLib's `BEq Int`); the
  record's wording "regionOwn_facts' base ∈ dynamicAddrs" is
  descriptive, not the dependency. No action.

## The questions, answered

### Q1. The mirror is the engine

`step_action`'s Alloc0 arm, Core_reduction.lean:424 (verbatim modulo
whitespace — the generated line is one physical line):

```
| Alloc0 pe1 pe2 pref => ( match act_valueFromPexpr pe1, act_valueFromPexpr pe2 with
  | some ( Vobject ( OVinteger ival1)), some ( Vobject ( OVinteger ival2)) =>
      ACTION_REQUEST "AllocRequest" loc1 ( AllocRequest2 pref ival1 ival2
        (fun (aid1 : Nat) (ptrval : CerbMem.PointerValue) => mk_value_e (Vobject (OVpointer ptrval)) ) )
  | some _, some _ => ACTION_ILLTYPED "Alloc"
  | _, _ => ACTION_EVAL "eval operands of Alloc" ( stExceptUndef_bind (full_eval_pexpr1 pe1)
      (fun (cval1 : value) => stExceptUndef_bind (full_eval_pexpr1 pe2)
      (fun (cval2 : value) => stExceptUndef_return (wrap (Alloc0 (mk_value_pe cval1) (mk_value_pe cval2) pref))))
```

The driver's AllocRequest2 arm, Driver.lean:273 (verbatim modulo
whitespace; the `CerbDebug.print_debug_located … "REQUEST ALLOC"` match
elided):

```
| AllocRequest2 pref align_ival size_ival mk_th_st' => ( … nd_bind (liftMem (CerbMem.allocateRegion tid1 pref align_ival size_ival))
    (fun (ptrval : CerbMem.PointerValue) => nd_update (fun (dr_st : driver_state) =>
      { { dr_st with trace := ME_allocate_region tid1 pref align_ival size_ival ptrval :: dr_st.trace }
        with core_state0 := update_thread_state tid1 (mk_th_st' aid1 ptrval) dr_st.core_state0 } )) )
```

- `Step.alloc`'s premises are exactly the dispatch: two INTEGER values
  (`h1`/`h2 : valueFromPexpr pe_i = some (Vobject (OVinteger _))`; the
  certification `step_ctx_alloc` closes by `dsimp only [step_action,
  act_valueFromPexpr, valueFromPexpr]; rfl`, so `act_valueFromPexpr` and
  `valueFromPexpr` coincide definitionally here as for create), and
  `hmem : applyMemM (CerbMem.allocateRegion 0 pref align size) σ = some
  (pv, σ')`.
- `tid` pinned honestly. CerbMem.lean:1533: `def allocateRegion (_ :
  Nat) (pref : prefix0) (alignIv sizeIv : IntegerValue) : memM
  PointerValue` — the first argument is a wildcard; `allocateRegion_arg_irrel
  (tid tid' …) : allocateRegion tid … = allocateRegion tid' … := rfl`
  type-checks (I measured its axioms; it is in the environment). The
  engine's result genuinely does not depend on `tid`.
- Continuation: `mk_value_e (Vobject (OVpointer ptrval))`; Core_aux.lean:
  `def mk_value_e {a} (cval : value) : generic_expr a Unit sym :=
  mk_pure_e (mk_value_pe cval)` — a bare pure value, no `Eannot`.
  `Step.alloc`'s successor `Expr [] (Epure (Pexpr [] () (PEval (Vobject
  (OVpointer pv)))))` is that term; `step_ctx_alloc` states the
  continuation as `apply_ctx ctx (Expr [] (Epure (Pexpr [] () (PEval
  (Vobject (OVpointer pv))))))` and closes by `rfl`. CONFIRMED bare.
- `Step.alloc_eval`: premise `hnv : valueFromPexprs [pe1, pe2] = none`
  (the engine's "not all values" `_, _` arm, mixed shapes included) and
  both operands evaluating to INTEGERS; the engine binds `pe1` (alignment)
  first — mirrored in `complete_alloc_op`'s order (`hv1` cased first;
  `step_ctx_alloc_eval_kill1` needs only `hd1`, `_kill2` needs `hv1`).
  A non-integer evaluated pair is ILLTYPED at distance one:
  `step_ctx_alloc_eval_ws'` (the round succeeds into `Alloc0 (PEval v1)
  (PEval v2)` for ANY pair) + `step_ctx_alloc_illtyped'` (`[Step_error2
  "Alloc"]` at `¬(v1, v2 integers)`), assembled as `ShippedRefusal.error_next
  … "Alloc"` in `complete_alloc_op`. This is K2's correction of the
  design note (§4 wanted the eval form general in the value) applied
  consistently; the classification is complete either way.
- The dynamic kill is the existing `Step.kill`. `#check @Step.kill`
  (environment): `… → applyMemM (CerbMem.killM loc (is_dynamic kind) pv)
  σ = some ((), σ') → Step M (… (Kill kind pe) …) (… PEval Vunit …, ρ,
  σ')` — kind-generic; `is_dynamic : kill_kind → Bool := fun x => match x
  with | Dynamic0 => true | Static0 _ => false` (Core.lean:996). At
  `Dynamic0` the premise is `killM loc true pv`, whose active arms are
  :1562 (`PVnull` at `isDynamic` → `(NDactive (), st)`) and :1573-1578
  (the record erase after `isDynamic && !st.dynamicAddrs.contains
  alloc.base` is false). CONFIRMED.

### Q2. Goal 3 closed?

- `MemWF.allocateRegion` and `MemWF.alloc`: measured `#print axioms`
  in the audited environment — both `[propext, Classical.choice,
  Quot.sound]`; no `sorryAx`. The proof cases on `alignedAddr == 0`; the
  active arm supplies `hsize := Int.natCast_nonneg _` (size 0 admitted —
  `size_nonneg` is the clause), `hle := freshBase_add_le_nat`, `hpos :=
  freshBase_pos_nat`, `hdyn := List.mem_cons.mp` (the fresh base
  satisfies `dyn_disj`'s inclusive clause reflexively; older addresses by
  the old `dyn_lo`), then `writeBytesTo`. Every active outcome, size 0
  included. CLOSED.
- `la_pos : 0 < σ.lastAddress` is an engine invariant. All writers of
  `lastAddress` in CerbMem.lean (grep `lastAddress :=`): :1522
  (`allocateObject`) and :1546 (`allocateRegion`), each under `else` of
  `if alignedAddr == 0 then (NDkilled (Other (MerrOther "out of
  memory")), st)` (:1513/:1541), with `alignedAddr := (alignDown … :
  Int)` — a `Nat` cast, so `≠ 0 → ≥ 1`. The only other mention is the
  structure default `lastAddress : Address := 0xFFFFFFFFFFFF` (:122).
  `killM` writes `deadAllocations`/`allocations` only (:1576-1578). No
  other writer exists. `prodMem₀_memWF` discharges it by `decide` at
  `errnoAddr` (N-4). Hence every state reachable from the cold start has
  a positive cursor, and adding `la_pos` to the `LaunchCoh.wf` HYPOTHESIS
  narrows the covered initial states only by unreachable ones — the K0
  audit's M-1 reading applies verbatim; the worker states it so (§8).
  Snapshot: no export's TYPE changed except `MemWF.mk`/`MemWF.alloc`
  (Q7).
- K2.5 M-2 closed. The header's (i) now reads (Heap.lean, verbatim):
  "The step to `allocCost = size + align − 1 ≤ headroom = (lastAddress −
  1).toNat` needs ONE of: a positive size … or the engine invariant `0 <
  lastAddress` (`MemWF.la_pos`, K3), under which `headroom` does not
  clamp and the equivalence holds at EVERY size, zero included
  (`freshBase_ne_zero_of_cost'`, `alloc_atomic`). Without either,
  `freshBase 0 1 0 = 0` while `0 ≤ headroom 0`". I re-decided both
  counterexample facts in the environment (`freshBase 0 1 0 = 0`; `0 +
  max 1 1 − 1 ≤ headroom 0`). `freshBase_ne_zero_of_cost'`/
  `headroom_freshBase'` carry `(hla : 0 < la)` with no size premise;
  axioms `[propext, Quot.sound]` (sub-trio, as recorded). Exact.
- Pricing. `regionCost alignN sizeN = sizeN.toNat + alignN.toNat.max 1
  - 1` is `allocCost`'s arithmetic at the raw size (the engine pads
  nothing: `size := sizeN.toNat`, :1538, vs `allocateObject`'s `.max 1`,
  :1511). `hcost : 0 < regionCost` is necessary and honest: at cost 0
  the fragment `allocBudget 0` is the unit, cannot force
  `budgetInterp`'s right disjunct, so no cursor cell and no `MemWF`
  (`CohG.wf` is conditional on cursor presence) — the OOM arm cannot be
  excluded. Outside: `sizeN ≤ 0 ∧ alignN ≤ 1` (N-2 for the `≤ 0`
  wording), classified by `complete_alloc`.

### Q3. Completeness

- OOM is `allocateRegion`'s ONLY failure. The whole body (:1533-1551)
  has one `if`: `if alignedAddr == 0 then (NDkilled (Other (MerrOther
  "out of memory")), st) else …`; no `fail_`/`failReason`, no UB. Huge
  sizes: `(st.lastAddress - size).toNat = 0 → alignDown 0 _ = 0 → OOM`.
  Negative sizes: `sizeN.toNat = 0`, a SUCCESS with a size-0 region;
  the mirror admits it as a step (`Step.alloc` at any `IntegerValue`)
  and the rule prices it at `max al 1 − 1` (N-2). Negative alignment:
  `alignN.toNat.max 1 = 1`. `allocateRegion_killed_inv : σ' = σ ∧ r =
  Other (MerrOther "out of memory")` and `allocateRegion_layer` (one ND
  layer: active or killed — no fork) match.
- `complete_kill`'s dynamic arm. `free(NULL)`: CerbMem.lean:1561-1563
  `| .PV _ (.PVnull _) => if isDynamic then (NDactive (), st) else fail_
  (MerrUndefinedFree Free_non_matching)` — a no-op STEP; `Step.kill`
  fires with `hmem : … = some ((), σ)`; `complete_kill` (kind-generic
  since K2) classifies it `.inl`. The refusals: `killM_killed_inv`
  (unchanged) gives `σ' = σ ∧ (r = Undef0 loc [UB179a] ∨ r = Undef0 loc
  [UB179b] ∨ r = Other (MerrUndefinedFree Free_out_of_bound))`; checked
  against `undefinedFromMem_error` (Mem_common.lean:392):
  `Free_non_matching ↦ some UB179a_non_matching_allocation_free`,
  `Free_dead_allocation ↦ some UB179b_dead_allocation_free`,
  `Free_out_of_bound ↦ none` (hence `Other`). The dynamic arm's own
  failure (:1573-1574, base ∉ `dynamicAddrs`) is `Free_non_matching` →
  UB179a — inside the three. Exact; no fork (`killM_layer`).
- `frag_round_complete`: statement unchanged (snapshot; not in CHANGED),
  dispatch gains `| alloc => complete_alloc …` and `| @alloc_op …`.
  `Frag` constructors (DERIVED from Soundness.lean): val_pure store load
  create kill kill_op alloc alloc_op sseq annot save if_ run sseq_spec
  pure_sym load_op sseq_sym memop_vals memop_op store_op case_value
  wseq = 22; the manifest (generated from the environment) reports 22.

### Q4. The rules

- `alloc_atomic`: pre `allocBudget (regionCost alignN sizeN)`; post `∃
  id a, ⌜w = ptr (cellPtr id a)⌝ ∗ regionOwn id a sizeN.toNat (.own 1)
  (List.replicate sizeN.toNat undefByte) ∗ ⌜0 < a ∧ a + sizeN.toNat ≤
  2^64⌝` (verbatim in Rules.lean). Byte image: the engine writes
  `writeBytesTo st' alignedAddr (List.replicate size { prov :=
  .Prov_none, copyOffset := none, value := none })` (:1549-1550);
  `undefByte : AbsByte := { prov := .Prov_none, copyOffset := none,
  value := none }` (Heap.lean:1354) — identical; `allocateRegion_success`
  states the post-state with `List.replicate sizeN.toNat undefByte` and
  closes by `rfl`. `regionOwn … (.own 1)` = `metaOwn id (.own 1)
  (regionCell a n true) ∗ bytesOwn a (.own 1) bs ∗ ⌜bs.length = n⌝`
  (Heap.lean:3310); `dynamic := true` is justified in `CohG.alloc`'s
  `metas` field by `List.mem_cons_self` against the pushed list
  (`dynamicAddrs := alignedAddr :: st.dynamicAddrs`, :1548), and every
  older cell by `MetaCoh.of_fields_dyn` (monotone in the list). The
  bounds: `freshBase_pos_nat` and `freshBase_add_le_nat` + `la_wf`.
- `free_atomic`: pre `regionOwn id a n (.own 1) bs` — the WHOLE region at
  full ownership (the liveness token is the `.own 1` `metaOwn`); post
  `⌜w = Vunit⌝ ∗ deadRegion id a n`. No partial-region free rule exists:
  the rules over `killExpr` are exactly `kill_atomic`, `free_atomic`,
  `wps_kill`/`_emp`/`_eval`, `wps_free`/`_emp`, `wpt_kill`/`_emp`/`_eval`,
  `wpt_free`/`_emp` (grep). The `∈`/`contains` bridge is applied at
  `killM`'s exact instance (N-5).
- Coupling preservation. `budgetInterp` lives in `stateInterp`, not
  `CohG`: `alloc_atomic` consumes via `budgetAuth_consume` and
  re-establishes `budgetInterp_intro … m` at the advanced cursor with `m
  ≤ headroom (freshBase …)` from `hBle` + `headroom_freshBase'` (omega);
  `free_atomic` threads `Hki` (cursor + budget) through untouched and
  uses `HG.kill id _ Hgetm rfl` (`CohG.kill`, any live cell since K2).
  `CohG.alloc`'s `wf` is `MemWF.alloc` at the grown list, then
  `writeBytesTo`.
- `wpt_alloc`/`wpt_free`/`wpt_free_emp` carry `(hk : 2 ≤ k)`;
  `wpt_create` (Wpt.lean:2348) and `wpt_kill` (:2179) carry `(hk : 2 ≤
  k)`. Consistent. `wpt_alloc_eval` is `k ⊢ k+1` as `wpt_store_eval`.

### Q5. The lift of `Frag.kill`

- `#check @Frag.kill` / `@Frag.kill_op` in the environment: no kind
  premise. Strict generalization: every K2 derivation transports by
  dropping the `rfl` (`createKillProg_frag`: `.kill_op rfl rfl …` →
  `.kill_op rfl …`, the only pre-existing consumer).
- No proof relied on the static premise: every sweep arm in the diff
  (`Frag.decomp`, `Frag.step`, `esize_step_bound`, `stateInert_step`,
  `esize_le_pot`, `pot_le_two`, `pot_step_bound`, `engine_step_matchU`,
  `outcomesU_of_step`, `loop_step_frag`, `frag_round_complete`) merely
  drops the binder `hstatic`/`hst`; none used it. Post-tree grep for
  `hstatic`: 12 hits, all in `kill_atomic`/`wps_kill`/`wps_kill_emp`/
  `wpt_kill`/`wpt_kill_emp` (the K2 rules' own premise, unchanged) and
  their docstrings.
- Existing exhibit/production statements textually unchanged: the
  snapshot's CHANGED set contains no theorem except `MemWF.alloc` (Q7).

### Q6. Smoke + manifest

- `alloc_free_wps`: `allocBudget (regionCost al n) ⊢ wps … (⌜w =
  Vunit⌝ ∗ ∃ id a, deadRegion id a n.toNat) (allocFreeProg al n pref)
  (ev0 :: evs)`, proved through `wps_seq_sym`, `wps_alloc`, `wps_kill_eval
  … Dynamic0 …`, `wps_free` — real consumers of the public rules (traced
  in the proof text). `free_launch_smoke`: `driveU spikeCtx aids 5 … =
  .done v σ' ∧ v = Vunit ∧ ∃ id, σ'.deadAllocations.contains id = true ∧
  σ'.allocations.get? id = none`, through `wpt_engine_boundU_alloc`,
  `wpt_alloc`, `wpt_kill_eval`, `wpt_free`, readout `deadRegion_dead`.
  PROVISIONAL labelled in the docstring, the AllocExhibit header, and
  the README table ("`driveU spikeCtx` — PROVISIONAL"). Both trio-exact
  (measured).
- `driveU` is `stepOutcomes` = `(step_ctx …).map (dischargeStep …)`
  (Adequacy.lean:157-160), so `free_launch_smoke`'s alloc round runs the
  NEW `dischargeStep` `AllocRequest2` arm (`.next (k aid pv) σ'` at
  `allocateRegion 0 …`) — the package's discharge, as its siblings; the
  genuine driver is certified separately (`ars_alloc_active`,
  `engine_step_matchU`, `loop_step_frag`, all with the `ME_allocate_region
  tid pref align size pv` trace event and `k aid pv`). The worker's
  classification of the arm as a proof device whose meaning-change
  touches only alloc rounds is right: pre-K3 the arm was `.offFragment`
  and no `Frag` program contained an alloc. The only pinned export whose
  TYPE mentions `dischargeStep` is `stepDischarge_run` (a run-redex
  equation; unaffected).
- Manifest: `ruleFor : Name → List Name`; per-rule rows; `Frag.kill →
  [wps_kill, wps_free]`; `MANIFEST: 22 constructors, 23 rule rows, 0
  red, 13 exhibit modules`, identical at `af309f4` and HEAD. The
  consumed-by check still bites (a rule with no exhibit-cone consumer is
  a red row and `throwError`). The two added consumers are instance
  theorems following the `wps_store_sym_lit` precedent (N-3).

### Q7. Snapshot

DERIVED (my own parse of the two files, entries split on `----`, keyed
by the declared name): pre 2335 entries, post 2404; ADDED 69, REMOVED 0,
CHANGED 25. `cmp` pre vs `2026-09-03_k2.5-signatures-post.txt`:
byte-identical. CHANGED, classified: recursor/`casesOn`/`below` entries
19 (`BareHead.{casesOn,rec,recOn}`, `Frag.{casesOn,rec,recOn}`,
`Frag.below.{casesOn,rec}`, `MemWF.{casesOn,rec,recOn}`,
`Redex.{casesOn,rec,recOn}`, `Step.{casesOn,rec,recOn}`,
`Step.below.{casesOn,rec}`); the deliberate lift 4 (`Frag.kill`,
`Frag.kill_op`, `Frag.below.kill`, `Frag.below.kill_op`); the M-2 clause
2 (`MemWF.mk`, `MemWF.alloc`). The ONLY `theorem`-kind entry in CHANGED
is `MemWF.alloc` (the new `hpos` hypothesis). The 69 ADDED names match
the record's §8 list. Pins: 248 (counted in `trioExports`; 221 + 27;
`wpt_create` among them — K2.5 M-1 done). Measured `#print axioms` on
17 of the new pins (`MemWF.allocateRegion`, `MemWF.alloc`, `CohG.alloc`,
`alloc_atomic`, `free_atomic`, `wps_alloc`, `wpt_alloc`, `wps_free`,
`wpt_free`, `alloc_free_wps`, `free_launch_smoke`,
`killM_success_dynamic`, `allocateRegion_success`, `complete_alloc`,
`complete_alloc_op`, `frag_round_complete`, `prodMem₀_memWF`,
`MetaCoh.of_fields_dyn`): all `[propext, Classical.choice, Quot.sound]`;
sub-trio as recorded: `freshBase_ne_zero_of_cost'`, `headroom_freshBase'`,
`freshBase_pos_nat`, `regionCost_pos` (`[propext, Quot.sound]`);
`regionCost_eq` axiom-free. No `sorryAx` anywhere measured.

### Q8. Docs

README "Scope, exactly": alloc and `kill` of either kind in the
fragment; the no-rule paragraph names all four companions (static kill of
a region, `free` of a created object, `free(NULL)`, the zero-cost alloc)
with the engine cites; manifest wording "22 constructors, 23 rule rows"
matches. ARCHITECTURE: atomic-spec list extended, goal 3 CLOSED with the
`la_pos` sentence, the K3 landing bullet, the limitations rows updated.
WALKTHROUGH §4 "The allocation and free rules" (the two `wps` rules
verbatim — checked against Wps.lean), MemWF "ten components" (counted:
10), §7 rewritten. The K2.5 record's §8 M-1 sentence corrected in place
with a dated note. Stale: M-1, N-1 above.

## What I could not verify

- The FULL gate run (no `lake build` permitted here). I rely on the
  committed verbatim tail (record §10) plus my own `#print axioms`/
  `#check`/`decide` queries against the primed environment, which is
  HEAD's.
- The capability-manifest "fired once" episode (an uncommitted
  intermediate; the committed 2/3 manifest is already green).
- The upstream OCaml behaviour cited in the upstream note (not re-read;
  the Lean port's arms were).
- The worker's timing/grind figures (§10-11); nothing in the diff
  suggests a bumped option (grep: no `maxHeartbeats`/`maxRecDepth`).

## Merge recommendation

Merge after M-1 (one sentence in Heap.lean's module header). N-1/N-2/N-4
are wording fixes of the same size and can ride along or be batched into
K4's slice; N-3/N-5 need no change. Nothing here alters a statement or a
proof; a re-gate after the header edit is a docs-only re-gate.
