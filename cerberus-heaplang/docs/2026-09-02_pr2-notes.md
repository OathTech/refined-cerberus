# PR-2 notes — the documentation response to professor review 1 (2026-09-02)

Review: `docs/2026-09-02_professor-review-1.md` (grade B+, at head
`9717836`). Remit of this slice ([USER] brief): required fixes 3, 4, 6,
7 and the §4 recommendations that are documentation; the code response
is `docs/2026-09-02_pr1-notes.md` (commits `639ee1b`, `1053469`,
`91bb372`). DOCS-ONLY: no statement, proof or import was edited; the
`.lean` edits are three module headers/docstrings (§6). Two commits:
A (`4a853e6`, the two front documents + the header edits) and B (this
record + the FULL gate). Every decision below is [AGENT] unless tagged.

The documents this record describes: `README.md` and
`docs/WALKTHROUGH.md` at commit A. The standard applied: the
professor's — every claim names a theorem present in the tree; no
process vocabulary; no hand-maintained line numbers.

## 1. Required fix 3 — the fragment stated exactly

**Finding (Q2, §3.3).** Every `Frag` constructor and every redex
spelling is at `Expr []`; the forcing fact was undocumented.

**Verified in the tree.** `Frag` (Soundness.lean): `val_pure`,
`sseq`, `annot`, `sseq_spec`, `sseq_sym`, `wseq` are stated at `Expr
[] …` directly; `store`/`load`/`create` range over
`storeRedex`/`loadRedex`/`createRedex` (Soundness.lean, each `Expr []
(Eaction …)`); `save`/`if_`/`run`/`case_value` over
`saveRedex`/`ifRedex`/`runRedex`/`caseRedex` (Soundness.lean, each
`Expr [] …`); `pure_sym`, `load_op`, `store_op`, `memop_vals`,
`memop_op` over `pureRedex`, `loadOpRedex`, `storeOpRedex`,
`memopPtrEqVals` (= `memopRedex PtrEq [...]`), `memopRedex` (Step.lean,
each `Expr [] …`). The forcing construct, in the pinned workspace's
generated `Core_reduction.lean`, `step_ctx`'s general arm:

```
| (ctx, expr1) => (match expr1 with | Expr e_annots expr_ => (
    let maybe_loc := get_loc e_annots;
    let th_st := match maybe_loc with | none => th_st | some loc1 =>
      (if CerbLocation.isLibraryLocation loc1 then th_st
       else { th_st with current_loc := loc1 })
```

(quoted with whitespace normalized; the generated file is one long
line). Against it, `MachineCtx.currentLoc` (Step.lean, a field of the
immutable context) and `MachineCtx.thread` (`current_loc :=
M.currentLoc`); `engine_step_matchU`'s conclusion `outcomesU M aid e
(ev0 :: evs) σ = [.next (M.thread e' ρ') σ']`.

**Where it now stands.** README "Scope, exactly" (the paragraph
"Every node of a fragment program carries the empty static annotation
list"), README's divergence register (one row, mover "make
`current_loc` live state"), WALKTHROUGH §7 ("Located Core") and §5
(the remark under `engine_step_matchU`), and the `Frag` header in
Soundness.lean (§6 below). The README paragraph, verbatim:

> **Every node of a fragment program carries the empty static
> annotation list.** Each `Frag` constructor, and each redex spelling
> it ranges over (`storeRedex`, `loadRedex`, `createRedex`,
> `loadOpRedex`, `storeOpRedex`, `memopRedex`, `pureRedex`,
> `saveRedex`, `ifRedex`, `runRedex`, `caseRedex`, and the
> `Esseq`/`Ewseq`/`Eannot` constructors), is stated at `Expr []`. The
> forcing fact: in the general arm of the engine's `step_ctx`
> (generated `Core_reduction.lean`, the `Expr e_annots expr_` match),
> `get_loc e_annots` reads a source location from the redex node's
> annotations and, unless it is a library location, rewrites the
> thread's `current_loc`; this package keeps `currentLoc` in the
> immutable `MachineCtx` (Step.lean), and `engine_step_matchU` equates
> the engine's successor thread with `M.thread e' ρ'`, whose
> `current_loc` is `M.currentLoc`. A located node would falsify that
> equation. Located Core — in particular every Core program the C
> elaborator produces — is therefore outside `Frag`; the programs
> proved here are authored Core with empty annotation lists. The
> mover is to make `current_loc` live state, part of the runtime
> tuple as the environment is.

## 2. Required fix 4 — the drive-statement referent

**Finding (Q2a, §3.4).** The trust claim named "the driver's request
discharge" as the referent of the drive statements; the referent is
`dischargeStep`, tied to the shipped driver only along the total
judgment.

**Verified in the tree.** `driveU`/`stepOutcomes` (Adequacy.lean) —
`(step_ctx …).map (dischargeStep …)`; `dischargeStep` (Soundness.lean,
"the sequential driver's protocol (action_request_sequential2 …)
projected"); `loop_step_frag` (DriverCollapse.lean) with hypothesis `hs
: Step M₀ (e, ev0 :: evs, dst.layout_state) (e', ρ', σ')` — the mirror
step; its consumers `wpt_driver_done`/`wpt_driver_done_alloc`
(ProdLoop.lean) and `prod_run_eqJ` (ProdEntry.lean), all total; the
opaque leaf: generated `Driver.lean` `| 0 => (fuelExhausted …)` in the
driver's fuelled loops, `fuelExhausted` (LemLib) a `def` wrapping the
`opaque fuelExhaustedWith` (LemLib, `@[implemented_by]`). No
`*_production` or `prod_*` theorem in the tree is an implication of
partial-correctness shape; all are `.done`/`Active` equations.

**Where it now stands.** README "The trust story", trust claim 1
(rewritten in full); README trust diagram (the final line "What the
diagram does not contain" gains "any partial-correctness statement
about the shipped pipeline"; the "drive statements" tier names
`driveU`); WALKTHROUGH §1.3 (a new subsection, "What 'the engine' is
in each statement") and §5 ("The production entry"). The former
"lane" vocabulary is gone: drive statements are named by their
execution function.

## 3. Required fix 6 — line-number citations

**Before (grep `\.lean:[0-9]`):** README 20 occurrences on 20 lines;
WALKTHROUGH 41 occurrences on 39 lines (the professor's stale list
included).

**Disposition: REMOVED**, all 61. Declarations are cited by name and
module. The exhibits table's hypothesis column, which cited section
variables by `File.lean:NNN`, now names every binder from the
machine-printed statement snapshot
`docs/2026-09-02_pr1-C-signatures-post.txt`
(`scripts/signature_snapshot.lean`), where section variables appear as
leading binders — a mechanically generated source, as the review
allowed. The one generated-semantics construct with no stable name
(the `current_loc` rewrite) is cited by construct (`step_ctx`'s `Expr
e_annots expr_` arm, `get_loc e_annots`) and the pin
(`../scripts/semantics-pin.env`, `ddcfc9199`).

**After:** WALKTHROUGH 0 occurrences. README: 0 hand-maintained
citations; the 3 remaining matches are the verbatim expected build
tail (`info: CerberusHeapLang/Audit.lean:191:0: …`), which is the
build's own output, re-verified against the FULL gate below (§9) and
not a citation.

**Verbatim-quote check (mechanical).** Every ```lean block in
WALKTHROUGH (26 blocks, 201 lines) was checked to be a verbatim
substring of a file under `CerberusHeapLang/` at commit A (the block
text, including the trailing `:= by` where present):
0 non-verbatim blocks. The README carries no Lean block.

## 4. Required fix 7 — process vocabulary

Terms purged from both documents (everywhere outside the README's
"Records" section): `QA-1`/`QA-2`, `P6.1`/`P3.5`/…, `R-03`/`R-0n`,
`S1b`, `D3`/`Dnn`, `[USER …]`/`[AGENT …]`, "arc", "slice", "lane",
"seam", "speedbump", "trio-exact", "shop window", "plant", "gate";
process references and provenance tags moved to "Records". The
build's own output line `export pins: 116 trio-exact` is quoted
verbatim in "How to build and verify" with the gloss "trio" = the
three classical axioms.

**Grep transcript** (`grep -ciE`, case-insensitive; README counted
outside "Records"):

| Pattern | README before | WALK before | README after | WALK after |
|---|---|---|---|---|
| `QA-[0-9]` | 5 | 2 | 0 | 0 |
| `P[0-9]\.[0-9]` | 3 | 0 | 0 | 0 |
| `R-[0-9][0-9]` | 3 | 0 | 0 | 0 |
| `\bD[0-9]+\b` | 1 | 1 | 0 | 0 |
| `\[USER` | 8 | 7 | 0 (1 in Records) | 0 |
| `\[AGENT` | 2 | 0 | 0 (1 in Records) | 0 |
| `\barcs?\b` | 6 | 2 | 0 | 0 |
| `\bslices?\b` | 5 | 1 | 0 | 0 |
| `\blanes?\b` | 16 | 7 | 0 | 0 |
| `\bseams?\b` | 8 | 2 | 0 | 0 |
| `speedbump` | 1 | 1 | 0 | 0 |
| `trio-exact` | 2 | 2 | 1 (the quoted build line) | 0 |
| `\bgates?\b` | 4 | 5 | 0 | 0 |
| `shop.window`, `\bplant`, `S1b`, `F-nn`, `H-n`, `M-n`, `\bphase` | 0/1 | 0 | 0 | 0 |

## 5. Documentation recommendations (§4 of the review)

- **`StorableAt`'s fields** (WALKTHROUGH §3.1, "What 'storable'
  means"): all five (`compat`, `fpm`, `len`, `bytes_fpm`,
  `stored_dec`) listed with their statements as in Heap.lean and one
  line each on the engine arm they defeat; `StorableView` and
  `StorableAt.toView` named.
- **Program variables are not heap** (WALKTHROUGH §3.1): the
  environment `ρ : EnvStack` is a judgment parameter, not assertion
  language; `wps_if`'s pure guard premise (quoted); `SymFrame` and
  `envAdd_lookup` (EnvLaws.lean) as what the logic knows about frames.
- **The resource-semantics reading** (WALKTHROUGH §4, "What an
  assertion means"): assertions are Iris propositions over the three
  `GenHeap`s coupled to `MemState` by `CohG`; the direct
  heap-predicate reading exists exactly for the pure `Sat`/`CellCoh`
  pre and the pure consequences of the post — what
  `project_triple_pure` exposes.
- **Allocation design** (WALKTHROUGH §4, "Why an ordered plan and not
  an additive budget"; README register row): weighed and NOT adopted
  in this package; registered as a future improvement (a derived,
  weaker additive face over the plan). The paragraph, verbatim:

  > **Why an ordered plan and not an additive budget.** The classical
  > shape would be an additive resource with a split law, `budget (s
  > + t) ⊣⊢ budget s ∗ budget t`, letting two components each own part
  > of the capacity and allocate independently. It is sound here in a
  > conservative form: one `create` of `ty` at alignment `al` moves
  > the cursor down by at most `sizeof ty + al − 1` bytes (`freshBase
  > la al size = alignDown (la − size) al`), so a budget of the summed
  > per-request costs guarantees `freshBase ≠ 0`. It was weighed and
  > not adopted, for one forcing fact about the engine and one about
  > the carrier. The engine's allocator is a single monotone cursor,
  > so the address of each fresh object is a function of the exact
  > sequence of preceding requests; the coupling invariant tracks that
  > cursor exactly (`CohG.cursor` equates the ghost cursor with
  > `⟨σ.lastAddress, σ.nextAllocId⟩`), and `create_atomic` is proved
  > by simulating `allocateObject`'s update step for step
  > (`advanceCursor` is that update on the cursor fields). An additive
  > budget tracks a bound on the cursor, not its value: it needs an
  > authoritative-sum ghost algebra (a total with splittable
  > fragments) in place of the exclusive `GenHeap` cell the cursor
  > lives in, and a coupling invariant stated as an inequality. The
  > cost of the plan shape is stated plainly: capacity is not a
  > separation-logic resource in this package — `allocCap reqs` cannot
  > be split across ∗, it is weakened only to a prefix
  > (`allocCap_weaken`), and every allocating specification is
  > parametric in `rest` and threads the plan in order. An additive
  > budget as a derived, weaker face over the plan, sound by the bound
  > above, is registered as a future improvement (README, "Registered
  > divergences and limitations"); it is not a correction to anything
  > proved.

  Sources: `freshBase` (Heap.lean: `alignDown (la - size).toNat
  (alignN.toNat.max 1)`), `advanceCursor`, `PlanFits`,
  `planFits_order_sensitive`, `allocCap`, `allocCap_weaken`,
  `CohG.cursor`, `allocateObject_success`, `create_atomic` (Rules.lean,
  "exact cursor" per the PR-1 notes). The bound `sizeof ty + al − 1`
  is the classical alignment-padding bound on `alignDown`; it is a
  design claim, not a theorem in the tree, and is labelled as the
  conservative form an additive budget would need.
- **`isReadonly` forced writable** (WALKTHROUGH §4, "Read-only
  allocations cannot be described"; README register row): stated as a
  registered limitation with the mover (a read-only flag in `MetaCell`,
  writability demanded by the store rule only). Sources:
  `CellCoh.alloc`, `MetaCoh.alloc` (`al.isReadonly = .IsWritable`),
  `MetaCell` (fields `addr`, `ty`, `size`).
- **`Frag.case_value`'s `hbsz`** (WALKTHROUGH §3.3; README register
  row): explained as carried, provable in principle by the whole-AST
  induction (the PR-1 stop), `rfl` for authored programs.
- **`hlib`** (README register row): its dischargeability on the success
  path and the price (a location conditional in the certification
  equations), per the PR-1 notes §5.

## 6. `.lean` header edits (comments only; no statement, proof or import)

| File | Edit |
|---|---|
| `CerberusHeapLang/Soundness.lean` | the `Frag` header (`/-! ### The unified fragment cone and the step-match` block) gains the paragraph "THE FRAGMENT IS ANNOTATION-FREE" — the `Expr []` restriction, the forcing construct, and the mover |
| `CerberusHeapLang/Step.lean` | header line "statement-level specification idiom (drive/driveJ, dischargeStep, …)" → "(`driveU`, `dischargeStep`, …)" (dangling retired names) |
| `CerberusHeapLang/ProdEntry.lean` | two docstring sites "the REGISTRATION tie at the driveJ lane" / "this one's lane is driveJ at the production run state" → stated over `driveU` at `procCtx mainSym rs` |

Other header mentions of retired names (`wpt_strongly_normalizing`,
`dg_not_normalizing`, `*_terminates`, `wpt_engine_boundJ`, `drive`/
`driveJ`) in TotalAdequacy.lean, DivergeExhibit.lean, FibExhibit.lean,
Exhibit.lean, ProdExhibit.lean, ProdLoopExhibit.lean, Adequacy.lean and
Step.lean (`rsCtx`'s docstring) are all historical statements of the
form "X was retired/deleted", not claims about the current tree; left
as they are.

## 7. Line counts and what remains

`wc -l` / `wc -c`, committed `91bb372` → commit A:

| File | Lines before | Lines after | Bytes before | Bytes after |
|---|---|---|---|---|
| `README.md` | 679 | 520 | 56,119 | 43,118 |
| `docs/WALKTHROUGH.md` | 1262 | 929 | 69,087 | 49,605 |

**FINDING, reported rather than softened:** the brief asked for both
documents roughly halved (README ≲ 260, WALKTHROUGH ≲ 500 by the
brief's counts of 497 and 980; `wc -l` at the head commit gives 679
and 1262). Achieved: README −23% lines / −23% bytes, WALKTHROUGH −26%
/ −28%. What was removed: the status notes, every process/history
paragraph, the duplicated trust-claim and configuration text (now
once, in the README), the build recipe duplicate, the `twoField`
block, the `SeedChain`/`allocMeta`/`locInBounds`/`SpikeCell`/`cellPtr`
blocks (described in one line each), the arrows list duplicating the
trust diagram, and the loop-client recipe (compressed to one
paragraph). What remains and bounds further cutting without removing
precision: in the README, the exhibits table (19 rows, exhaustive
hypotheses), the divergence register (13 rows), the trust diagram (55
lines) and the rule table — the parts the review asked to keep; in the
WALKTHROUGH, 201 lines of verbatim Lean quotation (26 blocks: the two
triples, the production statement, `isList`, `lr_wps_frame`, the
readout predicates, `wp_store`, `AtomicStep`, `frameLs`,
`wps_frame_labels`, `wps_create`, `blockSpecs_intro`, `wpt.pre`,
`wpt`, `wpt_sound`, `cellOwn`, `pointsToCell`, `allocCap`, the
`Language` instance, `CerberusRound`, `engine_step_matchU`) plus the
explanations the review asked to ADD (the five `StorableAt` fields,
program variables, the resource-semantics reading, the
additive-budget weighing, the drive referent, `hbsz`, read-only).
Halving under `wc -l` would require dropping quoted rules or the
exhaustive hypothesis column; this slice chose not to. The
re-marking professor can rule either way.

## 8. Retired names and identifier check

Grep of both documents for `SemTriple\b`, `ProvenTriple\b`, `driveJ`,
`spike_engine_adequacy`, `engine_adequacyJ`, `wpt_engine_boundJ`,
`wpt_strongly_normalizing`, `_terminates`, `semantic_triple_sound\b`,
`semantic_frame\b`, `dg_not_normalizing`, `hQsz`, `hfuel`, `rsCtx`:
0 hits in each document after (before: `SemTriple` 3, `ProvenTriple`
2, `driveJ` 8+1, `engine_adequacyJ` 1, `wpt_engine_boundJ` 1,
`wpt_strongly_normalizing` 1+2, `_terminates` 3, `hQsz` 1+1, `hfuel`
10, `rsCtx` 1+1). Every backticked identifier in both documents was
checked for existence in `CerberusHeapLang/`, `scripts/`, the pinned
workspace's `generated/` or LemLib, or iris-lean: the only non-matches
are file names (`AUDIT-BRIEF.md`, `_pr1-notes` …), the package name
`RefinedCerberus`, the banned tactic names `native_decide`/`bv_decide`,
and RefinedC's `AllocFailed` — none a claim about this tree.

## 9. FULL gate (claim point), verbatim tail

`scripts/test_unit.sh` (default = the claim gate), run on the tree of
commit A (the header edits rebuild Soundness/Step/ProdEntry and their
dependents; this notes file is not read by any check). Verbatim tail
of the run and its exit code:

```
Note: This linter can be disabled with `set_option linter.unusedVariables false`
ℹ [443/445] Built CerberusHeapLang.Audit (1.2s)
info: CerberusHeapLang/Audit.lean:191:0: CerberusHeapLang export pins: 116 trio-exact
info: CerberusHeapLang/Audit.lean:191:0: CerberusHeapLang axiom sweep: 1160 theorems bounded by the trio
info: CerberusHeapLang/Audit.lean:191:0: CerberusHeapLang banned-axiom sweep: 1951 constants of every kind checked; sorryAx/ofReduceBool/ofReduceNat absent from all cones
✔ [444/445] Built CerberusHeapLang (781ms)
Build completed successfully (445 jobs).
ok: cerberus-heaplang build green
== speedbump: capability manifest (regenerate; red on a red row or drift) ==
cerberus-lean-proj env: switch=/home/dev/projects/cerberus-lean-proj/cerberus-lean/_opam, git redirects active
ok: capability manifest regenerated, no drift
== speedbump: import direction (semantics → heap → rules → adequacy → clients) ==
ok: import direction — no core module imports an exhibit/example/production module
ALL GATES GREEN
EXIT=0
```

Gates 1 and 2 (the banned-method grep and the root package build)
are above the tail; both `ok` (the log's `ok:` lines: "no banned
proof-method references", "root build green", "cerberus-heaplang build
green", "capability manifest regenerated, no drift", "import direction
— no core module imports an exhibit/example/production module"). The
`EXIT=0` line is the runner's exit code, appended by the invoking shell.
The expected build tail quoted in README "How to build and verify"
(116 / 1160 / 1951 / 445 jobs) is this run's.

