# E5 t4 addition expressions — 2026-09-06

[AGENT, active demo-completion goal] Continued on `dialect-e5` from clean
`c086678`. The preceding turn was progress: the complete controlling
expression and annotated strong binding were proved and validated. The
semantics pin remains `f95ef8d9c317fa6b50cf6691216a8c37b1d3eabf`, Lean
4.32.2. Main, sibling repositories and the emitted transcription are
unchanged.

## Proved now

`wpt_t4AddSI` proves the actual `s + i` RHS, at total budget 18. The
public unsequenced-focus rule follows the driver's right-first order:
read i, then read s, then collect their annotated values into the operand
tuple. Both cells are preserved and both read footprints remain in the
result, in the order produced by the engine's collector. The proof
checks that these footprints do not race; it does not replace the
unsequenced expression with a sequence.

`wpt_t4AddI1` proves the actual `i + 1` RHS, at budget 14, using the
existing pure-right-operand rule. Its read footprint is also retained.
Both proofs use `wpt_t4Add`, a shared derivation of the emitted tuple
binder and exceptional-condition addition tail. This adds 3 to the
operand tuple's budget and uses the existing public C addition rule.

The hypotheses state integer representability for both operands and the
sum, cell reconstruction, absence of a load trap, source-pointer lookup,
frame well-formedness and identity external-symbol resolution. The
addition uses Core's built-in integer conversion and needs no new
standard-library premise. The existing condition's `StdE3` premise is
unchanged. Result frames explicitly contain the emitted temporary
bindings, so the assignment proof can establish preservation of the two
source-pointer bindings.

Assignments, their fresh symbols, the total loop budget, block
specifications and t4's production result remain open. These are
component proofs, not a shipped-driver certificate for t4. E5 range
review and the full-file/library criterion A7 remain open.

## Exact read footprints in the public API

The old whole-cell load rule quantified over an unconstrained footprint.
That interface was sufficient for clients that discarded annotations at
a bound, but could not establish the read/read race check for `s + i`.
The existing atomic proof already identified the concrete footprint from
`CerbMem.loadM`; its conclusion now preserves that equality instead of
hiding the footprint existentially.

`loadFootprint` names this read extent: access R, `addrOf pv`, and the
lvalue type's size. `do_race_loadFootprint` proves that two such reads
never race, including equal or overlapping ranges. Both judgment strata
expose the stronger whole-cell rule as `wps_load_footprint` and
`wpt_load_footprint`. `wpt_emittedIntLoad_footprint` carries it through the
emitted temporary-pointer binder. The prior raw-WP, partial and total
load interfaces and the shared emitted-load interface retain their
statements and budgets; they instantiate or forget the stronger result.
There is still one atomic load proof. No engine, mirror, fragment,
memory invariant or adequacy definition changed.

The total face is used by the t4 proof. Its partial twin is proved but
has no partial client; it is recorded as a seventh
RULE-PARTIAL-UNDEMONSTRATED row. The charter's useful-client or reviewed
API/coverage disposition remains required. This records a gap rather
than resolving that criterion by adding a duplicate example.

## Verification

All seven new theorems were individually measured as exactly
`[propext, Classical.choice, Quot.sound]`, and all seven were added to the
exact export pins. The changed `load_atomic`, `wp_load`, `wps_load`,
`wpt_load` and `wpt_emittedIntLoad` cones were individually checked and
remain exactly the same trio. The targeted build and fast gate pass
with 871 pins. The FULL gate exited 0. Selected output, verbatim:

```
info: CerberusHeapLang/Audit.lean:1032:0: CerberusHeapLang export pins: 871 trio-exact
info: CerberusHeapLang/Audit.lean:1032:0: CerberusHeapLang axiom sweep: every theorem bounded by the trio (5937 swept, internal details included — count informational, environment-dependent)
info: CerberusHeapLang/Audit.lean:1032:0: CerberusHeapLang banned-axiom sweep: sorryAx/ofReduceBool/ofReduceNat absent from all cones (9012 constants of every kind swept, internal details included — count informational, environment-dependent)
Build completed successfully (472 jobs).
ok: capability manifest regenerated, no drift
ok: corpus skeleton — every transcription matches its emitted text, every plant mismatches
ok: import direction — 18 core modules, none imports an exhibit/example/production module
BOUNDARY: 29 modules checked, 0 internals mention(s) in total, exit=0
ALL GATES GREEN
```

The deliberately regenerated manifest has 35 constructors, 77 rows,
40 RULE, 7 RULE-PARTIAL-UNDEMONSTRATED, 24 NO-RULE, 6 OUT-OF-SCOPE and
zero red. There are 25 consumers, 17 claim rows, 171 theorem-cell names
and 328 declaration-shaped spans. The extra row reports the exact
footprint face separately from the original whole-cell rule. The
manifest initially rejected that row when it was marked RULE; its
classification was corrected to the measured partial-undemonstrated
status before the full gate.

This revalidates the t1/t5/t6 production proofs on the unchanged pins.
The final targeted t4 build took 2.5 seconds. `git diff --check` passes.
The citation repair updated 16 reviewed declaration references shifted
by this change and one separately inspected store-atomic reference.
The final report is 283 citations: 176 EXACT, 52 DECL, 25 USE, 21 HAND,
9 PIN, zero NOFILE and 25 RANGE; no automatic fixes remain.

The API, claim C17, module classification, charter and known-item
register describe expression components and their remaining obligations.
The architecture's exact Rules.lean citations displaced by this change
were reviewed and repaired; its existing loose/range/usage review queue
remains open. No independent review has occurred in this checkpoint.

## Next action

Compose each RHS with the emitted assignment's outer unsequenced pointer
operand and mixed tuple binder, retaining its read annotations under the
full-expression bound. Factor the shared integer negative-store protocol
used by t5/t6/t4. The existing `wpt_neg_bound` accepts arbitrary annotations
and uses the fresh exclusion id to discharge the read/store race check;
it requires 16 units and returns a fresh symbol above the run-state
supply floor. Preserve the i/s source bindings through both fixed
emitted temporaries and fresh symbols.

Then compose the body and registered continuations with a loop invariant
and decreasing budget. The reachable source states are (i,s) = (0,0),
(1,0), (2,1), (3,3), (4,6), (5,10); a bound based on 5−i is the next
proof obligation, not a claim already established here. Continue to the
production result, E5 range review and later charter milestones. This
checkpoint continues the goal; it is not a park, merge or push.
