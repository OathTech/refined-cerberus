# CALLS C2 range audit (`4e03dd0..8d28c21`) — 2026-09-03

[AGENT] Independent auditor record. Audited in the fixed detached copy
`worktrees/audit-c2` (HEAD `8d28c21`, `.cerberus-ws` → cerberus-lean pin
`f95ef8d9c`), read-only except this file; no `lake build` was run. Brief:
`docs/AUDIT-BRIEF.md` (substantive logical/coverage gaps; claims the tree
does not support). Inputs: the range diff (38 files, +58980/−720, of which
the two snapshots are 54849 lines), the worker record
`docs/2026-09-03_c2-notes.md`, the two snapshots, the design note
`docs/2026-09-02_calls-design-spike.md` §3, the C1 range audit
`docs/2026-09-03_c1-audit.md` (M-1/M-2/N-1), DECISIONS.md, and the PINNED
SEMANTICS at `.cerberus-ws/lean_frontend/generated/`.

Method. (1) Every engine fact the record cites was re-measured on the pin
(string search on `Core_reduction.lean:484`, the one-line `step_ctx`
body; `Core_run.lean:93`; `Driver.lean:245,310,336`; `Core_run_aux.lean`
191–203, 285–296, 380). (2) Every changed proof module was read as a diff;
`Step.lean`'s new definitions and constructors were read in full.
(3) The snapshot census was re-derived by script from the two committed
snapshot files. (4) The environment was queried with
`../scripts/capped ~/.elan/bin/lake env lean` on scratch scripts (kept in
the container's ephemeral `.audit-c2-scratch/`, deleted afterwards):
`#print axioms` on 41 names, `#check` on the new constructors and the
restated `loop_step_frag`, and `rfl`/instance checks listed under Q1/Q3/
Q5. (5) The eight production statements and the key theorem statements
were extracted at both ends of the range (`git show`) and compared
textually.

Quotes below are verbatim modulo whitespace runs (the generated file has
double spaces). Columns are 0-indexed string offsets on line 484 as
measured here; the record's 1-indexed numbers differ by one.

## Verdict

**PASS — MERGE-READY for the stack up to `8d28c21`, with the record items
under "Findings" to be carried into the landing entry.** No High finding.
The mirror steps `Step.call`/`Step.ret`/`Step.ret_annot` are the engine's
PCALL, RETURN and REMOVE-ANNOT arms clause for clause; the captured
context is get_ctx's own (`Decomp.callRedex?_inv`/`_some`); the four
forced corrections are each genuinely forced and minimal; the `⌜False⌝`
guard restores `wps_sound`/`wpt_sound` (their statements are textually
unchanged, their cones trio-exact); `FragProcs` lands only on the
PROVISIONAL `driveU` lane and is `rfl`-vacuous at both profiles; the
production lane's eight statements are textually unchanged and
trio-exact; the snapshot census (+111/−0/49) re-derives exactly and the
49 classify as the record says; the completeness rows carry `call_proc`'s
two kill strings verbatim (checked by `rfl` on a concrete file).

## Findings

No High.

### M-1 (Medium — record, not logic): the landing entry is missing and four [AGENT] deviations from the dispatched plan await ratification

`docs/DECISIONS.md` at `8d28c21` has no "CALLS C2 LANDED" entry (`grep
"C2" docs/DECISIONS.md` finds only the C1 entries' forward pointers,
lines 1275–1303). The record `2026-09-03_c2-notes.md` §3 documents four
[AGENT] decisions that DEVIATE from the dispatched plan and are therefore
not covered by any existing ruling:

- (a) a JUDGMENT change in C2 — `wps.pre`/`wpt.pre` gain the arm
  `match callRedex? e with | some _ => ⌜False⌝ | none => …` — against
  the brief's "NO judgment changes (C3)" and the design note's "no
  judgment change until C3". Forced (the counterexample in the record §3(a)
  is correct — see Q2) and minimal, but a deviation.
- (e) `MachineCtx.FragProcs` added as a premise to nine PROVISIONAL
  exports — a NARROWING of `engine_adequacyU`, `engine_adequacyU_alloc`,
  `project_triple`×4, `semantic_triple_soundU`, `semantic_frameU`
  (`drive_classifyU` is internal). Vacuous at both profiles; still a
  statement change on shipped exports.
- (f) the design note's pre-registered criterion "`loop_step_frag`'s
  shape survives C2" FAILED as stated (the record §3(g)(iii) says so). The
  design note's own rule is "Any failure: park and re-adjudicate before
  C3". The worker did not park (the criterion concerned a statement that
  the C1 audit's M-1 had already shown to be not what the tree had); the
  re-adjudication is not recorded anywhere.
- (f) the production lane's `exec_loc`/`current_loc` tie is deferred to
  C4 (record §3(f)); the C1 audit's M-1 asked for the tie to land WITH
  `Step.call`/`Step.ret`. It landed as the theorem `loop_step_frag` (see
  Q4) but the production lane consumes only `loop_step_frag_same`.

Consequence: none for soundness; a provenance gap for the register.
Fix: the landing entry must state each of the four as [AGENT] with its
forcing fact and the operator's ratification, and carry the C4 obligation
explicitly (a production program with a call needs `hel`/`hcl` at the
Q4 entry control `⟨[], some mainSym, ELoc_normal [(mainSym, other
"Driver.drive")]⟩`).

### N-1 (Note): the `[]`-env-under-a-frame configuration is excluded by shape, not by a stated invariant

`complete_ret` is stated at `(ofVal w, ev0 :: evs, ⟨pc :: κ, q, ℓ⟩, σ)`.
The engine's RETURN arm at `th_st.env = []` is the panic `failwithI "end
of proc, found an empty Core_run env"` (col 2350); the mirror has no step
there and no `ShippedRefusal` row names it. The excluding invariant
`ctl.κ.length < ρ.length` is established only as the induction hypothesis
of `drive_classifyU_aux` (Adequacy.lean; entry `κ = []` with a cons env,
call pushes both, return pops both, every other step preserves the
cons-shape by `Step.env_cons`). README's new WF row states it as shape
discipline ("a frame is on the env stack whenever the call stack is
non-empty"). This is the same fail-closed shape discipline the package
already uses for the cons env at `κ = []`; it is honest. Suggestion: C3's
CPS collapse will need this invariant as a standalone lemma over `Reach`
(the design note's `Step.env_tail_eq`); state it then.

### N-2 (Note): the production lane's tie is partial and is consumed nowhere at the live control

`loop_step_frag` (live control, ties `hstack/hproc/hel/hcl/hfile`) is
pinned and proved for all three `Step.ctl_cases` disjuncts, but its only
consumer is the pin; `driverDone_step`/`wpt_driver_*` consume
`loop_step_frag_same`, whose statement is IDENTICAL to the C1
`loop_step_frag` (extracted and compared: identical), i.e. still no
ctl↔`th₀` tie inside the round; the ties `hp : ctl.proc = some p`,
`hstack : th₀.stack0 = ctl.toStack`, `hproc : th₀.current_proc_opt =
ctl.proc`, `hκ : ctl.κ = []` sit on `wpt_driver_aux/done/done_alloc` and
are used only for the done round (`driverDone_value`/`_annot` via
`Ctl.toStack_of_κ_nil`). `exec_loc`/`current_loc` are not tied. Sound
today, verified against the engine (Q4): on line 484 `exec_loc` occurs at
cols 16825/16843/16884 (the `Eccall` arm), 18803/18821/18862 (PCALL) and
22164 (thread spawn, `exec_loc := ELoc_normal []`) — no control-preserving
fragment round reads or writes it; `current_loc` is read by the general
arm's location shadowing (3434; inert at `Expr []`), the action
`requestLoc` (4120) and the MEMOP request (23698), all into request/kill
payloads that the existing certification states at `th.current_loc`
(`step_ctx_memop`, `requestLoc`) and never reads for the successor. The
record's §3(f) sentence "neither was read by any C1 round" is imprecise
on `current_loc` (read into payloads, location-irrelevant); the
conclusion stands.

### N-3 (Note): doc imprecisions

- README "Scope, exactly", Step.lean header, design note: "`Eproc ()
  (Sym f) pes`" — the first slot is `core_run_annotation`, not `Unit`;
  `Frag.call`/`callRedex ra f pes` quantify ANY `ra`. Write `Eproc _ (Sym
  f) pes`.
- Step.lean `Step.call` docstring and record §1: "The implementation-
  constant call `Eproc _ (Impl _) _` (`Step_fs2`) is outside the
  fragment" — the engine's Impl arm (col 18997) is `if is_fs_function
  iCst then Step_fs2 … else … process_impl_proc …`; `Step_fs2` is one of
  two shapes. The exclusion is right (`callRedex?_proc_impl = none`,
  `Frag` has no `Impl` constructor).
- `Ctl` docstring "col ≈2698" vs record §1/§9 "col 2734" for the
  `Stack_cons` panic: measured here, the arm `| Stack_cons _ _ _` begins
  at 2697 and its `failwithI "Core_reduction ==> Stack_cons"` at 2721.
  Both "≈"; harmless.
- The design note (`2026-09-02_calls-design-spike.md`) is unchanged in
  the range; its Q3 (`lookupProc M.file f` over `funs` only, `TSK_Misc`
  fixed, `ret` only, "no judgment change until C3") stands uncorrected
  in place — the erratum is record §3(g). Acceptable for a dated note;
  the landing entry should point at §3(g).

### N-4 (Note): the assembled classification stays at the empty stack

`cerberusRound_classify` (statement unchanged) still carries `hκ :
ctl.κ = []`; at `κ ≠ []` coverage is piecewise — `frag_round_complete`
(control-general, statement unchanged) for non-values and `complete_ret`
for values at a cons env. Complete on the fragment; C3 may want the
assembled form at a general control.

## The questions

### Q1. The mirror is the engine

PCALL, measured at col 18132 (`reduction: PCALL`), verbatim modulo
whitespace:

```
| Eproc _ ( Sym psym) pes => /- reduction: PCALL -/ Step_with_runstate2 (RSK_eval "Eproc")
  ( stExceptUndef_bind (stExceptUndef_mapM full_eval_pexpr' pes) (fun (cvals : List (value)) =>
    stExceptUndef_bind (runEU ( except_bind (call_proc core_extern1 file1 psym cvals) exception_undef_return ))
    (fun (p : (Fmap (sym) (value) ×generic_expr (core_run_annotation) (Unit) (sym))) => match p with | (proc_env, expr1) =>
      stExceptUndef_return { { { { { th_st with current_proc_opt := some psym } with env := proc_env :: th_st.env }
        with exec_loc := push_exec_loc psym th_st.current_loc th_st.exec_loc }
        with stack0 := Stack_cons2 th_st.current_proc_opt ctx th_st.stack0 } with arena := expr1 } )) )
```

- Arguments: `full_eval_pexpr'` is the closure `fun pe => full_eval_pexpr
  _lemReader_tagDefs th_st core_extern1 mem_st file1 pe` defined at the
  head of `step_ctx` (col ≈650) over the ORIGINAL `th_st`; the mirror's
  `hvs : evalPexprs M.tagDefs M.extern ρ pes = some vs` is bridged by
  `mapM_full_eval_bridge` (Soundness.lean) through the pre-existing
  certified `full_eval_bridge`, state-verbatim. `Frag.call` restricts the
  operands to `PePure` within `peDepth ≤ lemDefaultFuel` — the same
  grammar as `Frag.run`; operands the mirror cannot evaluate are
  classified (`complete_call`'s `.kill`/`.uncovered` rows, evaluation
  order arguments-first as in the engine). Covered grammar: yes.
- `call_proc` (Core_run.lean:93), quoted in the record §1 and in
  `lookupProc`'s docstring, matches the pin verbatim: stdlib FIRST (the
  engine's own NOTE), then `core_sym := match fmapLookupBy … psym
  core_extern1 with | some sym1 => sym1 | none => psym` (= `resolveExtern
  ext f`, Step.lean:332) into `funs`; only a `Proc` declaration binds —
  `Fun`, `ProcDecl`, `BuiltinDecl` (Core.lean:1481–1490) fall to `none`.
  `lookupProc` is exactly that. Verified by computation: on a file whose
  `funs` binds `smokeF` to a `Fun` declaration, `lookupProc … = none` and
  `call_proc fmapEmpty funFile smokeF [] = fail0 (Illformed_program
  ("calling an unknown procedure: " ++ show_symbol smokeF))` — both `rfl`;
  on a file whose `stdlib` binds `smokeF` to a `Proc`, `lookupProc` and
  `call_proc` both return the stdlib body — `rfl`.
- `procEnv` is the `foldl2` lambda verbatim (`foldl2`, Utils.lean:93,
  panics on unequal lengths — behind `hlen`). The arity test is the Lem
  `==` on `Nat`, `@BEq.beq Nat (@instBEqOfEq0 Nat Lem_Num.instEq0Nat_1)`;
  `call_proc_eq` spells that instance out and `lemNatBeq_iff`
  (EnvLaws.lean:173, pre-existing, by `defaultCompare`/`compare`
  unfolding) bridges it to `=`. Honest.
- The pushed control. get_ctx (Core_reduction.lean:375): `| Eproc _ _ _
  => [(CTX, expr1)]`; `| Esseq pat e1 e2 => if is_irreducible e1 then
  [(CTX, expr1)] else List.map (fun (ctx, e) => (Csseq annot1 pat ctx e2,
  e)) (get_ctx e1)`; same for `Ewseq`/`Cwseq`; `| Eannot _ (Expr _ (Eannot
  _ e)) => [(CTX, expr1)]` then `| Eannot xs e => List.map (fun (ctx, e)
  => (Cannot annot1 xs ctx, e)) (get_ctx e)`. `callRedex?` mirrors this
  outside-in (`Csseq a pat q.1 e2` at `Expr a (Esseq pat e1 e2)`, etc.),
  with `annotRooted b` as the double-annot guard — correct: the
  `is_irreducible` short-circuit before those arms is `true` only at a
  bare value and a single-annotated value, neither of which contains an
  `Eproc`, so the mirror's unconditional descent agrees. Certification:
  `Decomp.callRedex?_inv : Decomp e ctx r → callRedex? e = some (ctx', f,
  pes) → ctx' = ctx ∧ ∃ ra, r = callRedex ra f pes`, and
  `step_ctx_call_ws` states the engine's step at `get_ctx th.arena =
  [(ctx, callRedex ra f pes)]` (via `hd.get_ctx_default hsz`) and pushes
  THAT `ctx`. `Ctl.toStack` maps `(ctl.proc, ctx) :: ctl.κ` to
  `Stack_cons2 ctl.proc ctx ctl.toStack` — the engine's `Stack_cons2
  th_st.current_proc_opt ctx th_st.stack0`. `push_exec_loc psym
  th_st.current_loc th_st.exec_loc` ↔ `push_exec_loc f M.currentLoc
  ctl.execLoc` with `M.thread`'s `current_loc := M.currentLoc`. Same
  context, yes. (Under `Ebound`/`Eunseq` the engine also descends; the
  mirror does not — outside `Frag`, no claim.)
- RETURN (col 2227), verbatim: `| Stack_cons2 parent_proc_opt caller_ctx
  sk' => ( let tsk := … ; /- reduction: RETURN -/ Step_tau2 "end of
  procedure" tsk ( match th_st.env with | [] => (failwithI "end of proc,
  found an empty Core_run env" : thread_state) | _ :: env' => { { { { th_st
  with current_proc_opt := parent_proc_opt } with env := env' } with stack0
  := sk' } with arena := apply_ctx caller_ctx (Expr e_annots (Epure
  (mk_value_pe cval))) }))`. `Step.ret` pops one env frame (`ev0 :: evs →
  evs`), pops the stack, restores `proc := p`, leaves `execLoc`, plugs
  `apply_ctx ctx (pure v)` (`mk_value_pe cval = Pexpr [] () (PEval cval)`,
  `e_annots = []` at the canonical value). The `[]`-env arm is a PANIC,
  excluded by the cons-env premise (N-1). `tsk` (`TSK_Return` iff
  `file1.funinfo` has the procedure) reaches only the trace
  (`advance_step`'s tau arm, Driver.lean:336: `| TSK_Return sym1 mval_opt
  => nd_update (fun dr_st => { dr_st with trace := ME_function_return sym1
  mval_opt :: dr_st.trace }) | _ => nd_return ()`) — existential in
  `step_ctx_ret`/`advance_tau_tsk`/`loop_step_tau_tsk`. Match.
- REMOVE-ANNOT (col 2941 marker; the string `"CTX, Eannot(value)"` at
  2989) is the arm BETWEEN the value arm and the general arm and does not
  read `stack0`: `| (CTX, ( Expr _ ( Eannot _ ( expr'@(Expr _ ( Epure (
  Pexpr _ _ ( PEval _)))))))) => /- {A}v -> v (in empty context) -/ /-
  reduction: REMOVE-ANNOT -/ Step_tau2 "CTX, Eannot(value)" TSK_Misc ( {
  th_st with arena := expr' } )`. Confirmed "at ANY stack". `toValRt`
  (Step.lean:565) is `none` at `_ :: _`, so `{A}v` at `κ ≠ []` is a
  genuine non-terminal of the Language and `Step.ret_annot` is required
  for completeness (`complete_ret`'s `annot` arm) — the record's §3(b) is
  right. `Stack_cons` (col 2697/2721) is `failwithI`: unrepresentable by
  `Ctl.toStack`, stated in the `Ctl` docstring and README.
- Driver: `can_advance (Step_with_runstate2 _ _) = true`, `(Step_tau2 _ _
  _) = true` (Driver.lean:310, quoted); `liftCore_run` (Driver.lean:245):
  `Defined z => nd_return z | Undef loc1 ubs => kill (Undef0 …) | Error
  … => kill (Error0 …) | Exception err => kill (Other (DErr_core_run
  err))` — the two `call_proc` `fail0`s become `Other (DErr_core_run
  (Illformed_program …))` kills, which is what `complete_call` names.

### Q2. The judgment guard

`wps.pre`: after the value test and the jump clause, `| none => match
callRedex? e with | some _ => iprop(⌜False⌝) | none => <C1 step clause>`;
`wpt.pre` the same, before the budget match. A single arm; C3's call
clause replaces `some _ => ⌜False⌝` by `some (ctx, f, pes) => …`.
Contractivity: the new `cases callRedex? e; case some => exact .rfl`.

The counterexample (record §3(a)) is correct: at `hκ : ctl.κ = []`, `e :=
Eproc _ (Sym f) []` with `f`'s body `pure 1` and `Ψ := fun w ρ' => ⌜ρ' =
procEnv [] [] :: ρ⌝`, the unguarded step clause continues `F Ψ r.e r.ρ`
at the SOURCE control with `r = (pure 1, procEnv [] [] :: ρ, pushed)`;
the value clause there is `Ψ (.pure 1) (procEnv :: ρ) = ⌜True⌝`, so
`wps` holds; the raw WP's only run is `Step.call` then (the pushed
configuration is not a Language value, `toValRt_mk_cons`) `Step.ret` to
`(pure 1, ρ, ctl)`, a value with `w.ρ = ρ`, and `Ψ (.pure 1) ρ` is
`⌜ρ = procEnv :: ρ⌝`, false. So `wps_sound` was FALSE with `Step.call`
and no guard; with the guard the `some` case is `iintro #HB ⟨%hf⟩; exact
hf.elim` and every other step is control-preserving (`Step.ctl_eq hs hcr
htv`). `wps_sound`/`wpt_sound` statements: textually UNCHANGED across
the range (extracted, compared); cones trio-exact (`#print axioms`).

Minimality: the guard is the weakest change that makes the C1 statement
true (the alternative — CPS-indexing the collapse — is C3's design). Every
rule proof supplies the guard fact by `rfl` at a known redex or a `cases
hcr : callRedex? e` split whose `some` branch is closed by `⌜False⌝`;
`wps_of_atomic`/`wpt_of_atomic`/`wpt_det_step` gain `hnc/hcr : callRedex?
e = none`, discharged `rfl` at all 13 + 12 call sites; `wpt_step_eq`/
`wpt_zero_step_eq` gain `hcr`; `wpt_call_eq` is the guard equation
(checked: `wpt_call_eq rfl rfl rfl : wpt M ctl Ls k Ψ (callRedex ra f [])
ρ = ⌜False⌝`). No exhibit statement changed (the 49 changed entries
contain no exhibit; the 8 production statements are textually
unchanged); all exhibits are call-free (the manifest's `Frag.call` row
lists no consumer; `Frag.call` appears in no exhibit `Frag` proof).

### Q3. `FragProcs` on the PROVISIONAL lane

Necessary for the `driveU` lane: `engine_adequacyU` and its dependents
take `hwp : … ⊢ WP ⟨e₀, ρ, ctl, M⟩ {{…}}` — a RAW Iris WP, not a `wps`.
A raw WP at a call configuration is provable (Reducible by `Step.call`
plus the post at the callee's successors), and `Frag.call` puts the call
redex in the cone, so `hNS` (NotStuck from `spike_step_adequacy`) can
hand `drive_classifyU` a `Step.call`; the C1 classification assumed a
fixed control (`Step.ctl_eq` at every step) and is unprovable with calls
in the mirror. The guard alone does not suffice because the guard lives
in `wps`/`wpt`, not in `WP`. The TOTAL lane's oracle IS the judgment
(`wpt_drive_aux` rewrites with `wpt_call_eq` and closes the call case
by `⌜False⌝`), so `wpt_engine_boundU`/`wpt_driver_*` need no premise —
verified: their statements are unchanged except the production ties (Q4).
`drive_classifyU_aux` is the control-general induction under `ControlOk M
ctl` (current procedure's label fiber in the cone; each saved frame's
label fiber in the cone and `∀ v, Frag (apply_ctx pc.2 (pure v)) ∧ pot …
≤ fuel`, established at push by `Decomp.frag_plug_call`/
`pot_plug_call_le`) and `ctl.κ.length < ρ.length`; the call case installs
`hPf.body/potBound/labels`, the return case consumes the frame's plug and
`hlabC`. Vacuity: `lookupProc_spikeFile (ext) (f) : lookupProc spikeFile
ext f = none := rfl` (re-checked by `rfl` in scratch), so
`spikeCtx_fragProcs`/`procCtx_fragProcs rs` close all three fields; 16
exhibit call sites pass one of them. Which exports gained it: exactly
`drive_classifyU`, `engine_adequacyU`, `engine_adequacyU_alloc`,
`project_triple`, `project_triple_pure`, `project_triple_alloc`,
`project_triple_pure_alloc`, `semantic_triple_soundU`, `semantic_frameU`
(9 — the snapshot's list); all are `driveU`-lane and PROVISIONAL per the
README table; no shipped-pipeline export gained it. Recorded as a
narrowing: in the record §3(e), README "Deliberately not here" and the
new divergence row, Audit.lean header — not yet in DECISIONS (M-1).

### Q4. The tie (C1 audit M-1)

`loop_step_frag` (DriverCollapse.lean), `#check`ed in the environment:
premises `hp : ctl.proc = some p`, `hstack : th₀.stack0 = ctl.toStack`,
`hproc : th₀.current_proc_opt = ctl.proc`, `hel : th₀.exec_loc =
ctl.execLoc`, `hcl : th₀.current_loc = M₀.currentLoc`, `hfile :
dst.core_file = M₀.file`, `hs : Step M₀ (e, ev0 :: evs, ctl,
dst.layout_state) (e', ρ', ctl', σ')`; conclusion thread `{ arena := e',
stack0 := ctl'.toStack, errno := th₀.errno, env := ρ', current_proc_opt :=
ctl'.proc, exec_loc := ctl'.execLoc, current_loc := th₀.current_loc }` —
`M₀.thread e' ρ' ctl'` up to `errno` (untied, unread). Proof by
`Step.ctl_cases`: same → `loop_step_frag_same` + rewriting the three
control fields by the ties; call → `step_ctx_call_ws` +
`loop_step_withrs_eval` with `hcl` supplying the pushed location and
`hfile`/`hext` the lookup; return → `step_ctx_ret` (at `hstack`) +
`loop_step_tau_tsk` with `hel` for the untouched `exec_loc`. All three
cases proved; trio-exact.

Production lane: `driverDone_step` consumes `loop_step_frag_same
… (hproc.trans hp)`; `wpt_driver_aux/done/done_alloc` gained `hp hstack
hproc hκ` (replacing `hstack : th₀.stack0 = Stack_empty`, now derived by
`hstack.trans (Ctl.toStack_of_κ_nil hκ)`); the seven production exhibits
pass `rfl rfl rfl rfl` (htd hex hp hstack hproc hκ after `hlb`). The
EIGHT production statements are textually UNCHANGED (extracted at both
ends: `exhibitA_prod`, `prod_run_eqJ`, `counter_loop_certified_production`,
`fib_certified_production`, `list_reverse_certified_production`,
`dispose_list_certified_production`, `region_loop_certified_production`,
`malloc_list_certified_production` — all identical) and trio-exact.
`exec_loc`/`current_loc` untied in that lane: sound today (N-2 has the
engine evidence — no control-preserving round writes `exec_loc`; the
`current_loc` reads enter payloads only), and the production lane cannot
reach a call (`wpt_call_eq`). Recorded as C4 in the record §3(f); needs
the register (M-1).

### Q5. Completeness

`complete_call`'s messages against `call_proc` (Core_run.lean:93):
engine `fail0 (Illformed_program (String.append "calling procedure `"
(String.append (show_symbol psym) (String.append "' with the wrong number
of args: |args|=" (String.append (Lem_String_extra.stringFromNat
(List.length cvals)) (String.append "expecting: "
(Lem_String_extra.stringFromNat (List.length params))))))))` and `fail0
(Illformed_program (String.append "calling an unknown procedure: "
(show_symbol psym)))`; the mirror's rows are these with `f/vs/params`,
verbatim (note the engine's missing space before `expecting:` is
mirrored). Checked by `rfl` on the smoke file: `call_proc fmapEmpty
(smokeFile default) smokeF [Vtrue] = fail0 (Illformed_program ("calling
procedure `" ++ … ++ stringFromNat 1 ++ "expecting: " ++ stringFromNat
0))`. The `Fun` case: `Eproc` of a symbol bound to `Fun` is `lookupProc
= none` → the unknown-procedure kill (engine and mirror agree by `rfl`,
Q1) — classified by the `none` row. `complete_ret`: a value at a cons env
under a frame always steps (`Step.ret`/`Step.ret_annot`); the `[]`-env
configuration is out of the statement (N-1). `frag_round_complete`'s
statement is unchanged (extracted); its dispatch gains `| call ra f pes
=> complete_call hd hsz hpes hdep _ _ _`. The `.uncovered` row is
`OpenRound.eval_uncovered` with `hstuck` by `Step.call_inv` and the
round's shape by `step_ctx_call_shape`; `operandsOf` gains the `Eproc`
arm so the witness membership holds.

### Q6. Snapshot, pins, manifest

Re-derived by script from `docs/2026-09-03_c2-signatures-{pre,post}.txt`
(2705 → 2816 entries): ADDED 111, REMOVED 0, CHANGED 49 — exactly the
record's numbers and its 111-name list. The 49, classified from the diff
(derived): 15 recursor/eliminator auxiliaries (`Frag.{rec,recOn,casesOn}`,
`Frag.below.{rec,casesOn}`, `Redex.{rec,recOn,casesOn}`,
`Step.{rec,recOn,casesOn}`, `Step.below.{rec,casesOn,sseq_ctx,wseq_ctx}`)
+ 2 guarded constructors (`Step.sseq_ctx`, `Step.wseq_ctx`) = 17; 13
guards added (`Step.ctl_eq`, `ctl_eq'`, `retag`: `callRedex? e = none`,
`toVal e = none`; `Step.val_elim`, `toVal_none`,
`RoundClass.value_not_step`: `κ = []`; `Step.env_cons'`: `c'.2.2.1 =
c.2.2.1`; `Step.pure_inv`: `valueFromPexpr pe = none`; `wps_of_atomic`,
`wpt_of_atomic`, `wpt_det_step`, `wpt_step_eq`, `wpt_zero_step_eq`:
`callRedex? e = none`); 4 disjuncts (`Step.sseq_inv`, `wseq_inv`,
`annot_inv`, `Decomp.step_factor`); 1 generalized (`engine_step_matchU`,
free `ctl'`); 1 restated (`loop_step_frag`); 9 `hPf`; 4 production ties
(`driverDone_step`, `wpt_driver_aux`, `wpt_driver_done`,
`wpt_driver_done_alloc`). 17+13+4+1+1+9+4 = 49. Pins: `Audit.lean`'s
`trioExports` has 316 names (counted); the 20 new ones are the record's
list; `#print axioms` on all 20, plus `engine_step_matchU`,
`loop_step_frag`, `drive_classifyU`, `engine_adequacyU(_alloc)`,
`semantic_triple_soundU`, `wps_sound`, `wpt_sound`, `frag_round_complete`,
`cerberusRound_classify`, `wpt_driver_done(_alloc)`, `wpt_engine_boundU`
and the eight production statements — 41 names — all `[propext,
Classical.choice, Quot.sound]`. Manifest: `declaredNoRule` prints the
`Frag.call` row with its reason and does not count it red; an undeclared
unmapped constructor is still MISSING/red (the speedbump's purpose kept);
`docs/CAPABILITY_MANIFEST.md`: 23 constructors, 25 rule rows, 0 red. The
consumer column of a declared row is the literal `—` (not computed) —
honest by definition (no rule, no consumer).

### Q7. Docs

README "Scope, exactly": call/return at the mirror level, "NO logic rule
yet: the call rule, the specification table and recursion are C3, and
until then both judgments are `⌜False⌝` at a call redex (the guard)";
the `Stack_cons` sentence present; two new divergence rows (the guard,
`FragProcs`); the WF row extended with the RETURN's empty-env panic;
`prog`/`core_exec` rows now carry `ctl`. WALKTHROUGH: the C1 audit's M-2
listings fixed (`MemTripleU` with `ctl` on all three conjuncts,
`engine_step_matchU` at `ctl'`, `Step : Config → Config → Prop`,
`labelsAt ctl.proc` in the `wps_run` prose and `blockSpecs_intro`,
`wpt.pre` with the guard arm, `procCtx rs`/`procCtl p`,
`frag_round_complete` over `RoundComplete`); §7 procedures rewritten.
ARCHITECTURE §2/§4 updated (`loop_step_frag_same` as the production
consumer; the drive through calls). Round.lean's `panic_noproc`
docstring says `labelsAt`. Design-note corrections are in the record
§3(g) only (N-3).

## Not verified / limits

- The primed `.lake` in this copy was built in the worker's tree
  (`worktrees/calls-c1`, per the Lake trace's `LEAN_PATH`) at 15:31, 20
  minutes before commit `8d28c21` (15:51). I could not, without a build,
  confirm the oleans correspond byte-for-byte to the committed sources;
  the `#print axioms`/`#check`/`rfl` results above are against that
  environment, which the record's gate log (§12, `316 trio-exact`,
  `GATE-EXIT=0`) says is the final tree. The orchestrator's own gate
  re-run covers this.
- Build cost (§10, ≈2 h 30 min, no tripwire) not independently
  measurable; the `grep` for `maxHeartbeats`/`maxRecDepth`/`sorry`/
  `native_decide`/`bv_decide`/`ofReduce` on the range's added lines is
  clean (one docstring mention of "C2" only).
- Reachability of `Stack_cons` from `Driver.drive` is STATED, not
  proved (as the C1 audit's N-1 asked).
- A scratch directory `/tmp/c2audit/` created by the auditor could not
  be removed under the sandbox; it contains only a copy of the axiom
  script.

## Merge recommendation

MERGE the stack up to `8d28c21`, ff-only on operator sign-off, after
the landing entry (M-1) is written with the four [AGENT] deviations, the
re-adjudication of the failed pre-registered criterion, and the C4
obligation (production-lane `exec_loc`/`current_loc` tie at the Q4 entry
control). No code change is required by this audit; N-3's wording fixes
are optional hygiene for the next docs pass.
