# 2026-09-02 QA-1 — spec generalizations within the frozen fragment (slice record)

Worker record for QA-1 of the quality-audit response plan
(`../../docs/DECISIONS.md`, 2026-09-02 "QUALITY-AUDIT RESPONSE PLAN";
the audit: `docs/2026-09-02_quality-audit.md`, §3 H-1/M-3/M-4 and §4
Q1–Q17). Branch `heaplang-alloc-arc`, from `cc6a420`. Slice
classification ([USER] one-change-at-a-time): SPEC GENERALIZATION —
every public statement change below is a strict generalization (weaker
premises, or a more general shape whose old form is a derived instance),
a premise removal, an addition, or a rename; the two departures are
flagged as BORDERLINE in §7. Provenance of every judgment here is
[AGENT] (worker) unless marked.

Instrument: `scripts/signature_snapshot.lean` before
(`docs/2026-09-02_p6.1-signatures-post.txt (byte-identical to the former 2026-09-02_qa1-signatures-pre.txt, deduplicated 2026-09-02)`, at `cc6a420`) and after
(`docs/2026-09-02_qa1-signatures-post.txt`, at the final commit); the
diff is classified line by line in §6.

Commits (fast-gate each; FULL gate on the last):

| Commit | Group |
|---|---|
| `c994703` | 1+2a — H-1/Q1,Q2: `Step.store_eval` at the engine's dispatch, `Step.save_eval` added, rules generalized, engine re-certified |
| `e197130` | 2b — the three production programs restated in their natural shape |
| `fc6f218` | 3 — M-3/Q9,Q10: the six stratum twins; `wpt_store_cell` → `wpt_store` |
| `4141f24` | 4 — M-4: Q4, Q5, Q12, Q13 done; Q3 partial; Q6/Q7/Q8/Q16 stopped |
| (final) | 5 — records, manifest, snapshot, FULL gate |

## 1. H-1 / Q1 — the store operand-evaluation arm at the engine's generality

Engine (verbatim, `.cerberus-ws/lean_frontend/generated/Core_reduction.lean:424`,
`step_action`, whitespace collapsed): `| Store0 is_locking pe1 pe2 pe3 mo1 => (
match act_valueFromPexpr pe1, act_valueFromPexpr pe2, act_valueFromPexpr pe3
with | some (Vctype ty1), some (Vobject (OVpointer ptrval)), some cval => …
ACTION_REQUEST … | some _, some _, some _ => ACTION_ILLTYPED "Store" | _, _, _
=> ACTION_EVAL "eval operands of Store" ( … full_eval_pexpr1 pe1 … pe2 …
pe3 … wrap (Store0 is_locking (mk_value_pe cval1) (mk_value_pe cval2)
(mk_value_pe cval3) mo1) …)`. The arm fires exactly when the operand
triple is not all values.

**`Step.store_eval`** (Step.lean). Before:
```
(hnv2 : valueFromPexpr pe2 = none) (hnv3 : valueFromPexpr pe3 = none)
```
After:
```
(hnv : valueFromPexprs [pe2, pe3] = none)
```
Justification: the type operand of the redex is the canonical value
`Pexpr [] () (PEval (Vctype ty))`, so the engine's three-operand test
`valueFromPexprs [pe1, pe2, pe3] = none` is equivalent to the pair
test (`valueFromPexprs_cons` at `some (Vctype ty)`); the pair form is
the shape `Step.memop_eval` already uses (`valueFromPexprs [pe1, pe2] =
none`, `valueFromPexprs_pair`). Strictly weaker: `hnv2 ∧ hnv3 → hnv`
(if `pe2` is a non-value the pair is not all values). The mixed shapes
now step: `store_sym_lit_step` (symbol pointer, literal value) and
`store_lit_sym_step` (literal pointer, symbol value) in ProdExhibit.lean,
each `Step.store_eval rfl …`.

Same premise change (`hnv2, hnv3` → `hnv : valueFromPexprs [pe2, pe3] =
none`), each a strict weakening of the old conjunction: `Redex.store_op`,
`Frag.store_op`, `Step.store_op_inv` (conclusion also loses the now-
meaningless conjunct `valueFromPexpr pe3 = none` — a conclusion
weakening, i.e. the inversion says less; its consumers were adjusted),
`stepDischarge_store_eval`, `step_ctx_store_eval_ws`, `wps_store_eval`,
`wpt_store_eval`. Rule instances at the mixed shapes: `wps_store_sym_lit`,
`wpt_store_lit_sym` (ProdExhibit.lean).

Re-certification: `stepDischarge_store_eval` now cases both operands'
`PePure` shapes (the (value, value) cell is refuted by `hnv`; at a
value pointer operand the evaluator premise pins it to `Vobject
(OVpointer pv)` so the engine's match falls to the `_, _, _` arm);
`engine_step_matchU`'s `store_op` case and `loop_step_frag`'s (the
driver lane) take the new premise. Consumers of `Frag.store_op`
(`.store_op loc0_lib rfl rfl …` → `.store_op loc0_lib rfl …`) in
ProdExhibit, ProdLoopExhibit, StructExhibit, ListRevExhibit,
TreeRotExhibit.

## 2. H-1 / Q2 — the save evaluation arm

Engine (verbatim, `Core_reduction.lean:353`, `one_step0`, whitespace
collapsed): `| Esave sym_bTy sym_bTy_pes e => some ( match valueFromPexprs
(List.map (fun p => match p with | (_, (_, z)) => z) sym_bTy_pes) with |
some cvals => /- reduction: SAVE (tau part) -/ TAU "Esave" ( List.foldl (fun
acc p => match acc, p with | acc, ((sym1, ((bTy , _) , _)), cval) =>
update_env (mk_sym_pat sym1 bTy) cval acc ) env1 (List.zip sym_bTy_pes
cvals) ) e | none => /- reduction: SAVE (eval part) + SAVE-UNDEF -/ EVAL
"Esave" ( stExceptUndef_bind (stExceptUndef_mapM (fun p => match p with |
(sym1, (bTy, pe)) => stExceptUndef_bind (eval_pexpr1 pe) (fun pe' =>
stExceptUndef_return (sym1, (bTy, pe'))) ) sym_bTy_pes) (fun sym_bTy_pes' =>
stExceptUndef_return (Expr annots1 (Esave sym_bTy sym_bTy_pes' e))) ) )`.
What the engine does with the evaluated values: it does NOT proceed to
the TAU part — it RE-FORMS the Esave node with the evaluated
initializers (`sym_bTy_pes'`, node annotations preserved) and the TAU
arm fires on the next round. Mirrored exactly.

**`Step.save_eval`** (new, Step.lean):
```
| save_eval {a sb ps body cvals ρ σ}
    (hnv : valueFromPexprs (saveParamPexprs ps) = none)
    (hvals : evalPexprs M.tagDefs M.extern ρ (saveParamPexprs ps) = some cvals) :
    Step M (Expr a (Esave sb ps body), ρ, σ)
         (Expr a (Esave sb (saveParamsWithValues ps cvals) body), ρ, σ)
```
with `saveParamsWithValues ps cvals := (ps.zip cvals).map fun p => (p.1.1,
(p.1.2.1, Pexpr [] () (PEval p.2)))` (the engine's `sym_bTy_pes'`).
Supporting lemmas (Step.lean): `valueFromPexprs_cons/_nil/_map_val`,
`saveParamPexprs_withValues`, `valueFromPexprs_withValues`,
`bindSaveParams_withValues` (the entry binding reads only symbols and
base types, so the re-formed list binds as the original),
`evalPexpr_of_valueFromPexpr`, `evalPexprs_of_valueFromPexprs`,
`evalPexprs_length`, `valueFromPexprs_length`; `valueFromPexprs_pair`
moved earlier in the file (statement unchanged).

`Step.save_inv` — before: the TAU arm only; after: the disjunction of
the TAU arm and the EVAL arm (a weaker conclusion, as every inversion
over a relation with more rules must be). `Step.save_vals_inv` (the old
statement, at `valueFromPexprs … = some cvals`) and `Step.save_op_inv`
(the EVAL arm alone, at `… = none`) added as the two instances.

**Engine certification.** `stepDischarge_save_eval` (Soundness.lean):
one engine round through `one_step0`'s EVAL arm, `step_ctx`'s EVAL wrap
and the driver protocol, via `mapM_save_bridge` (the n-ary
`stExceptUndef_mapM` over the parameter list, each initializer through
`eval1_bridge`, the fold bodies abstract with pointwise
characterizations — the `mapM_eval1_bridge` pattern — and
`mapM1_id_defined` for the t0-monad sequencing). `engine_step_matchU`
gains the case (Step ⇒ engine round: the mirror's `save_eval` IS the
engine's round). `step_iff_cerberusRound` and `cerberusRound_classify`
(Round.lean) are generic over `Frag`/`Step`/`engine_step_matchU` and
re-elaborate unchanged: the two-sided arm covers the new steps
(store's mixed shapes, save's EVAL). The production lane's
`loop_step_frag` (DriverCollapse.lean) gains `step_ctx_save_eval_ws`
(the raw `RSK_eval` form) for the same case. `Frag.step`,
`Frag.esize_step_bound`, `Frag.pot_step_bound`, `Frag.stateInert_step`,
`EnvStable.step_env` take the new arm (esize/pot of the re-formed node
equal the original's — `1 + esize body`).

**`Frag.save`** — before: `Frag body → Frag (saveRedex sb ps body)`
(any initializers). After:
```
| save {sb ps body} (hd : ∀ pe ∈ saveParamPexprs ps, peDepth pe ≤ lemDefaultFuel) :
    Frag body → Frag (saveRedex sb ps body)
```
BORDERLINE (§7): this ADDS a premise to a `Frag` constructor. It is the
fuel-honesty side condition every other operand-evaluating constructor
already carries (`if_`'s `hdg`, `run`'s `hdep`, `load_op`/`memop_op`/
`store_op`'s `hd`), and it is exactly what the engine certification of
the new arm needs (`mapM_save_bridge` bridges `eval_pexpr1` at the
engine's fuel). Literal initializers satisfy it trivially
(`saveParams_depth_of_vals rfl`; every pre-existing `Frag.save` use
was so discharged). The audit's own minimal alternative was to narrow
`Frag.save` to the literal form; this is the wider narrowing: the
shapes excluded are initializers deeper than the engine's fuel or
outside the evaluator's grammar — shapes for which no rule and no
mirror step existed before or after.

**Rules.** `wps_save` — before:
```
(hvals : valueFromPexprs (saveParamPexprs ps) = some cvals) :
    wps M Ls Ψ body (bindSaveParams ps cvals (ev0 :: evs)) ⊢
      wps M Ls Ψ (Expr a (Esave sb ps body)) (ev0 :: evs)
```
After:
```
(hvals : evalPexprs M.tagDefs M.extern (ev0 :: evs) (saveParamPexprs ps) = some cvals) :
    (same conclusion)
```
Strictly weaker premise: `valueFromPexprs … = some cvals → evalPexprs …
= some cvals` (`evalPexprs_of_valueFromPexprs`). The old statement is
retained verbatim as `wps_save_vals` (the TAU arm); `wps_save_eval` is
the EVAL step (`wps … (Esave sb (saveParamsWithValues ps cvals) body) ⊢
wps … (Esave sb ps body)`); `wps_save` is proved from the two.

`wpt_save` — before: `wpt M Ls k Ψ body (bind …) ⊢ wpt M Ls (k + 1) Ψ
(Esave …)` at `valueFromPexprs … = some cvals`. After:
```
(hvals : evalPexprs M.tagDefs M.extern (ev0 :: evs) (saveParamPexprs ps) = some cvals) :
    wpt M Ls k Ψ body (bindSaveParams ps cvals (ev0 :: evs)) ⊢
      wpt M Ls (k + saveEntryCost ps) Ψ (Expr a (Esave sb ps body)) (ev0 :: evs)
```
with `saveEntryCost ps := match valueFromPexprs (saveParamPexprs ps) with
| some _ => 1 | none => 2` — the engine's own dispatch decides the
entry cost (`saveEntryCost_of_vals`/`_of_eval`). At literal
initializers `saveEntryCost ps = 1` by `rfl`, so the old statement is
the instance (retained verbatim as `wpt_save_vals`); `wpt_save_eval` is
the one-tau EVAL step. The existing literal-initializer consumers
(FibExhibit, LoopExhibit, ListRevExhibit) apply `wpt_save_vals`
(`iapply` does not unify `k + saveEntryCost ps` with `k + 1` on its
own); the restated production programs apply `wpt_save`.

## 3. The production programs, before → after (verbatim program terms)

**`progAProd`** (ProdExhibit.lean). Before:
```
def progAProd : CoreExpr :=
  Expr [] (Esseq (symPat [] pASym BTy_unit)
    (createExpr loc0 empty_annotation (.IV .Prov_none 4) intTy
      (PrefOther "spike-x"))
    (Expr [] (Esseq (symPat [] vASym BTy_unit)
      (ofVal (.pure sevenVal))
      (Expr [] (Esseq (Pattern [] (CaseBase (none, BTy_unit)))
        (storeOpRedex loc0 empty_annotation intTy
          (Pexpr [] () (PEsym pASym)) (Pexpr [] () (PEsym vASym)) NA)
        (loadOpRedex loc0 empty_annotation intTy
          (Pexpr [] () (PEsym pASym)) NA))))))
```
After:
```
def progAProd : CoreExpr :=
  Expr [] (Esseq (symPat [] pASym BTy_unit)
    (createExpr loc0 empty_annotation (.IV .Prov_none 4) intTy
      (PrefOther "spike-x"))
    (Expr [] (Esseq (Pattern [] (CaseBase (none, BTy_unit)))
      (storeOpRedex loc0 empty_annotation intTy
        (Pexpr [] () (PEsym pASym)) (Pexpr [] () (PEval sevenVal)) NA)
      (loadOpRedex loc0 empty_annotation intTy
        (Pexpr [] () (PEsym pASym)) NA))))
```
(`lets p = create(4, int) in lets _ = store(int, p, 7) in load(int, p)`;
`vASym` deleted; `progAProd_wpt` at budget 10, was 11.) The export
`exhibitA_prod`'s statement changes only through `progAProd`.

**`counterProdProg`** (ProdLoopExhibit.lean). Before:
```
def ctrBody (ra : core_run_annotation) (mo : memory_order)
    (bty : core_base_type) : CoreExpr :=
  Expr [] (Eif ctrGuardPe
    (Expr [] (Esseq (symPat [] ctrSSym bty)
      (ofVal (.pure sevenVal))
      (Expr [] (Esseq (Pattern [] (CaseBase (none, bty)))
        (storeOpRedex loc0 empty_annotation intTy
          (Pexpr [] () (PEsym ctrCSym)) (Pexpr [] () (PEsym ctrSSym)) mo)
        (Expr [] (Erun ra ctrLoopSym
          [ctrDecPe, Pexpr [] () (PEsym ctrCSym)]))))))
    (ofVal (.pure Vunit)))

def counterProdProg (ra : core_run_annotation) (mo : memory_order)
    (bty xbty cbty sbty : core_base_type) (n : Int) : CoreExpr :=
  Expr [] (Esseq (Pattern [] (CaseBase (none, bty)))
    (Expr [] (Esseq (symPat [] ctrPSym bty)
      (createExpr loc0 empty_annotation (.IV .Prov_none 4) intTy
        (PrefOther "spike-x"))
      (Expr [] (Erun ra ctrLoopSym
        [Pexpr [] () (PEval (ivVal n)), Pexpr [] () (PEsym ctrPSym)]))))
    (Expr [] (Esave (ctrLoopSym, sbty)
      [(ctrXSym, ((xbty, (none : Option (ctype × pass_by_value_or_pointer))),
        Pexpr [] () (PEval (ivVal 0)))),
       (ctrCSym, ((cbty, (none : Option (ctype × pass_by_value_or_pointer))),
        Pexpr [] () (PEval nullVal)))]
      (ctrBody ra mo bty))))
```
After:
```
def ctrBody (ra : core_run_annotation) (mo : memory_order)
    (bty : core_base_type) : CoreExpr :=
  Expr [] (Eif ctrGuardPe
    (Expr [] (Esseq (Pattern [] (CaseBase (none, bty)))
      (storeOpRedex loc0 empty_annotation intTy
        (Pexpr [] () (PEsym ctrCSym)) (Pexpr [] () (PEval sevenVal)) mo)
      (Expr [] (Erun ra ctrLoopSym
        [ctrDecPe, Pexpr [] () (PEsym ctrCSym)]))))
    (ofVal (.pure Vunit)))

def ctrParams (xbty cbty : core_base_type) (n : Int) :
    List (sym × ((core_base_type ×
      Option (ctype × pass_by_value_or_pointer)) × generic_pexpr Unit sym)) :=
  [(ctrXSym, ((xbty, (none : Option (ctype × pass_by_value_or_pointer))),
    Pexpr [] () (PEval (ivVal n)))),
   (ctrCSym, ((cbty, (none : Option (ctype × pass_by_value_or_pointer))),
    Pexpr [] () (PEsym ctrPSym)))]

def counterProdProg (ra : core_run_annotation) (mo : memory_order)
    (bty xbty cbty sbty : core_base_type) (n : Int) : CoreExpr :=
  Expr [] (Esseq (symPat [] ctrPSym bty)
    (createExpr loc0 empty_annotation (.IV .Prov_none 4) intTy
      (PrefOther "spike-x"))
    (Expr [] (Esave (ctrLoopSym, sbty) (ctrParams xbty cbty n)
      (ctrBody ra mo bty))))
```
(`lets p = create(4, int) in save loop(x := n, c := p) in if x > 0 then
(store(int, c, 7); run loop(x - 1, c)) else unit`.) `ctrSSym`,
`ctrFrameS` and its three lookups deleted; `ctrCost i = 6 * i + 2` (was
`7 * i + 2`); `ctrBody_pot = 4` (was 5), `counterProdProg_pot = 6` (was
7). The registration equation `collect_new_ctrProd` is `rfl` on the new
term; `ctrQ` (the label map) is unchanged.

**The export `counter_loop_certified_production`** — the statement
changes in two places: the program term (above) and the fuel premise
`hfuel : 7 * n.toNat + 7 ≤ lemDefaultFuel` → `hfuel : 6 * n.toNat + 8 ≤
lemDefaultFuel` (the budget is `2 + (ctrCost n.toNat + saveEntryCost
(ctrParams xbty cbty n))` = `6 * n.toNat + 6`, plus `prod_run_eqJ`'s
`+ 2`). BORDERLINE (§7): for every `n ≥ 1` the new premise is weaker
than the old; at `n = 0` it reads `8 ≤ lemDefaultFuel` where the old
read `7 ≤ lemDefaultFuel` — incomparable at that one point (both
trivially true numerically at the engine's `lemDefaultFuel = 10^6`,
but as statements). The program's entry through the save costs one
more engine step than the old jump-from-outside at `n = 0`.

**`lrProdProg`** (ProdLoopExhibit.lean). Before: `lrProdPrefix ra mo bty`
= two creates, three constant binds (`lets w1 = 1`, `lets w2 = 2`,
`lets nz = NULL`), four stores `store(long, n1, w1)`, `store(node*,
array_shift(n1, long, 1), n2)`, `store(long, n2, w2)`, `store(node*,
array_shift(n2, long, 1), nz)`, then `run loop(nz, n1)`; `lrProdProg =
Esseq _ (lrProdPrefix …) (Esave (loop, sbty) (lrParams pbty cbty
nullNode) (lrBody …))` (the save dummy-initialized `prev := NULL, cur :=
NULL`, in the untaken arm). After:
```
def lrProdPrefix (ra : core_run_annotation) (mo : memory_order)
    (bty : core_base_type) (k : CoreExpr) : CoreExpr :=
  Expr [] (Esseq (symPat [] lrN1Sym bty)
    (createExpr loc0 empty_annotation (.IV .Prov_none 8) nodeTy
      (PrefOther "lr-n1"))
    (Expr [] (Esseq (symPat [] lrN2Sym bty)
      (createExpr loc0 empty_annotation (.IV .Prov_none 8) nodeTy
        (PrefOther "lr-n2"))
      (Expr [] (Esseq (Pattern [] (CaseBase (none, bty)))
        (storeOpRedex loc0 empty_annotation longTy
          (Pexpr [] () (PEsym lrN1Sym)) (Pexpr [] () (PEval (longVal 1))) mo)
        (Expr [] (Esseq (Pattern [] (CaseBase (none, bty)))
          (storeOpRedex loc0 empty_annotation nodePtrTy
            (lrShiftPe lrN1Sym) (Pexpr [] () (PEsym lrN2Sym)) mo)
          (Expr [] (Esseq (Pattern [] (CaseBase (none, bty)))
            (storeOpRedex loc0 empty_annotation longTy
              (Pexpr [] () (PEsym lrN2Sym)) (Pexpr [] () (PEval (longVal 2))) mo)
            (Expr [] (Esseq (Pattern [] (CaseBase (none, bty)))
              (storeOpRedex loc0 empty_annotation nodePtrTy
                (lrShiftPe lrN2Sym) (Pexpr [] () (PEval nullVal)) mo)
              k)))))))))))

def lrProdParams (pbty cbty : core_base_type) :
    List (sym × ((core_base_type ×
      Option (ctype × pass_by_value_or_pointer)) × generic_pexpr Unit sym)) :=
  [(lrPrevSym, ((pbty, none), Pexpr [] () (PEval nullVal))),
   (lrCurSym, ((cbty, none), Pexpr [] () (PEsym lrN1Sym)))]

def lrProdProg (ra : core_run_annotation) (mo : memory_order)
    (bty sbty pbty cbty bbty nbty ubty : core_base_type) : CoreExpr :=
  lrProdPrefix ra mo bty
    (Expr [] (Esave (lrLoopSym, sbty) (lrProdParams pbty cbty)
      (lrBody loc0 empty_annotation ra mo bbty nbty ubty)))
```
`lrW1Sym`/`lrW2Sym`/`lrNZSym` deleted; `lrPFrame` is two binds;
`lrProdProg_pot = 13` (was 12: the save is now inside the nest);
registration: `col_lrProdSave` (`rfl` at cushioned fuel, the
`col_lrProg` twin — the initializers are not read by the registration)
and `collect_new_lrProd` peels the six prefix layers. The export
`list_reverse_certified_production`'s statement changes only through
`lrProdProg` (no fuel premise; the constant budget check is `omega`).
`lrProd_wpt` enters the loop with `wpt_save` at `cvals := [ptrVal
nullNode, ptrVal (cellPtr i₁ a₁)]`, then the generic body theorem
`lr_body_wpt_frame` at the built chain (with the four stores'
annotation residues absorbed into the readout).

## 4. M-3 / Q9, Q10 — the six stratum twins and one naming scheme

Added (all new statements): `wpt_load` (the `wps_load` twin: `pointsToCell
… ∗ (∀ fp, pointsToCell … -∗ Ψ (.annot [DA_pos [] fp] (loadedVal …)) ρ) ⊢
wpt M Ls k Ψ (loadExpr …) ρ` at `3 ≤ k` and `htrap`; the `off := 0`
instance of `wpt_load_cell_at`, decode from the cell's own inertness),
`wpt_case_value` (an instance of `wpt_det_step`), `wpt_wseq` (the
`wpt_seq` clone through the Cwseq frame, with `wpt_jump_frame_wseq`),
`wpt_fupd` (budget induction), `wps_mono_Ls` (Löb), `blockSpecs_mono`
(through `wps_wand`). Renamed `wpt_store_cell` → `wpt_store` (no alias
kept; the statement is unchanged); every consumer (Exhibit,
LoopExhibit, ProdExhibit, ProdLoopExhibit, API.lean, Audit.lean,
README) updated. Both strata now read `*_store`, `*_load`, `*_load_at`,
`*_store_at`, `*_load_cell_at`, `*_store_cell_at`.

## 5. M-4 — item by item

- **Q3 (derivable `hnv` premises).** DONE for the store: the new premise
  is the engine's own dispatch (§1). KEPT, with the reason, for
  `wps_pure`/`wpt_pure` (`hnv : valueFromPexpr pe = none`) and
  `wps_load_eval`/`wpt_load_eval` (`hnv2 : valueFromPexpr pe2 = none`):
  the audit's derivability claim ("`evalPexpr` is the identity on `PEval
  v`, so in the excluded case the rule's conclusion is the premise
  verbatim") holds only at CANONICAL annotation lists. At `pe = Pexpr (a
  :: _) () (PEval v)` the node `Expr [] (Epure pe)` is not a mirror value
  (`toVal` demands empty annotation lists — the registered canonical-
  annotation subrelation D3) and no `Step` rule fires (`pure_eval` needs
  `hnv`), so the premise-free `wps_pure` would be FALSE there
  (`NotStuck` fails). For the load the premise IS the engine's dispatch:
  the redex's type operand is the canonical value, so `valueFromPexprs
  [Vctype ty, pe2] = none ↔ valueFromPexpr pe2 = none`. The mirror needs
  the redex-shape distinction and the rule cannot be proved without it;
  the premises are the engine's own tests, not representation leaks.
- **Q4 (one conditional rule).** DONE: `wps_if` / `wpt_if` (verbatim
  statement in the README rule table), proved directly (`wps_if`) and
  through `wpt_det_step` at each verdict (`wpt_if`); `wps_if_true`,
  `wps_if_false`, `wpt_if_true`, `wpt_if_false` retained as derived
  instances (statements unchanged; their proofs are the corollary). The
  eight exhibit consumers of the verdict rules are unchanged.
- **Q5 (`StorableAt` as the one vocabulary).** DONE with the audit's
  two-field-class alternative: `StorableAt` is strictly stronger than the
  four inlined fields by `stored_dec` (write-side decode inertness), which
  the typed-subrange rules never use — so requiring `StorableAt` there
  would have STRENGTHENED the premise. `StorableView tds ty mv` (Heap.lean:
  `compat`, `fpm`, `bytes_fpm`, `len` — exactly the four inlined facts,
  the length at the empty table) is the one vocabulary of `wps_store_at`,
  `wpt_store_at`, `wps_store_cell_at`, `wpt_store_cell_at`;
  `StorableAt.toView` is the forgetful map (used by the whole-cell
  `wpt_store` and the exhibit clients with `StorableAt` facts). The
  premise is equivalent to the old four (a repackaging), recorded as such.
- **Q6 (`decIndep_splice`).** STOPPED — the obstacle is in the memory
  model, not something to paper over. `decIndep tds a aty bs` says the
  decode of `bs` at `aty` is independent of the union-member table and
  the function-pointer table. `reconstructValue` (CerbMem.lean:686)
  consults those tables in exactly two arms — `Union0` (`unionmap.find?`
  at the member address, default the first member) and `Pointer` to a
  `Function` pointee (`funptrmap.find?`) — recursively through struct
  members and array elements. A lemma "`decIndep bs` and `stored_dec`
  of the image imply `decIndep (spliceBytes off img bs)`" is false in
  general: a union member whose range the store overwrites at a
  DIFFERENT type may decode table-dependently on the new bytes even
  though the whole old image and the new image (at its own type) were
  table-independent. It holds only under a LAYOUT side condition (the
  spliced range coincides with a member/element region whose type is
  the stored type) — a new premise, not a removal. The fragment's
  consumers never reason about the splice: for every type in the
  fragment `decIndep` holds for ALL byte images (`intTy_decIndep`,
  `structTy_decIndep`, `nodeTy_dec_indep`, `treeTy_dec_indep` are stated
  `∀ bs`), so `hdec'` is discharged per type. Replacing `hdec'` by that
  per-type fact would strengthen the premise (it is the honest cleaner
  restatement for a later slice if the professor wants the type-level
  vocabulary; not a generalization).
- **Q7 (`esize_select_case_le`).** STOPPED. `select_case subst_sym_expr`
  substitutes through the engine's `subst_sym_expr_lemFuel`
  (Core_aux.lean:517): fuel-indexed (`fuelExhausted g` — an opaque
  constant — at fuel 0), over all 22 constructors of `generic_expr`,
  with `List.map` over sub-expression lists (`Eunseq`, `Ecase`
  alternatives, `End`), `if in_pattern …` guards, and `subst_sym_paction`/
  `subst_sym_pexpr` for the operand layer. An `esize`-preservation lemma
  is a nested structural induction over the whole engine AST with a fuel
  bound (`esize e ≤ fuel`), for constructors almost entirely outside the
  fragment — the grind class the charter forbids. The explicit
  per-branch premise `hbsz` (discharged by computing the substitution on
  the concrete branch, as `CaseExhibit` does) is the honest form;
  the audit's "if provable as expected" is answered: provable in
  principle, not as a fragment lemma.
- **Q8 (annotation-generic `wps_pure`/`wps_annot`/`wpt_pure`/`wpt_annot`).**
  STOPPED — not a valid generalization under D3. At `Expr a …` with
  `a ≠ []` the successor of `pure_eval` (`Expr a (Epure (PEval v))`) and
  the wrapped value of `wps_annot` (`Expr a (Eannot ds (ofVal (.pure
  v)))`) are NOT mirror values (`toVal` demands `a = []`) and no `Step`
  rule fires on them, so the `Expr a` statements would be false (the
  engine treats them as values; the mirror's value protocol is the
  registered canonical-annotation subrelation). The four rules are
  stated at `[]` because that is where the mirror's values live; the
  branch/entry rules (`wps_if_*`, `wps_save`, `wps_run`) take `a` because
  their successors are not values. Lifting D3 is a `Step`/`toVal` design
  change, outside this slice.
- **Q12 (annotation-insensitive forms).** DONE: `wps_store_plain`,
  `wps_load_plain`, `wpt_store_plain`, `wpt_load_plain` — for `Ψ` with
  `∀ ds v ρ', Ψ (.annot ds v) ρ' = Ψ (.pure v) ρ'` (`AnnotInsensitive Ψ`,
  Wpt.lean; spelled out in Wps.lean, which does not import Wpt), the
  continuation is `pointsToCell … -∗ Ψ (.pure Vunit) ρ` resp. `-∗ Ψ (.pure
  (loadedVal …)) ρ` — no `∀ fp`. Derived from the primitives in five
  lines each; stated once each alongside them.
- **Q13 (the extern seam).** DONE: `evalPexpr_sym_of_resolve (hx :
  resolveExtern ext x = x) : evalPexpr tds ext ρ (PEsym x) = lookup_env x
  ρ` and `resolveExtern_id_of_empty (h : ext = fmapEmpty) : ∀ x,
  resolveExtern ext x = x` (Step.lean). `struct_create_store_wps` (an
  export) and `progAProd_wpt` take `hex : ∀ x, resolveExtern M.extern x =
  x` ("no extern redirects") instead of `hex : M.extern = fmapEmpty` —
  strictly weaker (`resolveExtern_id_of_empty`). Chosen over folding
  `extern` into the profile: the profile route would put a `SeqWF`-class
  field on every adequacy theorem (a strengthening), whereas the lookup
  premise says exactly what the client uses.
- **Q16 (`stateInert (Ecase _ pats) = pats.all …`).** STOPPED — the same
  obstacle as Q7: `Frag.stateInert_step`'s case arm would need
  `stateInert (subst_sym_expr x v e) = stateInert e`, a nested fuel-
  indexed induction over the engine's whole AST. `stateInert (Ecase _ _)
  = false` stays (conservative, N-5).

## 6. The spec-delta: `signature_snapshot` diff, classified

`scripts/signature_snapshot.lean` at `cc6a420` (19,559 lines) vs at the
final commit (20,297 lines); the raw `diff` is 2,628 lines. Classified
at the declaration level (DERIVED by a script over the two snapshots:
names present in exactly one snapshot, or in both with different
pretty-printed types). NB the snapshot records TYPES: the three
production program terms (`progAProd`, `ctrBody`, `counterProdProg`,
`lrProdProg`) changed in their BODIES only, so they and the exports
whose statements mention them by name (`exhibitA_prod`,
`list_reverse_certified_production`) do not appear below — their
before/after is §3, verbatim.

**CHANGED (45).** Auto-generated recursor/`below` variants of the three
inductives that gained a constructor or a premise (`Frag.*`, `Redex.*`,
`Step.*` — 16 names) follow their inductive; the substantive ones:

| Name | Change | Class |
|---|---|---|
| `Step.store_eval`, `Redex.store_op`, `Frag.store_op`, `stepDischarge_store_eval`, `step_ctx_store_eval_ws`, `wps_store_eval`, `wpt_store_eval` | `hnv2 : valueFromPexpr pe2 = none`, `hnv3 : valueFromPexpr pe3 = none` → `hnv : valueFromPexprs [pe2, pe3] = none` | premise WEAKENED (old conjunction implies it) — §1 |
| `Step.store_op_inv` | same premise weakening; conclusion drops `valueFromPexpr pe3 = none` | inversion of a wider relation — §7.4 |
| `Step.save_inv` | conclusion: TAU arm → TAU arm ∨ EVAL arm | inversion of a wider relation — §7.3 (old form: `Step.save_vals_inv`) |
| `Frag.save` | `+ (hd : ∀ pe ∈ saveParamPexprs ps, peDepth pe ≤ lemDefaultFuel)` | premise ADDED — BORDERLINE §7.1 |
| `wps_save` | `valueFromPexprs … = some cvals` → `evalPexprs M.tagDefs M.extern (ev0 :: evs) … = some cvals` | premise WEAKENED (old form: `wps_save_vals`) — §2 |
| `wpt_save` | premise as `wps_save`; budget `k + 1` → `k + saveEntryCost ps` (`= k + 1` by `rfl` at the old premise) | GENERALIZED, old form the instance `wpt_save_vals` — §2 |
| `wps_store_at`, `wpt_store_at`, `wps_store_cell_at`, `wpt_store_cell_at` | `hcompat hfpm hbytes hlen(img)` → `hst : StorableView M.tagDefs vty mv` | REPACKAGED (equivalent premise) — Q5 |
| `struct_create_store_wps`, `progAProd_wpt` | `hex : M.extern = fmapEmpty` → `hex : ∀ x, resolveExtern M.extern x = x` | premise WEAKENED — Q13 |
| `counter_loop_certified_production` | `hfuel : 7 * n.toNat + 7 ≤ lemDefaultFuel` → `6 * n.toNat + 8 ≤ lemDefaultFuel` (program body also restated) | BORDERLINE §7.2 |
| `ctrProd_wpt`, `lrProd_wpt`, `progAProd_wpt` | budgets (`(2 + (ctrCost n.toNat + 1)) + 0` → `2 + (ctrCost n.toNat + saveEntryCost (ctrParams xbty cbty n))`; the reversal's five-bind budget → the four-store + save budget; 11 → 10) | exhibit-internal whole-program judgments, restated with the programs |
| `ctrCost_eq` (`7 * i + 2` → `6 * i + 2`), `ctrBody_pot` (5 → 4), `counterProdProg_pot` (7 → 6), `lrProdProg_pot` (12 → 13), `lrProdPrefix_pot`, `lrProdPrefix` (gains the continuation `k`), `lrProdPrefix_frag` (gains `hk : Frag k`), `ctr_backedge_args_eval` (at `ctrFrame`, no bound constant) | exhibit-internal helpers, restated with the programs |

**REMOVED (19)** — all exhibit-internal helpers of the pre-QA-1 program
shapes, plus one rename: `wpt_store_cell` (→ `wpt_store`, statement
unchanged); `vASym`, `prodAFrame_lookup_v` (progAProd's constant bind);
`ctrSSym`, `ctrFrameS`, `ctrFrameS_lookup_c/_s/_x`,
`ctr_store_operands_eval`, `ctr_entry_args_eval` (the counter's
per-iteration constant bind and jump-entry); `lrW1Sym`, `lrW2Sym`,
`lrNZSym`, `lrPFrame_lookup_w1/_w2/_nz`, `lrPFrame_entry_args`,
`col_lrStores`, `col_lrProdPrefix` (the reversal's three constant binds
and jump-entry, and the old registration peeling). No public rule or
export was removed.

**ADDED (71)** — the new statements: `Step.save_eval` (+ its `below`
variant), `Step.save_vals_inv`, `Step.save_op_inv`,
`saveParamsWithValues` (+ `_nil`, `_cons`, `saveParamPexprs_withValues`,
`valueFromPexprs_withValues`, `bindSaveParams_withValues`,
`saveParamsWithValues_depth`), `valueFromPexprs_cons/_nil/_map_val/
_length`, `evalPexpr_of_valueFromPexpr`, `evalPexprs_of_valueFromPexprs`,
`evalPexprs_length`, `saveParams_depth_of_vals`, `mapM1_id_defined`,
`mapM_save_bridge`, `stepDischarge_save_eval`, `step_ctx_save_eval_ws`
(the save EVAL arm and its certification); `wps_save_vals`,
`wps_save_eval`, `wpt_save_vals`, `wpt_save_eval`, `saveEntryCost`
(+ `_of_vals`, `_of_eval`) (the save rules' pieces); `wpt_load`,
`wpt_case_value`, `wpt_wseq`, `wpt_jump_frame_wseq`, `wpt_fupd`,
`wps_mono_Ls`, `blockSpecs_mono`, `wpt_store` (the six twins + the
rename target); `wps_if`, `wpt_if` (Q4); `StorableView` (+ `.mk`,
`.compat`, `.fpm`, `.bytes_fpm`, `.len`, `.rec`, `.recOn`, `.casesOn`),
`StorableAt.toView` (Q5); `AnnotInsensitive`, `wps_store_plain`,
`wps_load_plain`, `wpt_store_plain`, `wpt_load_plain` (Q12);
`evalPexpr_sym_of_resolve`, `resolveExtern_id_of_empty` (Q13);
`store_sym_lit_step`, `store_lit_sym_step`, `wps_store_sym_lit`,
`wpt_store_lit_sym` (the mixed store shapes, H-1); and the restated
exhibits' helpers `ctrParams`, `ctrParams_depth`, `bindSaveParams_ctr`,
`ctr_save_params_eval`, `ctr_store_ptr_eval`, `lrProdParams`,
`lrProdParams_depth`, `bindSaveParams_lrProd`, `lrPFrame_save_params`,
`col_lrProdSave`.

Capability manifest: regenerated, 18 rows, 0 red; the one changed row
is `Frag.store_op → wps_store_eval`, whose consumer list gains
`ProdExhibit` (the mixed-shape instance `wps_store_sym_lit`).

## 7. Borderline items (for the orchestrator)

1. `Frag.save` gains the fuel-honesty premise `hd` (§2). A premise
   addition on the adequacy premise's constructor — narrowing `Frag` on
   initializer shapes that no rule and no mirror step ever covered, and
   that the engine's certification cannot cover (fuel). Every other
   evaluating constructor carries the same condition. If the orchestrator
   rules this out of the slice's classification, the alternative is the
   audit's minimal one (narrow to the literal form) — a larger narrowing.
2. `counter_loop_certified_production`'s fuel premise `6 * n.toNat + 8`
   vs `7 * n.toNat + 7` (§3): weaker for all `n ≥ 1`, incomparable at
   `n = 0` (8 vs 7). Forced by the program's natural shape (save entry
   is two engine steps; the old jump-from-outside was one).
3. `Step.save_inv`'s conclusion weakened to a disjunction (§2) —
   inherent to adding a rule; the old form survives as
   `Step.save_vals_inv`.
4. `Step.store_op_inv`'s conclusion lost the conjunct `valueFromPexpr
   pe3 = none` (no longer derivable at the mixed shapes) — inherent to
   the premise weakening.

## 8. QA-1 disposition of the audit's items (the audit record is untouched)

| Item | Disposition |
|---|---|
| H-1 | CLOSED: store and save arms at the engine's generality; production programs restated; the manifest row `Frag.save → wps_save` is now true at every initializer shape within fuel |
| M-3 / Q9, Q10 | CLOSED: six twins added; one naming scheme |
| M-4 (a) / Q3 | store: closed; pure/load_eval: kept with the D3 reason (§5) |
| M-4 (b) / Q5 | CLOSED via `StorableView` (the audit's two-field-class alternative) |
| M-4 (c) / Q6 | STOPPED — memory-model obstacle recorded (§5) |
| M-4 (d) / Q4 | CLOSED: `wps_if`/`wpt_if` |
| M-4 (e) / Q8 | STOPPED — D3 (§5) |
| M-4 (f) / Q12 | CLOSED: the four `_plain` forms |
| M-4 (g) / Q13 | CLOSED: the extern lookup premise |
| Q7 | STOPPED — fuel-indexed engine substitution (§5) |
| Q16 | STOPPED — same obstacle |
| Q1, Q2 | CLOSED (H-1) |

## 9. Gates

Every intermediate commit: `scripts/test_unit.sh --fast` exit 0
(gates 1–3). The final commit: the FULL gate, `scripts/test_unit.sh`
from the repo root at the final tree, exit code 0. Verbatim tail (the
log filtered only of the Lean linter-warning lines — `warning:`/`Note:`/
`Hint`/`The binding …`/`✔`/`ℹ` — and of `trace:` lines; every remaining
line is as printed, including the stray fragment `instead of` from a
multi-line linter message):

```
ok: root build green
== gate 3: capped build, cerberus-heaplang (elaborates its axiom audit) ==
cerberus-lean-proj env: switch=/home/dev/projects/cerberus-lean-proj/cerberus-lean/_opam, git redirects active
instead of
info: CerberusHeapLang/Audit.lean:181:0: CerberusHeapLang export pins: 123 trio-exact
info: CerberusHeapLang/Audit.lean:181:0: CerberusHeapLang axiom sweep: 1232 theorems bounded by the trio
info: CerberusHeapLang/Audit.lean:181:0: CerberusHeapLang banned-axiom sweep: 2085 constants of every kind checked; sorryAx/ofReduceBool/ofReduceNat absent from all cones
Build completed successfully (444 jobs).
ok: cerberus-heaplang build green
== speedbump: capability manifest (regenerate; red on a red row or drift) ==
cerberus-lean-proj env: switch=/home/dev/projects/cerberus-lean-proj/cerberus-lean/_opam, git redirects active
ok: capability manifest regenerated, no drift
== speedbump: import direction (semantics → heap → rules → adequacy → clients) ==
ok: import direction — no core module imports an exhibit/example/production module
ALL GATES GREEN
exit 0
```
