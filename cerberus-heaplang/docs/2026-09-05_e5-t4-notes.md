# E5 t4 while — work record

## Whole-term membership checkpoint

[AGENT, 2026-09-05, active demo-completion goal] Continued from `41bee5d`
on `dialect-e5`, worktree `worktrees/dialect-e1`. The preceding t6
checkpoint was progress: its shipped-driver theorem and full gate were
committed and the worktree was clean. Main, the semantics pin
`f95ef8d9c317fa6b50cf6691216a8c37b1d3eabf`, and Lean 4.32.2 are unchanged.

`Examples/CorpusE0.lean` adds the t4 main transcription, retaining the
nested loaded-integer truth conversions in `i < 5 && s < 7`, both
short-circuit branches, both negative-store assignments, all four save
labels and the dead cleanup. Parameterized helpers name the repeated
comparison, truth conversion, addition and assignment shapes. They leave
the raw `unseq`, `case`, `bound` and action nodes in place.

The skeleton has 591 tokens. Its bound, Astd, specified-value,
save-initializer, case-scrutinee, if-condition and case-pattern mutation
plants all mismatch. Existing t1/t5/t6 and the three std.core rows pass.
The comparison does not check symbol identities, literal/type values,
hidden metadata or sequence association. The body and outer loop block
association were checked separately against the pinned frontend's
`erase_loop_control_aux`, `translate_stmt` and `mk_unit_sseq`: the
continue label belongs inside the transformed body block, followed by
the back-edge; the return follows the transformed while/break block.
This is source inspection, not an additional equality theorem.

`Examples/CorpusE5.lean` proves `t4Main_frag` compositionally, including
every syntactic branch. `t4And_select` characterizes the engine's case
selection at every value: specified object payloads select the
substituted conditional branch, unspecified payloads select UB_CERB004's
conditional case, and other values do not match. Both comparison cases
use the existing arbitrary-binding-list substitution theorem; membership
does not assume a reachable integer or a well-shaped two-element tuple.
All seventeen new theorem cones were individually measured at exactly
`[propext, Classical.choice, Quot.sound]` and pinned in Audit.

This checkpoint proves membership of the transcription. It does not
yet certify t4's execution or close the stronger emitted-file/library
connection (KOI A7). The visible-print transcription still shares the
existing corpus's unchecked identity/metadata boundary; the final file
criterion requires a stronger comparison, with any repairs re-proved.
No source-level compiler-correctness claim is made.

Validation: capped targeted builds of CorpusE0 and CorpusE5 passed;
the separate corpus-skeleton command passed after the sequence-grouping
correction; `CERB_MEM_MAX=40G scripts/test_unit.sh --fast` exited 0.
Selected fast-gate output, verbatim:

```
info: CerberusHeapLang/Audit.lean:992:0: CerberusHeapLang export pins: 835 trio-exact
info: CerberusHeapLang/Audit.lean:992:0: CerberusHeapLang axiom sweep: every theorem bounded by the trio (5860 swept, internal details included — count informational, environment-dependent)
info: CerberusHeapLang/Audit.lean:992:0: CerberusHeapLang banned-axiom sweep: sorryAx/ofReduceBool/ofReduceNat absent from all cones (8905 constants of every kind swept, internal details included — count informational, environment-dependent)
Build completed successfully (470 jobs).
ok: cerberus-heaplang build green
FAST-GATE GREEN (gates 1-2 only — not a claim-point result; say fast-gate in the commit)
```

## Next action

Prove t4 through the public total rules and shipped driver. Start with
the engine-collected continuations for while, continue, break and return,
and factor the t5/t6 load/assignment proof scaffolding as t4 supplies its
next client. The loop state should track `0 ≤ i ≤ 5` and
`s = i * (i - 1) / 2`, with a decreasing budget based on `5 - i`.
The anticipated return is 10; that is a source-level calculation here,
not yet a proved production result or a newly measured oracle result.

Preserve short-circuiting on the final iteration, the two reads in
`s + i`, the fresh symbols from both assignments, and the surrounding
continuations. Do not replace these with an unrolled engine run or a
private simplified program. Complete the total derivation, production
equation and full gate before calling t4 certified. E5's independent
range review and all later charter milestones remain open. This is a
continuing checkpoint, not a park, merge or push.
