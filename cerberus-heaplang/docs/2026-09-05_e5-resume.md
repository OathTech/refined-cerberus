# E5 resume and the demo acceptance target — 2026-09-05

## Authorization and scope

[L1 landing note, 2026-09-07, AGENT] This record is the t5 checkpoint's
record of the code (budgets, census, pins, gate tail, the manifest class it
introduced) and is taken onto main with the code. It was written by the
other agent on `dialect-e5`, resuming past the orchestrator's committed E5
park record `f433820`. The resume instruction as that agent recorded it (a
`[USER, this session]` quotation the landing could not verify) stays with
the source's DECISIONS entry "Resume E5 and establish the demo acceptance
target" on the parked branch `parked/demo-expansion-2026-09-07`; it is not
reproduced here. The operator confirmed the resume on 2026-09-07 — the
landing charter's ruling R6, verbatim in `docs/DECISIONS.md` (2026-09-07
"THE LANDING CHARTER'S RULINGS"): "R6 - yes, I approved this, you can mark
it as such". "The charter" and "the demo stop-state" below refer to the
other agent's `docs/2026-09-05_demo-completion-charter.md`, a process
document that stays on the parked branch; the acceptance interpretation
that follows is that agent's own [AGENT] reading, not a ruling.

This resumes the park at `f433820` on `dialect-e5`. The project is
refined-cerberus; sibling repositories are dependencies and references.
The existing per-merge and per-push sign-offs remain separate actions.

[AGENT] Acceptance interpretation: complete the emitted-Core E5–E7
milestones through the genuine semantics; make the supported fragment,
theorem assumptions, fuel behavior and whole-file/std.core boundary
precise; maintain useful examples through the public logic rules; finish
the coverage, export audits and documentation; review the resulting
architecture before the version-one claim. RefinedC features remain the
next project stage. A passing transcription checker is not a program
certification, and a fragment witness is not a WP derivation.

## Work in this continuation

The first target is t5's emitted conditional and negative assignment,
followed by t6 and t4. The existing supply-normalization definition is
retained while proving the production path, whose initial supply is
explicit. Its effect on seeded profiles remains a named range-audit item;
no unchanged statement text is evidence of an unchanged definition.

The old dependency blockers need remeasurement: the sibling mainlines now
contain fuel parameters and Pmap laws. After the bounded E5 milestone,
refresh the representation/fuel re-pin assessment before extending the
scheduler adequacy in E6. No old scout estimate is adopted as a current
measurement.

Progress and verification will be recorded here at coherent checkpoints.

## Build-command correction

[AGENT] During fragment development, two commands invoked `lake --dir
cerberus-heaplang` from the repository root. Elan selected the host's
4.33.0 toolchain before Lake changed directory. Both task-owned builds
were stopped; their output is not validation evidence. Subsequent builds
run from `cerberus-heaplang/`, with `lean --version` first confirming the
pinned 4.32.2. The affected worktree caches are rebuilt by that toolchain.
No dependency pin or source was changed by this correction.

## t5 fragment checkpoint

The t5 transcription matches 271 skeleton tokens from the recorded emitted
Core; t1 still matches its 121. The four existing plants mismatch on both
rows. New expression-case scrutinee, expression-if scrutinee and case-pattern
plants all apply and mismatch on t5; they report not applicable on t1.
The three std.core skeleton rows also match.

`Examples/CorpusE5.lean` proves membership for the complete t5 term,
including both assignments, dead cleanup and the unspecified-value choice.
`Substitution.lean` proves preservation of the covered pure-expression
grammar and depth under value substitution within the Lem fuel bound; this
supplies case membership for every successful pattern match, including the
engine's truncated tuple matches. No reachability assumption narrows `Frag`.
All 14 new named theorems were axiom-measured: 12 use exactly the classical
trio and are pinned in Audit; the two depth-list lemmas use propext only.
This checkpoint does not claim a WP or shipped-driver theorem for t5.

The intermediate fast gate passed with the pinned Lean 4.32.2: 468 jobs,
724 trio-exact export pins, every theorem bounded by the trio, and no
banned axiom in any constant's cone. `git diff --check` passed. This is a
fast-gate checkpoint, not E5 acceptance; the full reports are still due.

## t5 execution checkpoint

`CorpusT5Exhibit.lean` proves the complete emitted conditional through
public logic rules. `t5_wpt` has budget 88: initialization 17, conditional
55 and return 16. The conditional decomposes into the comparison-to-zero
protocol (25), boolean decoding (4), the if round (1) and assignment
block (25). The assignment's 25 units cover the two pure operands and
unseq completion (7), the negative-store protocol (16), and the two
statement units (2). These are rule-composed upper bounds, not a claim
about the exact number of driver rounds.

`t5_certified_production` concludes an equation over the genuine
`CerbND.runND (drive ...) (initial_driver_state ...).1`: exactly one Active
execution, result `lint 1`, blocked false, empty stdout and stderr. Its
initial supply assumption is `600 ≤ sup`. All source symbol numbers are
below that floor; the proof uses the fresh-symbol fact delivered by
`wpt_neg_bound` to retain x/r lookups after the assignment. The existing
recorded OCaml oracle result is Specified(1) (e5-notes §S2.6); this
checkpoint does not claim a new oracle run or a new dependency pin.

The file object remains `prodFileLib stdlibE3 [] CorpusE0.t5Main`, with the
three transcribed std.core functions and empty implementation map. The
program is real emitted Core entered by hand transcription, whose
constructor skeleton is checked against the fixture. Neither that check
nor the execution theorem certifies the C frontend or the full frontend
file/library boundary (KOI A7). Both branches and dead cleanup remain in
the term; every selected branch has a Frag witness for every possible
scrutinee, while the total derivation proves only the actual reachable
path.

Reusable additions: `wpt_unseq_pure_right` in Wpt expresses the driver's
right-first order when the right component is a pure operand; its empty
footprint cannot race with the left. It is a composition of existing
public rules, and is generic in the judgment's HasLC parameter. The
mixed-type two-leaf binder law is in EnvLaws. Local proof tactics in the
example apply the public frame/lookup laws and use kernel decision on
concrete symbol comparisons; freshness comparisons remain explicit.
Four MirrorCoverage witnesses certify the negative rewrite, excluded
store discharge, excluded-store operand evaluation and case evaluation
against the engine round.

### Coverage report and exact pins

The t5 proof consumes the total faces. The partial faces already proved
at the previous park remain undemonstrated by a corpus client. The report
previously represented only the reverse situation. Its symmetric
RULE-PARTIAL-UNDEMONSTRATED class checks that both theorems exist, the
total rule has a consumer and the partial rule has none; a future partial
consumer makes the row require reclassification. This replaces the five
incorrect NO-RULE rows without inventing a partial derivation. README,
CLAIMS, API and ARCHITECTURE state the current coverage and file boundary.

Measured against the first-slice signature snapshot: 136 added declarations
(86 theorems, 46 definitions, four opaque parser functions/auxiliaries),
two removed statements (`procCtx_runState`, `procCtxF_runState`), and
17 changed statements. The removals and the changes to the soundness and
driver lemmas reflect the previous slice's seeded supply normalization
and supply-floor premises, not a t5-only textual change. The complete
post snapshot is `2026-09-05_e5b-signatures-post.txt`. Of the 86 new
non-internal theorems, 67 have exactly the classical trio and 19 use
fewer axioms; `2026-09-05_e5b-axioms.txt` records every name. Twelve pins
were added at the fragment checkpoint and the remaining 55 here:
779 trio-exact pins in total. The exhaustive all-theorem/all-constant
sweeps remain in force, including internal details.

### Still required after this checkpoint

This is a completed t5 checkpoint, not E5 acceptance or the user's whole
demo stop-state. Continue with t6_switch and t4_while. Revisit the seeded
supply normalization in the E5 range audit. Partial corpus derivations
remain a disclosed coverage gap. The unreached seq_rmw, PtrValidForDeref
and Elet requirements remain on the wider dialect list. Refresh the
consumer re-pin scout and fuel assessment against the dependency work
that has now landed before undertaking E6's call/scheduler changes, then
complete E7 and the architecture/hygiene review. In particular, the full
frontend-file/std.core boundary is still open; do not describe this
checkpoint as closing it.

### Final checkpoint verification

The FULL gate (`CERB_MEM_MAX=40G scripts/test_unit.sh`) completed with
exit 0, on Lean 4.32.2 and the unchanged semantics pin. Selected output,
verbatim:

```
== gate 1: banned proof-method grep (native_decide / bv_decide / ofReduce*) ==
ok: no banned proof-method references
== gate 2: capped build, cerberus-heaplang (elaborates its axiom audit) ==
info: CerberusHeapLang/Audit.lean:932:0: CerberusHeapLang export pins: 779 trio-exact
info: CerberusHeapLang/Audit.lean:932:0: CerberusHeapLang axiom sweep: every theorem bounded by the trio (5731 swept, internal details included — count informational, environment-dependent)
info: CerberusHeapLang/Audit.lean:932:0: CerberusHeapLang banned-axiom sweep: sorryAx/ofReduceBool/ofReduceNat absent from all cones (8690 constants of every kind swept, internal details included — count informational, environment-dependent)
Build completed successfully (469 jobs).
ok: cerberus-heaplang build green
== speedbump: rule-use and classification manifest (regenerate; red on a red row or drift) ==
ok: capability manifest regenerated, no drift
== speedbump: corpus skeleton (hand-transcribed emitted Core vs docs/corpus-e0; scripts/corpus_skeleton.lean) ==
ok: corpus skeleton — every transcription matches its emitted text, every plant mismatches
== speedbump: import direction (semantics → heap → rules → adequacy → clients) ==
ok: import direction — 18 core modules, none imports an exhibit/example/production module
== speedbump: client boundary (positive clients mention no logic internals; scripts/boundary_check.sh) ==
BOUNDARY: 26 modules checked, 0 internals mention(s) in total, exit=0
ok: client boundary — no unallowlisted internals mention
ALL GATES GREEN
```

The final manifest has 35 constructors and 76 rows: 40 RULE,
5 RULE-PARTIAL-UNDEMONSTRATED, 25 NO-RULE and 6 OUT-OF-SCOPE;
0 red, 23 consumers. The claim matrix has 15 checked rows. The t5
transcription still matches all 271 tokens after naming its branch lists,
conditional and return suffix. The existing and new mutation plants all
behave as required.

`cite_check.sh --fix` corrected 44 tightly attributed declaration cites in
ARCHITECTURE. Its report still contains loose/range/usage citations that
need human review (283 cites inspected; initial categories: 131 EXACT,
97 DECL, 25 USE, 21 HAND, 9 PIN, 0 NOFILE). No claim is made that those
remaining citations were all repaired. This joins the E5 range-review
work, rather than being hidden by the green build. `git diff --check`
passes. Source, proofs and dependency pins outside refined-cerberus were
not changed.
