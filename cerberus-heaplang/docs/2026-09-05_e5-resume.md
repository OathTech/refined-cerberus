# E5 resume and the demo acceptance target — 2026-09-05

## Authorization and scope

[USER, this session]: "Yes, please work on E5 & beyond - our intended
near-term stop-state is that our 'demo' Reynolds/O'Hearn logic is (1) good,
clean, well-defined in every way, a good demo exemplar, and (2) it works on
real emitted core produced by cerberus."

This resumes the park at `f433820` on `dialect-e5`. The project is
refined-cerberus; sibling repositories are dependencies and references.
The existing per-merge and per-push sign-offs remain separate actions.

[AGENT] Acceptance interpretation: complete the emitted-Core E5–E7
milestones through the genuine semantics; make the supported fragment,
theorem assumptions, fuel behavior and whole-file/std.core boundary
precise; maintain useful examples through the public logic rules; finish
the coverage, export audits and documentation; review the resulting
architecture before the version-one claim. RefinedC features remain the
next project stage. A passing transcription checker is not a program
certification, and a fragment witness is not a WP derivation.

## Work in this continuation

The first target is t5's emitted conditional and negative assignment,
followed by t6 and t4. The existing supply-normalization definition is
retained while proving the production path, whose initial supply is
explicit. Its effect on seeded profiles remains a named range-audit item;
no unchanged statement text is evidence of an unchanged definition.

The old dependency blockers need remeasurement: the sibling mainlines now
contain fuel parameters and Pmap laws. After the bounded E5 milestone,
refresh the representation/fuel re-pin assessment before extending the
scheduler adequacy in E6. No old scout estimate is adopted as a current
measurement.

Progress and verification will be recorded here at coherent checkpoints.

## Build-command correction

[AGENT] During fragment development, two commands invoked `lake --dir
cerberus-heaplang` from the repository root. Elan selected the host's
4.33.0 toolchain before Lake changed directory. Both task-owned builds
were stopped; their output is not validation evidence. Subsequent builds
run from `cerberus-heaplang/`, with `lean --version` first confirming the
pinned 4.32.2. The affected worktree caches are rebuilt by that toolchain.
No dependency pin or source was changed by this correction.

## t5 fragment checkpoint

The t5 transcription matches 271 skeleton tokens from the recorded emitted
Core; t1 still matches its 121. The four existing plants mismatch on both
rows. New expression-case scrutinee, expression-if scrutinee and case-pattern
plants all apply and mismatch on t5; they report not applicable on t1.
The three std.core skeleton rows also match.

`Examples/CorpusE5.lean` proves membership for the complete t5 term,
including both assignments, dead cleanup and the unspecified-value choice.
`Substitution.lean` proves preservation of the covered pure-expression
grammar and depth under value substitution within the Lem fuel bound; this
supplies case membership for every successful pattern match, including the
engine's truncated tuple matches. No reachability assumption narrows `Frag`.
All 14 new named theorems were axiom-measured: 12 use exactly the classical
trio and are pinned in Audit; the two depth-list lemmas use propext only.
This checkpoint does not claim a WP or shipped-driver theorem for t5.

The intermediate fast gate passed with the pinned Lean 4.32.2: 468 jobs,
724 trio-exact export pins, every theorem bounded by the trio, and no
banned axiom in any constant's cone. `git diff --check` passed. This is a
fast-gate checkpoint, not E5 acceptance; the full reports are still due.
