# Alloc arc P0 — claims corrected before the logic changes (slice record)

Charter: `../../docs/2026-09-01_cerberus-heaplang-skeptical-re-audit.md`
§P0 ("Correct the claims before changing the logic"), adopted
verbatim by `2026-09-01_alloc-arc-plan.md`. Definition of done:
"generated and handwritten claims say exactly what the current proof
graph says, even before the defect is fixed"; merge-sequence row 1:
"Current gates green; create visibly downgraded."

ZERO proof-content change: docs / comments / instrument-output only.
Evidence: `2026-09-01_p0-signatures-pre.txt` /
`2026-09-01_p0-signatures-post.txt` diff EMPTY (18701 lines each;
the snapshot covers every constant kind's pretty-printed type). The
statement census is untouched (gate 5 drift-free); the Audit.lean
comment edit was line-count-neutral, so the quoted sweep tails
(`Audit.lean:622`) remain byte-valid.

## The five P0 items, disposition

1. **Create downgraded everywhere** — "LOCAL RULE ONLY; allocator
   resource not launchable" (no adequacy launcher grants `cursorOwn`;
   cursor ghost heap launched EMPTY; no `cursorHeap_alloc`; R-01) now
   stated at: README (intro parenthetical, Wps module-table row,
   StructExhibit row, divergence register — new R-01 row), walkthrough
   §4 (new allocation-caveats paragraph), manifest (create row logic
   cell + Notes 4, regenerated), and the module headers/docstrings:
   `Wps.lean` (file header + §CreateRule header + `wps_create`
   docstring), `Heap.lean` (CohG cursor note), `ProdEntry.lean`
   (the create/WP-layer note), `StructExhibit.lean` (header +
   CreateConsumer section), `Audit.lean` (Phase-2 pin comment).
2. **The three allocating production exports relabeled MIXED**
   logical/operational proofs, not create-rule consumers (R-02):
   `exhibitA_prod` (create prefix + six-round termination trace
   operational), `counter_loop_certified_production` (cold-start
   create prefix operational), `list_reverse_certified_production`
   (two creates + four field stores operational) — in the README
   exhibit table (each row), the README scope narrative (new
   disclosure paragraph) and exhibitA paragraph, the walkthrough §4,
   the manifest create row (prodLane + consumer cell notes), the
   README module table (ProdLoopExhibit/ProdExhibit rows), and the
   `ProdExhibit.lean`/`ProdLoopExhibit.lean` module headers. A new
   R-02 row in the README divergence register.
3. **FULL-ROW renamed CORE-DRIVE-ROW** (the audit's R-04 option b —
   the honest minimal change; the dependency-staged oracle is P3's
   work, deliberately NOT attempted here): `Row.fullRow` →
   `Row.coreDriveRow` in the generator; the machine-readable section
   now reports each lane separately (`CORE-DRIVE-ROW`,
   `LOGIC-RULE-LANE`, `TOTAL-LANE`, `PRODUCTION-LANE`,
   `LOCAL-RULE-ONLY`) with printed line semantics (lane membership =
   cell non-redness, name-and-kind checked, NOT dependency-traced —
   the R-04 caveat is generated into the manifest preamble).
   Manifest regenerated; gate 4 green on the new form (it consumes
   only the unchanged `ADEQUACY-EXPORTABLE` line and the drift diff).
4. **Stale statements removed** (R-10's enumerated list): the
   `Wps.lean:56-60` "NO `wps_create` exists at ANY layer" header
   (the theorem exists in the same file — replaced with the accurate
   local-only statement); the README per-byte-split divergence row
   (stale since Phase 2 — replaced by the R-01 row) and the
   production-loop-export-pending row (stale since Phase 5 —
   deleted); the module-table Ecase "local rule only" qualifier
   (case-value is certified with adequacy consumers) and the retired
   names `DecompJ.step_factor`/`engine_step_matchJ` (→ the current
   `Decomp.step_factor`/`engine_step_matchU`); the
   `Soundness.lean:3557-3575` "Ecase stays OUT"/`engine_step_matchJ`
   cone comment (rewritten to the unified S1b truth); the README
   production-loop narrative gained the handwritten-allocation-prefix
   disclosure. Also trued while passing (R-09 sub-item, cheap and
   claims-surface): the `SemTriple` docstring and walkthrough §3.2
   now state the fixed `spikeThread`/`spikeCtx` demo profile.
5. **The R-01..R-11 closure table** added to
   `2026-09-01_alloc-arc-plan.md`: per finding — owner phase,
   intended theorem/gate names, the audit's ACCEPTANCE TEST as the
   closure test ("theorem exists" is never a closure test), status
   (all OPEN; P0 sub-items marked closed on R-04/R-09/R-10). The
   struck records annotated without rewriting history:
   `../../docs/2026-09-01_foundational-re-audit.md` (header
   blockquote) and `2026-09-01_foundations-arc-summary.md` (arc-close
   section).

## Gate evidence

`./scripts/test_unit.sh` re-run after all edits + manifest
regeneration: see the commit's stated result (all five gates; the
manifest and census drift checks green on the new manifest form).
