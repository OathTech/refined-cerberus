# K5.1 + RE-PIN range audit (`226c0d3..f2f9701`)

Independent auditor, 2026-09-03. Audited in the fixed detached copy
`worktrees/audit-repin` (HEAD `f2f9701`), read-only except this file; no
`lake build` run. Brief: `docs/AUDIT-BRIEF.md` (grading follows it).
Provenance: every judgment below is [AGENT] (the auditor's). Quoted
outputs are verbatim; tallies marked DERIVED are grep/diff/script-derived.

Range: `226c0d3` (K5 audit record) → `f2f9701`. Commits: `367af77`,
`71825f1` (DECISIONS, docs only); `4e19bdc`, `932dfcf` (K5.1);
`a3eb560`, `f2f9701` (RE-PIN, rebased over K5.1).

## Verdict

- **K5.1 (region distinctness): PASS.** The two laws are exact
  instances of `metaOwn_ne` (= iris-lean `pointsTo_ne`) at the region
  bundles, trio-exact, pinned; `(ids ++ done).Nodup` is carried
  correctly through both phases; the four statements gain exactly one
  conjunct each; the snapshot tally 4/0/5 re-derives exactly; the
  "no `deadRegion_ne`" claim is right.
- **RE-PIN (to `f95ef8d9c`): PASS with one Medium (process) and Notes.**
  Pin correct, `--check` green in the copy, LemLib unchanged, every fix
  budget-class, side conditions strict generalizations, statements'
  referent is the shipped `drive`, `driveU`/PROVISIONAL untouched, zero
  `sorry` terms in the generated tree. The Medium: the combined tree at
  `f2f9701` (K5.1 + re-pin) has NO recorded gate — the only FULL gate in
  the record (and the commit message's "294 pins") is the PRE-rebase
  tree's; the audit copy's `.lake` oleans are also the pre-rebase
  tree's. I found post-rebase oleans in the `repin-fuel` worktree that
  show the combined tree elaborates (296 pins, trio), but that is not a
  gate and is not in the record.
- **Merge recommendation:** merge the combined range ff-only AFTER the
  orchestrator runs and records the FULL gate at `f2f9701` (or its
  successor with the two Note fixes), and after the stale "(294)" in the
  `Audit.lean` header is corrected. Nothing in the logic or its coverage
  is wrong.

## Findings

### Medium

**M-1. The combined head has no recorded gate; the record and the
audit copy's build state are the pre-rebase tree's.**
- Location: commit `f2f9701` message ("FULL gate: ALL GATES GREEN, exit 0
  (294 export pins trio-exact …)"); `docs/2026-09-03_repin-fuel-notes.md`
  §8 ("Rebased base: `367af77` … K5.1 not yet landed"); the audit copy's
  `cerberus-heaplang/.lake/build`.
- Claim: the range's head is gated green.
- Tree's truth: the head's `Audit.lean` `trioExports` has **296** entries
  (DERIVED: count of double-backtick names in the list = 296), and the
  worker's gate ran on a tree WITHOUT K5.1 (294). In the audit copy the
  oleans predate the rebase (`Heap.olean` 10:58:02, `MallocListExhibit.olean`
  11:16:16, `Audit.olean` 11:16:18; the K5.1 commit is 11:22:09, the
  rebased head 11:29:54). Environment query in the copy
  (`lake env lean`, verbatim excerpts):
  ```
  .lake/audit-scratch/q_k51.lean:4:8: error(lean.unknownIdentifier): Unknown identifier `regionOwn_ne`
  ...
  294
  ```
  i.e. the copy's environment is the re-pin WITHOUT K5.1. In the
  `worktrees/repin-fuel` checkout (HEAD `f2f9701`) oleans dated
  11:33:23–11:33:56 exist (post-rebase; not built by me; no log of that
  build found under the worktrees, the container, or `/tmp`). Read-only
  query against THOSE oleans (`LEAN_PATH` swapped to the repin-fuel
  build dir; verbatim tail):
  ```
  'CerberusHeapLang.regionOwn_ne' depends on axioms: [propext, Classical.choice, Quot.sound]
  'CerberusHeapLang.regionOwn_deadRegion_ne' depends on axioms: [propext, Classical.choice, Quot.sound]
  'CerberusHeapLang.malloc_list_certified_total' depends on axioms: [propext, Classical.choice, Quot.sound]
  'CerberusHeapLang.malloc_list_certified_production' depends on axioms: [propext, Classical.choice, Quot.sound]
  ...
  296
  ```
  and the production statement there carries BOTH slices
  (`25 * n.toNat + 9 ≤ CerbFuel.driverFuel →` and `ids.Nodup ∧`; full
  quote in Q5/Q6 below). So the combined tree very probably builds green
  — but "probably" is not a gate, and the record says 294.
- Consequence: a claim-point merge without a gate at the merged content;
  the working-practices rule "orchestrator independently re-verifies
  gates at boundaries" is exactly what is missing here. Not a logic gap
  (graded Medium as process, per the brief; nothing hidden in the tree).
- Fix: run `scripts/test_unit.sh` (FULL) at `f2f9701` (or the Note-fixed
  successor), record the verbatim tail in DECISIONS/the merge entry with
  the pin count 296, and regenerate the signature snapshot for the
  combined head (neither slice's post-snapshot is the combined tree's:
  the re-pin's post is 294-pin, pre-K5.1).

### Notes

**N-1. Stale pin count in the `Audit.lean` header.** `Audit.lean:64`
(verbatim): "pin list below is unchanged (294), and the production
statements' side conditions read `k + 2 ≤ CerbFuel.driverFuel`." At HEAD
the list has 296 entries (K5.1 added `regionOwn_ne`,
`regionOwn_deadRegion_ne`). The sentence was written on the pre-rebase
tree and survived the rebase. Fix: "(296 after K5.1)" or drop the number.

**N-2. `DriverCollapse.lean` header overstates the `_succ` discipline.**
Verbatim (lines 88–90): "Budget numerals are unfolded ONLY through the
`_succ` lemmas below (never a `show`-forced numeral defeq: 10^8 vs 10^6
hits the recursion-depth limit …)". In the package, the drive-cone budget
is also unfolded inline by `show CerbFuel.driverFuel = Nat.succ 99999999
from rfl` (Round.lean:4738, :4918) and `show CerbFuel.driverFuel =
99999999 + 1 from rfl` (ProdExhibit.lean:308, ProdLoopExhibit.lean:1498,
DisposeExhibit.lean:1653). These are cheap literal equations (not the
10^8-vs-10^6 defeq the sentence warns about) and they build — the
sentence is merely inaccurate about "ONLY". Cosmetic; either say "in this
file" or route the five sites through a shared `driverFuel_succ`.

**N-3. Record scope after the rebase.** `docs/2026-09-03_repin-fuel-notes.md`
§1/§8 describe the branch as based on `367af77` "K5.1 not yet landed"
and §9 anticipates the rebase; the record is honest about its tree, but a
reader of the merged history will find its "294 pins" and its snapshot
files describing a tree that is not the merged head. Fix: one dated
addendum line (rebased over `932dfcf`; gate re-run — see M-1).

**N-4. Commit message `f2f9701` states "294 export pins trio-exact"**
for a tree whose pin list has 296. Immutable under ff-only; record the
correction in the merge entry (folds into M-1's fix).

## The questions

### K5.1

**(1) Genuine consequences of `metaOwn_ne`, right fractions, trio-exact,
pinned — YES.**
`metaOwn` is `pointsTo (G := SpikeGS.metaGS) id dq mc` (Heap.lean:2430);
`metaOwn_ne` (Heap.lean:4057–4061, verbatim):
```lean
theorem metaOwn_ne {i₁ i₂ : Int} {dq₂ : DFrac} {mc₁ mc₂ : MetaCell} :
    ⊢@{IProp GF} metaOwn (hlc := hlc) i₁ (.own 1) mc₁ -∗
      metaOwn i₂ dq₂ mc₂ -∗ ⌜i₁ ≠ i₂⌝ := by
  letI := SpikeGS.metaGS (hlc := hlc) (GF := GF)
  exact pointsTo_ne
```
iris-lean `pointsTo_ne` (`.lake/packages/iris/Iris/Iris/BI/Lib/GenHeap.lean:187`,
verbatim): `⊢@{IProp GF} l₁ ↦ v₁ -∗ l₂ ↦{dq₂} v₂ -∗ ⌜l₁ ≠ l₂⌝` — left at
the full fraction, right at ANY `dq₂`. `regionOwn id a n dq bs :=
metaOwn id dq (regionCell a n true) ∗ bytesOwn a dq bs ∗ ⌜bs.length = n⌝`
(Heap.lean:3319) and `deadRegion id a n := metaOwn id .discard
(regionCell a n false)` (Heap.lean:3793). Both new laws unfold, project
the `metaOwn` conjunct(s), and apply `metaOwn_ne` with the live side at
`.own 1` and the other at `dq'` resp. `.discard` — the fractions the
brief asks about. Printed types in the combined environment (verbatim):
```
@regionOwn_ne : ∀ {hlc : Iris.HasLC} {GF : Iris.BundledGFunctors} [inst : SpikeGS hlc GF] (id id' a a' : Int)
  (n n' : Nat) (dq' : Iris.DFrac) (bs bs' : List CerbMem.AbsByte),
  regionOwn id a n (Iris.DFrac.own 1) bs ∗ regionOwn id' a' n' dq' bs' ⊢ ⌜id ≠ id'⌝
@regionOwn_deadRegion_ne : ∀ {hlc : Iris.HasLC} {GF : Iris.BundledGFunctors} [inst : SpikeGS hlc GF] (id id' a a' : Int)
  (n n' : Nat) (bs : List CerbMem.AbsByte), regionOwn id a n (Iris.DFrac.own 1) bs ∗ deadRegion id' a' n' ⊢ ⌜id ≠ id'⌝
```
`#print axioms` for both: `[propext, Classical.choice, Quot.sound]`.
Pinned: `Audit.lean:404` lists both in `trioExports`. The two exhibit-side
lifts (`regionOwn_isRegionList_ne`, `regionOwn_deadRegions_ne`) are
structural recursions applying the laws at each node via `keep_pure`;
trio-exact; unpinned (0 hits in Audit.lean — DERIVED), as the record says.

**(2) `Nodup` through both phases; four statements one conjunct each;
no `deadRegion_ne` — YES on all three.**
- ALLOC (`i > 0`, both `ml_body_wps` and `ml_body_wpt`): after
  `regionOwn_alloc16` yields `regionOwn id a 16 (.own 1) regionUndef16`,
  `keep_pure (regionOwn_isRegionList_ne …)` against `isRegionList pc ids`
  gives `hnotin : id ∉ ids`; `keep_pure (regionOwn_deadRegions_ne …)`
  against `deadRegions done` gives `hnotin' : id ∉ done`; the new invariant
  conjunct `((id :: ids) ++ done).Nodup` is closed by (verbatim)
  `rw [List.cons_append]; exact List.nodup_cons.mpr ⟨List.not_mem_append hnotin hnotin', hnd⟩`.
  Correct: `nodup_cons` needs `id ∉ ids ++ done` and `(ids ++ done).Nodup`.
- FREE, node (`i = 0`, `ids = id :: ids'`): goal `(ids' ++ id :: done).Nodup`
  from `hnd : ((id :: ids') ++ done).Nodup` — closed by (verbatim)
  `exact (List.pairwise_middle (fun h => Ne.symm h)).mpr hnd`. The library
  lemma (toolchain v4.32.2, `Init/Data/List/Pairwise.lean:166`, verbatim):
  `Pairwise R (l₁ ++ a :: l₂) ↔ Pairwise R (a :: (l₁ ++ l₂))` with a symmetry
  argument; `Nodup = Pairwise (· ≠ ·)`, `(id :: ids') ++ done` is defeq
  `id :: (ids' ++ done)`, so `.mpr` is exactly the move of the head from
  the live list to the dead one. Pure — no Iris law at the free, correctly
  (the distinctness was established when the node was allocated).
- FREE, null: `([] ++ done).Nodup` → `done.Nodup` by `rw [List.nil_append]`
  → the post's `ids.Nodup`. Entry: `(by simp)` for `([] ++ []).Nodup`.
- The four statements, before → after (from the snapshots, the whole
  difference in each): `malloc_list_certified_total` conclusion
  `ids.length = n.toNat ∧ ∀ (id : Int), …` → `ids.length = n.toNat ∧ ids.Nodup ∧ ∀ (id : Int), …`;
  `malloc_list_certified_production` likewise inside its `(∃ ids, …) ∧`
  conjunct; `mlPost_readout`'s `readoutPost` predicate likewise. `ml_wps`/
  `ml_wpt` print unchanged (their types name the abbrevs `mlLs`/`mlLsT`/
  `mlPost`, whose BODIES gained `(ids ++ done).Nodup` resp. `ids.Nodup`) —
  the record's explanation is correct. Premises of all four: untouched
  (source diff read in full).
- "No `deadRegion_ne` law is derivable (both persistent)": right.
  `deadRegion_persistent` is an instance (Heap.lean:3796); a law
  `deadRegion id a n ∗ deadRegion id' a' n' ⊢ ⌜id ≠ id'⌝` instantiated at
  `id = id'` with the duplicate of one `deadRegion` would give
  `deadRegion id a n ⊢ False`, contradicting that `wps_free` produces it.
  Therefore dead-id distinctness in the exhibit is honestly a
  consequence of the INVARIANT (established at alloc time by the two
  laws, transported by `pairwise_middle`), not of a law about dead
  regions — exactly what the module header, API row, README, WALKTHROUGH
  and ARCHITECTURE now say.

**(3) Snapshot ADDED 4 / REMOVED 0 / CHANGED 5 — re-derived exactly.**
`cmp` confirms `k5.1-signatures-pre.txt` is byte-identical to
`k5-signatures-post.txt`. My entry-level comparison (DERIVED; my parser
recognises fewer entry kinds than the worker's — 2413 vs 2665 entries —
but the delta classes agree exactly):
```
ADDED 4: regionOwn_deadRegion_ne, regionOwn_deadRegions_ne, regionOwn_isRegionList_ne, regionOwn_ne
REMOVED 0
CHANGED 5: malloc_list_certified_production, malloc_list_certified_total, mlPost_readout, ml_body_wps, ml_body_wpt
```
`ml_body_wps`/`ml_body_wpt` gained the hypothesis `hnd : (ids ++
done).Nodup`; both are internal (0 hits in `Audit.lean`), trio-exact
(`#print axioms` in the combined environment: trio). Confirmed.

### RE-PIN

**(4) Pin correctness — YES.** `scripts/semantics-pin.env`:
`CERBERUS_LEAN_COMMIT="f95ef8d9c317fa6b50cf6691216a8c37b1d3eabf"` with the
dated delta paragraph. `.cerberus-ws` in the copy is a symlink to the
`repin-fuel` workspace; `git -C .cerberus-ws log -1`: `f95ef8d9c fuel arc
CLOSE-OUT 2 …`. `bash scripts/setup-cerberus-dep.sh --check` run by me in
the copy, verbatim, exit 0:
```
== setup-cerberus-dep: A ok: workspace at pinned commit
== setup-cerberus-dep: B ok: primed (f95ef8d9c317fa6b50cf6691216a8c37b1d3eabf 2026-09-03T05:52:15Z)
check_lem_sync: lean OK (src 4f2e089b39d5b371973513b3350f81d1b89871976f77df9ba4a25da3421d0c54, gen 49ad8b2c359cb0b36ed12913eb0bb3a4986c0ee582fe8e735e700297d38eee00)
== setup-cerberus-dep: B ok: Lean lem-sync stamp verified in the workspace
== setup-cerberus-dep: C ok: 23 hand-written seams byte-identical to the pin
== setup-cerberus-dep: DONE. Lake consumes /home/dev/projects/cerberus-lean-proj/refined-cerberus/worktrees/audit-repin/.cerberus-ws/lean_frontend as a path dependency.
```
LemLib: both manifests `"inputRev": "045dcb0d57a171eb4fb3a6eb5abe288c227270ce"`
(root and cerberus-heaplang `lake-manifest.json`), URL still
`septract/lem-lean` — untouched, as claimed.

**(5) The delta — zero renames / signature changes / memory-model
breakage; all fixes budget-class; strict generalizations — YES.**
- The code diff of `a3eb560` (read in full) touches 9 Lean files; every
  edit is one of: `lemDefaultFuel → CerbFuel.driverFuel` in a wrapper
  argument or side condition; `999999 → 99999999` / `999998 → 99999998`
  in `nd_bind_lemFuel`/`bind_branch_active`/`runNDFuel*` numerals; a new
  `private theorem driverFuel_succ : CerbFuel.driverFuel = Nat.succ
  99999999 := rfl`; removal of one now-unneeded `rw [show lemDefaultFuel
  = 999999 + 1 from rfl] at hfuel ⊢` (counter loop; `omega` closes on the
  shared atom); and prose. No other proof step changed. Every `liftND`/
  `liftAction`/`pot`/`esize`/`peDepth` site stays at `lemDefaultFuel`
  (grep: 100+ `show lemDefaultFuel = 999999 + 1 from rfl` sites
  untouched; `runOne_liftNDF_active 999996` untouched in `memop_fork`).
  Cross-check on the semantics side (`git diff ddcfc9199 f95ef8d9c` in
  the workspace, `driver.lem`/`nondeterminism.lem`): the only
  non-comment changes are the fuel-zero arms (`fuelExhausted (… Undef0
  …)` → `NDkilled (Error0 CerbFuel.fuelExhaustedLoc
  CerbFuel.fuelExhaustedMsg)`), the budget declares
  `declare {lean} fuel val {print_eval_conv_aux, drive_nonmemory_steps_aux2, driver2, hack, nd_bind} = 100000000`,
  and `declare {lean} extra_import \`CerbFuel\``. Generated wrappers
  (verbatim tails): `driver2 … := driver2_lemFuel 100000000`,
  `nd_bind … := nd_bind_lemFuel 100000000`, `liftND_lemFuel lemDefaultFuel`,
  `liftAction_lemFuel lemDefaultFuel`. The snapshot's CHANGED set (below)
  contains no dependency SIGNATURE, only the six fuel-0 `.eq_def` bodies —
  consistent with zero signature changes in anything we cite.
- Before/after (source): `prod_run_eqJ`: `(hfl : k + 2 ≤ lemDefaultFuel)`
  → `(hfl : k + 2 ≤ CerbFuel.driverFuel)`; `malloc_list_certified_production`:
  `(hfuel : 25 * n.toNat + 9 ≤ lemDefaultFuel)` → `(hfuel : 25 * n.toNat
  + 9 ≤ CerbFuel.driverFuel)`. Strictness: in the environment,
  `example : lemDefaultFuel = 1000000 := rfl`, `example : CerbFuel.driverFuel
  = 100000000 := rfl`, `example : lemDefaultFuel ≤ CerbFuel.driverFuel :=
  by decide` all elaborate (my `q_repin.lean`, exit 0) — every old
  premise implies the new one, so each restated theorem implies its
  predecessor.
- Surfaces: `grep lemDefaultFuel` over README/ARCHITECTURE/WALKTHROUGH/
  API.lean/CAPABILITY_MANIFEST.md finds only evaluator/`get_ctx`/`pot`/
  `esize`/`peDepth` bounds (correctly still 10^6) and ARCHITECTURE:270's
  description of the substitution. Every production side condition on
  every surface reads `CerbFuel.driverFuel` (README exhibits table ×4,
  README:504 divergences row, README:593 trust diagram, ARCHITECTURE §6
  ×5, WALKTHROUGH §1.3 ×2, §4 ×2, §6, §7). No statement mentions
  `CerbFuel.driverFuel` where a doc still says `lemDefaultFuel`.

**(6) Genuine-driver rule under the new pin — the referent is the
SHIPPED `drive` — YES.** All six production statements and
`prod_run_eqJ` are over `CerbND.runND (_root_.drive fmapEmpty false
(prodFile …) args) ((initial_driver_state sup (prodFile …) fs).1)`
(ProdExhibit:267, ProdEntry:391, ProdLoopExhibit:80/626/1438,
DisposeExhibit:1595, RegionLoopExhibit:698, MallocListExhibit:1753).
`_root_.drive` is the generated `def  drive` (`.cerberus-ws/lean_frontend/
generated/Driver.lean:518`, root namespace), whose body calls the
`driver2` wrapper once (DERIVED: one `driver2` occurrence in the body),
and `driver2 … := driver2_lemFuel 100000000` (Driver.lean:386). In the
environment `example : _root_.drive = CerbND.drive_lemFuel
CerbFuel.driverFuel := CerbND.drive_wrapper_defeq` and `example : driver2
= driver2_lemFuel CerbFuel.driverFuel := rfl` elaborate. The statements
are NOT over `drive_lemFuel` (that restatement is the next slice, as
every surface says). `driveU`: one definition (Adequacy.lean:181),
untouched by the range (`git diff` shows no `driveU` code change); 185
references across 27 files (DERIVED). PROVISIONAL: README 32,
ARCHITECTURE 5, WALKTHROUGH 12 (DERIVED `grep -c`), none removed from a
theorem; the count matches the worker's.

**(7) "None (measured)" — VERIFIED.** `grep -rn 'sorry'
.cerberus-ws/lean_frontend/generated/*.lean`: 18 hits in 8 files, every
one comment text (e.g. `CerbStepInstances.lean:67: "sorry" above is
HISTORY, describing the arc-4 state; sorry is banned`); `grep -c
'(sorry'` = 0. `Cmm_op.lean:293` now reads (verbatim excerpt)
`String.append (CerbMem.stringFromMemValue  y)   (String.append " --- "  (CerbMem.stringFromMemValue  z))`
— the real printer, no `(sorry : String)`. The README paragraph's wording
matches the measurement.

**(8) The fuel-arc contract vs R1–R3 — spot-checked, present with the
stated forms; nothing in our package cites them (except by defeq).**
In the combined environment (verbatim `#check`):
```
CerbND.drive_wrapper_defeq : drive = CerbND.drive_lemFuel CerbFuel.driverFuel
CerbND.driverFuel_eq : CerbFuel.driverFuel = 100000000
CerbND.driver2_lemFuel_zero : ∀ (_lemReader_tagDefs : Fmap sym (CerbLocation.Loc × tag_definition))
  (with_concurrency : Bool),
  driver2_lemFuel 0 _lemReader_tagDefs with_concurrency = ND fun st => (NDkilled CerbND.fuelExhaustedKill, st)
@CerbND.fuelExhaustedKill : {err : Type} → kill_reason err
@CerbND.runNDFuel_zero : ∀ {a info err cs st : Type} (m : ndM a info err cs st) (st0 : st),
  CerbND.runNDFuel 0 m st0 = [(Killed st0 CerbND.fuelExhaustedKill, [], st0)]
@CerbND.nd_bind_wrapper_defeq : ∀ {a b c d e f : Type} (n : ndM f b d a c) (f1 : f → ndM e b d a c),
  nd_bind n f1 = nd_bind_lemFuel CerbFuel.driverFuel n f1
```
`def fuelExhaustedKill {err : Type} : kill_reason err := Error0
CerbFuel.fuelExhaustedLoc CerbFuel.fuelExhaustedMsg` (CerbND.lean:80);
`def ndDefaultFuel : Nat := CerbFuel.driverFuel` (:85); `drive_lemFuel`
(:450) with `drive_wrapper_defeq` (:467). R1 (fuel-parametric `drive`,
`rfl`-pinned), R2 (`_zero` lemmas + wrapper `rfl`s in importable
`CerbND`), R3 (`driverFuel` the citable name, the manifest §4 premise
shape `k + 2 ≤ CerbFuel.driverFuel`) — as the scout recorded. Package
cites (DERIVED grep over `CerberusHeapLang/*.lean` for `drive_lemFuel`,
`drive_wrapper_defeq`, `driverFuel_eq`, `_lemFuel_zero`,
`fuelExhaustedKill`, `_wrapper_defeq`, `runND_eq`, `runNDFuel_zero`,
`ndDefaultFuel`): every hit is in a comment or docstring
(Audit.lean:61, ProdEntry.lean:48/50, Adequacy.lean:73–76,
DriverCollapse.lean:71–73/84/88, Round.lean:4722). No proof term cites a
CerbND contract lemma. The fixes use the CONSTANT `CerbFuel.driverFuel`
and their own `rfl`s (`driverFuel_succ` in DriverCollapse; inline
`show CerbFuel.driverFuel = … from rfl` at Round:4738/4918,
ProdExhibit:308, ProdLoopExhibit:1498, DisposeExhibit:1653) — the same
defeqs the wrapper lemmas state, used by `show`/`rfl` rather than by
name. So the list of contract lemmas our code depends on by name is
EMPTY; they are all for the next slice.

**(9) The rebase's conflict resolutions — coherent, both changes
present, nothing duplicated or lost.**
- README exhibits table, `ml_*` row: has K5.1's untracked-counter
  qualification, `(ids ++ done).Nodup` in the invariant, the DISTINCT
  post with the M-1 mechanism sentence, AND (in `malloc_list_certified_
  production`'s row) `n.toNat DISTINCT allocation ids (ids.Nodup, K5.1)`
  with `hfuel : 25 * n.toNat + 9 ≤ CerbFuel.driverFuel`. The `rl_*` row
  carries K5.1's M-2 rewrite and the `hfuel … ≤ CerbFuel.driverFuel`.
  One sentence each; no residue of the old text.
- WALKTHROUGH §4 malloc'd-list paragraph: invariant with `(ids ++
  done).Nodup`; `ml_wps`/`ml_wpt` post with `ids.Nodup`; `_total` "n.toNat
  DISTINCT ids"; `_production` with `hfuel : 25 * n.toNat + 9 ≤
  CerbFuel.driverFuel` and "n.toNat DISTINCT allocation ids"; then the
  "DISTINCTNESS is stated, not implied" sentence and the counter-field
  qualification; then the readout sentence — one continuous paragraph,
  read in full, no duplication.
- ARCHITECTURE: §6 side conditions at `driverFuel` (re-pin), §7 goal 1
  "HAS LANDED AND IS PINNED" (re-pin), K5 bullet with DISTINCT + the
  new N-1 two-label bullet (K5.1). Both slices' edits present.

**(10) Re-pin snapshot ADDED 0 / REMOVED 1 / CHANGED 14 — re-derived
exactly; pins.** `cmp` confirms `repin-fuel-signatures-pre.txt` is
byte-identical to `k5-signatures-post.txt`. Entry-level (DERIVED, same
script as Q3):
```
ADDED 0
REMOVED 1: CerbMem.reconstructValue_lemFuel.eq_def
CHANGED 14: CerbND.runNDFuel.eq_def, counter_loop_certified_production, drive_after_setup, driver2_done,
  fib_certified_production, malloc_list_certified_production, prod_run_eqJ, region_loop_certified_production,
  runOne_bind_nd, drive_nonmemory_steps_aux2_lemFuel.eq_def, driver2_lemFuel.eq_def, liftAction_lemFuel.eq_def,
  liftND_lemFuel.eq_def, nd_bind_lemFuel.eq_def
```
= 8 package statements (the seven side-condition/wrapper-argument
restatements + `runOne_bind_nd`'s numeral) + 6 dependency `.eq_def`
auxiliaries, as recorded. Pins: the worker's "294 trio-exact" is the
PRE-rebase tree; at the combined head the list is 296 and, in the
`repin-fuel` post-rebase environment, `CerberusHeapLang.Audit.trioExports.length`
evaluates to `296` and every export I printed is trio (see M-1 for the
gate caveat).

## What I could not verify

- A FULL gate at the combined head `f2f9701`: not run by me (brief: no
  `lake build`) and not in the record (M-1). The `repin-fuel` worktree's
  11:33 oleans are the only evidence the combined tree elaborates; they
  are not mine and have no log.
- The 296-pin "trio-exact" line of `Audit.lean` at the combined head
  (it is emitted by the build); I checked `#print axioms` of the 21
  exports named in this report only.
- The worker's error census and build timings (three cycles) — accepted
  as recorded; the fixes' shapes are consistent with the census.
- The scout's per-name table of CerbMem signature stability — spot-checked
  through the snapshot (no dependency signature in CHANGED) and the
  untouched `StructExhibit`/`AllocExhibit`, not re-derived name by name.

## Merge recommendation

Merge the combined range `226c0d3..f2f9701` ff-only, conditional on:
(1) M-1 — the orchestrator runs and records the FULL gate at the merged
content (296 pins), and regenerates/records a snapshot for the combined
head; (2) N-1 — the `Audit.lean:64` "(294)" corrected (a one-line docs
commit on the branch before the gate, so the gate covers it). N-2/N-3/N-4
are cosmetic and may ride the same commit or the next slice. No finding
touches the logic: both slices are what their records say they are.
