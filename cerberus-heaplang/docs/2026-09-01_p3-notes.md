# Alloc arc P3 — proof-flow and semantic-coverage gates fail closed (slice records)

Charter: `../../docs/2026-09-01_cerberus-heaplang-skeptical-re-audit.md`
§P3 (P3.1 dependency-certified consumers + the layer-cut check + the
four planted negative tests; P3.2 two-sided relation coverage) and
findings R-03/R-04 in full, incl. R-04's five remediation items and
its warning that "a raw transitive ban on `Step.*` will not work";
merge rows 6-7 (must prove: every bypass plant detected; every `Frag`
row covered; mismatch plant fails). This file is the running record;
each slice appends its section at commit time.

Record note [AGENT 2026-09-01, P3]: the P3 brief referenced a
§"P3 readiness" map in `2026-09-01_p2-notes.md` (the stagable per-row
proof flow, the approved layer-cut crossing set, a retired-names list,
the `sem_triple_prod` hpre candidate). That section is NOT in the
committed P2 record (grep: no "readiness"/"crossing" hits at commit
5879eec; the P2 notes end at the slice-3 addendum). The map was
therefore RE-DERIVED here by measurement over the built environment
(§1.1 below) rather than transcribed; the `sem_triple_prod` hpre item
is handled in the closure section from the ProdEntry module note and
the P2 closure-sweep commit message, which do record it.

## Slice 1 (P3.1) — the dependency-certified manifest and the layer cut

### 1.1 Measurement before design (what a cone check can and cannot see)

The generator's checks were chosen by MEASURING candidate checks on
the P2-green tree (throwaway scripts over the built environment;
the numbers below are from those runs, verbatim where quoted):

1. **`ConstantInfo.value?` returns `none` for theorems** in this
   toolchain (4.32.2). A traversal written against it silently sees
   NO proof bodies (measured: `exhibitA_engine` "value consts 0" via
   `value?`, 63 via an explicit `.thmInfo t => t.value` match). The
   generator matches the constructor explicitly; the Audit's
   `carriesAx` (statement-rooted, through definitions) is unaffected.
2. **Transitive cone membership of a `Step` CONSTRUCTOR is
   VACUOUS.** Every consumer's full proof cone contains ALL 24 `Step`
   constructors — `exhibitB_engine` (a store-only program), `fib_certified`
   (no heap at all), `alloc_two_creates_wps` alike. Cause, measured
   by entry-edge tracing: every `cases`/inversion on `Step` names
   every constructor (edges `Step.casesOn → Step.*`, `Step.rec →
   Step.*`, and the inversion lemmas `Step.store_inv`/`val_elim`/
   `case_inv`/`create_inv → Step.*` — the dependent elimination
   re-builds indices with constructor terms), and every WP mention
   reaches those through the `Language` instance (`primStep := Step
   …`, `val_stuck` via `Step.toVal_none`). Excluding the auxiliary
   recursors does not help (the inversion lemmas remain). So the
   charter's "logical rule's cone contains the relational rule" is
   NOT implementable as a discriminating cone test in Lean 4, and the
   generator does not pretend to (manifest Notes 8a).
3. **Transitive cone membership of a Core SYNTAX constructor is
   equally vacuous** (every consumer reaches `Esseq, Ewseq, Esave,
   Eif, Erun, Ecase, Eannot, Ememop, Epure, Eaction, PEsym, PEval,
   Store0, Load0, PtrEq` — through `esize`/`Frag`/`Decomp`, which
   match over the whole AST).
4. **Cone membership of a public RULE is discriminating.** Measured
   rule sets, e.g.: `exhibitB_engine` → `{wp_store}` only;
   `case_certified` → `{wps_case_value, wps_ofVal, wps_unfold,
   wps_sound}`; `fib_certified` → `{wps_pure, wps_if_true, wps_run,
   wps_ofVal, wps_unfold, wps_if_false, wps_save, wps_sound}`;
   `alloc_two_creates_wps` → contains `wps_create`, NO launcher.
5. **Cone membership of an adequacy LAUNCHER is discriminating**:
   `alloc_two_creates_wps`/`alloc_create_wpt`/`struct_create_store_wps`/
   `progAProd_wpt`/`ctrProd_wpt`/`lrProd_wpt` reach NO launcher
   (they are rule-level); every engine-facing export reaches exactly
   its lane's launchers (e.g. `exhibitA_prod` → `{prod_run_eqJ,
   launchResources, wpt_driver_done_alloc}`; `struct_create_store_adequacy`
   → `{launchResources, wps_sound, spike_step_adequacy_alloc,
   engine_adequacyU_alloc, spike_engine_adequacy_alloc}`).
6. **A STATEMENT-ROOTED syntax witness is discriminating** when the
   traversal expands only definitions whose type is Core syntax
   (expr/pexpr/action/pattern/file/the runtime tuple/thread literal)
   plus the compiled-pattern auxiliaries of those definitions:
   `fib_certified` → `{Esave, Eif, Erun, Epure, PEsym, PEval}` (no
   `Store0`); `exhibitB_engine` → `{Eaction, PEval, Store0}`;
   `alloc_create_launch_smoke` → `{Eaction, PEval, Create}`;
   `case_certified` → `{Ecase, Epure, PEsym, PEval}`. A definitions-
   only traversal WITHOUT the syntax-type restriction is vacuous for
   the rule-level theorems (their statements mention the judgments,
   whose definitions reach the whole AST).
7. **The layer cut holds today.** With the cut = every declaration of
   {Rules, Wps, Wpt, Adequacy, TotalAdequacy, ProdLoop, ProdEntry}
   minus the operational names, plus the two Lang instances, ALL 608
   declarations of the 12 positive-exhibit modules reach no
   operational name (`clean 608 dirty 0`). The crossing points
   actually hit, grouped (load-bearing = transitively reaches an
   operational name): Rules `wp_store, wp_sseq, wp_load,
   exhibitC_triple, spike_wp_wand, stateInterp_readout`; Wps the
   `wps_*` rules + `wps, wps_sound, blockSpecs, blockSpecs_intro`;
   Wpt the `wpt_*` rules + `wpt, wpt_mono, wpt_mono_Ls, blockSpecsT,
   blockSpecsT_intro, blockSpecsT_mono`; Adequacy `drive, driveJ,
   driveU, SemTriple, ProvenTriple, semantic_triple_sound,
   semantic_frame, spike_engine_adequacy(_alloc), engine_adequacyJ,
   cells_readout`; TotalAdequacy `wpt_engine_boundU/J(_alloc),
   wpt_strongly_normalizing, readoutPost(_mono/_annot_absorb)`;
   ProdLoop `wpt_driver_done(_alloc)`; Lang `instLanguageCoreRtMemEmptyCoreRVal,
   instIrisGS`. Heap reaches no operational name (63 pure helpers
   hit, 0 load-bearing) and needs no approval; DriverCollapse is hit
   by nothing directly. `driveJ_step` (Adequacy) and `driverDone_step`
   (ProdLoop) live INSIDE cut modules and are excluded from the cut
   by name — the module-level cut alone would have approved them.
8. **Import-graph fact used for pruning**: Step, Soundness, Heap,
   Lang, StmtProbe import none of the target modules; DriverCollapse
   DOES import Rules (the generator's run-time import-closure
   assertion caught the first draft, which had listed it as a leaf:
   `manifest FAIL: leaf module CerberusHeapLang.DriverCollapse …
   imports target module CerberusHeapLang.Rules`).

### 1.2 The design (as installed — `scripts/capability_manifest.lean`)

Per row, STAGED fields: `synCtors` (Core AST constructor(s), the
execution-witness alternatives) / `mirror` (Step constructors;
coverage DERIVED as before) / `logic` (the public PARTIAL rules) /
`engineMatch` / `partialLane` (the generic adequacy theorems) /
`adequacyConsumers` (engine-facing partial) / `localConsumers`
(rule-level, never claimed as adequacy) / `total` (rules,
derivations, engine-facing consumers) / `prod` (consumers) /
`prodRequires` (extra names every production consumer must carry —
create: `wpt_create`, `launchResources`).

Checks, all fail-closed (every failure is collected and the run
throws with all of them; gate 4 is red on a throw):

- EXECUTION WITNESS: every public rule and every listed consumer's
  STATEMENT reaches one of the row's syntax constructors through
  program-valued definitions (`stmtSyntax`).
- RULE DEPENDENCY: adequacy/local consumer cone ∋ a partial rule
  (local: partial or total); total derivation/consumer cone ∋ a total
  rule; production consumer cone ∋ a rule of the row.
- ADEQUACY DEPENDENCY: adequacy consumer cone ∋ an approved PARTIAL
  launcher; total consumer ∋ a TOTAL launcher; production consumer ∋
  a PRODUCTION launcher (explicit, existence-checked sets).
- PRODUCTION REQUIREMENT: production consumer cone ∋ each
  `prodRequires` name.
- THE LAYER CUT + DIRECT-REFERENCE BAN over EVERY constant of the 12
  positive-exhibit modules: DFS from the body, stopping at approved
  crossings (cut modules minus operational names, plus the Lang
  instances), failing with the PATH on any operational name
  (`Step.*` namespace, `engineSteps*`, `outcomesU`, `dischargeStep*`,
  `step_ctx_*`, `stepDischarge_*`, `Decomp.*`, `Redex.*`,
  `driveJ_step`, `driverDone_step`, `loop_step_frag`,
  `engine_step_matchU`, `engine_complete*`, the P3.2 names); and no
  body names `Step.*`/`engineSteps_*`/`driveJ_step`/`driverDone_step`
  directly.
- SOUNDNESS OF THE PRUNING: every target lives in a target module;
  no leaf module imports a target module (import closure from the
  environment header) — else throw.

Cone computation: the dependency table is filled lazily and
memoized (`DepM`); the cone is the transitive closure over the
package's own constants (foreign constants cannot name ours — the
import graph is acyclic), not expanding the leaf modules (sound by
the assertion above; skips the certification's giant terms). Run
time: ~130 s on a quiet box (cost attribution: §1.6).

### 1.3 The instrument's first catches on the live tree (manifest corrections)

Running the staged checks against the P2 manifest's attributions
found THREE false lane claims, corrected in the row specs (the
generator would otherwise throw):

- `save` production lane listed `counter_loop_certified_production`
  and `list_reverse_certified_production`: NEITHER cone contains
  `wpt_save`. Their `save` is the never-entered registration site
  (the untaken sseq arm; the loop is entered by `run` against the
  driver-collected label), so no save rule is needed by the proof.
  Removed; `fib_certified_production` (which does execute its save)
  stays.
- `pure-sym` production lane listed `counter_loop_certified_production`:
  its cone has no `wpt_pure` (the counter exits through a bare
  value). Removed.
- The create row's single `consumer` cell mixed local rule-level
  theorems with engine-facing ones; split into `adequacyConsumers`
  (`struct_create_store_adequacy`) and `localConsumers`
  (`alloc_two_creates_wps`, `alloc_create_wpt`,
  `struct_create_store_wps`); `alloc_create_launch_smoke` is the
  TOTAL lane's engine-facing consumer; `progAProd_wpt`/`ctrProd_wpt`/
  `lrProd_wpt` are total DERIVATIONS.

Honesty additions in the same pass: `exhibitA_total` joins the total
lane of value/store/load/sseq/annot (it is the P2-retired trace's
total successor; its cone carries `wpt_engine_boundU` + the wpt
rules); the store/load rows list the generic subrange rules among
their partial rules (list-reverse and the struct client consume only
those); `struct_create_store_adequacy` joins the store/sseq-sym/
store-op adequacy consumers; `list_reverse_certified` joins save's.

### 1.4 Claims surfaces (same commit)

README manifest paragraph + gate-4 paragraph, WALKTHROUGH §the
fail-open coverage note, `scripts/test_unit.sh` gate 4 comment +
failure text, the manifest itself regenerated (new columns; Notes 3/4
extended, Notes 8 added; `DEPENDENCY-CERTIFIED` machine line added,
existing machine lines unchanged in name).

### 1.5 The four charter plants (run against commit d96d95f, each reverted)

All plants were run AFTER the slice-1 commit (the P2 plant-hygiene
lesson) and reverted; the generator variants for plants 2/3/4 were
run as copies against the same built environment (they change only
the row specs); plant 1 changed a proof and was BUILT (build green —
the plant is a valid proof of the same statement) before the
generator ran. Transcripts verbatim (the generator's message text;
paths elided):

PLANT 1 — a logical create proof replaced by a direct trace under the
SAME NAME: `alloc_create_launch_smoke` (AllocExhibit) re-proved by
`allocateObject_success` (Heap) → `Step.create_canonical` (the direct
relational step) → `engine_step_matchU` (the operational match) →
`driveU_succ`/`outcomesU_done` unfolding; `lake build` GREEN (445
jobs); the generator:

```
error: 3 HARD-check failure(s) (layer cut / dependency certification):
manifest FAIL (LAYER CUT): positive-exhibit declaration alloc_create_launch_smoke (CerberusHeapLang.AllocExhibit) reaches the operational name step_ctx_done WITHOUT crossing the approved logic/adequacy layer; path: alloc_create_launch_smoke → outcomesU_done → step_ctx_done
manifest FAIL (row 'create', RULE DEPENDENCY): total consumer alloc_create_launch_smoke does NOT depend on any total rule [wpt_create] — its proof bypasses the row's public abstraction (R-04).
manifest FAIL (row 'create', ADEQUACY DEPENDENCY): total consumer alloc_create_launch_smoke does NOT depend on any approved total launcher [wpt_engine_boundU, wpt_engine_boundJ, wpt_engine_boundU_alloc, wpt_engine_boundJ_alloc, wpt_strongly_normalizing, wpt_strongly_normalizing_alloc] — its proof bypasses the row's public abstraction (R-04).
```

(Gate 4 red. The first run of this plant, before the layer cut was
made to accumulate and run first, reported only the two dependency
failures — the row checks threw before the cut ran; the generator
was changed so BOTH classes report together. The plant used
`Step.create_canonical`, a Step-namespace LEMMA rather than the raw
constructor, so the constructor-level direct ban did not fire; the
direct ban was widened to the whole `Step.*` namespace per the
charter's wording, and the layer cut catches the path regardless.)

PLANT 2 — a local `wps` theorem listed as an adequacy consumer
(`alloc_two_creates_wps` added to the create row's
`adequacyConsumers`):

```
error: 1 dependency-certification failure(s):
manifest FAIL (row 'create', ADEQUACY DEPENDENCY): adequacy consumer alloc_two_creates_wps does NOT depend on any approved partial launcher [spike_engine_adequacy, spike_engine_adequacy_alloc, spike_step_adequacy, spike_step_adequacy_alloc, engine_adequacyJ, engine_adequacyU, engine_adequacyU_alloc, semantic_triple_sound] — its proof bypasses the row's public abstraction (R-04).
```

PLANT 3 — `wpt_create` removed, partial coverage kept. Two variants,
both with the BUILD UNTOUCHED (the manifest's reaction is the test —
a source-level deletion breaks gate 3 first, P2 plant B):
3b (the total and production lanes of the create row set to `none`,
everything else as committed):

```
error: 1 dependency-certification failure(s):
manifest FAIL (row 'create', RULE DEPENDENCY): local consumer alloc_create_wpt does NOT depend on any public rule of the row [wps_create, wps_create_cursor_internal] — its proof bypasses the row's public abstraction (R-04).
```

— a consumer of the removed rule cannot be attributed to the row any
more (the instrument refuses an inconsistent spec). 3c (3b plus the
now-unattributable `alloc_create_wpt` dropped): generator EXIT 0, the
create row renders `RED — PLANT 3b: wpt_create removed` in both its
total and production cells, and the machine lines lose `create`:

```
TOTAL-LANE: value store load sseq-wild annot save if run sseq-spec pure-sym load-op sseq-sym memop-ptreq memop-op store-op pure-operands
PRODUCTION-LANE: value store load sseq-wild annot save if run sseq-spec pure-sym load-op sseq-sym memop-ptreq memop-op store-op pure-operands
```

(vs the committed lines, which carry `create` in both) — drift, gate
4 red. The charter's "total and full-row statuses must turn red"
verbatim.

PLANT 4 — a `Frag` constructor without a row: adding a constructor to
`Frag` cannot keep the BUILD green (the `Frag`-indexed inductions —
`Frag.decomp`, `Frag.step`, `engine_step_matchU`,
`cerberusRound_classify` — become non-exhaustive), so the generator
path is exercised by its exact equivalent, the `Frag.wseq` row
mapping deleted from `rowSpec` (the same `rowSpec ctor = none`
branch the S1b drift test hit):

```
error: manifest FAIL: cone constructor CerberusHeapLang.Frag.wseq has NO manifest row mapping — the cone was extended without the manifest (fail-closed by design; add the rowSpec arm).
```

All four charter plants detected; every plant reverted; the full
gate re-run green after the reverts (the slice-2 commit's gate).

### 1.6 Coordinator steers applied in this slice ([USER 2026-09-01], two)

TWO-TIER GATING (the ACL2Lean policy, adopted against gate cruft):
`scripts/test_unit.sh --fast` = the HARD checks only — gate 1, the
AFFECTED package build(s) (working-tree changes vs HEAD, tracked +
untracked; both packages if nothing is detected — fail-closed), and
the manifest generator's HARD checks in check-only mode
(`CAPABILITY_MANIFEST_CHECK_ONLY=1`: every check runs, the rendering
and therefore the drift diff is skipped). The full run is unchanged
as the claim gate (phase/arc exits, merge candidates, any commit
claiming green); a fast-gated commit must say `fast-gate`.

HARD vs SPEEDBUMP classification (the table lives in the script
header; rationale per check):

| Check | Tier | Rationale |
|---|---|---|
| gate 1 banned methods | HARD | non-kernel methods are unsound trust |
| gates 2/3 builds + in-build axiom sweeps | HARD | sorry/axiom growth is unsound trust |
| gate 4h: cone-derived row set, exact Step mirror coverage, name/kind checks | HARD | silent coverage loss (a Frag/Step case without a row) |
| gate 4h: PROOF-FLOW (rule / launcher / witness / prodRequires cones) | HARD | the R-04 class — the check whose absence let R-01 masquerade as certified |
| gate 4h: LAYER CUT + direct-reference ban | HARD | the R-02 bypass class |
| gate 4h: pruning-soundness assertion (import closure) | HARD | guards the soundness of the cone pruning itself |
| gate 4s: manifest drift (committed ≠ regenerated) | SPEEDBUMP | "did you regenerate the claims surface" |
| gate 4s: README certified-scope token tie | SPEEDBUMP | prose-vs-manifest consistency, not soundness |
| gate 5: statement-census freeze | SPEEDBUMP | "did you notice a pinned statement surface changed" |

The HARD generator checks ARE separable from the regeneration
(check-only mode) and run in both tiers; the separation saves no
time (the cost is the import + traversal, not the rendering) — it is
a tier-semantics separation, not a speed one.

MEASURED COST of gate 4 (full run, uncapped in-sandbox): ≈ 130 s
wall-clock on a quiet box; 339 s (full) / 416 s (fast, check-only)
under the contention observed at phase exit (other agents' heavy
jobs on the shared box — the per-gate wall-clock lines in the gate
logs). Attribution, corrected at phase exit: the IMPORT of the built
environment is ≈ 2 s (an import-only script: `2.115 total`), so the
≈ 88 s fixed cost is Lean elaborating and interpreting the generator
SCRIPT itself (a ~1100-line file whose `#eval` block is compiled by
the interpreter on every run — the same class of cost the P0-era
generator already paid, ≈ 90 s then); ≈ 40 s are the checks. Moving
the generator's definitions into a compiled library module would
remove the fixed cost — registered follow-up, not done here. The
first draft
computed the dependency table for ALL 3826 package constants up
front (≈ 60 s — `getUsedConstants` over the certification's giant
proof terms dominated); the installed form fills the table LAZILY
and memoizes it (`DepM`), shares it between the layer cut and every
row check (single pass), and PRUNES cone expansion at the leaf
modules (Step/Soundness/Heap/Lang/StmtProbe — asserted from the
import graph at run time to import no target module; the assertion
caught DriverCollapse, which does import Rules, on the first draft).
The P3.2 relation-coverage check needs ONE unpruned cone (the
classification theorem's, through Soundness); its cost is included
in the ≈ 40 s. Per-gate wall-clock is now printed by the script
(phase-exit FULL run, loaded box: gate 2 269 s, gate 3 235 s, gate 4
339 s, gate 5 1 s, total 844 s).

## Slice 2 (P3.2) — the engine-facing relation named; the classification

### 2.1 The choice and the theorem (Round.lean)

`CerberusRound M aid c c'` := `outcomesU M aid c.e c.ρ c.σ = [.next
(M.thread c'.e c'.ρ) c'.σ]` — THE GRAPH OF THE DISCHARGED `step_ctx`
ROUND (the charter's preferred choice), defined with no reference to
`Step` or to any example. Granularity argument and the RelSemCore
disclaimer: the module header (consistent with the README's "two
presentations, one engine" paragraph; no bridge claimed).

The headline is the exhaustive sum classification (the charter's
accepted alternative to a global iff — which is FALSE here, not
merely awkward: at an annotated value the engine performs a
REMOVE-ANNOT round, a successful-next, while the mirror's value
protocol treats the configuration as a value and does not step):

```
theorem cerberusRound_classify {M} (hwf : M.SeqWF) (aid) {e ev0 evs σ}
    (hf : Frag e) (hsz : esize e ≤ lemDefaultFuel) :
    RoundClass M aid (e, ev0 :: evs, σ)
```

with `RoundClass` the four-arm Prop: `value_done v` (e = ofVal (.pure
v), engine round = [.done v]); `value_annot ds v` (e = ofVal (.annot
ds v), engine round = the REMOVE-ANNOT successful-next to the bare
value); `step c'` (Step M c c' AND ∀ c'', Step M c c'' ↔ CerberusRound
M aid c c'' — the TWO-SIDED arm, `step_iff_cerberusRound`: → is
`engine_step_matchU`, ← is matchU + injectivity of the thread literal
(mirror determinism falls out)); `refused` (toVal e = none ∧ ∀ c', ¬
Step M c c' — exactly ¬NotStuck). Arm exclusivity:
`RoundClass.value_not_step`. Proof: by cases on `toVal e`
(`ofVal_of_toVal` + `outcomesU_done`/`outcomesU_remove_annot`), then
classically on the existence of a mirror step.

Per-row outcomes (Frag's 18 constructors): value → the two value
arms exact; every other row → the step arm exact wherever the
mirror steps. REFUSAL arm (mirror stuck): TWO-SIDED for store, load,
create, case (`cerberusRound_refused_store/_load/_create/_case`:
mirror stuck ⇒ ∃ o, o.isRefusal ∧ outcomesU = [o]; store/case from
the existing completeness pairs, load/create from the NEW
`engine_complete_loadU`/`engine_complete_createU` — the store
template against `dischargeStep_load/create_refusal`, at any
MachineCtx); ONE-SIDED for the other 14 rows with the reason
recorded per row in the manifest (RELATION-REFUSAL-ONE-SIDED):
`failwithI` panic channels (if: non-boolean guard; run: unbound
label/arity; the binder betas: pattern mismatch; the operand-
evaluation rows: PEconstrained) — `failwithI` is an OPAQUE constant
(LemLib.lean:180, `opaque … := default`, no equations), so a kernel
proof that the panic value is "not a `.next`" is IMPOSSIBLE, not
merely unproved; save's EVAL round on non-value params (a genuine
fragment gap: the engine continues where the mirror is stuck);
memop-ptreq's differing-provenance ND fork (`offFragment`); the
context rows sseq/annot/wseq (refusal propagation through `Decomp`
not proved). This is the precise R-03 residual; the closure table
records it and what full closure would take.

### 2.2 The manifest derives its relation cases from the theorem

Generator: `cerberusRound_classify` must exist as a theorem whose
statement is indexed by `Frag` and concludes `RoundClass` with
exactly the four arms (a changed arm list throws — the manifest's
semantics would need re-reading); each row's engine equations
(`engineEqs` — the step_ctx_*/stepDischarge_* lemmas) must lie in
that theorem's UNPRUNED proof cone (a stale/decorative engine-match
claim throws); a row's `twoSided` theorem must exist, be about the
construct (statement witness) and state a refusal classification
(`EngineOutcome.isRefusal` + `outcomesU` in its statement); a row
without one must record `oneSidedWhy`. Machine lines:
RELATION-COVERAGE / RELATION-REFUSAL-TWO-SIDED / -ONE-SIDED.

Audit pins added (exact trio): `cerberusRound_classify`,
`step_iff_cerberusRound`, `engine_complete_loadU`,
`engine_complete_createU`.

### 2.3 The mismatch plant (run against commit e157941, reverted)

A Step.lean-level mismatch (a mirror rule's successor or premise
altered) would fail `engine_step_matchU` and hence
`cerberusRound_classify` — that is the certification working as
designed, but a Step.lean edit rebuilds the whole package twice
(plant + revert; each well over the ~1 h bar on this loaded box), so
the plant is placed at the RELATION itself: `CerberusRound`'s
definition altered so the successor round carries the SOURCE
configuration's environment (`M.thread c'.1 c.2.1` instead of
`M.thread c'.1 c'.2.1`) — an engine/mirror mismatch in what the
named relation claims the engine's round to be. Round.lean is a leaf
module, so only it and Audit rebuild. Transcript (`lake build`):

```
error: CerberusHeapLang/Round.lean:112:38: Application type mismatch: The argument
  hs
has type
  Step M (e, ev0 :: evs, σ) (e', ρ', σ')
but is expected to have type
  Step M ((e, ev0 :: evs, σ).fst, ev0 :: evs, (e, ev0 :: evs, σ).snd.snd)
    ((e', ρ', σ').fst, (e, ev0 :: evs, σ).snd.fst, (e', ρ', σ').snd.snd)
in the application
  engine_step_matchU aid hf hsz hs
error: CerberusHeapLang/Round.lean:130:49: unsolved goals
  ... ⊢ M.thread e₀ ρ₀ = M.thread e' ρ'
error: Lean exited with code 1
error: build failed
```

(`Step.toCerberusRound` — the match-given-step inclusion — no longer
follows from `engine_step_matchU`, whose engine round carries the
SUCCESSOR's environment; and `step_iff_cerberusRound`'s injectivity
step no longer closes. Gate 3 red; gate 4 never reached.)

Reverted (`git checkout`), rebuilt green, the full gate re-run green
(the closure commit's gate).

### 2.5 Phase exit — closure and P4 readiness

Closure table (`2026-09-01_alloc-arc-plan.md`): R-04 CLOSED; R-03
CLOSED AT THE CLASSIFICATION LEVEL, RESIDUAL RECORDED (the refusal
arm's engine side two-sided for store/load/create/case only; the
other rows' channels are opaque panics / save's EVAL round / the
memop ND fork / context propagation — each named per row in the
manifest's RELATION-REFUSAL-ONE-SIDED line and Notes 7). README:
R-03 paragraph rewritten to the P3.2 state; R-03/R-04 register rows.

Oracle status at phase exit: `./scripts/test_unit.sh` (FULL tier)
ALL GATES GREEN — see the closure commit. Signature-snapshot oracle:
this phase changed no exported statement (the census gate 5 is
drift-free; P3 added `Round.lean` and the four Audit pins only;
ProdEntry's change is a docstring).

P4 READINESS (charter P4 — the raw separation-logic API with
RefinedC-compatible boundaries): P4.1 (three allocation facts —
bytes / bounds+metadata / allocation result): the public create
rules already export the pure address bounds (P2 slice 1); the
persistent metadata split is the P4.1 work proper; the manifest's
create row will need its `logic`/`total.rules` lists extended when
the rule shapes change (the dependency check will flag any consumer
whose cone stops containing a listed rule). P4.2 (statement-level
frame transformations, R-05): the manifest's cut modules already
cover Wps/Wpt, so new frame rules there are approved crossings by
module; exhibits re-derived through them need no manifest change
unless lane attributions move. P4.3 (representation accidents,
R-08/R-09): LoopExhibit's `IsXFrame` port and the `SemTriple`
profile qualifier touch no manifest row. Gate discipline for P4:
intermediate commits `--fast` (labelled), the phase exit FULL. Open
instrument follow-ups (not blockers): a per-row `refusalEqs` field
(the refusal equations checked in the twoSided theorem's cone) once
more rows become two-sided; the runaway `lean Soundness.lean`
process observed on the box (39 h, 18 GB, not this session's) should
be looked at by the operator.

### 2.4 The `sem_triple_prod` hpre honesty item

Finding: `hpre` (the prefix drive equation) and `hterm` are
OPERATIONAL hypotheses; the theorem has had no consumer since P2
(only its Audit pin and the README paragraph name it). Decision
[AGENT 2026-09-01]: ANNOTATE, not retire — an HONESTY NOTE in the
ProdEntry docstring + one README sentence state the operational
nature of the conditions, the consumerlessness, and mark it a
RETIREMENT CANDIDATE for the P5 scaffolding pass (R-07); deleting an
exported face is a design-level removal left operator-visible.
