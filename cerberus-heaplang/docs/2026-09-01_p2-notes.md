# Alloc arc P2 — whole-program logic proofs replace operational example prefixes (slice records)

Charter: `../../docs/2026-09-01_cerberus-heaplang-skeptical-re-audit.md`
§P2 (the six conversion steps IN ORDER, the production-route diagram
with "No arrow may be supplied by an example-specific engine trace",
and the definition of done = the R-01/R-02 closure tests) + merge
row 5 ("no direct operational proof terms in positive example
declarations"). Execution: three slices (struct = steps 1-2;
progAProd + the Exhibit trace = step 3; loops + deletions + closure
flips = steps 4-6). This file is the running record; each slice
appends its section at commit time.

## The P2 pointer-flow design record (binds all three slices)

THE FORCING FACT (recorded before any conversion): the public create
rules quantify the fresh pointer universally in the continuation
(`∀ p, p ↦cell … -∗ Ψ(ptr p)` — the charter P1.4 shape). An
entailment `allocCap reqs ⊢ wps/wpt (…create; suffix(π)…)` where the
program TEXT hard-codes a concrete pointer π is FALSE in general:
`allocCap reqs` is satisfiable at other launch cursors, where the
engine mints p ≠ π and the suffix would access unowned memory. So
the pre-P2 allocating programs (create result DISCARDED by a
wildcard `lets _`, suffix authored against the allocator's concrete
output) are not verifiable through the public rule — by design, not
by accident: their shape was an artifact of the operational era.
CONSEQUENCE [AGENT 2026-09-01]: the P2 conversions re-author each
allocating program so the created pointer flows through the
PROGRAM's own binders (`Esseq (symPat p …) create …` — the mirrored
LETS-PURE at the sym binder — with heap access through the evaluated
operands `PEsym p` / `array_shift(PEsym p, …)`), which is also the
shape real Cerberus-generated Core has for `p = malloc(); …*p…`.
Statement consequence (the charter's sanctioned class, itemized per
slice): concrete cold-start addresses/allocation ids move OUT of the
program texts and conclusions and INTO the boundary evaluation of
the concrete plans (the PlanFits arithmetic at the cold-start
cursor); converted conclusions bind the allocation id/address
existentially — the logic binds the pointer, the engine picks it.

THE BOUNDS EXPORT (needed by slice 3's list build, landed in slice 1
so the rule statements move once): `isList`'s node-WF facts demand
`0 < a < 2^64` for every node address, and the generic list logic is
never parameterized or weakened (the charter's demand). The public
create rules therefore export the fresh pointer's pure
machine-address bounds `⌜0 < addrOf p ∧ addrOf p < 2^64⌝` in the
continuation — the charter P1.4 allowance ("the postcondition may
also provide … bounds knowledge"; the pure form pending P4's
metadata split). Provenance of the bound: `allocCap`'s hidden cursor
now carries `c.lastAddr ≤ 2^64` (`LaunchCoh.la_wf` at launch — real
Cerberus starts the downward cursor at 0xFFFFFFFFFFFF < 2^64 — and
`freshBase_lt_two64`/`freshBase_add_le` preserve it across creates:
the downward allocator never mints above its cursor).

## Slice 1 (charter steps 1-2) — the struct client on the public rule + its adequacy theorem

Landed:

- `freshBase_lt_two64` (Heap.lean): the fresh-base upper bound from
  the cursor bound (companion of `freshBase_pos`).
- `allocCap` (Heap.lean) now `∃ c, cursorOwn c ∗ ⌜PlanFits c reqs ∧
  c.lastAddr ≤ 2^64⌝` (implementation-internal change; clients stay
  on intro/weaken/create); `allocCap_intro` takes the bound;
  `LaunchCoh` gains `la_wf`; `LaunchCoh.empty` takes it;
  `launchResources` threads it; `prodMem₀_launchCoh` discharges it
  concretely (`errnoAddr ≤ 2^64` by decide).
- PUBLIC `wps_create`/`wpt_create` (Wps/Wpt §CreateRule) continuation
  gains `⌜0 < addrOf p ∧ addrOf p < 2^64⌝` (statements still
  cursor-free — transcript below). AllocExhibit's P1 consumers
  adapted (patterns discard the new conjunct).
- `engine_adequacyU_alloc` + `spike_engine_adequacy_alloc`
  (Adequacy.lean): the partial lane's allocation-aware engine faces
  (the launcher-variant family P1.3 opened, completed for the wps
  lane), through the one `launchResources` helper.
- `update_env_sym` (EnvLaws.lean): the whole-stack env update at the
  plain sym binder (the `lets p = create(...)` binding step).
- STEP 1 — `struct_create_store_wps` (StructExhibit.lean) CONVERTED:
  program `progCreateInit` re-authored per the design record
  (`lets p = create(align, struct) in lets v = 5 in
  store(int, PEsym p, PEsym v)` — the stored constant rides a binder
  too because the mirrored store-EVAL arm requires non-value
  operands); premise `allocCap [⟨align, structTy⟩]` ONLY (the old
  `cursorOwn ⟨la,nid⟩` premise and closed-form `freshBase` pointer
  are GONE); proof = `wps_seq_sym` + PUBLIC `wps_create` +
  `wps_ofVal` + `wps_store_eval` (symbol lookups via the
  `SymFrame`/`envAdd_lookup` law) + the existing generic
  `wps_struct_x_store`; postcondition: existential pointer with the
  spliced five-image and `allocCap []`.
- STEP 2 — `struct_create_store_adequacy` (StructExhibit.lean, NEW):
  the local-entailment-to-engine gap CLOSES — the program launched
  from the PRODUCTION cold-start memory `prodMem₀` with the
  one-request plan (`struct_plan_fits`: closed allocator arithmetic
  at `⟨errnoAddr, 1⟩` — the boundary evaluation of the concrete
  plan), through `spike_engine_adequacy_alloc`/`launchResources`;
  conclusion in engine vocabulary: drive never kills, never
  derails, and a delivered value is `Vunit` with the final memory
  holding the initialized fresh struct at an EXISTENTIAL allocation
  id/address (`CellCoh σ' i ⟨a, structTy, spliced-five-image⟩`).

THE GREP TEST (transcript, derived from the signature snapshot):

```
$ for t in wps_create wpt_create struct_create_store_wps struct_create_store_adequacy; do
    grep -cE "AllocCursor|lastAddress|nextAllocId|freshBase|cursorOwn" stmt_$t.txt; done
0
0
0
0
```

FROZEN-CORPUS oracle (block-wise per-declaration diff of the
signature snapshot vs the committed P0 baseline — a strictly
stronger check than P1's line diff, adopted this slice):
REMOVED 0; CHANGED 3, all deliberate and disclosed:

1. `wps_create` — the P0 name carried the exact-cursor statement
   (P1 moved it to `wps_create_cursor_internal` verbatim and made
   the name public); this slice adds the bounds conjunct to the
   public continuation (the design record above).
2. `progCreateInit` — the program reshape (the design record: the
   create result is BOUND, the concrete `freshBase` pointer leaves
   the text).
3. `struct_create_store_wps` — the charter step-1 conversion itself
   (premise `cursorOwn ⟨la,nid⟩` → `allocCap [⟨align,structTy⟩]`;
   `SymFrame ev0` + `M.extern = fmapEmpty` hypotheses added for the
   symbol-operand evaluation; existential-pointer postcondition).

(`wpt_create`'s bounds conjunct does not surface vs the P0 baseline
— the name itself is a P1 addition; disclosed here instead.)

Claims surfaces trued same-commit: manifest generator create row
(consumer cell + Notes 4 rewritten; `struct_create_store_adequacy`
added; regenerated, gate 4 drift-free), README divergence-register
R-01 row + StructExhibit module row, WALKTHROUGH §4 allocation
paragraph, Wps §CreateRule header (legacy-client clause retired),
StructExhibit header. The statement census (gate 5) is drift-free:
no pinned export's statement moved this slice.

Gate evidence (slice 1): `./scripts/test_unit.sh` → ALL GATES GREEN
(1-5).

## Slice 2 (charter step 3) — the complete create/store/load in `wpt`; the Exhibit trace retired

Landed:

- `wpt_driver_done_alloc` (ProdLoop.lean): the production lane's
  allocation-aware launcher variant — `wpt_driver_done` through the
  one shared `launchResources` helper; the arrow that replaces the
  example-specific `driverDone_step` create prefixes.
- `prod_run_eqJ` MOVED from ProdLoopExhibit.lean to ProdEntry.lean
  (generic production machinery — the pipeline theorem composing
  `drive_after_setup` + a `DriverDoneAt` fact + `driver2_done`/
  `finalize_done` — had been living in an exhibit module; statement
  verbatim, no change). Likewise `val_mergeInto_annot`/
  `readoutPost_mergeInto_annot` moved ProdLoop → TotalAdequacy
  (readoutPost lemmas belong beside `readoutPost`), joined by the
  new element-wise `readoutPost_annot_absorb`.
- STEP 3 — `progAProd` RE-AUTHORED per the P2 pointer-flow design
  record: `lets p = create(4, int) in lets v = 7 in
  lets _ = store(int, p, v) in load(int, p)` — the fresh pointer is
  program-bound; the concrete cold-start pointer (`pxPtr`/`pxAddr`)
  is GONE from the program text and the conclusion.
- THE ONE LOGICAL TOTAL THEOREM `progAProd_wpt`: the complete
  create/store/load at budget 11 (derived: create 2 + constant bind
  1 + store-operand eval 1 + store 3 + load-operand eval 1 + load 3)
  from `allocCap [⟨4, intTy⟩]` ALONE — create through the PUBLIC
  `wpt_create`, store/load through the generic heap rules at the
  bound pointer. REPLACES `prodA_pre` (the operational create
  prefix) and `prodA_terminates` (the six-round termination trace),
  both DELETED with their scaffolding (`semAProd`/`provenAProd`/
  `eP1-3`/`σ1P`/the `*_applies_P` seams/`mAP7`/`cellXP7`...).
- `exhibitA_prod` REPROVED on the generic route: `progAProd_wpt` →
  `wpt_driver_done_alloc` (launch from `prodMem₀` with the
  one-request plan) → `prod_run_eqJ`. Statement delta (the
  sanctioned class, itemized): the conclusion's exact-address fact
  `readBytesFrom dst'.layout_state pxAddr 4 = 7-image` becomes
  `∃ i a, CellCoh dst'.layout_state i ⟨a, intTy, sevenBytes⟩`
  (existential allocation id/address — the logic binds the pointer,
  the engine picks it; the concrete pointer moved INTO the boundary
  evaluation of the plan, per the charter), and the statement GAINS
  `dres_blocked = false`/`stdout = ""`/`stderr = ""` (prod_run_eqJ's
  stronger face). Value fact unchanged: `dres_core_value = sevenVal`.
  Census re-baselined same-commit (gate 5; exhibitA_prod is pinned).
- THE AUDIT'S PARENTHETICAL (Exhibit.lean): the generic route COVERS
  the old straight-line trace, so `exhibitA_terminates` (the
  six-step `engineSteps_*` simulation) is RETIRED with its
  scaffolding (`σ₁`/`eA1-3`/`fpS`/`fpL`/`store_applies`/
  `load_applies`/`coh_after_store`/`drive_next`/`drive_done`) and
  replaced by `progA_wpt` (the seeded store/load at total budget 6)
  + `exhibitA_total` (an unconditional `driveU .done` equation at
  fuel 6 through `wpt_engine_boundU` — zero operational proof terms;
  Audit.lean pin moved `exhibitA_terminates` → `exhibitA_total`,
  trio-only).

GREP TRANSCRIPT (per-module, this slice's converted modules):

```
$ grep -cE 'Step\.|engineSteps_|driveJ_step|driverDone_step' Exhibit.lean ProdExhibit.lean
Exhibit.lean: 1      (a COMMENT documenting the retirement)
ProdExhibit.lean: 2  (COMMENTS documenting the deletion)
```
Zero hits in any proof body of either module.

FROZEN-CORPUS oracle (block-wise, vs the slice-1 snapshot):
REMOVED 34 — the operational trace corpus, exactly the charter step
3's sanctioned deletion class (itemized above); CHANGED 1 —
`exhibitA_prod` (the disclosed production statement change); ADDED
17 (the new logical route). vs the P0 baseline the cumulative
removals are these same 34 (P0 names; every removal is a deleted
operational-proof artifact, no logical surface lost).

Claims surfaces trued same-commit: manifest create row (prodLane
note per-theorem honest: exhibitA_prod CONVERTED, the two loop
exports still MIXED; totalLane gains `progAProd_wpt`; Notes 3/4
rewritten; regenerated, gate 4 drift-free), README (intro
parenthetical, exhibit table `exhibitA_prod` row, R-02 register row,
Exhibit/ProdExhibit module rows, §exhibitA_prod narrative),
WALKTHROUGH §4, Exhibit/ProdExhibit headers, Audit pin swap,
STATEMENT_CENSUS re-baseline (exhibitA_prod).

Gate evidence (slice 2): `./scripts/test_unit.sh` → ALL GATES GREEN
(1-5).
