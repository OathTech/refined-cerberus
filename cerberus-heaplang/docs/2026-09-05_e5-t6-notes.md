# E5 t6 switch — work record

[L1 landing note, 2026-09-07, AGENT] Record of the code, taken onto main with the code from the other agent's `dialect-e5` (landing charter §2 L1). "The charter" / "the active demo-completion goal" below is that agent's `docs/2026-09-05_demo-completion-charter.md`, a process document that stays on the parked branch `parked/demo-expansion-2026-09-07` and is not on main; its scope statements are [AGENT] readings, not rulings.

## Whole-term membership checkpoint

[AGENT, 2026-09-05, active demo-completion goal] Continued from `7a1ea1f`
on `dialect-e5` (worktree `worktrees/dialect-e1`). The semantics pin and
Lean 4.32.2 are unchanged. This is an intermediate fast-gate checkpoint,
not E5 acceptance or a t6 execution certificate.

`Examples/CorpusE0.lean` transcribes the complete t6 switch body from
`docs/corpus-e0/t6_switch.annot.core`, including the three case assignments,
the default and break jumps, all five save labels, and the dead cleanup.
Its skeleton matches 299 tokens. The bound, Astd, specified-value,
save-initializer, case-scrutinee, if-condition and case-pattern plants all
mismatch as required. t1 and t5 still match 121 and 271 tokens; the three
std.core rows also pass. These are structural comparisons; the charter's
stronger emitted-file/library connection remains open.

`Examples/CorpusE5.lean` proves whole-term fragment membership.
`t6Switch_select` characterizes the selected branch for every value:
specified object payloads select the substituted dispatch branch,
unspecified values select the exceptional-condition branch, and other
values do not match. The branch witness accepts every object payload,
without imposing the reachable integer-2 condition. The eleven new
theorems were individually axiom-measured; each uses exactly propext,
Classical.choice and Quot.sound, and all eleven have exact pins in Audit.

Validation: capped `lake build CerberusHeapLang.Examples.CorpusE5`
completed successfully; the separate corpus-skeleton command exited 0;
`CERB_MEM_MAX=40G scripts/test_unit.sh --fast` exited 0. Selected fast-gate
output, verbatim:

```
info: CerberusHeapLang/Audit.lean:944:0: CerberusHeapLang export pins: 790 trio-exact
info: CerberusHeapLang/Audit.lean:944:0: CerberusHeapLang axiom sweep: every theorem bounded by the trio (5751 swept, internal details included — count informational, environment-dependent)
info: CerberusHeapLang/Audit.lean:944:0: CerberusHeapLang banned-axiom sweep: sorryAx/ofReduceBool/ofReduceNat absent from all cones (8739 constants of every kind swept, internal details included — count informational, environment-dependent)
Build completed successfully (469 jobs).
ok: cerberus-heaplang build green
FAST-GATE GREEN (gates 1-2 only — not a claim-point result; say fast-gate in the commit)
```

`git diff --check` passes. The ARCHITECTURE citation report was rerun;
the pre-existing loose/range/usage review remains part of E5's range
review. Also corrected the t5 postcondition's stale docstring from
Specified(4) to its already-proved Specified(1).

## Next action

Prove t6 through public total rules and the shipped driver, starting with
the continuations registered by `collect_saves` for the case, break and
return labels. The selected path initializes x=2/r=0, jumps to case 2,
assigns r=20, then jumps through break to return. Registered case
continuations include their surrounding sequence suffixes; they must be
checked against the engine collector, not guessed from a save's immediate
body. Keep t4, the full E5 review and the remaining charter milestones
open. This checkpoint continues the active goal; it is not a park record.

## Execution checkpoint

[AGENT, 2026-09-05] `CorpusT6Exhibit.lean` now proves t6 through public
total rules and the shipped driver. `t6_wpt` has budget 78: initialization
20, controlling-expression load 7, case evaluation/substitution 2,
integer promotion 2, the first case test/skip 2, the second case test 1,
and its jump plus label budget 44. The label budgets are case 2 = 43,
break = 18 and return = 2. Case 2 uses 24 units for the assignment
statement and 19 for the jump through break. These are compositional
upper bounds, not a measurement of the minimum driver fuel.

`t6Q` is the engine's `collect_saves t6Main`. The five continuation
equations retain their surrounding sequences; `t6Q_frag` and `t6Q_pot`
cover every registered continuation. The label specifications admit the
reachable case-2, break and return entries, passing ownership of x=2 and
r=0/20 through the jumps. The other case bodies remain in both the term
and fragment witnesses. The fresh symbol introduced by assignment cannot
overwrite x/r because `600 ≤ M.runState.sym_supply` bounds its number.

`t6_certified_production` concludes an equation over the genuine
`CerbND.runND (drive ...) (initial_driver_state ...).1`: exactly one
Active result, Specified(20), blocked false, empty stdout and stderr.
Its user inputs are `sup`, `600 ≤ sup`, filesystem and argument list.
The file remains `prodFileLib stdlibE3 [] t6Main`. KOI A7 and the charter's
whole-file/library acceptance criterion remain open. The existing oracle
record agrees on Specified(20); no new oracle run or re-pin is claimed.

One reusable map fact was needed. The collector's `fmapAddBy symCmpL`
uses a different BEq instance from the environment's `symAdd`; treating
the entire map representations as definitionally identical was wrong.
`SymMap.addLabel` and `labelAdd_lookup` prove preservation and lookup for
the collector's actual spelling. No symbol comparison or engine behavior
was changed. A failed whole-map `rfl` attempt hit the ordinary heartbeat
limit; the structural lookup proof replaced it without increasing limits.

The execution slice adds 30 named theorems. Twenty-eight are measured
trio-exact and pinned; `t6Kill_eq` uses no axioms and `t6Main_pot` uses
propext. Every measurement is retained in
`2026-09-05_e5-t6-execution-axioms.txt`. With the previous eleven fragment
pins, the package now has 818 exact pins. The core judgments and adequacy
statements are unchanged by this execution slice.

### Full verification

`CERB_MEM_MAX=40G scripts/test_unit.sh` exited 0 on Lean 4.32.2 and the
unchanged semantics pin. Selected output, verbatim:

```
info: CerberusHeapLang/Audit.lean:974:0: CerberusHeapLang export pins: 818 trio-exact
info: CerberusHeapLang/Audit.lean:974:0: CerberusHeapLang axiom sweep: every theorem bounded by the trio (5823 swept, internal details included — count informational, environment-dependent)
info: CerberusHeapLang/Audit.lean:974:0: CerberusHeapLang banned-axiom sweep: sorryAx/ofReduceBool/ofReduceNat absent from all cones (8831 constants of every kind swept, internal details included — count informational, environment-dependent)
Build completed successfully (470 jobs).
ok: capability manifest regenerated, no drift
ok: corpus skeleton — every transcription matches its emitted text, every plant mismatches
ok: import direction — 18 core modules, none imports an exhibit/example/production module
BOUNDARY: 27 modules checked, 0 internals mention(s) in total, exit=0
ALL GATES GREEN
```

The manifest still has 35 constructors and 76 variants: 40 RULE,
5 RULE-PARTIAL-UNDEMONSTRATED, 25 NO-RULE and 6 OUT-OF-SCOPE; 0 red.
There are now 24 consumer modules and 16 checked claim rows. This grows
the emitted examples, not the declared fragment. README, ARCHITECTURE,
API, CLAIMS and the known-items register now describe t6. The citation
report was rerun after the source changes: 283 inspected, 175 EXACT,
53 DECL (0 automatically fixed), 25 USE, 21 HAND, 9 PIN, 0 NOFILE and
25 RANGE. The outstanding manual citation review remains disclosed.

The shared load/assignment proof scaffolding in t5 and t6 should be
factored as t4 clarifies the common inputs, before final exemplar closure.
This is implementation cleanup owed by the charter, not new logic scope.
The next target is t4's emitted loop: retain short-circuit evaluation of
`i < 5 && s < 7`, both assignments, continue/break/return labels and dead
cleanup; prove the result 10 with a decreasing loop budget. Then finish
the complete E5 range review before the refreshed dependency/fuel scout.
