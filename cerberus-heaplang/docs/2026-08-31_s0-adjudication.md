# S0 flag adjudication + records rider

[AGENT: orchestrator, 2026-08-31]

## The S0 flag: package-local fixpoint judgment — NOT a kill

The probe established that the donor's literal single-Φ stmt_wp
cannot sequence through Core-grammar jumps (Caesium never faces
this: its statement grammar is syntactically CPS), and the working
shape is the classical LABEL-CONTEXT statement logic (de Bruin
lineage) as a package-local guarded fixpoint via iris-lean's PUBLIC
fixpoint/Contractive API. Adjudication against the readiness kill
wording ("needs a new WP definition inside iris-lean itself"):
NOT FIRED — iris-lean is untouched; `wps` is a derived, package-
local judgment ELIMINATED into the unmodified base WP (`wps_sound`),
which remains the sole adequacy interface. Checked against the
COMPOSITIONALITY GUARD: G1 index audit passes (indices = static
label map, assertion context, expr, env — no configurations, no
traces, no fuel); derived-authority discipline intact (any wps/base
disagreement is a wps defect). Custom derived WPs by guarded
fixpoint are standard Iris practice with classical lineage; recorded
here with the flag preserved verbatim in the probe report §5 for
the operator's later read.

## Rider: effect-retirement consumer review (mirror pointer)

Our consumer review of the lem-side runEffectful retirement design
was filed 2026-08-31 as a suffix to
`worktrees/lem-lean-arc/effect-retirement/doc/lean-backend/
2026-08-31_effect-retirement-external-review.md` (lem-lean repo,
[USER]-authorized in-place write). Verdict RATIFY with two asks:
(A1) state whether the retired entry is seeded-constant or
supply-parameterized (statement-visible for our production
theorems); (A3) a Lean-side changed-definitions manifest at the
adoption pin (our certification unfolds the exec cone; re-proof is
then scoped, not discovered) + classify whether fuel budgets alter
default-fuel semantics + the final drive/entry signature. Non-goals
requested: entry purity; no new ND branch points on the sequential
path. Adoption lands as a deliberate re-pin + re-certification
slice when their pin dance completes.
