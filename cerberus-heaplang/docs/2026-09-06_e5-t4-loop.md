# E5 t4 loop and shipped-driver result — 2026-09-06

[AGENT, active demo-completion goal] Continued on `dialect-e5` from
`12a37f9`, the fully validated assignments/body checkpoint. The preceding
work was progress; this slice closes the loop and production proof. The
semantics pin remains `f95ef8d9c317fa6b50cf6691216a8c37b1d3eabf`, Lean
4.32.2. Main, sibling repositories and the emitted AST are unchanged.

## Result and boundary

`t4_wpt` proves the complete emitted main through public total rules at
budget **915**. `t4_certified_production` proves that the actual shipped
`drive`, from its cold initial state with any filesystem and argument
list and symbol supply at least 600, has exactly one Active result:
`Specified(10)`, unblocked with empty stdout and stderr. The conclusion
is a complete outcome-list equation, with no assumed termination premise.
The two integer allocations are paid from the cold-start budget; their
loads, stores and kills are derived through the public memory rules.
The production conclusion does not separately expose a final-heap readout.

The theorem's file is still `prodFileLib stdlibE3 [] t4Main`: the
transcribed main, the three-function checked standard-library fragment
and empty implementation map. It does not establish the charter's full
emitted-file connection (KOI A7). The unchanged skeleton speedbump checks
constructor/annotation structure and cannot certify literal, type, symbol
or file/library-map equality. No compiler-correctness claim is made.

## Invariant and proof structure

`t4Sum 0 = 0` and `t4Sum (n+1) = t4Sum n + n`. The while-label invariant
owns i's integer cell containing n and s's containing that sum, with
n ≤ 5 and both pointers passed as the registered label's arguments. The
source frame is restored by the engine's parameter binding. The body
updates s before i, so its new cells establish the invariant at n+1.
Fresh-symbol preservation uses the existing floor of 600; no particular
fresh symbol or supply reset is assumed.

On this invariant, the actual conjunction `i < 5 && s < 7` equals n < 5.
At n=5 the left comparison is false and the emitted short-circuit path
skips the right read. Arithmetic bounds and the six scalar byte-image
readouts are proved explicitly. Only these finite scalar encodings are
reduced; the program's execution is derived compositionally, without
unrolling the engine.

The total label budget is `t4Budget n = 159 * (5-n) + 98`. A continuing
iteration spends 77 on the guard/Boolean selection and 82 on the body
and back edge, leaving `t4Budget (n+1)`. The final path spends 77 on
selection, 1 on the final unit, 4 on the after-while save and unit, and
16 on return: the sum load and bound (7), both kills (3 each), and the
return jump plus its continuation (3). These are proved sufficient
budgets, not claims of minimal concrete run lengths. The initial save
adds 2 to `t4Budget 0 = 893`, and allocations/initialization add 20:
**20 + 2 + 893 = 915**.

`t4_blockSpecsT` verifies the reachable while and return jump entries.
Continue and break are also registered, and their saves execute on the
normal path; this program never jumps to them. All four collector entries
retain their fragment/potential proofs. The return's emitted dead cleanup
is retained after the jump and covered by whole-term membership. No branch
or continuation is deleted to make the proof fit.

This slice changes no engine, mirror, fragment, memory-invariant or
adequacy definition. The existing generic total driver theorem closes
the client proof, retaining the same production vocabulary and assumptions.

## Axiom measurement

Seventeen new theorem declarations were individually measured with
`#print axioms` after the target built. Eleven are exactly the classical
trio and now have exact export pins: the two byte-image readouts, return
parameter binding, the return/test/step/while-continuation proofs, block
specifications, entry, main and production theorem.

`t4Index_cases`, `t4Sum_le`, `t4Guard` and `t4Budget_succ` use only
`propext` and `Quot.sound`; `t4Sum_succ` and `t4Kill_eq` use no axioms.
These six remain covered by the exhaustive sweep. No new axiom boundary
or proof method is introduced. The fast gate passes with **896** exact
pins, 6050 swept theorems and 9140 swept constants (the latter two counts
are informational and environment-dependent).

## Verification

Commands run from the worktree root unless a package cwd is specified:

- Package cwd: `CERB_MEM_MAX=40G ../scripts/capped ~/.elan/bin/lake build CerberusHeapLang.CorpusT4Exhibit`.
- `CERB_MEM_MAX=40G scripts/test_unit.sh --fast`.
- Package cwd: capped `lake env lean` on the temporary individual axiom probe and `scripts/capability_manifest.lean`; inspect then install the regenerated manifest.
- `CERB_MEM_MAX=40G scripts/test_unit.sh`.
- Package cwd: `scripts/cite_check.sh` (report only).
- `git diff --check`.

The FULL gate exited 0. Selected output, verbatim:

```
info: CerberusHeapLang/Audit.lean:1061:0: CerberusHeapLang export pins: 896 trio-exact
info: CerberusHeapLang/Audit.lean:1061:0: CerberusHeapLang axiom sweep: every theorem bounded by the trio (6050 swept, internal details included — count informational, environment-dependent)
info: CerberusHeapLang/Audit.lean:1061:0: CerberusHeapLang banned-axiom sweep: sorryAx/ofReduceBool/ofReduceNat absent from all cones (9140 constants of every kind swept, internal details included — count informational, environment-dependent)
Build completed successfully (472 jobs).
ok: capability manifest regenerated, no drift
ok: corpus skeleton — every transcription matches its emitted text, every plant mismatches
ok: import direction — 18 core modules, none imports an exhibit/example/production module
BOUNDARY: 29 modules checked, 0 internals mention(s) in total, exit=0
ALL GATES GREEN
```

The regenerated manifest has 35 constructors, 77 variants, 40 RULE,
7 RULE-PARTIAL-UNDEMONSTRATED, 24 NO-RULE, 6 OUT-OF-SCOPE and zero red.
All 55 modules are classified, with 25 consumers. Its 17 claim rows now
check 184 theorem-cell names and 341 declaration-shaped spans. The changed
consumer lists reflect t4's allocation, initialization and disposal rules;
no classification is promoted merely because a total theorem was added.
All t1/t4/t5/t6 skeletons and the seven applicable plants pass. t4 remains
591 tokens. The full build revalidates the t1/t5/t6 production proofs.
The final t4 rebuild took 3.1 seconds without t4 warnings.

The citation report exited 0: 277 citations, 176 EXACT, 52 DECL, 19 USE,
21 HAND, 9 PIN, zero NOFILE and 25 RANGE, with no automatic fixes. Six
obsolete Audit line references were removed while updating its stale
818-pin summary to 896 with a record-backed breakdown. The remaining
manual citation queue is unchanged and stays open. `git diff --check`
passes. Updated the stale closed-statement/driver-route counts and C14's
pre-E5 description of ccall-free nested branches against the current
`ccallFree` definition. These checks do not constitute an independent review.

## Remaining work

The E5 execution milestones t5/t6/t4 are now proved on the current file
boundary. Prepare the full range review from `8eeaf92`, explicitly
including the bound-rule correction, supply normalization, protocol and
label proofs, client boundary, axiom census and public claims. Resolve
the seven partial-undemonstrated rows through useful clients or an
explicit reviewed API/coverage disposition, as the charter requires.
E5 has not yet received that review and M1 is not accepted complete.

Then continue the fresh dependency/fuel scout before E6. Investigate the
full-file seam during that scout and integrate it during E6; complete the
remaining corpus, all-outcomes work, cleanup and fresh full architecture
review under the active charter. This checkpoint is not a park, merge,
push or completion of the full goal.
