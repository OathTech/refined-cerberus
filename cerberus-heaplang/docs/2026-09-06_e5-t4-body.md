# E5 t4 assignments and loop body — 2026-09-06

[L1 landing note, 2026-09-07, AGENT] Record of the code, taken onto main with the code from the other agent's `dialect-e5` (landing charter §2 L1). "The charter" / "the active demo-completion goal" below is that agent's `docs/2026-09-05_demo-completion-charter.md`, a process document that stays on the parked branch `parked/demo-expansion-2026-09-07` and is not on main; its scope statements are [AGENT] readings, not rulings.

[AGENT, active demo-completion goal] Continued on `dialect-e5` from clean
`4f06efc`. The previous goal turn was progress: both addition RHSs and
the exact read-footprint rules were proved and fully validated. The
semantics pin remains `f95ef8d9c317fa6b50cf6691216a8c37b1d3eabf`, Lean
4.32.2. Main, sibling repositories and the emitted program are unchanged.

## Proved now

`wpt_t4Assign` composes an annotated, effectful RHS with the emitted
assignment scaffolding. It adds 22 to the RHS budget: 5 for evaluating
the pure pointer operand and collecting the tuple, 16 for the negative
store under the bound, and 1 for the statement's final unit. The tuple
binder retains the RHS annotations under the same full-expression bound;
the negative-store protocol uses its fresh exclusion id to discharge the
read/store race check. No read annotation is discarded early.

`wpt_t4AssignS` proves `s = s + i` at budget 40, preserving i's cell.
`wpt_t4AssignI` proves `i = i + 1` at budget 36, preserving s's cell.
Both return the stored byte images and a frame retaining the two source
pointers. Integer representability, cell reconstruction and non-trap
loads are explicit hypotheses, as are the `StdE3` file premise, identity
external resolution and symbol-supply floor of 600. The source-frame
freshness lemma proves that generated symbols above the source-variable
numbers preserve those lookups. It does not assume a particular fresh
symbol or silently reset the supply.

`wpt_t4Body` proves the entire emitted body through its back edge:
both assignments, the intervening unit values, the continue save and the
run to the registered while continuation. Its budget is **82 + m**.
The precondition's continuation must establish the next while-label
specification at budget m using the new cells. This is a compositional
body proof conditional on that label precondition, not yet a complete
termination proof. Pointer-save evaluation, save binding and run-argument
binding are checked explicitly; each rebinds both source pointers.

The controlling expression remains proved at 72 and its Boolean decoding
at 4. The decreasing invariant, loop exit, return and t4 shipped-driver
result remain open. E5 range review and the full-file/library criterion
A7 are also still open.

## Shared proof plumbing

`Examples/EmittedInt.lean` now contains the signed-int memory-value
construction, encoding and storability facts, and `wpt_emittedIntStore`.
The latter specializes the existing public `wpt_neg_bound` once for the
emitted integer-store shape, with arbitrary source location, symbols and
RHS annotations. t4, t5 and t6 use it. Their transcriptions are unchanged;
t5/t6's assignment statements and total budgets are unchanged. Historical
t5 memory-value and byte-image names remain as aliases, and its encoding
and storability theorem names delegate to the shared facts. Other generic
helper relocation and broader exemplar cleanup remain queued.

`wpt_unseq_pure_left` is a derived public total rule. The right operand
runs first and can change the environment. The caller supplies the pure
left operand's evaluation in that resulting environment, and the result
retains the right operand's annotations. It follows the existing focus,
pure-evaluation and value-collection rules, covering bare and annotated
right results. The manifest lists it beside the existing pure-right
helper. No engine, mirror, fragment, memory invariant or adequacy
definition changed. The seven E5 partial-undemonstrated rows remain open.

## Verification

The fourteen new theorem cones were individually measured as exactly
`[propext, Classical.choice, Quot.sound]` and pinned. The changed t5
encoding/storability and t5/t6 assignment theorem cones were also checked
and retain the same trio. Targeted builds and the fast gate pass with
885 pins. The FULL gate exited 0; selected output, verbatim:

```
info: CerberusHeapLang/Audit.lean:1047:0: CerberusHeapLang export pins: 885 trio-exact
info: CerberusHeapLang/Audit.lean:1047:0: CerberusHeapLang axiom sweep: every theorem bounded by the trio (5984 swept, internal details included — count informational, environment-dependent)
info: CerberusHeapLang/Audit.lean:1047:0: CerberusHeapLang banned-axiom sweep: sorryAx/ofReduceBool/ofReduceNat absent from all cones (9065 constants of every kind swept, internal details included — count informational, environment-dependent)
Build completed successfully (472 jobs).
ok: capability manifest regenerated, no drift
ok: corpus skeleton — every transcription matches its emitted text, every plant mismatches
ok: import direction — 18 core modules, none imports an exhibit/example/production module
BOUNDARY: 29 modules checked, 0 internals mention(s) in total, exit=0
ALL GATES GREEN
```

The manifest retains 35 constructors, 77 variant rows, 40 RULE,
7 RULE-PARTIAL-UNDEMONSTRATED, 24 NO-RULE, 6 OUT-OF-SCOPE, zero red and
25 consumers. There are 17 claim rows, 177 checked theorem-cell names
and 334 declaration-shaped spans. The changed consumer lists include
t4's use of the negative-store and save rules. The full gate revalidates
the existing t1/t5/t6 production proofs. The final t4 build took 2.8
seconds with no t4 warnings; `git diff --check` passes.

Claim C17, the API, README, module classification, charter and known-item
register now distinguish the completed body from the still-open loop
closure. The citation report is unchanged: 283 citations, 176 EXACT,
52 DECL, 25 USE, 21 HAND, 9 PIN, zero NOFILE and 25 RANGE, with no
automatic fixes. Its manual-review queue remains open. This checkpoint
has not received an independent review.

## Next action

Close the while-label specification with an invariant indexed by n ≤ 5:
i contains n and s contains the sum 0+…+(n−1). Prove the small reachable
integer byte-image readouts, arithmetic bounds and guard characterization.
A recursive sum with `sum(n+1) = sum(n)+n` aligns directly with the
assignment result; no execution unrolling is needed.

Compose condition (72), Boolean decoding (4), if (1) and the body
(82+m), then establish the resulting 159-unit per-iteration decrease
against the loop budget. Prove the false-condition path, after-while save,
both kills and the return label before fixing the final exit allowance.
The current registered continuations and their fragment/potential facts
remain the program referent. Do not infer a loop theorem from the body
lemma or silently drop the unused emitted cleanup.

Then prove entry from the cold-start allocation/setup and the complete
t4 production result, run full validation, and prepare the complete E5
range review. Continue to the dependency/fuel scout and the remaining
charter milestones. This checkpoint continues the active goal; it is not
a park, merge or push.
