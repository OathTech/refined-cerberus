/-
CerberusHeapLang.Phase1Probe — PHASE-1 PROBE (S1a) umbrella.

The foundations arc's Phase-1 architecture probe: the unified
machine-context configuration, the re-indexed relation for a
representative construct subset (store / run / value-scrutinee
case), its engine characterization, the iris Language instance
with `primStep` = the unified relation, the store small axiom and
adequacy chain over it, the frozen-corpus oracle re-proof
(`exhibitB_semantic_unified` — statement verbatim), and the Ecase
engine-facing regression.

PROBE-ONLY by charter: lives alongside the existing cones, consumed
by nothing in the exported corpus; the S1b/S1c migration replaces
the old cones with this shape. Design record:
docs/2026-08-31_phase1-design-record.md.
-/
import CerberusHeapLang.Phase1Probe.Machine
import CerberusHeapLang.Phase1Probe.Match
import CerberusHeapLang.Phase1Probe.Lang
import CerberusHeapLang.Phase1Probe.Adequacy
