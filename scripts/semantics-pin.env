# The cerberus-lean SEMANTICS PIN — the single source of truth for
# which semantics this repo builds against ([USER 2026-08-29]: pin
# the post-merge mainline commit; landed 2026-08-30).
# Move only deliberately: re-run scripts/setup-cerberus-dep.sh after
# removing .cerberus-ws, then re-gate (the pin dance).
#
# a8f86112d = mdd/cerberus-lean after the core/semantics-first merge
# + the S-basket close-out (incl. the Lean-side lem-sync stamp gate).
#
# 2026-08-31 bump 8fb380c9c -> 07a7fca29 ([USER] mission order): the
# delta is the gcc second-oracle lane (db93d12f3 + 07a7fca29) —
# scripts + records only, ZERO semantics code (verified by the
# orchestrator via diff --stat: 5 files, docs/ + scripts/ only).
# 2026-08-31 pin bump 07a7fca29 -> 58ec50779: docs-only delta (stack-ceiling
# park record + upstream-tray drafts; zero generated/native changes, verified
# by filtered diff --stat).
#
# 2026-09-02 pin bump 58ec50779 -> ddcfc9199 ([USER 2026-09-02] resume note
# Slice 1, THE RETIREMENT RE-PIN; 38 commits): the EFFECT RETIREMENT arc
# (C1 adoption + C2 deletion) — `runEffectful` is gone from generated/,
# native/ and LemLib (LemLib pin 861ed81 -> 045dcb0, zero axioms); the
# production entry is supply-threaded (`initial_driver_state : Nat -> file ->
# fs_state -> driver_state × Nat`, `initial_core_run_state : Nat -> ... ->
# core_run_state × Nat`, new `_given`/`_with` pure builders); the CerbMem
# memory ops take the tagDefs reader as an explicit leading argument
# (reader_consumer: loadM/storeM/allocateObject/sizeofCtype/...); the CerbTags
# global + native/tags.c + native/fresh_int.c are deleted. Also riding: the
# trust-basket slice (CerbFS fail-closed, driver freshness stamps), the gcc
# second-oracle lane baselines, and the cgroup-direct `capped`. Code-bearing
# delta on the primed paths (git diff --stat 58ec507..ddcfc91 -- frontend
# lean_frontend/generated lean_frontend/native lean_frontend/lakefile.toml):
# 14 files changed, 330 insertions(+), 255 deletions(-) — generated/ is
# gitignored upstream, so its delta shows via the workspace re-prime
# (cerberus-heaplang/docs/2026-09-02_repin-notes.md).
#
# 2026-09-03 pin bump ddcfc9199 -> f95ef8d9c ([USER 2026-09-03] OVERNIGHT
# AUTONOMY ruling (2): re-pin after K4/K5, before calls; 35 commits): THE
# FUEL ARC — the driver family's fuel exhaustion is now a kernel-transparent
# kill (`CerbND.fuelExhaustedKill = Error0 CerbFuel.fuelExhaustedLoc
# CerbFuel.fuelExhaustedMsg`; the nine ND-typed `_lemFuel_zero` lemmas and the
# wrapper `rfl`s in `CerbND.FuelContract`; the fuel-parametric mirror
# `CerbND.drive_lemFuel` with `drive_wrapper_defeq`); the drive-cone budget
# moves to `CerbFuel.driverFuel = 10^8` (`driver2`, `drive_nonmemory_steps_aux2`,
# `print_eval_conv_aux`, `hack`, `nd_bind`, `CerbND.ndDefaultFuel`) while every
# other fueled declaration stays at `lemDefaultFuel = 10^6` (LemLib rev
# UNCHANGED, 045dcb0 — no `lake update`). Also riding: the mem-scale C1/C3
# memory-model body changes (`reconstructValue_lemFuel` array arm,
# `memValueToBytes_lemFuel` struct arm; signatures unchanged; zero measured
# breakage — scout record docs/2026-09-03_repin-scout.md §3.3, copied onto main from branch repin-scout), the RelSem
# prune (relsemcore/RelSem/* deleted upstream), the cmm_op `sorry` target_rep
# closed, and `lean_frontend/handwritten_copy.manifest` (NEW: the authoritative
# hand-written seam list, 23 seams — section C of setup-cerberus-dep.sh now
# reads it). Code-bearing delta (git diff --stat ddcfc91..f95ef8d9c -- frontend
# lean_frontend/generated lean_frontend/native lean_frontend/lakefile.toml
# lean_frontend/handwritten_copy.manifest 'lean_frontend/*.lean' Makefile
# lean_frontend/Makefile): 39 files changed, 1469 insertions(+), 1811
# deletions(-); generated/ is gitignored upstream, so its delta shows via the
# workspace re-prime (cerberus-heaplang/docs/2026-09-03_repin-fuel-notes.md).
CERBERUS_LEAN_COMMIT="f95ef8d9c317fa6b50cf6691216a8c37b1d3eabf"
